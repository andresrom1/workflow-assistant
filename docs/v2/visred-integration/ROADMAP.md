# ROADMAP — Integración Visred (cotización + emisión)

> Tracking de ejecución **específico de este plan**. El plan canónico está al lado en [`PLAN.md`](PLAN.md).
> Para el estado global del backend ver [`../../../ROADMAP.md`](../../../ROADMAP.md) (filas V2-M, V2-1…V2-5).
> Modelo de dominio (gate): [`../10-modelo-dominio-cotizacion-emision.md`](../10-modelo-dominio-cotizacion-emision.md). Contrato Visred: [`../08-visred-quote-adapter.md`](../08-visred-quote-adapter.md).
> **Rama:** `prepare-proyect-for-API-refactor`.

**Leyenda:** ⬜ Sin empezar · 🚧 En progreso · ✅ Completa · ⏸️ Bloqueada

---

## Hechos verificados (sandbox)

| Hecho | Valor | Estado |
|---|---|---|
| Base URL sandbox | `https://sandbox-api.visred.com.ar` (endpoints en `/v1/...`, **sin** `/api`) | ✅ verificado live |
| Auth | `POST /v1/accounts/token/` `{username,password}` → `200 {access, refresh}` | ✅ smoke-test OK (2026-06-07) |
| Refresh | `POST /v1/accounts/token/refresh/` `{refresh}` → `200 {access}` | ✅ smoke-test del `VisredClient` OK (2026-06-07) |
| Credenciales | en `workflow-assistant/.env` (`VISRED_BASE_URL`/`VISRED_USERNAME`/`VISRED_PASSWORD`) — **no commiteadas** | ✅ |
| Egress de red | OK (visred.com.ar → 200) | ✅ |
| Emisión vehículo | `POST /v1/patrimoniales/vehicles/emitir/` (`PreSaleVehicleRequest`: req `person_holder`+`quotation_result_id`+`vehicle`) → `200 TaskItem` → poll → `APIBasePreSaleResultDTO` (`presale_id`/`proposal_number`/`policy_number`/`status`/`require_inspection_after_emission`/`tasks_list[]`) | ✅ shape del OpenAPI live (`GET /v1/schema/`, read-only, 2026-06-07) |
| Inspección post-emisión | `POST .../emitir/{presale_id}/inspeccion/` body `{inspections:[{id, document_base64}]}` → `200 TaskList`; tipos vía `GET .../params/inspection-types/?company_id=&product_id=` | ✅ schema live |
| Documento | `POST /v1/documents/` `{presale_id, product_id, task_type_id:"download-poliza"}` → `TaskDTO {result.url}` (SÍNCRONO) | ✅ schema live |
| **Emisión REAL (D6)** | `emitir/`→`TaskItem`(con `company_id`)→poll→`APIBasePreSaleResultDTO {presale_id, proposal_number, policy_number, status, require_inspection_after_emission, tasks_list}`. `status` puede venir **null** (SUCCESS se determina por `presale_id`). | ✅ **pre-venta real creada** (presale_id 32092, experta, 2026-06-08) |
| Params holder | `tax-condition`/`sex`/`person-type`/`document-type` en `/v1/params/...` (singular, top-level), shape `{id, description}` | ✅ verificado live (2026-06-08) |
| `fuel_type_id` | string único (opcional) en `QuotationVehicleDataRequest`. Catálogo: `nafta`/`diesel`/`electrico`/`hibrido`/`gnc`/`con-gnc`/`sin-gnc` | ✅ verificado live (2026-06-08) |
| Descuento | `discount_id` solo en emisión (`PreSaleVehicleRequest`). Catálogo `/params/discount/` por-compañía `{value, discount%}` lista **sobre** el tope del productor (emitir >tope → 400) | ✅ verificado live; emit con discount OK (presale 32093) |

---

## Estado por fase

| Fase | Tema | Estado | Nota |
|------|------|--------|------|
| 0 | Modelo de dominio agnóstico (gate) | ✅ | `docs/v2/10`. Aprobado por el usuario (2026-06-06). |
| 1 | Extraer puerto `QuotationProvider` | ✅ | Refactor sin cambio de comportamiento. PHPStan 0 · suite 274/274 (2026-06-06). |
| — | Smoke-test auth real | ✅ | Login contra sandbox → 200 con access+refresh (2026-06-07). |
| 2 | `VisredClient` (HTTP + JWT) | ✅ | login/refresh cacheado (Cache server-side), refresh-on-401 (reintenta 1 vez; si falla → re-login), Bearer + Accept, `VisredApiException` (status/code/field_errors), `X-Mock-Scenario` solo sandbox, `config/visred.php`. Tests `Http::fake` (14) + smoke-test live. PHPStan 0 · suite 288/288 (2026-06-07). |
| 3 | `VisredQuotationProvider` + polling | ✅ | Lee el token de `risk_provider_refs`; `RiskSnapshot`→request; `cotizar/`→TaskList; polling acotado (`Sleep`, tolerante a parcial/budget); aplanado company→covers→`parsed_alternatives`; `cover`→`normalized_grade` (solo en el adapter). Firma intacta. Tests herméticos (5). **`quotation_result_id` por-alternativa diferido a Fase 5** (cada alt lo lleva en `external_quote_id`; `saveResults` intacto). |
| 4 | Bind directo Visred + E2E cotización | ✅ | Mock (`QuotingEngine`) **eliminado**; `QuotationProvider`→`VisredQuotationProvider` bind directo (real-always, sin selector); `flatten`/`mapCover` reconciliados al shape real (`result.company_id`+`quotation_results[]`), nombre de compañía vía `/discovery/companies/` (en vivo, sin cache); filtro de coberturas placeholder/inactivas. Smoke E2E real OK (13 compañías, 13.8s). Suite 304/304, PHPStan 0. |
| 5 | `EmissionProvider` + emisión real | ✅ | **Re-scope (2026-06-07): SOLO la API de Visred — cerrado.** **A/B/D/E/F/G ✅**: refs por-alternativa (+`company_id`), puerto+`VisredEmissionProvider`, orquestación, inspección after-emisión, **D4.1 before-emisión** (`company_id` por-alt → bloque neutro `inspection_photos` que el adapter arma y embebe) y **D4.2 idempotencia** (guard por `poliza_emitida`). **Deuda fuera de scope:** checkout (titular, fotos GNC → WS-B) y cartera (materialización en `polizas` → WS-C). El **emit E2E live** (WS-D) espera WS-B. Suite 330/330, PHPStan 0 (2026-06-08). |
| 6 | `VehicleCatalogResolver` (gate de quotability) | ✅ | Implementado **aplanado**: puerto agnóstico `Quotability` (la única abstracción que ve el canal), `VisredQuotabilityResolver` (árbol de 5 GETs + desambiguación Tier 1 léxico / Tier 2 `DisambiguationAgent` LLM, beam por groups), tri-estado `QuotabilityResult` (Quotable/NeedsFact/NotQuotable). Refina `Vehicle.version` + token → `risk_provider_refs`. Tests herméticos (6 resolver + 3 gate). |

---

## Decisiones abiertas (model §9)

Aceptadas en sus valores recomendados vía "dale"; **revisitar al llegar a Fase 3/5**:
1. `Risk` sin versionar (identidad + estado actual; historia comercial = cadena `risk_snapshots`). **Cerrada** (doc 10 §4a; confirmada vs transcript sesión "Visred API integration plan" 2026-06-07).
2. Repurpose de `polizas` a tabla de referencia — **✅ IMPLEMENTADO (2026-06-08, WS-C)** (doc 10 §5b, decisión b). Migración `2026_06_09_001617`. Resta **avisar a mango-mobile** (modelo compartido).
3. TTL cache de cartera ~2h. → pendiente (se activa al implementar el endpoint de cartera on-demand).
4. Dedup `Risk` por patente — **✅ IMPLEMENTADO (2026-06-08, WS-C)** en `PolicyReferenceService` (`metadata->patente`). Abierto: `chasis` como identidad más fuerte cuando esté.
5. `person_holder`: capturar en checkout vs defaults. → **EMISIÓN ✅ CERRADO** (se captura en checkout, D1/WS-B 2026-06-08). **COTIZACIÓN:** se manda un **placeholder DNI configurable** (`visred.default_holder_dni`), sobrescrito por el real al emitir; cotización en dos fases **RECHAZADA**. El placeholder **funciona** (San Cristóbal cotizó con `30000000` en test manual). ⚠️ **Galicia/San Cristóbal fallan intermitentemente por `external_service_unavailable`** (backend de la compañía caído en sandbox), NO por nuestro request ni por el DNI. Consulta a Visred: ¿limitación del sandbox o también en prod? Ver bitácora + docs/v2/08 §2.2.

## Deuda — registro completo

Separación de scopes (decisión del usuario 2026-06-07): la Fase 5 toca **solo la API de Visred** (puerto, adapter, mapeos, polling). Todo lo demás se anota acá como deuda, agrupado por scope: **qué falta · por qué · ancla en código · qué lo desbloquea**.

### D1 · Scope CHECKOUT — captura del titular (`person_holder`) — ✅ CERRADO (2026-06-08, WS-B)
- **D1.1 ✅** `birthdate`, `sex_id`, `tax_condition_id` capturados. `tax_condition_id` es un select poblado por `VisredCatalogService::taxConditions()` (cacheado, agnóstico de canal, pasado como prop desde `CheckoutController::show`). ⚠️ Ruta/​shape del endpoint de tax-conditions **no verificados live** — configurable en `config/visred.tax_conditions_path`, re-confirmar en D6.
- **D1.2 ✅** Split de `telefono` → `phone_prefix` (≤3) + `phone_number` (≤9), ambos capturados como inputs separados.
- **D1.3 ✅** Split de `nombre` → `first_name` + `last_name`.
- **Columnas nuevas** en `checkout_sessions` (migración `2026_06_08_221648`) + `$fillable`/casts; `nombre`/`telefono` se mantienen poblados (compuestos) para mail/admin. `PolizaEmisionService::buildRequest` completa el `holder[]`. Defaulteables (`person_type_id`→`fisica`, `document_type_id`→`dni`) siguen en el adapter.

### D2 · Scope CHECKOUT — fotos de inspección faltantes — ✅ CERRADO (2026-06-08, WS-B)
- **✅** Slot `velocimetro` agregado siempre; `tubo_gnc`/`oblea_gnc` condicionados a `has_gnc` (toggle en checkout, default desde `combustible`; al destildar se sueltan las fotos GNC). `photoSlots` pasó a computed.
- **Mapa** `inspection_photo_map` extendido en `config/visred.php` (`velocimetro`/`tubo-gnc`/`oblea-gnc`); `has_gnc` se captura y se propaga al `vehicle[]` del request neutro (`buildVehicle` del adapter ya lo consume). ⚠️ Los `inspection_type_id` GNC/velocímetro re-confirmar live en D6.
- **Ancla:** `VisredInspectionService::buildInspections()` envía solo los tipos requeridos con foto.

### D3 · Scope CARTERA — materializar la referencia de póliza — ✅ CERRADO (2026-06-08, WS-C)
- **Decisión aplicada (doc 10 §5b, decisión b — ya definida):** **repurpose de `polizas`** como tabla de referencia (migración `2026_06_09_001617`): se relajaron a nullable `estado`/`company`/`coverage`/`sum_asegurada`/`vigencia` y se agregaron `quote_id`, `presale_id`, `company_id`, `product_id`, `last_synced_at`. mango-mobile sigue leyendo `polizas`.
- **find-or-create `Risk`** por (`customer_id`, `type=vehicle`, patente) (§7/§9.4) — la patente vive en `Risk.metadata` (no es columna), dedup vía `metadata->patente`.
- **Desacople:** la materialización se movió a un servicio de cartera propio, `PolicyReferenceService::materialize()` (agnóstico, sin Visred), invocado desde `PolizaEmisionService::persistEmission()`. **Se eliminó el stash `Quote.metadata['emission']`** (acoplaba el dominio de cartera dentro del de cotización); la referencia vive ligada a `Risk`+`Quote` en `Poliza`, y el guard de idempotencia (D4.2) la reconstruye desde ahí. Los extras de la emisión (`proposal_number`/`emission_status`/`requires_inspection_after_emission`) van en `Poliza.metadata`.
- **Campos autoritativos del contrato** (sum_asegurada/vigencia/cuota) quedan null → los compone el endpoint de cartera on-demand desde la compañía (cache-aside, doc 10 §5). El display que MANGO ya conoce (company/coverage) se llena de la alternativa elegida.

### D4 · Scope VISRED-WIRING — pendientes internos (no requieren otro equipo) — ✅ CERRADO (2026-06-08, WS-A)
- **D4.1 Inspección before-emisión ✅:** `company_id` por-alternativa se persiste en `quote_alternative_provider_refs` (lo emite `VisredQuotationProvider::mapCover`, lo stripea/persiste `QuoteRepository::saveResults`). Cuando la cobertura elegida tiene `requires_inspection_before_emission=true`, `PolizaEmisionService::buildRequest` pasa el bloque neutro `inspection_photos` (company_id opaco + product + fotos de dominio) y `VisredEmissionProvider` arma las inspecciones (lógica Visred) y las embebe en el `emit()`. **Respeta la invariante:** el servicio agnóstico NO importa `VisredInspectionService` — el building vive en el adapter (corrección al plan, que proponía llamarlo desde el servicio).
- **D4.2 Idempotencia de emisión ✅:** guard al inicio de `PolizaEmisionService::emitir()` — si el quote ya está `poliza_emitida`, devuelve el resultado stasheado sin re-emitir. Queda documentada (TODO) la ventana de carrera entre `emit()` y `persistEmission()`.

### D5 · DIFERIDO explícito (decisión de producto)
- **Documento `download-poliza`** (`POST /v1/documents/`): diferido hasta tener emisión real (sin `presale_id` no hay de dónde traer). Decisión en `hallazgos-visred-task-type.md`. La Pieza D ya deja el `presale_id` disponible.

### D6 · Verificación live — ✅ EMIT REAL OK (2026-06-08)
- **Emit E2E real ✅:** smoke creó una **pre-venta real** en el sandbox (`presale_id=32092`, compañía `experta`) vía catálogo→cotizar→emitir→poll. El `APIBasePreSaleResultDTO` live **calza con `mapResult`**: keys `presale_id`(int)/`proposal_number`/`policy_number`(strings)/`status`/`require_inspection_after_emission`(bool=true)/`tasks_list`; `company_id` viene en el `TaskItem` (que es de donde lo lee el adapter). 
- **Holder verificado live:** `tax-condition` (`/v1/params/tax-condition/`, `{id,description}`: CF/EX/RMT/no_responsable/no_categorizado/RI), `sex` (M/F), `person-type` (`fisica`), todos top-level singular. **Config corregida** (`tax_conditions_path`).
- **Inspección GNC verificada:** `tubo-gnc` y `oblea-gnc` existen live (sancor). `velocimetro` **NO** apareció en sancor → confirmar por-compañía (puede ser de otras).
- **`company_id` (slug) ≠ slug de `coverage_documents`** (`rus` vs `rio-uruguay`) para el RAG — pendiente al tocar coverage-check (sin cambios).

### D7 · ~~`fuel_type_id` inválido~~ — ❌ DESCARTADO como bug de `sin-gnc`, pero ⚠️ INCOMPLETO (ver 2026-06-09)
- Verificación inicial truncó la lista de `fuel-type` a 6 filas y concluí mal que `sin-gnc` no existía. La lista **completa** es `["con-gnc","diesel","electrico","gnc","hibrido","nafta","sin-gnc"]` → **`sin-gnc` SÍ es válido** ("sin equipo GNC"). Smoke de `cotizar/` con `sin-gnc` y con `nafta` devolvió 13 tasks en ambos.
- ⚠️ **Lo que el smoke NO probó (2026-06-09):** el path **`gnc`** exige `insured_amount_fuel` (400 si falta). Probar solo `sin-gnc`/`nafta` lo ocultó. Arreglado: ver bitácora 2026-06-09.
- Pregunta abierta menor (no-bug): el binario colapsa nafta/diesel/eléctrico/híbrido en `sin-gnc`; si en algún momento se quisiera tarifar por combustible específico, habría que confirmar con Visred si cambia el precio (hoy ambos cotizan). No accionar sin esa confirmación.

### D8 · Descuento (`discount_id`) — ✅ CERRADO (2026-06-08)
- **No es checkout, es nuestro:** el descuento se decide en `workflow-assistant`. `discount_id` está en `PreSaleVehicleRequest` (**emisión**), NO en `QuotationVehicleRequest` → Visred no permite "cotizar con descuento".
- **Diseño:** el adapter de cotización (`VisredQuotationProvider`) trae el catálogo de descuentos por-compañía (`/params/discount/`, `{value, discount, description}`), una **`DiscountPolicy` (Strategy agnóstica)** elige cuál, el adapter **aplica el % al fee** que ve el dominio y persiste el `discount_id` por-alternativa (`quote_alternative_provider_refs.discount_id`). En emisión, `PolizaEmisionService` manda ese `discount_id` → el precio cotizado coincide con el cobrado.
- **Hallazgo live (tope del productor, POR COMPAÑÍA):** el catálogo lista descuentos por encima del **máximo permitido para el productor** (Triunfo cataloga 30%, pero emitir con >20% da 400 `"supera el máximo permitido (20%)"`). Ese tope **no lo expone Visred** y es **por-compañía** (en sandbox solo Triunfo devuelve catálogo; el resto `[]`). → `MaxDiscountPolicy` es **cap-aware**: elige el mayor descuento ≤ tope de esa compañía, leído de `config('visred.max_discount_percent')` (mapa `company_id → %` + `default`, default **0** = sin bonificar). Hoy: `triunfo → 20`. Verificado live: tope 20 → Triunfo acepta 20% (value "V") → `presale_id 32093`.
- **Aproximación documentada:** aplicar el % al fee del lado nuestro es aproximado; el premio bonificado exacto lo fija la compañía (emitir no devuelve el premio) → reconciliar contra la póliza on-demand (cartera) a futuro.
- **A confirmar con Visred:** los topes por-compañía no tienen endpoint (se descubren por el 400). Hoy solo conocemos Triunfo=20%. **Consultar al proveedor** si existe forma de obtenerlos y si el `[]` de las demás significa "sin descuentos" o "no cargado en sandbox".

**Consecuencia global:** los adapters de Visred (cotización, quotability, emisión, inspección) quedan **completos y verdes herméticos**; el **emit E2E live** espera a D1 (checkout). La cartera espera a D3 (mango-mobile). Correcto con la separación de scopes.

---

## Riesgos abiertos
- ~~**Budget de polling vs timeout del job de IA** (120s vs 180s) — puede forzar async.~~ **Resuelto (2026-06-07):** smoke real midió **13.8s** para que **13 compañías** llegaran a terminal (7 SUCCESS), holgado bajo el budget de 120s y el timeout de 180s. No fuerza async. Muestra única (auto año 1997); revisar con un smoke periódico si cambian los tiempos del proveedor.
- ~~**`polizas` rica vs referencia mínima** — cambio de modelo compartido con mango-mobile.~~ **Resuelto (2026-06-08, WS-C):** repurpose aplicado (columnas de cartera relajadas a nullable + `quote_id`/`presale_id`/`company_id`/`product_id`/`last_synced_at`). **Pendiente operativo:** coordinar/avisar a mango-mobile que la migración corrió (lectura de cartera).
- **`version_id`** — token opaco del catálogo, resuelto por el `VehicleCatalogResolver` en identify-vehicle y guardado en backing store A (NO en columna de dominio). Diseño cerrado, ver [`RESOLVER-DESIGN.md`](RESOLVER-DESIGN.md) §7–§10.
- **Catálogo Visred = dato vivo** — `version_id` opacos pueden rotar; consulta en vivo + cache TTL, sin bakear valores como fixtures. Drift se cubre con smoke/contract test contra sandbox.
- **Catálogo origin-aware** — depende del usuario autenticado; confirmar usuario de servicio vs personal.

---

## Preguntas pendientes a Visred

Consolidado de consultas abiertas al proveedor (las dispersas en docs/v2/08, 10 y D8 se reflejan acá). Actualizar al resolverse.

1. **Galicia / San Cristóbal — `external_service_unavailable` (cotización).** En el sandbox, sus tasks de cotización fallan **intermitentemente** con `code: external_service_unavailable` (San Cristóbal → `"Error desconocido."`, Galicia → `"Field required"`), aunque a veces responden SUCCESS con el mismo request (DNI placeholder `30000000`, mismo usuario `andresrom@gmail.com`, ambas listadas en `/discovery/companies/`). Verificado por aislamiento (combustible/vehículo/zip/permisos descartados, request manual reproducido). **¿Es una limitación/inestabilidad del backend de esas compañías solo en sandbox, o esperable también en producción?** Si pasa en prod, evaluar reintento acotado para `external_service_unavailable`. (docs/v2/08 §2.2)
2. **DNI placeholder en cotización vs DNI real en emisión.** Cotizamos con un DNI placeholder (`visred.default_holder_dni`) y emitimos con el DNI real del checkout (no re-cotizamos). **¿La prima cotizada depende del DNI** (scoring/perfil del titular)? **¿Aceptan emitir contra un `quotation_result_id` cotizado con un DNI distinto** al de emisión, o exigen que coincidan? **¿Riesgo de re-tarifación silenciosa o rechazo al emitir?** (docs/v2/08 §2.2)
3. **Topes de descuento por compañía (D8).** El catálogo `/params/discount/` lista descuentos **por encima** del máximo permitido al productor (Triunfo cataloga 30%, emitir >20% → 400). El tope no tiene endpoint (se descubre por el 400) y es por-compañía. **¿Hay forma de obtener los topes por compañía?** Y el `[]` que devuelven las demás en sandbox, **¿significa "sin descuentos" o "no cargado en sandbox"?** (ROADMAP D8)
4. **Usuario de servicio vs personal.** `/discovery/companies/` es origin-aware (filtra por permisos del productor). Hoy autenticamos como `andresrom@gmail.com`. **¿Existe un usuario de servicio dedicado** para la identidad de MANGO, o seguimos con el del productor? (docs/v2/08 §2.4)
5. **Tarifación por combustible específico (menor, no-bug).** El mapeo colapsa nafta/diésel/eléctrico/híbrido cuando no hay GNC. **¿El precio cambia según el combustible específico** (más allá de gnc/sin-gnc)? Hoy ambos cotizan; no accionar sin confirmación. (ROADMAP D7)

---

## Bitácora

- **2026-06-10** — **Faltaban alternativas en las cotizaciones (análisis Quote #23): 3 causas, 2 accionadas.** Confirmado live replicando la Quote #23 contra el sandbox (script `<?php` temporal, sin secretos, borrado). `VisredQuotationProviderTest` **14**, PHPStan **0**, Pint.
  - **Causa A — filtro `active` tiraba planes vendibles (bug nuestro, arreglado).** `mapCover` descartaba todo cover con `cover.active === false`, pero Visred marca `active=false` en coberturas **con fee y nombre reales** (Mercantil B/B0/B3/M BASICA/M PLUS, Sancor Auto Max 6, RUS…). El filtro ahora es **solo por placeholder** (nombre vacío) — los placeholders reales de Visred vienen con `name`/`id` vacíos, así que se siguen atrapando. **Impacto live: 26 → 38 alternativas** (Mercantil 4→14, Sancor 1→2). La premisa documentada ("active=false = discontinuado") era falsa.
  - **Causa B — San Cristóbal y Galicia fallan en su servicio externo (instrumentado; NO resuelto).** Sus 6 tasks (3 c/u) terminaban en `FAILURE` y `poll()` solo logueaba el `task_id`. Se **instrumentó**: cada FAILURE ahora loguea `company_id` (correlacionado `task_id → company_id` desde el `tasks_list`), un `reason` legible y el `body`. **Decisión aplicada:** mandar un **DNI placeholder configurable** en cotización (`config('visred.default_holder_dni')`, env `VISRED_DEFAULT_HOLDER_DNI`, default `30000000`), sobrescrito por el DNI real al **emitir**; **cotización en dos fases RECHAZADA** (una vez en checkout no se re-cotiza). **Supersede** la parte del 2026-06-09 que *omitía* `person_holder` sin DNI: ahora se manda el placeholder (y solo se omite si tampoco hay config).
    - **El DNI placeholder FUNCIONA; Galicia/San Cristóbal fallan por infra del sandbox (corrige conclusión previa, verificado live 2026-06-10).** Una cotización manual con `document_number: 30000000` devolvió **San Cristóbal SUCCESS** (5 coberturas) → el placeholder es aceptado, NO valida contra padrón. Lo que las bloquea en nuestras corridas es **`external_service_unavailable`** (503, §2.5): el backend de la compañía no responde (San Cristóbal → `"Error desconocido."`, Galicia → `"Field required"`, ambas `code: external_service_unavailable`). Aislado: se reprodujo el request manual **exacto** (vehículo `AALCVEeg`, zip 5000, `sin-gnc`, DNI placeholder, **mismo usuario `andresrom@gmail.com`**) y falla; descartado combustible/vehículo/zip/permisos (ambas en `/discovery/companies/`). Es **intermitente**: el SUCCESS manual fue cuando su backend estaba arriba. (Schema live: `QuotationPersonHolderRequest` solo acepta DNI+email+teléfono; `first_name`/`birthdate`/`sex`/`tax_condition` solo existen en emisión — pero no hacen falta, el bloqueo es infra.)
    - **Integración correcta; `poll` tolerante a parcial ya lo cubre.** Posible mejora: **reintento acotado para `external_service_unavailable`** (transitorio), no para `validation_error`.
    - **⚠️ CONSULTAS a Visred:** (1) ¿el `external_service_unavailable` de Galicia/San Cristóbal es limitación del sandbox o esperable en prod? (2) ¿la prima depende del DNI / se puede emitir con DNI distinto al cotizado / re-tarifación silenciosa? Ver docs/v2/08 §2.2.
  - **Causa C — set acotado de covers por compañía (upstream, NO accionada).** Triunfo (solo A/B/C/D3, sin C2 Full) y Experta (sin XL+granizo) devuelven menos planes; todos `active`, todos guardados. No es filtro nuestro → es config de producto del lado Visred/cuenta del productor. Pendiente de verificar con Visred (no es código).
  - **Bonus (no accionado):** Mercantil emite 2 tasks con fees idénticos (duplicado real, distinto de los pares de Triunfo que son medios de pago `cupon`/`tarjeta-cbu`); `saveResults` no deduplica. Anotado.
  - **Archivos:** `VisredQuotationProvider` (`mapCover` filtro, `taskCompanies` map, `poll`+`failureReason` instrumentación, `buildRequest` placeholder DNI), `config/visred.php` (`default_holder_dni`). Tests: filtro `active` reescrito (conserva vendibles), +2 person_holder (placeholder / omisión si todo vacío).
- **2026-06-09** — **Fix cotización: 2 validaciones reales que bloqueaban `cotizar/` en prod (Quote #22).** Confirmadas live contra el sandbox (smoke por archivo temporal, sin secretos, borrado), arregladas y verdes (`VisredQuotationProviderTest` 13, PHPStan 0, Pint).
  - **`person_holder` con `document_number` vacío → 400** (`field_errors: {person_holder}`). El DNI se captura recién en checkout; en cotización es `null`. `person_holder` es **nullable en cotización** (docs/v2/08 §2.2) → `buildRequest` ahora **omite el bloque** cuando no hay DNI (antes mandaba `""`).
  - **`fuel_type_id='gnc'` sin `insured_amount_fuel` → 400** ("Debe indicar el monto asegurado del equipo de GNC"). **Esto invalida la conclusión de D7**: aquel smoke probó `sin-gnc` y `nafta` (que NO exigen el monto) pero **nunca el path `gnc`**. El monto del equipo no se captura hoy en cotización (el checkout solo toma `has_gnc` booleano) → se manda un **default configurable** (`config('visred.default_gnc_amount')`, hoy `1.500.000`; decisión del usuario) y se refina en emisión. Solo se envía cuando `fuel_type_id === 'gnc'`.
  - **Diagnóstico:** se sumó `field_errors` al log de `ApiQuoteResolution` (antes solo el mensaje genérico "Error de validación." — opaco). Verificación final live: `cotizar/` aceptó y devolvió **13 tasks**.
  - **Archivos:** `VisredQuotationProvider::buildRequest`, `ApiQuoteResolution` (log), `config/visred.php` (`default_gnc_amount`). Tests +2 (omisión de person_holder, `insured_amount_fuel` en GNC).
- **2026-06-08** — **Descuento (D8) ✅ + mapeo de combustible + D7 descartado.** Suite **348/348**, PHPStan **0**, Pint. Investigación contra el OpenAPI + smokes live (archivos `<?php` temporales, sin secretos, borrados).
  - **`fuel_type_id` es string único** (no array, opcional). `sin-gnc` es válido (D7 era falsa alarma). Se **mejoró igual el mapeo**: `RiskSnapshot.combustible` → id real del catálogo (`nafta`/`diesel`/`electrico`/`hibrido`/`gnc`), **sin default** — combustible desconocido **omite** el campo (no asumir).
  - **Descuento (D8):** `discount_id` está en emisión (no en cotización) → Visred no permite cotizar con descuento. Se modeló: el adapter de cotización trae el catálogo `/params/discount/` por-compañía, una **`DiscountPolicy` (Strategy agnóstica, `MaxDiscountPolicy`)** elige, el adapter **aplica el % al fee** y persiste el `discount_id` por-alternativa (`quote_alternative_provider_refs.discount_id`); la emisión lo manda → cotizado == cobrado.
  - **Hallazgo live — tope del productor (POR COMPAÑÍA):** el catálogo lista descuentos sobre el máximo permitido (Triunfo: 30% en catálogo, emitir >20% → 400). El tope no lo expone Visred y es por-compañía (solo Triunfo devuelve catálogo en sandbox; el resto `[]`). → `MaxDiscountPolicy` es **cap-aware y pura** (recibe el tope); el adapter lo resuelve de `config('visred.max_discount_percent')` (mapa `company_id → %` + `default 0`; hoy `triunfo → 20`). Verificado live: Triunfo acepta 20% → `presale_id 32093`. **A confirmar con Visred** (no hay endpoint de topes).
  - **Params del holder verificados live** (otra pre-venta real, `presale_id 32092`, experta): `tax-condition`/`sex`/`person-type` en `/v1/params/...` (singular). `tax_conditions_path` corregido a `/v1/params/tax-condition/`.
  - **Archivos:** `app/Contracts/DiscountPolicy.php`, `app/Services/MaxDiscountPolicy.php` (nuevos), `VisredQuotationProvider` (fuel map + descuento), migración `discount_id`, `config/visred.php`. Tests +8 (`MaxDiscountPolicyTest`, fuel map, descuento cap-aware, persistencia, propagación a emisión).
- **2026-06-08** — **WS-D / D6 ✅ — emit E2E REAL contra el sandbox.** Smoke por archivo `<?php` temporal (sin secretos, borrado al terminar), credenciales de `.env`.
  - **Pre-venta real creada:** catálogo (`auto`→brands→years→groups→versions) → `cotizar/` (13 compañías) → `emitir/` → poll → `APIBasePreSaleResultDTO`. Compañía `experta`, `presale_id=32092`, `proposal_number=3081890`, `policy_number=02292201`, `require_inspection_after_emission=true`, `tasks_list`=2. **El shape live calza con `VisredEmissionProvider::mapResult`**; `company_id` viene en el `TaskItem`. Hallazgo: `status` puede venir **null** (nuestro adapter ya determina SUCCESS por `presale_id`, no por `status` — robusto).
  - **Params del holder verificados live** y **config corregida**: `tax-condition`/`sex`/`person-type`/`document-type` viven en `/v1/params/...` (singular, top-level), shape `{id, description}`. `tax_conditions_path` apuntaba a una ruta inexistente (404) → corregido a `/v1/params/tax-condition/`. `VisredCatalogService.normalize` ya lee `description` ✓.
  - **Inspección GNC:** `tubo-gnc`/`oblea-gnc` confirmados live (sancor). `velocimetro` no apareció en sancor → re-confirmar por-compañía.
  - **Hallazgo (deuda):** **D8** — algunas compañías exigen `discount_id` al emitir (Triunfo lo rechazó por falta de descuento); el checkout no lo provee. Anotado en "Deuda".
  - **Corrección posterior (mismo día):** lo que anoté como **D7** (`fuel_type_id='sin-gnc'` inválido) fue **falsa alarma** — el recon truncó la lista de `fuel-type` a 6 filas; la completa incluye `sin-gnc` (7ª). Smoke confirmó que `cotizar/` acepta `sin-gnc` (13 tasks, igual que `nafta`). El código de cotización es correcto; D7 descartado, sin cambios de código.
- **2026-06-08** — **WS-C ✅ — materialización en cartera (D3) + des-acople del stash.** Suite **340/340**, PHPStan **0**, Pint.
  - **Repurpose de `polizas`** (decisión doc 10 §5b, ya definida — migración `2026_06_09_001617`): se relajaron a nullable `estado`/`company`/`coverage`/`sum_asegurada`/`vigencia` y se agregaron `quote_id`, `presale_id`, `company_id`, `product_id`, `last_synced_at`. La compañía sigue siendo System of Record; MANGO guarda la referencia mínima. mango-mobile sigue leyendo `polizas` (modelo compartido — **avisar el cambio**).
  - **Nuevo `PolicyReferenceService`** (dominio cartera, agnóstico): `materialize()` hace find-or-create del `Risk` (dedup por (`customer_id`, `type=vehicle`, patente vía `metadata->patente`) — doc 10 §7) y upsert de la `Poliza`-referencia por `quote_id` (idempotente), ligada a `Risk`+`Quote`. Llena el display que MANGO conoce (company/coverage de la alternativa) y deja los campos autoritativos del contrato (sum_asegurada/vigencia/cuota) null para componer on-demand.
  - **Eliminado el acoplamiento `Quote.metadata['emission']`** (mezclaba el dominio de cartera dentro del de cotización, observación del usuario). `PolizaEmisionService::persistEmission()` ahora transiciona el `Quote` y delega la materialización; el guard de idempotencia (D4.2) reconstruye el resultado neutro desde la `Poliza` (extras en `Poliza.metadata`), no desde el `Quote`.
  - **Tests (+3):** `PolicyReferenceServiceTest` (crea Risk+Poliza ligada al Quote; idempotente por `quote_id`), `PolizaEmisionServiceTest` (materializa la referencia en cartera y NO escribe `Quote.metadata`; dedup de Risk por patente). Mobile (`PolizasTest`/`SiniestroTest`/`SharedRisksTest`) siguen verdes con el schema repurposed.
- **2026-06-08** — **WS-B ✅ — checkout completo (D1 titular + D2 fotos GNC/velocímetro).** Suite **337/337**, PHPStan **0**, Pint, `npm run build` OK.
  - **D1 (titular):** `checkout_sessions` gana `first_name`/`last_name`, `birthdate`, `sex_id`, `tax_condition_id`, `phone_prefix`/`phone_number` (migración `2026_06_08_221648` + `$fillable`/casts). El form (`Checkout/Show.vue`) parte nombre→nombre/apellido y teléfono→característica/número, agrega fecha de nacimiento, sexo y un select de condición fiscal poblado por `VisredCatalogService::taxConditions()` (catálogo nuevo, cacheado, agnóstico de canal, pasado como prop desde `CheckoutController::show`). `nombre`/`telefono` se siguen poblando (compuestos) para no romper mail/admin. `PolizaEmisionService::buildRequest` completa el `holder[]`. **No se tocó el pipeline de canvas/fotos** (regla de memoria móvil).
  - **D2 (fotos):** slot `velocimetro` siempre + `tubo_gnc`/`oblea_gnc` condicionados a un toggle `has_gnc` (default desde `combustible`; `photoSlots` ahora computed; al destildar GNC se sueltan esas fotos). `has_gnc` se persiste y se propaga al `vehicle[]` neutro. `config/visred.inspection_photo_map` extendido con los 3 tipos.
  - **Honestidad sobre live:** la ruta/​shape de `tax-conditions` y los `inspection_type_id` GNC/velocímetro **no están verificados live** — configurables y marcados para re-confirmar en D6. `VisredCatalogService` es tolerante (normaliza `{ref,label}` desde varios nombres de campo y devuelve `[]` sin romper el checkout si falla).
  - **Tests (+7):** `VisredCatalogServiceTest` (normalización, tolerancia, fallback a [], cache), `CheckoutControllerTest` (persistencia de los campos partidos + GNC + composición de `nombre`/`telefono`, validación de `birthdate`, `has_gnc=false`); `PolizaEmisionServiceTest` extendido (holder completo + `has_gnc` en el request neutro).
- **2026-06-08** — **WS-A ✅ — cierre del wiring Visred (D4.1 + D4.2). Fase 5 ✅ en scope Visred.** Suite **330/330**, PHPStan **0**, Pint.
  - **D4.1 (inspección before-emisión):** `company_id` (slug opaco) ahora se emite por-alternativa en `VisredQuotationProvider::mapCover` (threadeado desde `flatten`), se stripea del dominio y se persiste en `quote_alternative_provider_refs` vía `QuoteRepository::saveResults` (columna nueva + `$fillable`; migración `2026_06_08_220322`). En emit-time, `PolizaEmisionService::buildRequest` pasa el bloque neutro `inspection_photos` (`{company_id, product_id, photos}`) cuando la cobertura elegida exige inspección before; `VisredEmissionProvider` lo detecta y arma las inspecciones vía `VisredInspectionService` (tipos requeridos + R2→base64 + mapeo `photo_key`), embebiéndolas en el `emit()`.
  - **Corrección al plan (invariante de desacople):** el plan proponía llamar a `VisredInspectionService` desde `PolizaEmisionService`, pero eso acoplaría el servicio agnóstico a una clase Visred (viola CLAUDE.md / doc 10 §3). El building Visred-específico se dejó en el adapter; el servicio solo pasa ingredientes neutros (el `company_id` ya era vocabulario del puerto vía `emit()`/`uploadInspection`).
  - **D4.2 (idempotencia):** guard al inicio de `emitir()` — si el quote ya está `poliza_emitida` devuelve el resultado stasheado (nuevo `stashedResult()`, ahora `metadata.emission` incluye `company_id`) sin re-emitir. TODO documentado para la ventana de carrera `emit()`↔`persistEmission()`.
  - **Tests (+4):** `QuoteRepositoryTest` (persistencia de `company_id` + ADR-001 lo excluye del dominio), `PolizaEmisionServiceTest` (pasa/omite `inspection_photos` según el flag; idempotencia con 0 invocaciones al puerto), `VisredEmissionProviderTest` (el adapter arma R2→base64 y embebe `inspections[]` desde el bloque neutro, hermético con `Storage::fake('r2')`).
- **2026-06-08** — **Plan de cierre de la deuda (D1–D6) abierto y documentado.** Se creó
  [`CLOSURE-PLAN.md`](CLOSURE-PLAN.md) (complemento canónico de `PLAN.md`): ordena el cierre de D1–D6
  por dependencia/dueño en 4 workstreams (WS-A wiring Visred · WS-B checkout · WS-C cartera gated por
  mango-mobile · WS-D verificación live; D5 diferido). El plan original se **validó contra el código**
  (line-anchors de `VisredQuotationProvider`/`QuoteRepository`/`PolizaEmisionService`/`Checkout/Show.vue`/
  `config/visred.php`/`polizas` confirmados); se anotaron 3 precisiones: (1) `mapCover` no recibe hoy
  `$companyId` (hay que threadearlo, no solo agregar la key); (2) `buildInspections` toma `$productId`
  como 2º arg, no un literal `'auto'`; (3) `Risk` no tiene columna `patente` → el dedup la resuelve vía
  relación. Sección "Próximo paso" del ROADMAP actualizada (citaba Pieza E como pendiente, ya ✅).
  **Solo documentación — cero código tocado.**
- **2026-06-07** — **Fase 5 arrancada — re-scope + shape de emisión verificado live. 🚧**
  - **Re-scope (decisión del usuario): Fase 5 = SOLO la API de Visred.** Checkout (captura de titular) y cartera (persistencia en `polizas`) son **scopes distintos → deuda** (no mezclar). Detalle en la sección "Deuda fuera de scope" arriba.
  - **Shape de emisión confirmado contra el sandbox** (smoke **read-only**: `GET /v1/schema/` vía `VisredClient` por tinker, sin emitir nada, sin secretos; archivos temporales borrados). OpenAPI live (62 schemas): `PreSaleVehicleRequest` (req `person_holder`+`quotation_result_id`+`vehicle`), `emitir/`→`200 TaskItem` (una task, **no** TaskList)→poll→`APIBasePreSaleResultDTO`; inspección `{inspections:[{id, document_base64}]}` (base64, **no** URLs); `documents/` síncrono. Filas agregadas a "Hechos verificados".
  - **3 gaps nuevos del schema** (van a deuda de checkout): `phone_prefix`+`phone_number` **ambos requeridos** (hoy un solo `telefono`), `nombre`→`first_name`/`last_name`, y `birthdate`/`sex_id`/`tax_condition_id` no capturados.
  - **Persistencia de emisión: marco confirmado y alineado con doc 10 §4/§5/§9.2.** Verificado contra el transcript de la sesión "Visred API integration plan" (2026-06-07): *"referencia mínima + on-demand, la compañía es system of record"*; *"`Risk` = identidad + estado actual; la historia comercial vive en la cadena de `risk_snapshots`"* (→ la referencia **NO** va en `risk_snapshot`); endosos = dominio de la compañía, no los modelamos. El binario repurpose `polizas` vs `policy_refs` sigue abierto → deuda scope cartera (coordinar mango-mobile).
  - **Plan por piezas (solo API Visred):** A — persistir `quotation_result_id` + `require_inspection_before_emission` por-alternativa (paga la deuda diferida de Fase 3/4: fila por alt en `quote_provider_refs`). B — puerto `EmissionProvider` + `VisredEmissionProvider` (+ `StubEmissionProvider` en `TestCase`, bind directo). E — adapter de inspección (R2→base64, mapeo `photo_key→inspection_type_id`, endpoints before/after). Orquestación (parte de D) arma el `EmissionRequest` y llama al puerto; persistencia final detrás del puerto. **Documento `download-poliza` diferido** (decisión `hallazgos-visred-task-type.md`). Orden A → B → (C deuda ∥) → D → E.
  - **Pieza E ✅ (mecanismo + after-emisión)** — `VisredInspectionService`: `requiredTypeIds()` (tipos en vivo por compañía), `buildInspections()` (R2→base64 + mapeo `photo_key→inspection_type_id`, manda solo lo que hay foto, loggea gaps), `submitPostEmission()` (`POST emitir/{presale}/inspeccion/`). Mapa **verificado live** en 6 compañías (`config/visred.inspection_photo_map`). Puerto `EmissionProvider` extendido (`uploadInspection()` + `company_id` en el resultado); `VisredEmissionProvider` delega; stub no-op. `PolizaEmisionService` dispara la inspección **after-emisión** best-effort (un fallo no relanza → no doble emisión). Tests `VisredInspectionServiceTest` (4) + 2 de orquestación. **Pendiente (D4.1):** inspección **before-emisión** necesita `company_id` pre-emit → persistir `company_id` por-alternativa. Suite **326/326**, PHPStan **0**, Pint.
  - **Pieza D ✅** — `PolizaEmisionService` (ya no skeleton) orquesta: resuelve la alternativa elegida + su `quotation_result_id` (Pieza A), arma el **request neutro** desde `CheckoutSession`+snapshot (holder/address/vehicle/payment; el pago parsea `cc_expiry` MM/YY), llama a `EmissionProvider::emit()` y persiste el estado propio del `Quote` (`status='poliza_emitida'` + identificadores en `metadata`). Falla con excepción si no hay ref o si la emisión no devuelve `presale_id` → retry de `EmitirPoliza` (sin cambios en el job). **Deudas marcadas en código:** titular incompleto (scope checkout) y materialización de la referencia en `Risk`/`polizas` (scope cartera, TODO → doc 10 §5/§9.2). Tests `tests/Feature/Services/PolizaEmisionServiceTest.php` (4: emite+stash, request neutro vía spy, sin ref→throw, FAILURE→throw sin marcar emitida). Anotar `Quote::riskSnapshot()`/`RiskSnapshot::vehicle()`/`QuoteAlternative::providerRef()` con genéricos bajó más la baseline de PHPStan. Suite **320/320**, PHPStan **0**, Pint.
  - **Pieza B ✅** — puerto `EmissionProvider` (`emit(array): array`, espeja `QuotationProvider`) + `VisredEmissionProvider` (`emitir/`→`TaskItem`→polling acotado de la única task→`APIBasePreSaleResultDTO` neutro). Request neutro→`PreSaleVehicleRequest`: defaults de catálogo (`person_type_id`→física, `document_type_id`→DNI), aplanado del pago (`{method,card,cbu}`→`credit_card_*`/`bank_cbu`), inspecciones `{type_id,image_base64}`→`{id,document_base64}`. `StubEmissionProvider` en `TestCase`, bind directo en `AppServiceProvider`. Tests `tests/Feature/Services/Visred/VisredEmissionProviderTest.php` (7: emit+parse, mapeo del request, flag post-emisión, inspecciones, budget, FAILURE, `VisredApiException`). Suite **316/316**, PHPStan **0**, Pint.
  - **Pieza A ✅** — `quote_alternative_provider_refs` (tabla per-alternativa, convención `*_provider_refs`): `external_quote_id` (quotation_result_id) + `requires_inspection_before_emission` por alt, vía `QuoteAlternative::providerRef()`. `saveResults` zipea las alts creadas con `parsed_alternatives` y persiste la ref de cada una; `quote_provider_refs` (auditoría per-quote) intacto. El adapter emite el flag desde `require_inspection_before_emission`. Bonus: anotar `Quote::alternatives()` con `HasMany<QuoteAlternative,$this>` arregló 30 errores baselineados de PHPStan (143→113). Tests: `tests/Feature/Repositories/QuoteRepositoryTest.php` (5). Suite **309/309**, PHPStan **0**, Pint.
- **2026-06-07** — **Fase 4 implementada (bind directo a Visred + E2E real). ✅** Suite **304/304**, PHPStan **0**, Pint.
  - **Mock eliminado (reversión de decisión).** Se borró `app/Services/QuotingEngine.php` (700 líneas, catálogo hardcodeado). Esto **revierte** "el mock no se borra" de [`PLAN.md`](PLAN.md): el mock era andamiaje hasta tener la API; ya está la API → mantenerlo era un punto débil de cara a producción (decisión del usuario). `QuotationProvider`→`VisredQuotationProvider` **bind directo** en `AppServiceProvider` (real-always, mismo criterio que `Quotability`; **sin** selector por config). En tests, `TestCase` bindea `StubQuotationProvider` (espejo de `StubQuotability`).
  - **Sin selector de config.** Se eliminaron `quotation_provider` y `emission_provider` de `config/visred.php` + `.env.example` (config muerta: nadie las leía). Visred es **EL** proveedor, implícito. **Futuro:** el día que entre un 2º proveedor → una sola clave `INSURANCE_PROVIDER` (array), gobernando cotización **y** emisión juntas (no se pueden mezclar). YAGNI hasta entonces.
  - **Shape real confirmado contra el sandbox** (smoke live): `GET /v1/tasks/{id}/` → `result.company_id` (slug) + `result.quotation_results[]` (NO `company`/`covers` como se había asumido). `flatten`/`mapCover` reescritos. El **nombre de compañía** se resuelve por `company_id` vía `GET /v1/discovery/companies/` (`rus`→"Rio Uruguay") — **consultado en vivo en cada cotización, sin cache** (el listado cambia poco y una llamada extra es despreciable vs cotizar+polling; sin el refresco "al pedo" de un TTL fijo). La traducción id→nombre vive solo en el adapter.
  - **Filtro de coberturas:** Visred devuelve filas placeholder (`cover.id`/`name` vacíos) e ítems discontinuados (`active=false`). `mapCover` descarta lo no presentable: solo `cover.name` presente **y** `active===true`.
  - **Limitación conocida:** `normalized_grade` es léxico (sobre `cover.id`+`name`); códigos de compañía como `Sigma`/`C1-80`/`B80` caen a `basic` por default. Best-effort en Fase 4; mejora (mapa por `cover.id`) es su propio tema.
  - **Pendiente río abajo (no Fase 4):** `company_id` de Visred (`rus`) ≠ slug de `coverage_documents` (`rio-uruguay`) → el RAG de coberturas no encontraría docs. Revisar al tocar el coverage-check.
  - **Smoke E2E real** (tinker, sin secretos): catálogo→version_id→cotizar→polling de 13 compañías a terminal en **13.8s** (7 SUCCESS), `company_id=triunfo` resuelto a "Triunfo", 4 presentables. Confirma el chain live extremo a extremo.
  - **Tests:** fixtures de los 5 tests herméticos reabiertos al shape real (+ `companiesResponse()`); +1 test de filtro placeholder/inactivas; +1 E2E hermético (`ApiQuoteResolution`→`saveResults`→alternativas + `quote_provider_refs`). `quotation_result_id` por-alternativa sigue **diferido a Fase 5** (cada alt en `external_quote_id`).
- **2026-06-07** — **Fases 3 + 6 implementadas (resolver de quotability + adapter de cotización Visred). ✅** Suite **302/302**, PHPStan **0**, Pint. Diseño **aplanado** respecto del handoff (a pedido del usuario: "exceso de ingeniería, más llano"):
  - **El canal NO conoce el catálogo.** `identify_vehicle` depende de un puerto agnóstico **`Quotability::check(Vehicle): QuotabilityResult`** y solo lee el tri-estado. Se descartó el puerto separado `VehicleCatalogResolver` + VOs (`VehicleQuery`/`CandidateVersion`): catálogo + desambiguación viven **adentro** de `VisredQuotabilityResolver` (un solo proveedor, YAGNI; el Tier 2 `DisambiguationAgent` queda extraíble si entra un 2º).
  - **Tri-estado** `QuotabilityResult`: `Quotable(resolvedVersion, provider, externalRef)` / `NeedsFact(missingFact, options)` / `NotQuotable`. El `provider`/`externalRef` (token) es opaco: el adapter lo usa SOLO para persistir; **nunca** entra a `ai_state` ni a un mensaje (test lo verifica).
  - **`VisredQuotabilityResolver`**: árbol de 5 GETs (rutas singulares, shapes heterogéneos normalizados a {ref,label}), cascada Tier 1a igualdad exacta (candado del re-cotizar) → Tier 1b subconjunto léxico → Tier 2 LLM (`DisambiguationAgent`, parsea `version_name`, fakeable). Beam por groups (`matchAllRefs`). Error de Visred → rama honesta `NotQuotable`. Refina `Vehicle.version` (dominio) en Resolved.
  - **Gate en `WhatsAppAdapter::identifyVehicle`** (capa de canal, sirve WhatsApp + web-chat): `Quotable`→crea Quote + persiste token en `risk_provider_refs`(snapshot, provider, ref); `NeedsFact`→pide el hecho de dominio, sin prometer; `NotQuotable`→rama honesta. `vehicle_identified` (identidad) sigue siendo NLU, separado de quotability.
  - **`risk_provider_refs`** (migración nueva, genérica: `risk_snapshot_id`+`provider`+`external_vehicle_ref`, unique). Relación genérica `RiskSnapshot::providerRefs()`. **Sin** columna de proveedor en el dominio.
  - **`VisredQuotationProvider`**: lee el token, `cotizar/`→polling acotado (`Sleep`, tolerante a FAILURE parcial y budget agotado), aplana company→covers, `normalized_grade` mapeado solo acá. `generateAlternatives(RiskSnapshot): array` **sin cambio de firma**.
  - **Decisión: "resolver real siempre" en producción** (bind `Quotability→VisredQuotabilityResolver` en `AppServiceProvider`, independiente del seam de cotización que sigue en `QuotingEngine`). En tests, `TestCase` bindea por defecto `StubQuotability` (quotable, sin red); los tests de quotability usan el resolver real con `Http::fake` + `DisambiguationAgent::fake`.
  - **Diferido a Fase 5:** persistencia del `quotation_result_id` por-alternativa (lo consume la emisión). En Fase 3 cada alternativa lo lleva en `external_quote_id`; `saveResults`/`quote_provider_refs` intactos.
  - **NLU intocable** (no se tocó `VehicleIdentifierAgent`/`IdentifyVehicleTool`/`VehicleIdentificationService`). **Seam de cotización NO flipeado** (eso es Fase 4).
- **2026-06-07** — **Diseño del `VehicleCatalogResolver` (Fase 6) mayormente cerrado (sin código).** Sesión de arquitectura sobre [`RESOLVER-DESIGN.md`](RESOLVER-DESIGN.md). Decisiones:
  - **Hogar + trigger:** la resolución corre en **identify-vehicle, con el cliente presente**, pero el **NLU no llama al resolver** — lo dispara la **capa de canal** vía una app action agnóstica `ResolveVehicleCatalogRef`. Dirección `canal → action → resolver → dominio`; la flecha prohibida `VehicleIdentifier* → resolver` se respeta. (cierra sub-temas #1/#2)
  - **Gate por *quotability* agnóstica, no por el token:** avanzar sin match contra el proveedor genera **promesa rota** ("dame un momento que cotizo…" que nunca pasa). Identify-vehicle **sí resuelve contra proveedores** antes de avanzar, pero el gate cuelga del **tri-estado agnóstico** (Resolved→avanza / Ambiguous→pide hecho de dominio / NotFound→rama honesta, sin promesa falsa). `vehicle_identified` (identity) ≠ quotability; el nombre del proveedor y el `version_id` **nunca** entran a `ai_state`. La ambigüedad se reencuadra como **hecho de dominio faltante** (p.ej. transmisión), no como token.
  - **Cascada de 3 tiers:** exacto/léxico → LLM → preguntar al cliente. Por nivel: marca/año Tier 1; modelo exacto-normalizado + LLM de fallback (**embedding proscripto como árbitro** en numéricos `208`/`2008`, pero el LLM sí los diferencia); versión es el laburo real. Modelo↔trim entrelazados → navegación **beam** (una sola pregunta al cliente).
  - **Catálogo Visred verificado live:** árbol de 5 GETs (`vehicle-types→brands→years→groups→versions`), año **estructural**. **Shapes heterogéneos** entre niveles (08 estaba mal con `BaseParam[]` para todos) → normalizar a `{ref,label}`. **Versiones SIN atributos estructurados** → parsear `version_name` (motor/trim/transmisión). Backing store **C+A** (B opcional como cache del árbol, que cambia lento).
  - **Fixtures:** se descartó bakear el catálogo como JSON (dato vivo, tokens rotables). Si hace falta, sample mínimo de shape; drift vía smoke/contract test. Se borró el dir `tests/Fixtures/Visred/catalog/` que se había empezado.
  - **Artefactos:** [`RESOLVER-DESIGN.md`](RESOLVER-DESIGN.md) §7–§10 nuevas + §0/§5 actualizadas. **Cero código.** Desbloquea Fase 3 una vez implementado el resolver.
- **2026-06-07** — **Fase 3 frenada para corrección de arquitectura (sin código).** Al arrancar, la migración `risk_snapshots.visred_version_id` se rechazó: acopla el dominio (`RiskSnapshot`) a Visred (viola el modelo agnóstico / ADR-001). Decisiones de la sesión:
  - **El `version_id` NO es atributo de dominio** — es un token opaco del catálogo del proveedor, propiedad de `(auto, año, proveedor)`, necesario **solo en quote-time**. Se resuelve detrás de un puerto agnóstico **`VehicleCatalogResolver`**; el adapter de cotización lo lee **ya resuelto** del backing store del resolver. La firma `generateAlternatives(RiskSnapshot): array` no cambia. **Sin migración sobre `risk_snapshots`.** El stub de migración creado por error se borró.
  - **Frontera dura:** el stack de identificación del chat (`VehicleIdentifierAgent`/`IdentifyVehicleTool`/`VehicleIdentificationService`) es **solo NLU, intocable**; el resolver es un componente **separado, agnóstico de canal** (depende solo del dominio, nunca del chat/orquestador). Se rompió esa frontera dos veces en la discusión → se blindó con una **regla general de desacople en `CLAUDE.md`** (no solo para esas 3 clases).
  - **Resolver = 2 responsabilidades separadas:** consulta de catálogo (determinística, por proveedor) + desambiguación (LLM, agnóstica). *Un desambiguador, N adaptadores.* La versión real desambiguada ("Active VTI") **sí es dominio** (`risk_snapshots.version`); solo el token opaco se aísla.
  - **Backing store ABIERTO:** A (`risk_provider_refs`, por snapshot = la decisión) vs B (`provider_vehicle_versions`, clave natural = cache del catálogo); no excluyentes. Lean del usuario: **C+A**, sin cerrar.
  - **Artefactos:** [`RESOLVER-DESIGN.md`](RESOLVER-DESIGN.md) (handoff del resolver), correcciones en [`PLAN.md`](PLAN.md) (Fase 3/6) y [`../10-…`](../10-modelo-dominio-cotizacion-emision.md) §8/§9, regla en `CLAUDE.md`. **Cero código de Fase 3 escrito.**
- **2026-06-07** — **Fase 2: `VisredClient` (HTTP + JWT) ✅.** Cliente agnóstico de proveedor que espeja el patrón de `WhatsAppOutboundService` (`Http::acceptJson()->timeout()->retry()`):
  - `app/Services/Visred/VisredClient.php` — `get()`/`post()` autenticados; login (`POST /v1/accounts/token/`) + refresh (`/refresh/`) con tokens cacheados server-side (Cache, no en estado de instancia → sirve transient); refresh-on-401 (refresca y reintenta **una** vez; si el refresh falla, re-login con credenciales de servicio); `Authorization: Bearer` + `Accept: application/json` en cada request; retry transitorio (red/5xx/429, **no** 4xx) vía `when`/`throw:false`; `X-Mock-Scenario` adjuntado **solo** si `config('visred.sandbox')`.
  - `app/Exceptions/Visred/VisredApiException.php` — normaliza el envelope `{success:false,error:{message,code,field_errors}}` a una excepción tipada (`status()`/`errorCode()`/`fieldErrors()`); capa anticorrupción (ADR-001): el dominio nunca la ve. Falla de red → `503`/`external_service_unavailable`.
  - `config/visred.php` (espeja `config/whatsapp.php`): `quotation_provider`/`emission_provider` (`mock` default), `base_url`, `username`/`password`, `timeout`, `poll_budget`/`poll_interval`, `sandbox`. Claves `VISRED_*`/`MANGO_*` agregadas a `.env.example` (placeholders, sin secretos).
  - **Tests herméticos** `tests/Feature/Services/Visred/VisredClientTest.php` (14) + fixtures `tests/Fixtures/Visred/` (login, refresh, 401→refresh→reintento, re-login, cada código 400/401/403/404/409/503, field_errors, gating de `X-Mock-Scenario`, falla de red).
  - **Smoke-test REAL** del `VisredClient` contra el sandbox (login + refresh) vía `tinker`: `login_ok access_len=232 refresh_present=yes refreshed_access_len=232` (sin imprimir secretos). Confirma que login y refresh funcionan extremo a extremo a través del cliente.
  - Cierre: Pint · PHPStan **0 errores** (level 5) · suite **288/288** (882 assertions). **NO** se tocó el seam de selección (sigue bind a `QuotingEngine`) — eso es Fase 4.
- **2026-06-07** — **Smoke-test de auth real ✅.** Tras corregir el base URL a `https://sandbox-api.visred.com.ar` (el host `sandbox.visred.com` no resolvía), `POST /v1/accounts/token/` con las credenciales de `.env` devolvió `200` con `access` + `refresh`. Confirma credenciales válidas, base URL y endpoint. Credenciales en `.env` (no commiteadas). Egress de red OK.
- **2026-06-06** — **Fase 1: puerto `QuotationProvider` extraído** (refactor sin cambio de comportamiento). `app/Contracts/QuotationProvider.php`; `QuotingEngine implements`; `ApiQuoteResolution` depende del puerto; bind `→ QuotingEngine` (mock, default) en `AppServiceProvider`. PHPStan 0 · Pint · suite 274/274 (845 assertions).
- **2026-06-06** — **Fase 0: modelo de dominio (gate) escrito y aprobado.** `docs/v2/10-modelo-dominio-cotizacion-emision.md`: modelo agnóstico de proveedor, límites de contexto (compañía = system of record), referencia mínima + on-demand, `Risk` sin versionar, repurpose de `polizas`. Aprobado por el usuario.

---

## Próximo paso

**Plan de cierre de la deuda (D1–D6): ver [`CLOSURE-PLAN.md`](CLOSURE-PLAN.md).** La maquinaria Visred
está completa y verde (Pieza E ✅ incluida — mecanismo before/after, ver bitácora 2026-06-07). Lo que
resta es **saldar la deuda** para emitir end-to-end y materializar en cartera, ordenado por workstream:

| Orden | Workstream | Cubre | Estado | Bloqueante |
|---|---|---|---|---|
| 1 | **WS-A** — cierre wiring Visred | D4.1 (before-emisión: `company_id` por-alt) + D4.2 (idempotencia) | ✅ **cerrado** (2026-06-08) | — |
| 2 | **WS-B** — checkout completo | D1 (titular) + D2 (fotos GNC/velocímetro) | ✅ **cerrado** (2026-06-08) | — |
| 3 | **WS-C** — cartera / materialización | D3 (`Risk` + referencia de póliza) | ✅ **cerrado** (2026-06-08) | — |
| 4 | **WS-D** — verificación live | D6 (emit E2E real) | ✅ **emit real OK** (2026-06-08) | — |
| — | D5 — documento `download-poliza` | diferido | ⏸️ | decisión de producto |
| — | D7 — `fuel_type_id` cotización | ~~hallazgo D6~~ | ❌ descartado (falsa alarma); luego se mejoró el mapeo de combustible | — |
| — | D8 — descuento (`discount_id`) | hallazgo D6 | ✅ cerrado (cap-aware, verificado live) | — |

**Fase 5 ✅** (WS-A), **checkout completo ✅** (WS-B), **materialización en cartera ✅** (WS-C, sin el
stash acoplado) y **emit E2E real verificado ✅** (WS-D/D6 — pre-venta real `presale_id 32092`). El plan
de cierre (D1–D4, D6, D8) está **completo**. Queda solo **D5** (documento, diferido por producto).
**D7 se descartó** (falsa alarma: `sin-gnc` es válido; luego se mejoró el mapeo de combustible al id real).
**D8 cerrado** (descuento cap-aware **por-compañía**, verificado live; hoy `triunfo → 20%`).
**Pendientes operativos/comerciales:** avisar a mango-mobile del repurpose de `polizas`; **consultar a
Visred** por los topes de descuento por-compañía (no hay endpoint; solo conocemos Triunfo=20%).

Ver [`CLOSURE-PLAN.md`](CLOSURE-PLAN.md) (detalle por pieza + precisiones verificadas contra el código),
[`RESOLVER-DESIGN.md`](RESOLVER-DESIGN.md) §7–§10, [`../08-visred-quote-adapter.md`](../08-visred-quote-adapter.md) §§2/3/4/5.
