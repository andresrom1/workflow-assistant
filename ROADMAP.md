# workflow-assistant — Roadmap de implementación

> **Fuente de verdad de "dónde estamos"** en el backend. Trackea ejecución, no decisiones de producto.
> El detalle de arquitectura vive en [`docs/v2/`](docs/README.md). La app cliente tiene su propio roadmap en `mango-mobile/ROADMAP.md`.
>
> **Protocolo de mantenimiento (Claude):** cada vez que hagamos un cambio, actualizar este archivo en el mismo turno —
> mover el estado de la fase, completar fecha + commit/PR, y agregar una línea a la **Bitácora**. Si un cambio
> abre o salda deuda, reflejarlo en la tabla de deuda.

**Leyenda:** ⬜ Sin empezar · 🚧 En progreso · ✅ Completa · ⏸️ Bloqueada · ❌ Descartada

---

## Estado actual (snapshot)

### Base ya construida (v1 → operativa, mayormente con mocks)

| Área | Estado | Nota |
|------|--------|------|
| Flujo de cotización (chat web ChatKit) | ❌ | **Deprecado (2026-07-07)** — OpenAI discontinúa Agent Builder (sunset ≤ 30-nov-2026). El adapter `OpenAI\AgentToolAdapter` sigue en el código pero sin continuidad; **WhatsApp es el único canal activo**. Reverb retirado. Ver Bitácora 2026-07-07. |
| Flujo de cotización (WhatsApp) | ✅ | Orquestador + sub-agentes, pipeline de inbox de 3 etapas. Adapter `AIProviders\WhatsAppAdapter`. Incluye retroceso de etapa (`revert_to_stage`), takeover humano, botones interactivos en el cierre, seguimiento de estancadas y cierre del gate de desambiguación de versión (`provide_vehicle_fact`) — ver Bitácora 2026-07-06. Post-checkout (gracias + documentación por WhatsApp) y guía de siniestros (`siniestro_guidance`, sin notificar al PAS) — ver Bitácora 2026-07-19. |
| Integración WhatsApp Cloud API | ✅ | Webhooks in, `WhatsAppOutboundService`, media STT/TTS, idempotencia por `wamid`. |
| Motor de cotización | 🚧 | `QuotingEngine` **es mock** (`sleep(30)` + catálogo simulado). Seam para Visred. |
| RAG coberturas (pgvector) | ✅ | Dos agentes (frontal + experto), `ChunkAndEmbedService`, `SearchCompanyDocumentationTool`. |
| Checkout web + fotos R2 | ✅ | Token opaco, 7 fotos inspección, tarjeta cifrada, auditoría admin. |
| Vista pública de cotizaciones | ✅ | `/cotizaciones/{token16}`, sin indexar y sin PII. El link le llega al cliente en un mensaje de WhatsApp aparte y el CTA "La quiero" abre el checkout directo. Muestra los grados de **las dos** presentadas: cuando difieren y una contiene a la otra, la comparación se arma como escalón. El grado nunca se imprime (`normalized_grade` es el `default` del match del adapter). `/{token}/B` sirve la vista anterior, temporal. Estado en [`docs/comparador-coberturas-pendientes.md`](docs/comparador-coberturas-pendientes.md). Ver Bitácora 2026-08-07. |
| API Mobile (mango-mobile) | ✅ | auth, pólizas, siniestro, emergencia, shared-risks. Guard `auth:mobile`. |
| Panel admin / observabilidad | ✅ | Conversaciones, prompts versionados, analytics funnel, Studio, settings, usuarios. |
| Aislamiento de datos de proveedor | ✅ | ADR-001 — `quote_provider_refs`. Vigente. |
| Documentación v2 | ✅ | `docs/v2/` canónico + índice `docs/README.md` (2026-06-02). |
| Landing pública de marca | ✅ | `/` → `Landing/Index.vue` (brandbook MANGO): modelo invertido, features de mango-mobile, PAS. CTAs por config (`whatsapp.public_number` / `MANGO_APP_DOWNLOAD_URL`). |
| Facturación de comisiones (AFIP, Factura C) | ✅ | Módulo aislado solo-admin (`/admin/facturacion`). AFIP local (WSAA+WSFEv1 por SOAP sobre HTTP, sin ext-soap ni terceros). Lote → **un job por comprobante encadenado** y serializado por pto de venta → CAE → PDF con QR (RG 4892) → ZIP. Reserva de número + reconciliación por `FECompConsultar` para que un crash no duplique comprobantes; lote reanudable desde la UI. Ver Bitácora 2026-07-05 y 2026-08-04. |

---

## Cirugía v2 — forward roadmap

> Objetivo: reemplazar el rol de `pas_mobile`/`pas-web` (cotización manual + fallback) por **Visred**
> (cotiza y emite sin intervención humana), y retirar lo legacy. Diseño base en
> [`docs/v2/08-visred-quote-adapter.md`](docs/v2/08-visred-quote-adapter.md).

| Fase | Tema | Estado | Detalle |
|------|------|--------|---------|
| V2-0 | Reorden + doc de arquitectura | ✅ | docs/v2 + ROADMAP (2026-06-02). |
| V2-M | Modelo de dominio (agnóstico) — **gate** | ✅ | `docs/v2/10` (2026-06-06). Límites de contexto (compañía = system of record), puertos `QuotationProvider`/`EmissionProvider`, referencia mínima + on-demand, `Risk` sin versionar, repurpose de `polizas`. **Aprobado por el usuario** (2026-06-06). |
| V2-1 | Extraer `QuotationProvider` | ✅ | `app/Contracts/QuotationProvider.php`; `QuotingEngine implements QuotationProvider`; `ApiQuoteResolution` depende del puerto; bind `→ QuotingEngine` (mock, default) en `AppServiceProvider`. Sin cambio de comportamiento. PHPStan 0, suite 274/274 (2026-06-06). |
| V2-2 | `VisredClient` (HTTP + JWT) | ✅ | `app/Services/Visred/VisredClient.php` + `app/Exceptions/Visred/VisredApiException.php` + `config/visred.php`. login/refresh cacheado (Cache server-side), refresh-on-401 (1 reintento → re-login si falla), Bearer+Accept, retry transitorio, `X-Mock-Scenario` solo sandbox, envelope de error normalizado. 14 tests `Http::fake` + smoke-test live (login+refresh). PHPStan 0 · suite 288/288 (2026-06-07). Detalle en `docs/v2/visred-integration/`. |
| V2-3 | `VisredQuotationProvider` + gate de quotability | ✅ | **Cotización:** `VisredQuotationProvider` lee el token de `risk_provider_refs`, `cotizar/`→polling acotado (tolerante a parcial/budget), aplana company→covers→`parsed_alternatives`, `cover`→`normalized_grade` (solo en el adapter). Firma `generateAlternatives` intacta. **Gate (resolver real):** puerto agnóstico `Quotability` + `VisredQuotabilityResolver` (árbol de 5 GETs + desambiguación Tier 1 léxico / Tier 2 `DisambiguationAgent` LLM) corre en identify-vehicle; tri-estado `QuotabilityResult`; refina `Vehicle.version` + token → tabla genérica `risk_provider_refs`. NLU intocable. **`quotation_result_id` por-alternativa diferido a V2-5.** PHPStan 0 · suite **302/302** (2026-06-07). Detalle en `docs/v2/visred-integration/`. |
| V2-4 | Config seam (`QUOTATION_PROVIDER`) | ⬜ **próxima** | Bind condicional `mock`/`visred` de `QuotationProvider` en `AppServiceProvider` (lee `config('visred.quotation_provider')`, espeja `WhatsAppDispatcher`). Flip por env + E2E real contra sandbox. El gate de quotability **ya** corre real. |
| V2-5 | Emisión Visred | ⬜ | `PolizaEmisionService` (hoy skeleton) → emisión real (`emitir/` → polling → `presale_id`/`policy_number`) + descarga de documento. |
| V2-6 | Retiro de legacy PAS | ✅ | **pas_mobile extirpado** (rama `prepare-proyect-for-API-refactor`): `MobileAppQuoteResolution`, `QuoteOfferedToPas`, `FallbackToApiListener`, `QuoteWebhookController` + ruta `/webhooks/quote-update`, `MobileSyncLog` + columnas `mobile_*` de `quotes`, settings/config `mobile_app.*`, badges `offered_pas`/`rejected_pas`, y stub `n8n-whatsapp/`. `QuoteService` colapsado a estrategia única (`api`). Puente WhatsApp real = `WhatsAppWebhookController` (Cloud API nativa, sin n8n). **pas-web también extirpado:** auditado cero huella en código/config/`.env` de `workflow-assistant` (ya estaba desacoplado de facto); queda solo en docs marcado `legacy, extirpado`. ADR-001 (docs/v1, histórico) se conserva intacta — el invariante `quote_provider_refs` sigue vigente para Visred. **Residuo final retirado (2026-06-03):** job `CheckQuoteAcceptance` (watchdog de timeout que originalmente esperaba la aceptación del PAS) + setting `pas.opportunity_timeout_minutes`. La resolución sincrónica vía `coveragePreference → resolveQuote` se conserva. |
| V2-7 | Deuda WhatsApp dispatch | ✅ | **Código completo** (2026-06-05). Puerto `WhatsAppDispatcher` (seam por `whatsapp.dispatch_driver`: `cloud` vía Job `SendWhatsAppTemplate` / `log` no-op). `WhatsAppOutboundService::sendTemplate()`. Los 3 TODO cableados (siniestro al PAS, emergencia Estado 1 con Google Maps, Estado 2 con `tracking_url` — nunca el `update_secret`). Tests en `EmergencyDispatchTest`. **Go-live pendiente de MANGO:** aprobar los 3 templates en Meta + setear nombres reales y `WHATSAPP_DISPATCH_DRIVER=cloud` en `.env` (hoy default `log`). |

---

## Deuda técnica abierta

| Deuda | Ubicación | Estado |
|-------|-----------|--------|
| Dispatch WhatsApp — Siniestro → PAS | `Mobile/SiniestroController.php` | ✅ cableado (V2-7). Pendiente template Meta + `driver=cloud`. |
| Dispatch WhatsApp — Emergencia Estado 1 | `Mobile/EmergencyController.php` | ✅ cableado (V2-7). Pendiente template Meta + `driver=cloud`. |
| Dispatch WhatsApp — Emergencia Estado 2 | `Mobile/EmergencyController.php` | ✅ cableado (V2-7). Pendiente template Meta + `driver=cloud`. |
| `QuotingEngine` mockeado (`sleep(30)`) | `Services/QuotingEngine.php` | 🚧 se resuelve en V2-1→V2-4 |
| `PolizaEmisionService` skeleton | `Services/PolizaEmisionService.php` | ⬜ se resuelve en V2-5 |
| Prompt `.md` de `CoveragePreferenceAgent` desincronizado del `tool_output` | `resources/prompts/agents/CoveragePreferenceAgent.md` | ⬜ la sección "Output Format → Transición" prohíbe cerrar prometiendo el aviso y describe el mensaje fijo `quote_wait_notice`, eliminado el 2026-08-22; `WhatsAppAdapter::PEDIDO_DE_AVISO` pide lo contrario. Falta verificar si la versión activa en `agent_prompts` también quedó vieja. Detectado 2026-09-02 |
| Presupuesto de lock de `EmitInvoice` | `Jobs/EmitInvoice.php` | ⬜ espera el lock `afip-ptovta:{n}` hasta 20s (2 × 10s) pero el lock de un job muerto dura 180s: el segundo comprobante se queda sin intentos antes de entrar. Lo cubre la reconciliación por `FECompConsultar`, por eso no es urgente. Detectado 2026-09-02 |
| **Gestión manual de documentos de póliza (admin panel)** | `PolicyDocumentController` + `PolicyDocuments/{Index,Show}.vue` | ✅ resuelto 2026-06-15 — buscador de pólizas → gestor de documentos (subir PDF/imagen `source=admin_upload`, togglear `visible_to_client`, eliminar con borrado en R2). Ver Bitácora |
| **CRUD de clientes y pólizas (admin panel)** | `ConversationController` (ex-`CustomerController`) + `PolizaController` + `PolizaService` + `Conversations/{Create,Edit}.vue` + `Polizas/{Index,Create,Edit,PolizaFields}.vue` | ✅ resuelto 2026-06-15 — alta/edición/baja de clientes (soft-delete bloqueado si hay póliza vigente) y de pólizas (sección propia "Pólizas" + atajo desde el cliente; Risk reusar-o-crear; constraint "una vigente por Risk" en `PolizaService`). Renombrada la vista a "Conversaciones" el 2026-06-16. Ver Bitácora |
| Credenciales sandbox Visred | — externo | ✅ resueltas (2026-06-07) — login 200 contra `https://sandbox-api.visred.com.ar`; viven en `.env` (no commiteadas) |
| 24 tests rojos pre-existentes | `ProcessConversationInboxTest`, `ProcessMediaAttachmentTest`, `IdentifyVehicleTest`, `AI/CheckoutAgentPromptTest`, `Admin/ConversationShowTest`, `Jobs/SendWhatsAppMessageTest`, `SendWhatsAppMessageTest` | ✅ resuelto 2026-06-03 — suite 264/264 (ver Bitácora). Causa real ≠ "AI sin mock": eran mocks incompletos + dato UUID inválido + test pollution + orden no-determinista |
| **Escritura de prompts `.md` desde request HTTP** (clobber de archivos versionados) | `AgentPromptController::writePromptFile()` (`promoteDraft`/`store`) | ✅ resuelto 2026-06-03 — **Solución A**: eliminado `writePromptFile()` y sus 2 llamadas; la DB (`agent_prompts`, cacheada) queda como única fuente de verdad, el `.md` es seed/fallback. Test sin blindaje (ya no escribe disco); historia documentada en el header de `AgentPromptDraftFlowTest`. Ver Bitácora 2026-06-03 |
| **Queues — worker de IA reclamaba jobs a los 90s** (doble llamada al LLM / doble respuesta) | `run.md` (worker), `config/queue.php` (`database_ai`) | ✅ resuelto 2026-06-05 — el worker corría `queue:listen` sin conexión → usaba `database` (retry_after=90) con `--timeout=180`, anulando la conexión `database_ai` (retry_after=200) que existe para esto. Fix: worker de IA ahora bindea `database_ai`, separado del liviano. Ver Bitácora |
| **Queue `semantic-analysis` sin consumidor** (Tier 2 QA) | `AnalyzeConversationSemanticsJob`, workers | ⏸️ latente — hoy apagado (`AI_SEMANTIC_ANALYSIS_ENABLED=false`), no acumula. Es auditoría offline (panel admin), NO afecta el chat en vivo. Statu quo decidido: dejar apagado durante cirugía v2. **⚠️ Antes de encenderlo hay que revisar la arquitectura:** (1) cablear la cola `semantic-analysis` a un worker (sin esto los jobs caen en el vacío), (2) confirmar conexión `database_ai`, (3) decidir si el resultado solo alimenta el panel (QA offline) o si se rediseña para impactar el chat en vivo (orquestador reacciona a `semantic_loop`/`context_loss`) |
| **`ExtractCoverageDocumentText` (timeout=300) en cola `default`** (retry_after=90 → reclaim) | `app/Jobs/ExtractCoverageDocumentText.php` | ✅ resuelto 2026-06-05 — **Opción A aplicada**: nueva conexión `database_long` (retry_after=360 > timeout=300) + cola `documents` con worker dedicado (`queue:listen database_long`). El job ahora `onConnection('database_long')`; sacado del hot-path de WhatsApp. No corrompía pgvector (chunking borra antes de insertar) |
| **Prod: `queue:listen` a mano sin supervisor** (Hallazgo 5) | `run.md`, deploy | ⬜ `run.md` es cheatsheet de **dev**. En prod: (1) `queue:work` (no `listen` — `listen` rebootea el framework por job) con `--max-time`/`--max-jobs`, (2) bajo supervisor (supervisord/systemd) que reinicie procesos caídos. Modo de falla actual: un worker muere y su cola se frena en silencio (es lo que pasó con `semantic-analysis`). 4 conexiones/colas a supervisar: `database_ai`(whatsapp-ai), `database`(whatsapp-outbound,default), `database_media`(media), `database_long`(documents) |
| **Quotes en `pending` sin estado terminal** (abandono pre-cobertura) | `QuoteService::createPendingQuote`, enum `status` en `Quote` | ⬜ a implementar — tras borrar `CheckQuoteAcceptance` nada expira los `pending` abandonados (cliente que crea la quote pero nunca elige cobertura). **Regla decidida:** un `pending` expira (`status = 'expired'`) **al día siguiente de su creación a las 00:00hs**. Implementación futura: command programado (`schedule`) que barre `pending` con `created_at < hoy 00:00` → `expired`. Se alinea con el rediseño del ciclo de vida de la quote en V2-3 (polling `TaskList` Visred) |
| **Ramos no-vehiculares (AP, hogar/combinado, vida) sin materializar** | `PolicyReportImportService::evaluate`, `AssetType` | ⬜ el modelo `InsurableAsset`/`Risk` ya soporta el llaveo por tipo (2026-07-15), pero falta (1) mapear producto/ramo del reporte a `AssetType` — el parser ya extrae `ramo` (vive en `policy_report_rows.payload`, sin columna dedicada) — y (2) decidir la clave natural de `Person`/`Property` (ojo: el asegurado de una AP puede no ser el tomador). Hasta entonces una fila sin patente sigue siendo `Exception` en el import. Ver `docs/v3/05-modelo-insurable-asset.md` §Pendiente |
| **Consolidación de atributos del `InsurableAsset` — solo backfill, sin resolución de conflictos** | `PolicyChainResolver::backfillMissing` | ⬜ cuando dos fuentes matchean el mismo asset (misma patente) y traen valores **no vacíos distintos** para un atributo (p. ej. `modelo`), hoy se conserva el existente y se ignora el nuevo (backfill monótono: solo rellena faltantes). La corrección/ponderación por fuente + provenance + divergencias ya está modelada para `Customer` en `docs/v2/11`; falta extenderla al asset (misma máquina). Sin esto, un dato cargado mal por una fuente pobre no se puede corregir desde una de más confianza vía `resolveRisk` |
| **Prompts `coverage_preference` (upsell no bloqueante) y bloque nuevo `shared_siniestro`** | `agent_prompts` (DB) | ⬜ código + `.md` de referencia listos (ver Bitácora 2026-07-19); el contenido final se entregó en chat para carga **manual** de Andrés en `agent_prompts` + `Cache::forget`. Hasta cargarlos, `CoveragePreferenceAgent` sigue con el upsell viejo (fricción de dos preguntas) y `siniestro_guidance` no tiene bloque de instrucciones propio (los 5 agentes ya tienen la tool inyectada, pero sin el `.md` activo en DB el LLM no sabe cuándo llamarla) |
| Plantilla de reapertura (ventana 24h) para documentación de póliza que se emite tarde | `SendPolicyDocumentsToClient` | ⬜ hoy se asume que el envío de PDFs cae dentro de la ventana de 24h post-checkout; si `CapturePendingPolicyDocuments` agota los 10 reintentos (~10min) y la ventana ya cerró, el mensaje con los documentos no se puede enviar hasta que el cliente vuelva a escribir. Falta decidir si se resuelve con un template de Meta aprobado o con carga manual del admin como fallback |
| Notificación al PAS ante siniestro reportado por WhatsApp | `SiniestroGuidanceTool` | ❌ descartada explícitamente (2026-07-19) — la tool solo devuelve indicaciones + contacto del PAS al cliente; a diferencia de `Mobile/SiniestroController` (que sí notifica al PAS), acá el cliente es quien contacta. Si se reconsidera, el puerto `WhatsAppDispatcher::siniestroNoticeToPas()` ya existe y está probado |
| **Deriva prompts repo ↔ producción** | `resources/prompts/agents/*.md` vs `agent_prompts` (DB), `agent:sync-prompts`, `AI/CheckoutAgentPromptTest` | ⬜ abierto (2026-08-19, medido 2026-08-21: **8.296 caracteres de deriva**, 16.223 en el repo contra 24.519 en prod, ya con la v6 de saliencia) — `checkout_closer` en prod tiene **miles de caracteres que no están en ningún commit**: las secciones *El precio ordena NUNCA filtra*, *Cuál C+A elegir*, *El piso cuando pide terceros completo*, *Nivel superior al pedido* y *Leer `descripcion`*, cargadas por el admin el 08 y el 09-08 junto con el trabajo de `normalized_grade` (el código se comiteó, el prompt no). `customer_identifier` tiene 65 chars de deriva. **Riesgo activo: `agent:sync-prompts` carga los `.md` del repo a la DB y pisaría todo eso** — no ejecutarlo hasta resolverlo. Y `CheckoutAgentPromptTest` assertea contra el archivo, o sea contra una versión que hace días no es la que atiende clientes. Falta bajar producción al repo y re-chequear los asserts |
| **Fotos de inspección con acceso anónimo en R2** | `CheckoutController`, `CheckoutAuditController`, `Admin/ConversationController`, `CustomerController`, `SendPolicyDocumentsToClient` | ✅ resuelto en código 2026-09-03 (E3 del plan de seguridad) — ⚠️ **falta cerrar el acceso público del bucket en Cloudflare**, que es el paso que efectivamente cierra. Ver Bitácora |
| **Checkout público — cuatro brechas medidas** | `CheckoutController`, `routes/web.php`, `routes/mobile.php` | ✅ resuelto 2026-09-03 (E2 del plan de seguridad) — throttle en las tres rutas de escritura + el login móvil, fotos resueltas contra la cotización, `photo_key` con vocabulario cerrado, token fuera del log y lock con recheck. Ver Bitácora |
| **Settings — el campo secreto vacío borra el secreto** | `SettingsController::updateGroup`, `SettingsService::saveGroup` | ⬜ abierto (2026-09-02) — el `continue` corta la iteración de validación pero no saca la key de `$incoming`, que se entrega entero a `saveGroup()`; guardar el grupo con el campo secreto en blanco escribe `''`. El comentario del código afirma lo contrario. Alrededor: `index()` manda el valor del secreto al navegador (enmascarado solo en el front), `saveGroup()` escribe con `where()->update()` de query builder —no pasa por casts ni mutators, así que un cast `encrypted` no cifraría nada por ese camino— y `loadAll()` deja todos los valores una hora en la tabla `cache`. Ver `docs/plan-remediacion-...` E5 |
| **Rutas de `api.php` sin consumidor conocido** | `routes/api.php`, `ToolsController`, `QuoteService::getRaw` | ⬜ abierto (2026-09-02) — `POST /api/tools/test` (público, loguea el request entero), seis `POST /api/tools/*` "sin implementar", `POST /api/v1/quotes` (pasa `$request->all()` al servicio) y `GET /api/quotes/{quote}/raw` (devuelve `raw_response` del proveedor sin auth, con ID enumerable). Ninguna tiene llamador en `pas_mobile`, `openai-chatkit-starter-app` ni `resources/js`; falta confirmación acotada de accesos en prod antes de retirarlas. `/api/web-chat/v1/tools/*` depende de la decisión de continuidad de ChatKit (sunset Agent Builder ≤ 2026-11-30). Ver `docs/plan-remediacion-...` E4/K-01/K-02 |
| **La suite salía a la red real** | `phpunit.xml`, `tests/TestCase.php`, 4 archivos de test | ✅ resuelto 2026-09-03 (E1 del plan de seguridad) — la causa no era "falta un fake" sino que el `.env` de cada máquina decidía qué hacía la suite. Ver Bitácora |
| **Emisión de póliza inemitible por el DNI placeholder de cotización** | `VisredQuotationProvider::buildRequest`, `PolizaEmisionService`, `EmitirPoliza`, `CustomerIdentificationService`, `DeclineDniTool` | 🚧 Fases 0-4a ✅ (diagnóstico, sonda, visibilidad del fallo, checkout prudente, placeholder eliminado, gate de DNI en identificación — ver Bitácora 2026-07-19). ⬜ Fase 4b: cargar el prompt de `CustomerIdentifierAgent` en `agent_prompts` + `Cache::forget` — sin esto el agente todavía no pide el DNI |

---

## Bitácora

> Entrada por cada cambio relevante. Formato: `fecha — qué — commit/PR`.

- **2026-09-03 (3)** — **Las fotos del checkout estaban abiertas en internet, y el plan había contado la mitad de los lectores 🔓.** E3 del plan de seguridad. `uploadPhoto()` subía con visibilidad `'public'` a `checkout/{quote_id}/photos/photo_{photo_key}.jpg`: el id es secuencial y la clave sale de un catálogo cerrado de 10, así que conocer el dominio equivale a conocer **todas** las URLs. Y el dominio se entregaba — la respuesta devolvía `'url' => $storageUrl` en cada subida, un campo que **nadie usaba**: `Show.vue` guarda `public_id` y muestra un thumbnail de 64px, y el único consumo de `data.url` era un `console.log`. O sea que cualquiera que alguna vez hubiera subido una foto se llevaba la llave para recorrer la cartera entera: tarjeta verde con nombre, DNI, dominio, chasis y motor, más el auto con la patente. **La sonda no pidió la foto de nadie.** Un `HEAD` a una clave inexistente contra el host `pub-*.r2.dev` devolvió **404 y no 401**: el endpoint público está sirviendo y solo no encontró el objeto. Alcanza para actuar sin descargar PII. **El plan nombraba dos lectores y eran cuatro.** Además de la vista de auditoría y el envío de PDFs a Meta, dependían del bucket público el reproductor de audios del panel de conversaciones (`MessageAttachment`) y los documentos de la ficha del cliente (`PolicyDocument`). Cerrar el acceso cubriendo solo los dos del plan habría dejado el admin roto — que es exactamente el modo de falla que la secuencia *lectores → verificar → cerrar* existía para evitar. Los cuatro pasan a `temporaryUrl()` de 15 minutos, el patrón que el repo ya usaba en otros tres lugares. Meta baja el documento con un GET común, así que una URL firmada le sirve igual. **La conversión de datos que pedía el plan resultó innecesaria:** en vez de migrar los `photo_paths` viejos que guardan URLs, la vista de auditoría pasa a resolver desde las filas `InspectionPhoto` de la cotización — el mismo movimiento que E2 hizo con el submit. De paso arregla las etiquetas del admin: `photo_paths` se guardaba como lista y el Vue lo tipa como `Record<string, string>` y le pasa la clave a `photoLabel()`, así que ahora va como mapa y las fotos quedan rotuladas. **`inspection_photos.storage_url` es NOT NULL**, así que se sigue escribiendo aunque ya no resuelva; queda documentado en el modelo para que nadie lo use. **Lo que falta y no es código:** cerrar el acceso público del bucket en Cloudflare. Hasta que eso pase, lo ya subido sigue expuesto — el código deja de crear objetos públicos y de filtrar el dominio, pero el interruptor está del otro lado. _(1072 ✅ · PHPStan 0 ✅ · Pint ✅)_

- **2026-09-03 (2)** — **El checkout dejó de creerle al cliente 🔒.** E2 del plan de seguridad: cinco brechas medidas en el único flujo público con tráfico real, más el login móvil. **Fotos.** `submit()` armaba `photo_paths` mapeando los `photo_ids` que mandaba el navegador —validados solo como `string|max:255`—, así que se le podían colgar a un checkout archivos de otra cotización. Ahora se resuelven contra las filas `InspectionPhoto` de **esa** quote y se **rechaza** el envío si alguno es ajeno, inexistente o está repetido: filtrarlos en silencio dejaría pasar un envío con menos fotos de las que el cliente cree haber mandado. Y el mínimo se cuenta **sobre el conjunto aceptado**, no sobre el total de filas `Temp` — si no, unos paths ajenos "completaban" el mínimo sin que las fotos existieran. El mensaje de rechazo no distingue "no existe" de "no es tuya". **`photo_key`.** Era `string|max:50` y se interpolaba en el path de almacenamiento. La lista canónica ahora vive en `InspectionPhoto::CLAVES` —vocabulario del dominio, las fotos que el checkout sabe pedir— y `config/visred.php` la sigue traduciendo al catálogo del proveedor, que es donde tiene que estar. `InspectionPhotoKeysTest` verifica que el mapa del adapter no nombre una clave que el checkout no pide: si se desincronizaran, la foto nunca se subiría y el desajuste recién se vería en una emisión contra la compañía. **Token.** El `catch` de `submit()` logueaba `checkout_token` —la credencial del flujo— junto al trace completo. **Concurrencia.** El guard de estado lee antes de la transacción, así que dos envíos simultáneos lo pasaban los dos y duplicaban emisión, aviso y mail; el caso secuencial ya daba 409 y por eso nunca se vio. Ahora hay `lockForUpdate()` **adentro** de la transacción con **relectura**: un lock sin recheck no sirve, porque el valor leído antes del lock ya es viejo. Eso obligó a tocar el manejo de errores — el `catch (\Throwable)` abarca toda la transacción, así que el `abort(409)` de adentro salía como **500**; las `HttpException` esperadas se relanzan antes del manejo genérico. **El test de concurrencia no es concurrente y está dicho en el archivo:** bajo `RefreshDatabase` el test corre dentro de una transacción y una segunda conexión no ve la cotización, así que no puede competir por su fila. Lo que sí se prueba —y es la propiedad que importa— es que el recheck corta: se cambia el estado justo antes de que se ejecute el `for update`, que es exactamente lo que deja hecho el envío que gana la carrera, y se verifica que no se despacha ni `EmitirPoliza` ni `NotifyClientCheckoutCompleted`. **Throttle.** Las tres rutas de escritura del checkout no tenían límite mientras la vecina `cotizaciones.checkout` sí, y `POST /api/mobile/v1/auth/session` era la única ruta pública de mobile sin uno. Middleware inline, que es la convención del repo —no hay un solo `RateLimiter::for` en el proyecto—, con los valores marcados `// opcion-de-configuracion` y registrados en `docs/configuracion-hardcodeada.md`. **Fuera de alcance** (E3): las fotos siguen subiéndose con visibilidad pública a una ruta adivinable; eso se cierra aparte. _(1070 ✅ · PHPStan 0 ✅ · Pint ✅)_

- **2026-09-03** — **La suite mandaba WhatsApps de verdad, y el diagnóstico había visto la punta equivocada 📡.** El diagnóstico anotaba "4 tests de siniestro sin fake". La causa real no estaba en el test: `.env:134` fija `WHATSAPP_DISPATCH_DRIVER=cloud`, no existe `.env.testing` y `phpunit.xml` fija `QUEUE_CONNECTION=sync`, así que el binding de `AppServiceProvider:73` entregaba `CloudApiWhatsAppDispatcher`, el job de template corría **inline** y le pegaba a la Cloud API con las credenciales de producción, `tries = 5`. El default de `config/whatsapp.php:14` ya era `log`: **lo que hacía la suite dependía del `.env` de la máquina que la corriera.** Fijar el driver en `phpunit.xml` puso `SiniestroTest` en 6/6 sin tocar una línea del test. **Y ahí empezó lo interesante:** con `Http::preventStrayRequests()` en `TestCase::setUp()` la suite pasó de 4 a **88 fallos**, que resultaron ser dos causas y no ochenta problemas. (1) **19 clases** dependían de `public/hot` —el archivo que deja `npm run dev`— porque el gateway de Inertia usa el dev server de Vite para SSR cuando existe: la suite le pegaba a `192.168.0.12:5174/__inertia_ssr`, o sea que el resultado cambiaba según si alguien había levantado Vite en esa máquina. Se apaga con `INERTIA_SSR_ENABLED=false` (el vendor ya lee ese env). (2) **4 clases mandaban WhatsApp de verdad** —`PresentQuoteOptionsToolTest`, `ProvideVehicleFactFlowTest`, `QuotabilityGateTest`, `RevertStageFlowTest`—, el pipeline del agente completo contra `graph.facebook.com`. Ninguna de ellas estaba en el diagnóstico. **Los dos últimos fallos enseñaron algo:** venían pasando *porque* la llamada real devolvía error y el código tomaba la rama de fallo; con un `200` falso recorrieron por primera vez la rama de éxito y aparecieron dos cosas tapadas. `PresentQuoteOptionsToolTest` reventó con `SQLSTATE[25P02]` porque `messages.external_message_id` es `unique()` y el fake devolvía siempre el mismo `wamid` (arreglado con un id por llamada), y `WhatsAppCoveragePreferenceTest` devolvía `success => false` sin explicación porque `handleToolCall()` atrapa `\Throwable` y convertía la `StrayRequestException` en un fallo genérico — el mismo modo de falla silencioso que el CLAUDE.md documenta para las tools, acá aplicado a una excepción de red. **Resultado: 1060 tests, 0 fallos, y la suite bajó de 305 s a 109 s** porque dejó de esperar timeouts de red. Verificado además con una sonda descartable: un `Http::get()` sin fake ahora falla por `preventStrayRequests()`. **Fuera de alcance a propósito** (no son seguridad): el presupuesto de memoria de la suite y qué tests de `tests/Unit` arrancan Laravel. _(1060 ✅ · PHPStan 0 ✅ · Pint ✅)_

- **2026-09-02 (3)** — **El plan de remediación endurecía rutas muertas y no miraba el checkout 🎯.** La r1 (`59cb2b0`) mezclaba tres cosas sin distinguirlas: hallazgos verificados, higiene genérica y trabajo que el repo ya tenía resuelto. Consecuencia práctica: la fase "crítica", 2-4 días, protegía `/api/v1/quotes`, `/api/tools/*` y `/api/quotes/{quote}/raw` con abilities de Sanctum, compatibilidad dual y 48 h de monitoreo — **ninguna tiene llamador** en `pas_mobile`, `openai-chatkit-starter-app` ni `resources/js`; el único consumidor de `/api/web-chat/v1/tools/*` es la app ChatKit ya declarada deprecada. Mientras tanto el inventario era solo `routes/api.php`, así que **el checkout quedaba afuera**: el único flujo público con tráfico real, el que sube siete fotos y guarda `cc_pan_encrypted`. **Cuatro brechas medidas ahí** (throttle ausente en las tres rutas de escritura, `photo_ids` del cliente convertidos en URLs sin verificar pertenencia, `photo_key` libre interpolado en el path, y el `checkout_token` en el log del `catch`), y **tres cosas que la r1 proponía "agregar" y ya existían**: MIME real, tope de 10 MB y el guard de estado `409`. La revisión no fue de un solo lado. **La r1 tenía razón en algo que la primera lectura crítica dio por resuelto:** el patrón "vacío conserva el secreto" de settings está escrito en un comentario pero **no funciona** — el `continue` corta la validación y `$incoming` llega entero a `saveGroup()`, que escribe `''`. Es pérdida de datos activa, y alrededor hay tres más: el valor del secreto viaja al navegador, `saveGroup()` usa `where()->update()` (ningún cast ni mutator corre por ahí, así que cifrar por atributo no tendría efecto) y `loadAll()` deja todo una hora en la tabla `cache`. **Cuatro ítems salen del plan por estar mal diagnosticados o chocar con reglas del repo:** `DeepSeekProbe` es una sonda de consola de un intento deliberado —moverla a una cola o darle retries corrompe lo que mide—; DTOs y `JsonResource` están prohibidos por el `CLAUDE.md` raíz; "reemplazar **todos** los `$request->all()`" rompería el agente, porque 20 de las 36 ocurrencias son `Laravel\Ai\Tools\Request` y `handleToolCall()` las exige; y el objetivo de 256 MB para la suite nunca se midió. **La r2 conserva la estructura de aceptación y rollback y reemplaza las tareas genéricas por una matriz de evidencia** — *confirmado / ya resuelto / condicional / descartado*, con archivo y símbolo por ítem —, reordena las entregas (aislamiento de tests primero, después checkout, después retirar rutas) y saca CI, detección de N+1 y frontend a entregas propias: las tres tenían costo desconocido escondido dentro de otra fase. Lo *condicional* es explícito: un grep del monorepo no prueba ausencia de consumidores externos, y una migración no prueba el contenido de cada base desplegada, así que retirar rutas y cifrar settings quedan detrás de una confirmación operativa acotada, sin construir telemetría nueva. 660 → 415 líneas. **Nada de esto está ejecutado**: es plan.

- **2026-09-02 (2)** — **El estado decía presentada y el cliente no había recibido nada ☠️.** Segunda mitad de la conversación 26. El cliente contestó "ok" (#462), `CoveragePreferenceAgent` registró la cobertura correctamente y `NotifyClientQuoteReady` arrancó a las 14:41:50 con un deadline de 180 s. `QuoteAgent` tardó **17,9 s** (54.694 tokens de entrada), prendió `quote_ready` y encadenó a `CheckoutAgent`, que a las **14:44:48** ejecutó `present_quote_options` —160 s adentro del turno— y escribió `presented_at`, los ids presentados, el `public_token` y los botones. **Dos segundos después, a las 14:44:50, el alarm del job mató el proceso con el texto todavía generándose.** El job volvió a entregarse 202 s más tarde (el `retry_after` de `database_ai`), gastó nueve intentos rebotando contra el lock del job muerto —que no se suelta: vence— y cuando por fin entró, a las 14:47:00, vio `quote_ready` en true y salió por *"cotización ya enviada"*. `failed_jobs` vacío, `worker-ai-err.log` de 0 bytes, ni una línea en `laravel.log`: una muerte por `pcntl_alarm` no deja rastro en ningún lado. **La enfermedad: el estado avanza antes de la entrega y todos los caminos de reintento leen "estado avanzado" como "ya está hecho".** Cinco arreglos. (1) `presented_at` deja de sellarlo la tool y pasa a sellarlo `DespachaRespuestaDelAgente` al despachar; el guard del job mira esa marca y no `quote_ready`, y si encuentra `quote_ready` sin entrega vuelve el flag atrás y rehace el turno. (2) El presupuesto: un turno encadenado son **dos** llamadas al LLM, así que los dos jobs de `whatsapp-ai` pasan a `timeout = 400`, `database_ai.retry_after` a 450, el `expireAfter` del lock a 450 y los `tries` a 95/47 — el presupuesto de espera del lock se mide contra el **vencimiento del lock**, no contra el timeout del job, que es lo que decía mal el CLAUDE.md. (3) `CheckoutAgent` tenía `#[Timeout(360)]` **dentro de un job de 180 s**: la llamada podía esperar el doble de lo que vivía el proceso, así que el alarm ganaba siempre y el tope del SDK no llegaba a cortar nada. Baja a 300, y `WorkerConfigTest` ahora verifica que la suma de los topes de una cadena entre en el job. (4) Los pendientes de `metadata` van sellados con `pending_at` y `pullPending()` descarta los vencidos: la 26 tenía los botones `Sancor $54.2K` / `San Cristobal $78.5K` y el link público esperando para pegarse al próximo mensaje que saliera, sea cual fuera. (5) El turno encadenado loguea al empezar y al terminar — un proceso muerto por alarm no puede loguear su propia muerte, pero un "encadenando" sin su "encadenado listo" al lado hace visible el hueco. **Anotado, sin tocar:** `EmitInvoice` tiene el mismo desbalance de lock (presupuesto de 20 s contra un lock de 180 s), con la reconciliación de AFIP como red; y `RevertStageFlowTest` corría el pipeline entero —LLM real incluido— porque `identifyVehicle()` despacha `ResolveQuote` inline.

- **2026-09-02** — **Los logos de las aseguradoras reemplazan al monograma, sin ninguna caja atrás 🏷️.** El comparador y el checkout dibujaban un **círculo de color con la inicial** de la compañía (`colorDeCompania` + `charAt(0)`), un placeholder que el propio archivo declaraba como tal. Ahora va el logo real en los 10 sitios del comparador y en el header del checkout, y **el logo reemplaza al nombre en texto**: los archivos son lockups 3:1 que ya dicen "TRIUNFO SEGUROS". Sobrevive la línea secundaria (título, franquicia, cantidad de planes) y el badge *Recomendada*, que pasa a ir junto al logo. **El problema estaba en dark, y se midió en vez de suponerse.** Muestreando la tinta dominante de cada PNG contra `--mg-surface` de noir (`#161412`): San Cristóbal 1,20:1 · Mercantil 1,54:1 · RUS 1,79:1 · Experta 1,98:1 · Sancor 2,80:1 · Triunfo 2,81:1 · Galicia 3,51:1. **Los siete fallan** — son logos hechos para papel. La lectura a ojo previa decía "cuatro" y erraba en Sancor y Triunfo: de ahí que el número reemplace al criterio. **La primera solución fue una placa clara detrás del logo, y se descartó mirándola.** El color estaba acotado por arriba y por abajo —blanco puro es un cartel sobre noir, y el margen para apagarlo lo fija Galicia, que sobre `#f7f4ee` da 4,76:1 y sobre `#efebe3` ya cae a 4,40:1— así que se eligió `#f7f4ee`, el papel del tema light. **En la página real se veía mal igual**: rectángulos claros apilados sobre una página negra. Se armó una vista de exploración con **26 tratamientos** (rampas de neutros cálidos y fríos, blancos translúcidos del 4% al 55%, solo hairline, y tratamientos que sacan la caja resolviendo sobre el logo: invertir la claridad conservando el tono, realce de brillo, halo sobre la silueta, knockout monocromo), cada uno sobre los dos fondos reales y dentro de la tarjeta del comparador. **Ganó no tener ninguna caja.** **El fix real es el negativo de la compañía, no un truco de CSS:** San Cristóbal —la peor de todas, 1,20:1— usa en dark el negativo que publica la marca (`LOGOS_DARK` en `lib/companias.ts`), lockup completo con el verde institucional y ratio 3,36, casi el 3:1 del resto, así que no rompe la escala del set. Se descartó una segunda variante blanca *sin isotipo* justamente por eso: ratio 1,45, a igual altura renderiza la mitad de ancho. **El swap es CSS puro** contra `[data-theme="dark"]`, el mismo selector con el que `app.css` conmuta los `--mg-*`: se renderizan las dos y se muestra una, sin JS ni `MutationObserver`. **Queda abierto** que Experta (1,98:1) y Mercantil Andina (1,54:1) siguen en color sobre fondo oscuro — decisión tomada a la vista, y se cierra el día que aparezcan sus negativos. **Los assets van por Vite, no por `storage`.** Estaban en `storage/app/public/assets/logos`, que exige un `storage:link` que **no existe** y que no crea ni el Dockerfile ni `start.sh` → 404 en producción, y además `/public/storage` está gitignoreado. Pasan a `resources/js/assets/logos/` **renombrados al slug** — los nombres no coincidían (`logo-rus.png` es Río Uruguay, `logo-mercantil.png` es `mercantil-andina`) — con imports explícitos, así que **un archivo renombrado rompe el build** en vez de dejar un hueco que se descubre mirando la página. `companyColors.ts` se absorbe en `lib/companias.ts`: una sola pieza de identidad de compañía, que el checkout también usa. **`colorDeCompania` se queda** — la lista es abierta (`la-caja` ya está en el mapa sin PNG), y sin archivo se cae al monograma. `CheckoutController` pasa a mandar `companiaSlug` con la misma derivación que `QuoteComparisonService::plan()`. **Verificado en el navegador** en los dos temas: sin placa (`background` transparente, `padding` 0), y el swap de San Cristóbal comprobado mirando qué `<img>` queda visible en cada tema. **Fuera de alcance**: las vistas de admin, que usan los tokens `--bg-*` del panel. _(build ✅ · Checkout 60 ✅ · PHPStan 0 ✅ · Pint ✅)_
- **2026-09-02** — **El turno que deja la cotización en vuelo ahora sigue con el agente de cobertura 🔗.** El cliente pidió un segundo auto y dio todo junto: *"cotizame también un Peugeot 2008 año 2017, con GNC, ab413bs, con 5152. Terceros Completo"*. El agente entendió bien —repitió "Terceros Completo" en dos mensajes— registró el vehículo, disparó la consulta y cerró: *"Dale unos segundos que las compañías responden y te paso las opciones"*. **Ese mensaje fue el último de la conversación.** La cotización llegó 30s después y `NotifyClientQuoteReady` no la presentó, porque `coverage_set` seguía en false (conversación 26, mensaje #461; quote 25 `processed` a las 13:55:42, log del guard a las 13:55:43). Ningún job falló. **La cobertura nunca se registró porque `coverage_preference` no está en `VehicleIdentifierAgent`**, y el agente que la tiene corre recién en el turno siguiente — que no existió: al cliente le habíamos dicho que esperara, así que no tenía por qué escribir. Es el único punto del flujo donde se juntan las dos condiciones; las demás etapas cierran **preguntando** algo, y ese mensaje del cliente abre el turno donde el agente dueño de la etapa lee la historia y registra lo que se dijo antes. **Fix, dos capas.** (1) El orquestador encadena el turno cuando `vehicle_identified` flipea y quedó una quote en curso —mismo mecanismo que ya existía para `quote_ready`—: se descarta el texto del VehicleIdentifierAgent, se lo marca como no entregado en la memoria (helper `Support\MemoriaDelAgente`, compartido con la intercepción del inbox) y corre `CoveragePreferenceAgent`, que ve el "Terceros Completo" del mensaje anterior y lo registra sin re-preguntar. Corre en toda identificación cotizable, no sólo cuando el cliente se adelanta: una llamada extra al LLM (~4-6s) contra los 30-174s que ya tarda la consulta. Las ramas NeedsFact y NotQuotable no encadenan — no crean quote y cierran preguntando. (2) `NotifyClientQuoteReady` deja de salir en silencio: con la quote lista y `coverage_set` en false, abre un turno para pedir la cobertura en vez de loguear y morir (guard de `isAiPaused()` para no pisar un takeover humano). La primera capa evita el silencio en el camino conocido; la segunda lo convierte en un mensaje venga por donde venga, porque se ancla en el estado y no en el camino. **Descartado**: darle `coverage_preference` a todos los agentes — en CustomerIdentifier no hay vehículo todavía, en Quote/Checkout duplica `revert_to_stage('coverage')` con efectos distintos, y en VehicleIdentifier depende de que el modelo encadene una segunda tool por iniciativa propia, que es justo el eslabón que falló acá.

- **2026-09-01** — **La presentación nombra el plan, no el nivel de cobertura 🏷️.** El texto decía *"Galicia Terceros Completos"* y *"Galicia Todo Riesgo"*; los productos se llaman `C Clima` y `Todo Riesgo Franquicia 4%`. **El cliente nunca supo cómo se llamaba su plan**, y cuando volvió a preguntar por él dijo *"la cobertura premium"*: el agente lo resolvió contra las **137 alternativas** de la cotización y contestó sobre `Premium Max` de **Sancor**. Tres turnos para desarmarlo, y el agente terminó explicándole al cliente cómo se llama su propio plan: *"el Terceros Completos que te pasé se llama C Clima"* (conversación 25, mensajes #427 a #430). El nombre se le entrega al LLM **ya armado desde el dominio** en el `tool_output` de `PresentQuoteOptionsTool` — el mismo canal por el que ya recibe los títulos de botón— y se le pide copiarlo, no componerlo. Va por ahí y no por el prompt porque es el canal que mejor obedece, y porque así el modelo no redacta el nombre. **El botón no se toca, y está medido por qué:** Meta corta los títulos en 20 caracteres y `buttonTitle()` ya gasta 5 en el precio; de los **71 títulos distintos de producción, 41 pasan los 20 caracteres**, así que `Todo Riesgo Franquicia 4%` quedaría en `Todo Riesgo Fra`. El comparador público ya mostraba el nombre (`ComparadorMobile.vue:73`) e incluso arma un WhatsApp precargado con `${plan.aseguradora} ${plan.titulo}`: el hueco era sólo el mensaje de WhatsApp. **Lo que queda abierto**, anotado en el plan: el agente resolvió la referencia ambigua contra las 137 alternativas y no contra las 2 presentadas, aunque `quotes.presented_alternative_ids` está persistido y nadie lo lee en tiempo de consulta.

- **2026-09-01** — **Los siete manuales curados, cargados en producción — antes había cero 📚.** Los PDF vivían sólo en la máquina de Andrés y el texto sólo en su base local; producción respondía coberturas sin ninguna documentación. **El texto se curó a mano contra el PDF, no con un LLM**: una transcripción automática puede comerse o inventar un número en silencio, que es el modo de falla que todo este trabajo evita, y la frecuencia lo permite (un manual nuevo por año por compañía). Se sacó lo que **no es información de cobertura** — períodos de facturación, medios de pago, bonificaciones sobre tasa: describen opciones que MANGO no ofrece y el agente las leía como promesas. Por eso los documentos ADELGAZARON (Triunfo 26.286 → 16.778, Sancor 37.040 → 24.147, Mercantil 18.865 → 11.200). **Experta entró nueva**: cotizaba 11 planes sin una línea de documentación. Lo que ve el agente hoy: San Cristóbal 29.643 chars en 3 documentos, Sancor 24.147, Río Uruguay 21.045, Galicia 20.650, Triunfo 16.778, Experta 14.684, Mercantil 11.200 — el mayor a un cuarto del presupuesto de 120.000, así que **el manual entero se inyecta y la búsqueda por similitud no se usa nunca**. Tres documentos quedaron sin chunks por cuota de Gemini y no importa: los chunks sólo alimentan esa búsqueda. Eso fue justamente lo que obligó a `d604f38` — en la primera corrida local el indexado volteó el comando entero en el séptimo archivo y dejó la base a medias sin avisar. Un `company_name: Expertaa` en el encabezado habría roto todo en silencio: el slug no coincidía y el agente nunca habría encontrado ese manual.

- **2026-08-31** — **Se van cuatro chequeos de coberturas y dos planes salen de la oferta ✂️.** El criterio: **decide lo que es binario sin criterio, registrá lo que depende de cómo el modelo eligió redactar.** (1) **El chequeo de presencia del plan se borra.** Exigía que el título comercial de Visred apareciera en el manual, y el manual nombra el plan por su código (`## C1`) o directamente no lo nombra en las secciones generales (`## Zona de Aplicación`, `### Pautas de Inspección (Todas las coberturas)`). Medido sobre producción: **20 de 80 planes cotizados figuran en la documentación**; el chequeo tiraba tres de cada cuatro respuestas buenas, y satisfacerlo exigía reescribir los manuales. (2) **El piso de 25 caracteres se borra** — número elegido a ojo que ya había rechazado `Responsabilidad Civil`. (3) **La cita deja de descartar y pasa a `Log::warning`**: saltaba en 22 de 161 corridas y **no mide si la respuesta es correcta, mide si el modelo copió y pegó** — para una misma respuesta buena hay muchas citas válidas y cuál elige no es determinista. Se observa un tiempo y se decide con los casos reales. Lo que sigue decidiendo: que el veredicto esté en el vocabulario y que haya campo de cita, que son binarios sin juicio de formato. (4) **`CoverageTextMetrics` se borra entero**: `pipes/1k` no distinguía "se perdieron las tablas" de "este manual no usa tablas" — Triunfo daba 0,7 con sus secciones completas en prosa y su única tabla intacta. (5) **`Auto Max 15` y `Garage` dejan de ofrecerse**: Visred los manda sin `features_tags` — 35 filas de 2.002, los dos únicos, los dos de Sancor — y sin la enumeración no se puede explicar contractualmente qué cubren. `esOfrecible()` pasa a componer `hasFeatureTags()` con el medio de pago, y `visiblePlans()` deja de repetir la condición a mano. Eso **borra también el bloque de la Fase 1** ("la lista no vino, no niegues por ausencia"), porque ese plan ya no le llega al agente. **El banco `ai:probe-coverage-qa` queda desacoplado**: sus 11 preguntas y respuestas esperadas las escribió el agente, y dos resultaron equivocadas (`gnc` —el GNC declarado con cobertura de robo SÍ está cubierto— y `triunfo-tope-granizo`). No bloquea nada hasta que se revisen. **Próximo escalón, no en este cambio:** un agente que juzgue si la respuesta está sostenida por el material — tolera la paráfrasis y tiene una tarea más chica que el que responde, pero no entra hasta que exista una medición validada con qué compararlo.

- **2026-08-26** — **Medir el camino de coberturas encontró cuatro defectos, y uno de ellos era el banco 📊.** Hasta hoy no había ninguna medición: ni un test ni una sonda. `ai:probe-coverage-qa` corre 11 preguntas con respuesta conocida N veces cada una y las clasifica en tres celdas que **no valen lo mismo** — acierto, **GRAVE** (afirmó sin poder sostenerlo, que es la promesa que hace el PAS sin que la compañía la valide) y **venta perdida** (se abstuvo pudiendo contestar, que existe para que apretar la verificación no salga gratis en la medición). A diferencia de las otras sondas **ésta sí ejecuta la tool real**, y puede: todo el camino de `CheckCoverageRuleTool` es de lectura. `CoverageProbeSeeder` trae las 7 alternativas reales de producción que el banco necesita — los textos de `full_details` importan palabra por palabra, porque la verificación de la Fase 3 chequea la cita contra ellos. **Resultado, 3 corridas por caso: de 20 acierto / 8 grave / 2 venta perdida (66,7% / 26,7% / 6,7%) a 27 / 4 / 1 (81,8% / 12,1% / 3,0%).** **Los cuatro defectos, todos con test:** (1) el chequeo de presencia del plan buscaba el título en TODO el material, y el bloque de producto siempre contiene `Plan: {titulo}` — o sea que **nunca podía fallar**; ahora busca sólo en la documentación. (2) La comparación literal rechazaba el **reformateo de tablas**: el material trae `| **Rotura de Cerraduras (...)** | $300.000 |` y el modelo cita `Rotura de Cerraduras (...): $300.000` —mismas palabras, mismo número, sin pipes y con dos puntos agregados— y se rechazaba en 2 de 3 corridas, tirando a la basura una respuesta correcta; `normalizar()` pasa a dejar sólo letras y dígitos, y **sigue siendo substring**, así que el orden tiene que ser exacto y el texto inventado sigue sin pasar. (3) **`fuente` se deriva en vez de creerle al modelo**: etiquetaba `documentacion` la línea de franquicia, que vive en el bloque de producto, y el chequeo de presencia degradaba una respuesta correcta. (4) El piso de 25 caracteres para la cita **aplica sólo a citas del manual**, donde el texto es largo y una cadena corta matchea por azar; en el bloque de producto el nombre de una cobertura ES una cita válida — exigirlo degradaba 3 de 3 corridas de *"¿si me roban el auto?"* sobre el plan de RC, cuya cita legítima es `Responsabilidad Civil`, 20 caracteres útiles. **Y una corrección al propio banco, que es el hallazgo más útil:** `triunfo-tope-granizo` esperaba abstención sobre el supuesto de que `C2 FUll` no figuraba en el manual de Triunfo. **Figura**: hay una sección `## C2 FULL` que dice textualmente *"Cláusula CA-DA 1.1 Adicional de Daño Parcial por Granizo hasta la suma asegurada de $500.000.-"*, que es exactamente lo que el modelo respondía y yo puntuaba como GRAVE. El supuesto venía de la sonda RAG vieja, que ante esa pregunta devolvía el cuadro de **CAMIONES** — artefacto del retrieval, no del manual. O sea que el caso pasó de "fallo" a **prueba de que la Fase 2 arregló justo eso**. **Lo que queda en GRAVE se concentra en `gnc`** —el predicado vago: el texto de Robo Parcial cubre "los elementos fijos que hacen al funcionamiento" y excluye una lista donde el GNC no está, así que no resuelve— y **oscila entre 2 y 4 sobre 33 según la corrida**, lo que confirma que con n=1 no se concluye nada. Latencia p50 10,2 s, max 35,7 s. _(sonda + seeder + 14 tests nuevos · suite 1030 ✅ · PHPStan 0 ✅ · Pint ✅)_

- **2026-08-26** — **El grado se iba comiendo la diferencia entre productos, y la cita pasa a verificarse en código 🔍.** `CheckCoverageRuleTool` recibía **dos** identificadores del plan y razonaba sobre el peor: `cobertura` (la letra A/B/C/C+/CM/D, derivada de `normalized_grade`) y `quote_alternative_id`, que identifica la fila exacta. La letra colapsa productos que cubren cosas distintas — en la cotización #21 de producción, San Cristóbal ofrece `C - Auto Plus` (12 coberturas, **sin** granizo) y `Auto Plus +` (15, **con** granizo), y los dos entran como "C"; lo mismo `C1` (9) y `C Mega` (18). Preguntar *"¿C cubre granizo?"* tiene dos respuestas correctas distintas y la tool ya había tirado el dato que las separa. **Se elimina el parámetro**, del esquema, de la descripción y del prompt al experto; el test que fijaba `liability->A` se da vuelta para fijar lo contrario. **Salida estructurada:** la tool devuelve `{veredicto, respuesta, fuente, cita, verificado}` en vez de `->text` suelto, y `verificarFundamento()` **degrada a `no_especificado`** todo lo que no sostenga su cita — veredicto fuera del vocabulario, afirmación sin cita, cita que no aparece en el material, y negación por ausencia con la enumeración vacía, que es **la regla de la Fase 1 movida del prompt al código**. Eso es lo que la vuelve una garantía: el ROADMAP ya registra que el prompt v7 prohibía una promesa y el modelo la hacía igual en 3 de 4 conversaciones. **Alcance honesto del chequeo:** atrapa la cita **inventada**, no la **mal atribuida** — un fragmento real pero de otro plan pasa el chequeo igual, y contra eso lo que juega es el documento completo de la Fase 2. La comparación normaliza espaciado y caja pero **no acentos**: exigir igualdad byte a byte degradaría respuestas buenas y llenaría la celda de "venta perdida", que es el costo del lado opuesto. **La franquicia se resuelve en código, y era incontestable hasta hoy:** Visred no manda un campo de franquicia, el porcentaje viaja adentro del nombre del producto (`Todo Riesgo Franquicia 7,5% suma asegurada`, `Todo Riesgo 8% Suma Aseg, Franquicia`, `T37 - Todo Riesgo Franq 7% Suma Aseg`) — y el bloque de producto **no incluía `sum_asegurada`**, así que el experto no tenía la base para multiplicar. `QuoteAlternative::franquicia()` la deriva (verificado a mano: 28.140.000 × 7,5% = 2.110.500) y el bloque la entrega ya calculada, con el título como origen citable. Devuelve `null` cuando el título no la expresa —`Franquicia Fija`, `Franquicia Variable`, planes sin franquicia—, y **null NO significa "sin franquicia"**: significa "no derivable del título". **Hallazgo del SDK:** salida estructurada y tools son **excluyentes** en DeepSeek (`Providers/DeepSeek/Handlers/Structured.php` no mapea tools), así que el camino de búsqueda —hoy muerto, ninguna compañía supera el techo— conserva la prosa y se marca `verificado: false` en vez de fingir una garantía que no se dio. _(23 tests nuevos entre los tres archivos · suite 1017 ✅ · PHPStan 0 ✅ · Pint ✅)_

- **2026-08-26** — **El umbral de distancia se midió y no sirve; el experto pasa a leer el manual entero 📐.** La Fase 2 iba a proteger el camino RAG con un umbral de distancia coseno: si ningún fragmento lo pasa, `found: false`. **La medición lo refutó.** Sobre las 7 consultas cuyo dato SÍ estaba en la documentación local, los dos aciertos quedaron **más lejos** que todos los fallos — remolque 0,2608 · franquicia Sancor 0,2733 · ruedas 0,3150 · reposición 0km 0,3426 · **granizo→camiones 0,3766** · **destrucción total 0,3890 ✅** · **cristales/cerraduras 0,4034 ✅** —, con dos controles absurdos ("receta de milanesas", "contraseña del router") en 0,4932 y 0,5036. **El orden está invertido: no hay corte posible.** Cortar en 0,30 mata los dos aciertos; aceptar hasta 0,42 deja pasar todos los fallos. Lo único que un umbral distingue son las preguntas que no tienen nada que ver, que no son el modo de falla. La causa: **los fallos son de especificidad, no de tema** — "granizo en camiones" y "granizo en autos" son casi idénticos para un embedding, y los chunks largos (hasta 600 palabras) se promedian hacia un centroide genérico de "documento de seguros". **Corolario que también cayó:** el chequeo mecánico de cita (que el texto citado exista literalmente en la fuente) tampoco atrapa el caso camiones — ese fragmento dice textualmente *"Granizo e inundación Sin Deducible"*, así que la cita existe y el chequeo pasa con la respuesta mal. Sirve contra la cita **inventada**, no contra la **mal atribuida**. **Fix, adelantado desde la Fase 5 porque no dependía de curar nada:** `CheckCoverageRuleTool` inyecta la documentación **completa** de la compañía vía `CoverageDocument::activeForCompany()` —que estaba escrita y sin un solo llamador— y le pasa al experto el `titulo` del plan cotizado, **que antes nunca viajaba**: la tool recibía `quote_alternative_id` y no lo usaba para esto. Con el manual entero el modelo ubica la columna de su plan, ve que un cuadro es de camiones, y puede determinar que un plan **no figura** — imposible con 4 fragmentos, que nunca dejan ver lo que no se recuperó. **Medido en local: las 6 compañías entran completas**, máximo ~10.926 tokens (Sancor, 37.040 chars) contra un techo de 120.000 caracteres; `search_company_documentation` sólo se entrega si se supera ese techo, o sea hoy nunca. Una compañía sin documentos (probado con Federación Patronal) deja de fallar en silencio: el bloque dice `NO HAY DOCUMENTACION CARGADA` y prohíbe deducir del nombre del plan. **Prompt:** el Paso 2 pasa de *"buscá"* a *"leé, ubicá tu plan, chequeá el segmento"*; el Paso 3 queda como **estado válido y frecuente** con los cuatro casos que lo disparan (incluido *"el texto es ambiguo y tendrías que interpretar"*, que cubre las tablas aplanadas); y la `description()` de la tool deja de afirmar que su resultado siempre es certeza — ahora tiene dos estados de salida. _(4 tests nuevos, 7 en el archivo · suite 991 ✅ · PHPStan 0 ✅ · Pint ✅)_

- **2026-08-26** — **La ausencia dejaba de significar "falta el dato" y pasaba a significar "no está cubierto" 🚫.** El prompt de `coverage_check` decía *"Feature AUSENTE en la lista → NO está cubierta en este plan. Punto."*, y esa regla se aplicaba también cuando la lista llegaba **vacía**, donde toda feature está ausente por definición. **Visred manda algunos covers sin `features`: `Auto Max 15` y `Garage` de Sancor, 31 de 2002 alternativas en producción.** `Auto Max 15` se vende entre $31.687 y $67.737, así que "no cubre nada" es falso — y el agente lo afirmaba para cualquier pregunta. Riesgo real: la promesa (o la negación) la hace el PAS y la compañía no la validó. **Fix:** `CheckCoverageRuleTool::productBlock()` arma el bloque `DATOS DEL PRODUCTO` en dos formas, y el prompt las distingue — con enumeración se declara COMPLETA y la negación por ausencia sigue valiendo (una póliza cubre lo que enumera); sin enumeración se declara `NO DISPONIBLE` y se prohíbe negar. **`normalizedGrade()` no se tocó a propósito:** sigue devolviendo `basic` para lista vacía, lo cual es una etiqueta inventada, pero afecta **presentación** (vista pública, comparador), no la respuesta de cobertura — y la fase que saca `normalized_grade` del camino de respuesta lo vuelve irrelevante. Tocarlo tenía ramificaciones que no pagaban acá: el blade cae a imprimir el string crudo, hay un test que fija `'basic'`, y `gradesAMostrar()` agrupa por grado. **Requiere sync del prompt por `/admin/agent-prompts/coverage_check`** — el runtime lee de `agent_prompts`, el `.md` es fallback. **Fase 1 de un plan de 5**, cuyo diagnóstico salió de medir en vez de suponer: (a) el RAG acertó **2 de 7** preguntas cuyo dato *sí* estaba en la documentación local, con fallos sistemáticos —el índice del PDF compitiendo y ganando, chunks casi duplicados ocupando el top-k, tablas decapitadas en el corte, y una pregunta de auto devolviendo el cuadro de **camiones** de Triunfo—; (b) ese camino **no está vivo en producción**: `coverage_chunks` está vacío ahí, los PDFs viven solo en la máquina de Andrés (`storage/app/private`, fuera de git) y el texto solo en su base local, así que **se enciende recién el día que se cargue**; (c) el payload de la cotización resultó mucho más rico de lo que se suponía — `features_tags` **es** la enumeración completa (verificado en tres compañías: el plan de RC de San Cristóbal trae 2 entradas, el de Sancor 1, y la escalera de precios acompaña la cantidad de coberturas), y `full_details` trae alcance con umbrales y **exclusiones enumeradas** (`Robo Parcial` excluye textualmente *"equipo de audio, tasa de rueda, escobilla, espejos retrovisores, insignias"*), así que pertenencia y alcance se contestan **sin tocar el manual**. Plan completo en `~/.claude/plans/no-se-esty-medio-clever-lemon.md`. _(3 tests nuevos · suite 987 ✅ · PHPStan 0 ✅ · Pint ✅ · rama `feat/coberturas-fuente-de-verdad`)_

- **2026-08-22** — **De siete procesos residentes a cuatro, y tres defectos de colas que estaban ahí hace meses 🧵.** El disparador fue de costo: el trial de GCP ($300/90 días) sobre el `t2a-standard-2` (2 vCPU / 8 GB, ~$20-24 mes) se agota, y hay que caer en un tier más chico. El relevamiento confirmó la sospecha de fondo —**las capas de workers se apilaron de a una, sin una discusión de conjunto**: seis workers residentes, uno por cola, para un flujo conversacional de bajo volumen— y encontró tres cosas rotas. **(1) Los `retry_after` de `config/queue.php` no los aplicaba nadie.** Los workers arrancaban como `queue:work --queue=X`, **sin el argumento de conexión**, así que Laravel caía a `queue.default` y todos usaban el `retry_after` de `database` (90 s). Los 200/420/150/360 de `database_ai`/`database_quotes`/`database_media`/`database_long` estaban escritos y muertos: `retry_after` **lo aplica la conexión del worker que SACA el job**, no la del código que lo despachó (`DatabaseConnector:39` + `WorkCommand:117`). No mordía porque había exactamente un worker por cola —nadie más podía re-reservar—, pero **es lo que bloqueaba subir `numprocs` en `whatsapp-ai`**, el pendiente anotado el 11-08: con dos workers ahí habría habido dos turnos en paralelo sobre la misma conversación. **(2) La cola `semantic-analysis` no tenía worker.** Defecto latente, no activo: el feature está apagado por flag, así que no hay filas apiladas ni consumo de LLM. Pero el día que se prendiera, el botón del admin habría contestado "encolado, refrescá en unos segundos" y no habría corrido nunca, sin error visible en ningún lado. **(3) Jobs sin política propia.** `EmitirPoliza` corría con los 60 s del worker `default` —un valor elegido para otra cola— y `SendPolicyDocumentsToClient`, que descarga PDFs y los sube a Meta, con los 30 s pensados para un POST de texto. **Fase 1 — la política del job vive en el job:** los 23 jobs declaran `$timeout`, `$tries` y su cola; cada worker recibe su conexión explícita; se elimina `onConnection()` del lado del despacho (las cuatro conexiones apuntan a la **misma tabla `jobs` de la misma base**, no aíslan nada) y con él la conexión `database_media`. **Fase 2 — 6 workers residentes → 3:** `worker-ai` (whatsapp-ai, aislado), `worker-realtime` (default + whatsapp-outbound + media, todo corto y de cara al cliente) y `worker-quotes` (aislado, retiene 30-360 s). `worker-documents` **deja de ser residente**: la extracción de un manual de compañía pasa del orden de 15 veces en la vida del sistema y la ingesta local una vez por noche, así que no justifica ~90 MB durmiendo 28.800 veces por día — la levanta el scheduler con `--stop-when-empty`, que mata el proceso apenas vacía la cola. Cola `background` nueva para lo lento y no urgente (emisión, facturación, extracción, limpieza, análisis semántico), que **además arregla una latencia que ya existía**: `default` mezclaba la ingesta de WhatsApp —la que manda el acuse azul— con `EmitirPoliza`, así que una emisión en curso dejaba al cliente sin tilde hasta dos minutos; el mismo síntoma que se peleó el 11-08. Ninguna cola vieja se renombra, así que **ningún job en vuelo se pierde en el deploy**. **Fase 3 — costo por proceso:** **OPcache estaba apagado** (la imagen `php:8.4-fpm-alpine` lo trae compilado pero deshabilitado y el Dockerfile nunca lo habilitaba), así que cada request web recompilaba todo Laravel; php-fpm pasa de `dynamic` con 2 hijos permanentes a `ondemand` (RAM en reposo ≈ 0 para un panel que usa una sola persona); Postgres dimensionado para un box chico; y `--max-time` escalonado para que los tres workers no reinicien sincronizados cada hora. **Lo que NO se tocó:** el feature de análisis semántico —**no se borró código y no se prendió el flag**, sólo cambió la cola a la que apunta, de una sin lector a una con lector—, la ventana de silencio deslizante, la intercepción en el envío y el presupuesto del lock `inbox:{id}` (`worker-ai` sigue con `numprocs=1`, así que esos cálculos valen tal cual). **`tests/Feature/Queue/WorkerConfigTest.php`** verifica los cinco invariantes leyendo `.docker/start.sh` y `routes/console.php` —el mapa completo de jobs por cola, que todo job declare su política, que toda cola tenga worker, y las dos desigualdades de `retry_after`—, que es lo que evita que la próxima feature vuelva a apilar una capa sin discusión. **Hallazgo lateral:** `PendingChainFake` de Laravel llama `allOnQueue($this->queue)` **sin guard**, a diferencia del `PendingChain` real (que hace `if ($this->queue)`); con `Bus::fake()` eso nulea la cola que el job fijó en su constructor. Es un artefacto del fake, no de producción, pero dejó a la vista que **una cadena tiene que declarar su cola** porque el `chainQueue` es el que gobierna cada eslabón siguiente. **Verificado contra la imagen real, y dos correcciones que salieron de ahí.** (a) **`opcache.enable_cli=1` sin `file_cache` es PEOR que no tener OPcache en CLI.** La memoria compartida de OPcache en CLI es **por proceso**: nace y muere con cada `php artisan`, así que cada invocación paga compilar *y* guardar en una SHM que después tira. Medido dentro del contenedor (Alpine, con `config:cache` aplicado, arranque de `artisan inspire`, 5 corridas): **0,33-0,38 s** con `enable_cli` solo, **0,18-0,19 s** con OPcache CLI apagado, **0,14-0,15 s** con `enable_cli` + `opcache.file_cache`. Importa porque el scheduler arranca un `schedule:run` por minuto **más** el `queue:work` bajo demanda: ~2.880 arranques por día. Con `file_cache` (25 MB en disco, ya tibio antes de que arranque supervisord porque `start.sh` corre `config:cache` primero) son ~7 min de CPU al día — **el tick de cada minuto se sostiene y no hace falta bajarlo a 5**. (b) **`bootstrap/cache` se horneaba en la imagen.** Está gitignoreado, así que en la VM no existe tras un `git pull` y el build anda; pero al buildear desde una máquina de desarrollo, `packages.php` viaja adentro listando `Laravel\Boost\BoostServiceProvider` —una dependencia de **dev** que `composer install --no-dev` no instala— y **cualquier `php artisan` revienta, incluido el `migrate` de `start.sh`**. Se agrega a `.dockerignore`. Ojo con el método: la primera corrida del build reportó `exit 0` **tapando un `ERROR: error during connect`** del engine caído, y casi se da por verificado algo que nunca corrió. **Pendiente:** medir RSS real en la VM (`docker stats`) antes de elegir tier — la estimación de ~800-900 MB con 4 procesos apunta a 4 GB (Hetzner CAX11, u Oracle Always Free si aparece capacidad). **Desplegado y medido en `mango-prod` (2026-08-22 23:05).** Antes → después, en reposo: **host 1.104 → 920 MB** (−184, −17%), **contenedor app 299,1 → 190,6 MB** (−108,5, −36%), **7 → 4 procesos PHP residentes**. `/up` responde 200 en 0,35 s en frío (php-fpm levantando su hijo `ondemand`) y **0,043 s tibio**; el `file_cache` de OPcache quedó en 29,1 MB; el scheduler dispara el `queue:work` de `background` cada minuto (verificado en `scheduler-out.log`); las siete colas en 0 y sin errores en `laravel.log`. **Dos correcciones a mis propias estimaciones:** un worker pesa **47-52 MB**, no ~90 MB — o sea que el ahorro por worker eliminado es la mitad de lo que había estimado; y **la VM está dominada por el SO del host (~724 MB), no por la app**, que en reposo pasa a ser ~160 MB de stack propio. Eso mueve la conclusión de tier: **2 GB entra con holgura**, y el margen hay que dejarlo para el pico (turno de LLM con contexto grande + cotización + extracción de PDF), no para el idle. El 34,7 MB de Postgres post-deploy **no es un ahorro**: es el buffer pool frío tras el reinicio del contenedor. **Prueba de humo por WhatsApp ✅ (conversación #25, 23:44-23:48).** Flujo completo: vehículo → cobertura → cotización → presentación con botones e interactive → link público. **0 jobs fallidos** en 24h y colas vacías al cerrar. Latencia por turno **10-16 s**, descompuesta contra `AgentExecutionLog`: 4-6 s de ventana de silencio + poll (diseño), **3,8-7,8 s de LLM**, y ~2-3 s de espera al `--sleep=3` del worker de salida. Ese último tramo **no es regresión** —el `worker-whatsapp-outbound` viejo tenía el mismo sleep, que además es el default de Laravel copiado en 5 de los 6 `[program:...]` viejos sin que nadie lo eligiera— y se decide **dejarlo en 3**: gana ~1 s promedio en un turno dominado por el LLM, no justifica un deploy propio, y va de arrastre con el próximo cambio que toque `start.sh` (midiéndolo, no estimándolo). **El número de latencia que sí importa y que este refactor NO toca: `CheckoutAgent` tardó 69 s** en el turno de presentación, encadenado a `QuoteAgent` (14,5 s) — ~84 s de LLM en un job que declara `timeout = 180`, o sea **la mitad del techo**. Si creciera a ~110 s el job se mataría y la cotización no se entregaría; el `retry_after` de 200 s cubre el caso pero el margen es 2×, no un orden de magnitud. **Queda sin verificar la nota de voz** (decisión: se ve en uso real): `media` ahora comparte proceso con la ingesta y los envíos, así que un STT lento puede demorar hasta 60 s los salientes. **Suite 984/984 · PHPStan 0 · Pint ✅.** _(rama `refactor/queues-workers`)_

- **2026-08-22** — **La vista admin de una cotización muestra los links que recibió el cliente.** `/quotes/{id}` mostraba el snapshot y las alternativas, pero para saber qué le llegó al cliente por WhatsApp había que ir a la base a buscar `public_token` / `checkout_token` a mano. Ahora `QuoteController::show()` manda **`presented_at`** (en `America/Argentina/Buenos_Aires`, o "Sin presentar"), **`public_url`** (`cotizaciones.show`) y **`checkout_url`** (`checkout.show`), cada una solo si el token ya existe. **La vista no acuña tokens:** se descartó un botón "generar link" para el comparador porque `ensurePublicToken()` es un `update()` y un GET de auditoría no debería escribir. **Tampoco linkea `/admin/checkout-sessions/{id}`:** esa ruta pide middleware `admin` y `/quotes` solo pide `auth`, así que a un usuario no-admin le daría 403. Los tokens crudos no viajan a la vista, solo las URLs armadas. Test nuevo: `tests/Feature/QuoteShowTest.php` (antes el controller no tenía ninguno).
- **2026-08-22** — **Se va el aviso de espera hardcodeado: lo dice el agente 💬.** Al elegir cobertura salían **dos mensajes del bot con cuatro segundos de diferencia** — el texto fijo `whatsapp.quote_wait_notice` que despachaba el adapter, y el del `CoveragePreferenceAgent` diciendo lo mismo. Pasó en las cuatro conversaciones que se miraron (#19, #22, #23, #24). **El motivo por el que el aviso salía aparte ya no existía:** se agregó cuando la consulta a las compañías corría *adentro* del turno (25-60 s) y el mensaje del LLM llegaba **después** de la espera que anunciaba; desde `f40e79c` (10-08) la consulta se adelantó al paso del vehículo y el turno tarda 4-6 s medidos. **Y confiar en el prompt para evitar la repetición no funcionaba:** la v7 de `coverage_preference` prohíbe textualmente *"no cierres prometiendo avisarle"* y cita como ejemplo *"en cuanto tenga las opciones te las paso"* — el modelo lo desobedeció en 3 de las 4 conversaciones. **Decisión de Andrés: eliminar el hardcodeado y quedarse con el del agente**, sobre el argumento de que un agente siempre responde, así que siempre va a haber un mensaje. Se descartó interceptar y borrar ese mensaje: caro y frágil para algo que se arregla borrando código. **Los datos lo respaldan:** 3 de 4 conversaciones ya mencionaban la espera *a pesar de la prohibición*, así que pidiéndoselo la tasa sube. **Fix:** se borran el despacho, el método `avisarEsperaDeCotizacion()` y la clave de config; y **el `tool_output` de la rama `$enVuelo` se da vuelta** — de *"NO menciones la consulta, NO hables de la espera, NO cierres prometiendo avisarle"* a pedirle el aviso explícitamente y decirle **por qué es load-bearing**: es el único mensaje que el cliente recibe antes de 100-130 s de silencio. El peso va en el `tool_output` y no solo en el prompt porque **es el canal que mejor obedece en este sistema** — el cambio a *"ACABA de salirle"* corrigió al agente al primer intento, mientras la prohibición del prompt se ignoraba. **Orden de ejecución, que importa:** primero el prompt (v8, por el admin), después el código. Al revés queda una ventana con el aviso ya borrado y el prompt todavía prohibiendo hablar de la espera; en el orden correcto, el peor caso intermedio es el doble mensaje de hoy. **Riesgo aceptado:** si en algún turno el modelo igual no menciona la espera, el cliente lee *"Dale, te cotizo X e Y"* y después 100-130 s de silencio. Lo acota el pedido explícito por los dos canales, pero sigue siendo probabilístico — se mira en las próximas conversaciones y, si aparece, se evalúa con `ai:probe-presentation` antes de volver atrás. _(1 test dado vuelta · suite ✅ · PHPStan ✅ · Pint ✅)_

- **2026-08-21** — **La presentación dejó de llamar su tool, y el diagnóstico corrigió tres premisas propias 🔎.** Desde el deploy del 19-08 el closer escribía la comparación a mano: sin botones, sin recomendación anclada y **sin link al comparador** — `presented_at` y `public_token` en NULL en las quotes 17 y 18, contra 5 de 5 presentadas bien entre el 07 y el 13-08. **Premisa corregida #1: el despliegue no era continuo.** Un único build el 19-08 19:40:44 llevó producción de `f40e79c` (10-08) a `817c197`, tres commits juntos, y la imagen anterior ya no está en la máquina. O sea que **nunca existió un estado en producción con `54404f7` y sin glosario**, y el diff real entre "andaba" y "falla" es `f40e79c..817c197`, no el commit del glosario solo; la #21 arrancó **83 segundos** después de que levantara el contenedor. **Método — caminata hacia atrás t-3 → t sobre `agent_conversation_messages`**, que es literalmente lo que lee el modelo, con las quotes 15, 17 y 18 byte a byte idénticas (mismo auto, 137 alternativas): en t-3 los siete argumentos de `identify_vehicle` salieron **idénticos** partiendo de dos frases distintas del cliente; en t-2 la misma decisión de cobertura; en **t-1 el agente eligió las mismas dos alternativas con los mismos precios** leyendo un payload 3× más chico. La divergencia es local al closer — en la fila 10 el `tool_calls` pasa de 665 caracteres a `[]`. **La deduplicación no perdió información**, verificado sobre la quote 18: 825 apariciones de tag, 22 tags distintos, **22 descripciones distintas**, cero tags sin descripción. **Quedaron cuatro variables vivas, no una:** el payload; el `tool_output` de `coverage_preference` que reescribió `54404f7` (+167 chars, de dos "NO" a cuatro, que además quedan flotando en el contexto de todos los agentes siguientes); una palabra en la `description` de `check_coverage_rule`; y **el alias flotante `deepseek-v4-pro`**, que no está pineado a un snapshot fechado y no se puede verificar retroactivamente. Las dos últimas se debilitaron —estuvieron presentes en los cuatro turnos y los tres previos no divergieron— pero ninguna quedó eliminada. **Fix — saliencia, no contenido.** La instrucción *"Ejecutá `present_quote_options` ANTES de escribir"* **ya estaba** en el prompt, a ~18.000 caracteres de profundidad, y no se ejecutaba; se subió a la línea 5 con un bloque de gatillo y se incorporó a la Misión, +656 caracteres sobre 23.952, sin recortar ni reordenar nada (v6, id 24, cargada por el admin). **Dos corridas verdes** (#23 y #24): tool llamada, `presented_at`, token, `recommended_alternative_id`, mensaje `interactive` con 3 botones y link aparte, con la misma forma de salida que la #19 que funcionaba. **Premisa corregida #2, y el hallazgo que más cambia el método: la varianza entre corridas idénticas invalida medir con n=1.** #23 y #24 —mismo prompt, mismo payload, mismo código, mismo guión— tardaron **108,2 s y 63,5 s** y generaron **7.699 y 4.578** tokens; factor 1,7. La lectura previa *"menos contexto, más razonamiento"* era **suposición sobre 3 puntos que diferían en 2-3 variables cada uno** —uno de ellos un turno de un solo paso— y sin ninguna medida de varianza intra-condición: se retira. **Premisa corregida #3:** `reasoning_tokens` tampoco lo llena el handler de DeepSeek de Prism, así que llamar "razonamiento" a `completion_tokens` menos texto visible era inferencia, no medición. **Consecuencia — nueva sonda `ai:probe-presentation`**, que repite N veces el turno del closer sobre el contexto histórico del store. Manda **un solo paso** y lee `choices.0.message.tool_calls`: no ejecuta ningún handler, así que no escribe en `quotes`, no toca metadata, no despacha jobs y no manda WhatsApp — **segura por construcción, no por convención**. Con el SDK era imposible: `DeepSeek\Handlers\Text::handleToolCalls()` llama a `callTools()` **antes** de chequear `shouldContinue()`, o sea que ni con `maxSteps = 1` se evitan los efectos. La fidelidad no se traduce a mano — el payload se arma con los **mismos mappers de Prism** (`ToolMap`, `MessageMap`) sobre los mismos value objects, con `tool_choice: auto` y **sin `temperature`**, porque `CheckoutAgent` no declara `#[Temperature]` y mandarla mediría otra cosa. **Por qué no servía el Studio que ya existía:** `PromptReevaluationService::resolveToolsForAgent('checkout_closer')` no ofrece `PresentQuoteOptionsTool` —el modelo no puede llamar lo que no se le da— y su `buildContext()` lee la tabla `messages`, solo texto visible, así que el resultado de `get_quote` no estaría en el contexto. `--prompt-id` pinea una versión histórica, lo que habilita reevaluar el contexto que falló con el prompt que corría entonces. **Fuera de alcance a propósito:** la latencia —133 s de espera del cliente en la #23, de los cuales 103 s son el paso 1 del closer—, que ahora depende de esta sonda para medirse con n suficiente. _(9 tests nuevos · suite 957 verdes · PHPStan ✅ · Pint ✅)_

- **2026-08-19** — **El tamaño del prompt no cuesta latencia: medido 📏.** Tras deduplicar el payload, el prompt pasó a ser el ítem más grande del turno de `CheckoutAgent`, y la pregunta era si convenía recortar los seis. **Bloqueo previo: Prism descarta los tokens de caché de DeepSeek** — su handler lee solo `prompt_tokens` y `completion_tokens`, así que `Usage::cacheReadInputTokens` del SDK da 0 siempre aunque la caché funcione. Se decidió **no parchear `vendor`** (el SDK está en actualización permanente y el parche se perdería en el próximo `composer update`) y medir con una sonda que le pega a la API directo con `Http`: nuevo comando **`ai:probe-cache`**, que compone el prompt real con `AgentPrompt::compose()` y saca los bloques compartidos y el tier de modelo **por reflexión** de la clase del agente, para que no quede un mapa que se desfase. Tres llamadas con `max_tokens = 1` (miss forzado con una cadena aleatoria **antepuesta**, que rompe el prefijo; repetición; y el prompt tal cual), de modo que el tiempo de pared sea casi todo prefill. **Resultado: la caché funciona** —7.040 de 7.064 tokens pegaron hit— **pero el tamaño no mueve la aguja.** El control lo desarma: un prompt de **133 tokens** tardó 1.704/1.367/1.753 ms y uno de **7.064** tardó 1.515/1.430/1.951 ms. **53× más tokens, el mismo tiempo**, y la variación entre corridas frías (>1 s) supera cualquier delta de caché. Los ~1,5 s son el piso fijo de un request a DeepSeek, no el prefill. **Dos correcciones de estimaciones propias:** la relación real es **3,39 caracteres por token** (se había estimado 2,25), así que el prompt compuesto son ~9.200 tokens y el **30 %** del turno, no el 46 % que se dijo. **Consecuencia — recortar prompts baja costo, no segundos: prioridad baja.** Los 54,6 s de `CheckoutAgent` son **generación de salida** (6.271 tokens), así que el esfuerzo se va a eliminar la **doble redacción**: hoy el modelo escribe el mismo argumento dos veces, una en `recommended_reason`/`alternative_reason` para la vista pública y otra como texto de chat. Lo único que se saca del prompt igual, y no por tamaño, es el bloque *"Deuda conocida"*: le dice al modelo que el link a la comparación no se manda, cuando `PresentQuoteOptionsTool` lo mintea y sale en un mensaje aparte. _(sonda: 9 tests · PHPStan ✅ · Pint ✅)_

- **2026-08-18** — **`tarjeta-cbu` nunca fue un medio de pago, y Visred dice que Triunfo ya está arreglado 💳.** Dos cosas, una de ellas una corrección de diagnóstico propio. **Primero, el error que veníamos arrastrando:** `GET /v1/params/payment-method/?company_id=triunfo` devuelve **tres** valores — `cupon`, `cbu`, `tarjeta` — y **`tarjeta-cbu` no está entre ellos**. Es un agregado de COTIZACIÓN (un precio que vale para tarjeta *o* CBU), igual que `todos`; el vocabulario de cotización y el de emisión son namespaces distintos. O sea que la conclusión del 05-08 —"mandábamos mal el `payment_method_id`"— **era falsa**: mandábamos `tarjeta`, que es exactamente el valor correcto del catálogo. Y el crash `invalid literal for int() with base 10: 'tarjeta-cbu'` no era Visred rompiéndose con un valor válido, era su manejo feo de un valor fuera de catálogo que mandamos nosotros al probar. **Segundo, el ticket:** se reportó con el caso real de producción (quote 9, `quotation_result_id 16659237`, Triunfo + `tarjeta` + `naranja` → 400 `payment.credit_card_number`, log del 2026-08-04 22:15:08) mostrando que el payload usa los `id` de sus dos catálogos y aun así se rechaza. Visred respondió **"ya fue resuelto, del área de soporte se olvidaron de avisar"**, sin decir qué corrigieron. **Decisión: no se toca una línea de código y la verificación es la próxima venta real de Triunfo con tarjeta** — con el payload sin cambios, el resultado dice directo si el arreglo de ellos funciona contra lo nuestro. Esta vez se va a poder leer: gracias al normalizador del 03-08, un fallo llega al mail como `payment.credit_card_number` y no como `[""]`. **Consecuencia para [#8](https://github.com/andresrom1/workflow-assistant/issues/8):** la Fase 2 **no es un passthrough verbatim** sino una **traducción** cotización→emisión (`tarjeta-cbu` → `tarjeta`, `todos` → `tarjeta`), y **no destraba Triunfo**, porque `tarjeta` ya es lo que se envía. Queda por decidir qué hacer cuando la alternativa elegida se tarifó `cupon` o `cbu`: el comparador toma la más barata y puede caer ahí, y no capturamos CBU en el checkout. **Se retira el "bloqueado"** de la entrada del 05-08 — nunca lo estuvo del modo en que lo escribí. **Sigue sin respuesta** el otro punto del mismo ticket, el de descuentos (¿el `fee` de `cotizar/` viene bonificado o es premio de tarifa?, ¿cómo se sabe si una compañía exige `discount_id` cuando su catálogo viene vacío?): es lo que traba el PR [#14](https://github.com/andresrom1/workflow-assistant/pull/14). **Nota de método:** de este episodio las conclusiones que sobrevivieron fueron las que salían de payloads y responses guardados en archivo; las que salieron de memoria o de generalizar desde una sola compañía hubo que retirarlas. _(sin cambios de código)_

- **2026-08-13** — **El payload de `get_quote` repetía 33 definiciones 48 veces 📚.** Midiendo por qué la presentación de la conversación #19 tardó 68 s se llegó al contexto: `CoveragePreferenceAgent` 15.302 tokens de entrada → `QuoteAgent` **70.129** → `CheckoutAgent` **143.665** (dos pasadas de ~71k, 54,6 s). El salto lo mete `get_quote`, que inyecta ~55.000 tokens de una: las 71 alternativas ofrecibles con todos sus campos. Desglosado por campo sobre esa cotización: **`full_details` 108.827 caracteres, el 80%**; `features_tags` 17.716; todo el resto 9.180. **La primera hipótesis —que `full_details` fuera relleno— se cayó con los datos:** de 1.588 entradas, **cero** son el placeholder `Incluido.` y 776 superan los 80 caracteres; son definiciones reales con umbrales ("supera el 80% del valor del vehículo"), exclusiones ("a excepción de equipo de audio, tapa de rueda…") y condiciones ("siempre que haya sido asegurado desde 0KM"). Sacarlo le hubiera quitado al closer la letra chica justo en el momento de vender. **Lo que sobraba era la repetición:** esas descripciones definen el término, no la alternativa — "Robo Total" significa lo mismo en las 48 filas que lo incluyen —, y el vocabulario del proveedor es **cerrado y auditado** (33 tags en `config/quotes.php`). Las 1.588 entradas eran 33 definiciones repetidas ~48 veces. **Fix — un `glosario` único al mismo nivel que `alternatives`**, armado con `QuoteComparisonService::glossary()`, que es el mismo componente que la vista pública ya usaba para esto y cuyo docblock planteaba textualmente el problema ("repetir la descripción por plan multiplicaría el payload por quince sin agregar información"). Cada alternativa conserva su `features_tags`; ninguna lleva `full_details`. **No se pierde nada:** cada tag tiene una única descripción idéntica entre compañías, verificado sobre la base entera de producción (22 tags, ~3.700 apariciones, 7 compañías, cero excepciones), y `CheckCoverageRuleTool` sigue cargando `full_details` **de la base** por `quote_alternative_id`, así que el experto RAG ve la redacción exacta por alternativa. Se reescribieron las 6 referencias de `CheckoutAgent.md` para que el modelo busque el detalle en el glosario y no adentro de cada fila — **requiere carga manual en `/admin/agent-prompts/checkout_closer`**. Tests nuevos en `WhatsAppGetQuoteGlosarioTest`: la definición aparece una sola vez en el JSON, ninguna alternativa trae `full_details`, y **todo tag de `features_tags` tiene entrada en el glosario** (el invariante que hace que deduplicar no pierda información). **Fuera de alcance a propósito:** los 13,6 s de generación descartada de `QuoteAgent` y la doble redacción de `CheckoutAgent` — un cambio a la vez.

- **2026-08-11** — **El acuse de recibo dependía de que hubiera un worker libre 🔇.** Conversación #19 en prod, la primera con la cotización ya paralelizada. La consulta arrancó al identificar el vehículo (`11:27:53`) y terminó sola; el problema fue lo que vino después. `NotifyClientQuoteReady` tomó el **único** worker de la cola `whatsapp-ai` para correr el turno de presentación —`QuoteAgent` con el JSON de alternativas, encadenado a `CheckoutAgent`— y tardó **~50 segundos**. Durante ese rato el cliente escribió "ok" (`11:28:46`) y "estas bloqueado?" (`11:29:13`): **ninguno de los dos recibió siquiera la tilde azul hasta `11:29:40`**, porque tanto el acuse de lectura como el "escribiendo…" salían recién al **empezar** el turno, y el turno no podía empezar. Desde el teléfono se ve idéntico a un bot colgado. **Fix — el acuse se mudó a la ingesta.** `ProcessWhatsAppMessage` (cola `default`, que no espera a nadie) llama a `sendTypingIndicator()` apenas persiste el mensaje; `ProcessConversationInbox` lo sigue llamando al arrancar el turno, ahora para **rearmar** la burbuja (Meta la sostiene 25s). El flag `typing_indicator_enabled` bajó del job al servicio, que es donde vive la política. **Además, dos cosas mal dimensionadas que todavía no habían mordido:** el presupuesto de espera del lock `inbox:{id}` era de 45s en el inbox y 90s en la notificación, contra un hold máximo de 180s — con más de un worker en esa cola, el mensaje del cliente se habría **descartado en silencio** (`tries` 10 → 45 y 25 respectivamente; ver la regla en [CLAUDE.md](CLAUDE.md)); y el `expireAfter(120)` de `NotifyClientQuoteReady` era **menor** que su propia duración, así que el lock se soltaba a mitad del turno (→ 300s, y el job pasó a declarar `timeout = 180`). **Y el aviso de espera salía duplicado:** el texto fijo (`11:28:15`) y la respuesta del agente (`11:28:15`, un segundo después) decían lo mismo — se endureció el `tool_output` de `coverage_preference` y el prompt para que el agente confirme **solo** la cobertura y no cierre prometiendo avisar. **Queda abierto:** la cola `whatsapp-ai` sigue con un solo worker, así que una conversación lenta congela a **todas** las demás; subir `numprocs` está condicionado a la RAM de la VM.

- **2026-08-10 (b)** — **La cotización vivía adentro del turno y se moría esperando 🕓.** La conversación #18, la primera real después de desplegar la UX de turnos, expuso el defecto que la entrada anterior había dejado condicionado a medir. Timeline: `15:33:10` arranca la consulta a Visred, `15:33:12` sale el aviso "ya estoy consultando" (la Fase 1 funcionó), `15:36:04` Visred termina —**174 segundos**, 12 compañías OK y 2 sin contestar—, `15:36:05` el worker mata el proceso a los 180s (`--timeout=180`), `15:37:01` `MaxAttemptsExceeded`. **`Quote #14` quedó en `pending` con 0 alternativas: las 12 compañías que sí respondieron se perdieron porque el proceso murió después de recibirlas y antes de que `saveResults()` las guardara.** [config/visred.php](config/visred.php) tenía escrito el invariante que sostenía el diseño —*"poll_budget + overhead_LLM < timeout del job de IA (180s)"*, con `poll_budget` en 120s— y la realidad lo rompió. **La causa era una incoherencia interna: la entrega ya era asíncrona, el disparo no.** `NotifyClientQuoteReady` inyecta un turno sintético y el agente pide los datos con `get_quote` — nadie polea del lado del agente. Pero `coveragePreference()` llamaba a `tryResolveQuoteById()` **inline**, y ahí adentro `VisredQuotationProvider::poll()` es un `while` con `Sleep::for(4)->seconds()` corriendo en el mismo proceso PHP que atendía el turno. **Y esa espera no compraba nada:** el `$resolved` que devolvía se usaba *solo* para elegir entre dos strings del `tool_output`. **Fix — `ResolveQuote` en cola propia (`quotes`), y adelantado al vehículo.** No se despacha al elegir la cobertura sino en `onQuotable()`, o sea al identificar el auto, porque `buildRequest()` manda vehículo, año y CP y **no manda la preferencia de cobertura**: no hay nada que esperar, y los 30-174s transcurren mientras el agente indaga. El job **no lleva el lock `inbox:{id}`** — el punto entero es que el bot siga escuchando; antes, si el cliente escribía "¿cuánto falta?", su mensaje hacía cola detrás del turno dormido. **El efecto no obvio, y lo que más fácil se rompía:** adelantar la consulta hace que pueda terminar **antes** de que el cliente elija cobertura, y ahí `NotifyClientQuoteReady` presentaba las alternativas **salteándose la pregunta**. Presentar pasa a depender de dos condiciones que ya no tienen orden fijo —quote `processed` y `coverage_set`— con la regla de que **el que completa la segunda despacha la notificación**: `ApiQuoteResolution` si la cobertura ya estaba, `coveragePreference()` si la cotización ya estaba, y nadie si falta una (guard nuevo de `coverage_set`). `coveragePreference()` despacha el job en vez de presentar directo porque **`CoveragePreferenceAgent` no tiene `get_quote`** (la tiene solo `QuoteAgent`): el job abre un turno nuevo y para ese turno `coverage_set` ya es true, así que el orquestador entrega el agente correcto. El aviso de espera de la Fase 1 queda **condicional** — si al elegir cobertura las opciones ya están, no sale ningún anuncio. **`NotifyClientQuoteReady` sube de `tries = 2` a `10` + `maxExceptions = 3`:** con la consulta en paralelo la ventana de solapamiento con el lock se agranda mucho (puede terminar justo mientras el cliente escribe), y con 2 intentos moría contra el lock y la cotización no se entregaba nunca. **Camino de fallo, que no existía:** `QuoteService::resolveQuote()` atrapa sus propias excepciones y devuelve `false`, así que el fallo **no** llega por `failed()` — `NotifyClientQuoteFailed` (texto fijo, sin LLM, idempotente por `quote_id`) se despacha desde los dos lados. Presupuestos encadenados: `poll_budget` 120 → **240s** < `ResolveQuote::$timeout` **360s** < `retry_after` de `database_quotes` **420s**. **La cola `quotes` va en `.docker/start.sh`: si no está, los jobs se encolan y no los corre nadie, y el síntoma es indistinguible del bug que se está arreglando.** Se deja intacto todo el camino de presentación (turno sintético → `get_quote` → encadenado a CheckoutAgent). Suite **931 verdes** · PHPStan ✅ · Pint ✅. **El prompt `coverage_preference` se entrega para carga manual** en `/admin/agent-prompts`. **Costo aceptado:** se cotiza también a clientes que abandonan antes de elegir cobertura — la request es idéntica a la que se mandaría después, cambia *cuándo* y no *qué*. Pendiente: recuperar o descartar la `Quote #14` de #18. _(rama `feat/ux-turnos-whatsapp`)_

- **2026-08-10** — **El bot esperaba de gusto y avisaba de la espera cuando la espera ya había terminado ⏱️.** Se arrancó por la queja de UX conocida —el cliente escribe en seguidilla y el agente contesta cada burbuja por separado— y **la medición dio vuelta la prioridad**. Sobre prod (133 turnos, 134 mensajes entrantes; piloto, muestra chica): la seguidilla real es **4 de 134 mensajes (3%)** —el debounce agarró 1 y se le escaparon 3—, mientras el debounce le cobraba **8s al 100%** de los turnos contra un p50 de LLM de **4,5s**: la espera artificial era el doble que el trabajo. Y los gaps de esas 4 continuaciones son de **8-20s**, o sea que agrandar la ventana hasta atraparlas era exactamente el impuesto que había que sacar. `failed_jobs` de `ProcessConversationInbox`: 0. **El hallazgo grande estaba en otro lado.** Latencia por agente: `CoveragePreferenceAgent` **p50 25,1s · p90 59,7s · máx 95s**, contra 2,5s del `CustomerIdentifierAgent` y 4,7s del de vehículo — **10× el resto**. La causa es que `WhatsAppAdapter::coveragePreference()` resuelve la cotización **sincrónicamente dentro del tool** (`tryResolveQuoteById`), así que los 30-60s de las compañías corren **adentro** del turno. Efecto compuesto: el *"Listo, ya estoy consultando las mejores opciones…"* del prompt se redacta **después** de que la consulta terminó, o sea que el cliente pasaba ~33s en silencio (8s de debounce + 25s de turno) y recién ahí le avisábamos que íbamos a empezar, con los resultados pisándole los talones. **El anuncio no llegaba tarde: llegaba invertido.** **Cuatro frentes.** (1) **El aviso se adelanta**: texto fijo (`whatsapp.quote_wait_notice`) despachado por el adapter **antes** de llamar a las compañías, sin pasar por el LLM — que es justamente lo que lo hacía llegar al final. El `tool_output` posterior le instruye al agente que **no lo repita**, y se sacó la línea del `CoveragePreferenceAgent.md`; sin eso el cliente lee dos veces el mismo anuncio y duda de si algo se rompió. Se eligió esto sobre sacar la cotización a un job: es una fracción del trabajo y se lleva casi todo el beneficio percibido. **Lo que no cura y queda anotado:** el turno sigue bloqueado 25-60s con el lock `inbox:{id}` tomado, así que **durante la espera el bot está sordo** — defecto que ya existía. (2) **Debounce fijo → ventana de silencio deslizante** (`inbox_quiet_seconds`, 3s): el job, al correr, mira si el mensaje **más nuevo** tiene menos de esa antigüedad y se re-libera con `release()` sin tocar el LLM; cada mensaje nuevo corre la ventana. Tope duro `inbox_max_wait_seconds` (15s) contra el **más viejo**, para que alguien que escribe sin parar no difiera el turno para siempre. **`$tries` sube de 3 a 10 + `maxExceptions = 3`**: cada `release()` consume un intento y con 3 el job se mataba solo esperando. Se agrega `$timeout = 180` explícito, por debajo del `retry_after = 200` de `database_ai` —si lo superara la cola lo re-reserva y quedan dos corriendo sobre la misma conversación— y el `expireAfter` del lock pasa de 120s a 300s para cubrir el peor turno medido más las intercepciones. (3) **El typing indicator nunca funcionó.** Lo que había mandaba una **`reaction` vacía** (`message_id: ''`, `emoji: ''`) — un truco de antes de que Meta sacara la feature —, Meta la rechaza, y el `catch` **mudo** se comía el error: por eso nadie lo notó en meses. Se reescribe con la API real (`status: read` + `typing_indicator`, anclado al **wamid del entrante**, que de paso deja tildes azules) y **se muda de `SendWhatsAppMessage` a `ProcessConversationInbox`**: en el envío la respuesta ya está generada y se veía medio segundo. El fallo ahora se loguea. Meta lo sostiene **25s**, así que el 12% de los turnos que exceden ese tope quedan con un hueco al final — y la espera de la cotización **no se puede cubrir con él** (supera el tope, se cancela sola al mandar cualquier mensaje, y no se puede rearmar porque no entra ningún mensaje al cual anclarlo). Un "escribiendo…" de un minuto se lee como que el bot se colgó; esa espera la cubre el punto (1). (4) **Intercepción en el envío**, que es lo que recupera la agrupación sin pagar latencia: antes de despachar se re-consulta el inbox, y si llegó algo mientras el LLM generaba la respuesta **no sale** — se rehace el turno con lo nuevo. La ventana es el tiempo de generación que se gasta igual. **Se marca la fila del assistant como no entregada, no se borra:** esa fila carga también `tool_calls` y `tool_results`, y `DatabaseConversationStore::getLatestConversationMessages()` reconstruye el contexto del modelo desde ahí, así que borrarla le sacaría el registro de que la tool ya corrió y el redo tendería a **re-ejecutarla**; marcándola, el modelo ve que el trabajo está hecho y solo redacta. La marca va en `content` y no en `meta` porque `meta` no se reconstruye en el contexto (verificado en el vendor). **Tres guardas que salieron de leer el código, no de diseñarlo en abstracto:** no se intercepta un turno que llamó `coverage_preference` o `checkout` (ya dispararon algo irreversible hacia afuera — la lista corta, y no "ninguna tool", que habría desactivado la intercepción justo en el caso de interés: CP / patente / combustible son find-or-create e idempotentes); no se intercepta un turno **encadenado**; y hay que **arrastrar `buttons` y `public_link`**, porque `pullPending()` los **lee y borra** de la metadata al armar la respuesta y el redo ya no los encontraría — el cliente se quedaba sin botones ni link a la comparación. Tope `inbox_max_intercepts` (2). Los efectos de dominio no se tocan: se descarta el texto, no los hechos. **Tests:** 6 casos nuevos de intercepción (archivo propio), 2 de la ventana deslizante y 2 del indicador en el inbox, 2 del payload del indicador en el service y 3 del aviso adelantado — **el debounce no tenía ningún test**. `phpunit.xml` fija `WHATSAPP_INBOX_QUIET_SECONDS=0` para que los tests que crean mensajes en el instante no queden todos diferidos. De paso se limpió una entrada obsoleta del baseline de PHPStan y se tiparon `tool_calls`/`chained`/`inbound_message_ids` en `AgentExecutionLog`, que no tenían `@property`. Suite **921 verdes** · PHPStan ✅ · Pint ✅. **El prompt `coverage_preference` se entrega para carga manual** en `/admin/agent-prompts`; hasta cargarlo el agente sigue repitiendo el anuncio. **Pendiente de medir en prod** antes de decidir si hace falta sacar la cotización a un job: si aparecen mensajes del cliente durante la espera que quedan sin contestar o mueren en `failed_jobs`. Y una advertencia sobre el 3%: un bot lento entrena a la gente a esperar, así que al acelerar las seguidillas probablemente **suban** — por eso la intercepción se hizo igual. _(rama `feat/ux-turnos-whatsapp`)_

- **2026-08-09** — **El terceros completo eran dos productos con el mismo nombre 🪜.** Con el clasificador nuevo andando (ver entrada de ayer), la prueba en producción mostró que el caso arreglado era solo la mitad: cuando el cliente **nombra** una cobertura el closer elige bien —conversación #16, pidió granizo y salió Triunfo C2 a $75.878, la más barata que lo trae, con los seis bullets correctos contra los tags y el diferencial sin inflar—, pero cuando dice *"terceros completo"* **a secas** el Paso 1 queda sin features, no hay piso y gana la más barata del nivel: **Sancor Auto Max 3 a $65.579**, sin granizo ni cristales ni cerraduras, recomendada como *"cubre todo lo importante"*. **El bucket C mezclaba dos intenciones de compra, y no es una distinción nuestra: las siete compañías tienen los dos escalones y los nombran** — Experta **L** vs **XL**, Mercantil **M BASICA** vs **M PLUS**, San Cristóbal **C - Auto Plus** vs **Auto Plus +**, Sancor **Auto Max 3** vs **Auto Max 6**, Galicia **C80** vs **C Clima**, Triunfo **C1** vs **C2**. **Quinto grade `third_party_complete_plus`**, dentro de la rama C y solo ahí: la separa traer `Luneta`, `Parabrisas`, `Granizo`, `Cristal de Techo`, `Inundación` (por prefijo, existe `Inundación o Desbordamiento`) o `Caída de árboles`. `Cristales Laterales` y `Cerraduras` **no** cuentan — es justo lo que trae la Tercero Completo L de Experta, el escalón base de esa compañía. Calibrado contra la cotización #12 antes de escribirlo: de 43 terceros completos, **19 C y 24 C+A**, y las 26 reclasificaciones se revisaron una por una contra el nombre del plan; la implementación se validó aparte contra 9 tag-sets reales. Sin migración (`normalized_grade` es un `string` sin enum). **`payment_method_id` pasa a filtrar y no solo a persistirse:** el checkout solo procesa tarjeta, así que se ofrecen `tarjeta`/`tarjeta-cbu`/`todos` y quedan afuera `cupon` y `cbu`. El efecto es más grande que no mostrar precios de cupón — **deja una sola fila por producto, 137 → 71**, y resuelve los duplicados que hacían aparecer el mismo plan hasta tres veces. `cbu` sale porque no se puede cobrar, no por precio: donde convive con `tarjeta` vale exactamente lo mismo. Las alternativas sin medio de pago (anteriores a la columna) se conservan para que los links ya enviados sigan abriendo. **Criterio de selección del closer**, replicando lo que hace un productor a mano: por cada compañía su C+A **más completa** —la más cara de esa compañía, a precio de tarjeta—, y entre esas la **más barata**. En la #12 da **Galicia C Clima $80.984 (20 coberturas)** en vez de Triunfo C2 $75.878 (17): $5.106 más por tres coberturas más. **Y el bug que llegó al cliente en #16:** pidió terceros completo *y* todo riesgo, el modelo llamó `coverage_preference` dos veces en el mismo turno —lo correcto, estaba registrando las dos cosas que el cliente manifestó—, la segunda no encontró quote `pending` porque la primera ya la había resuelto, y el adapter contestó su rama de "no hay cotización en marcha, ofrecé derivar a un asesor". El agente lo relevó tal cual: *"el sistema no me dejó generar Todo Riesgo… un Productor Asesor te contacta"*. **Doblemente falso:** `buildRequest()` no manda ninguna preferencia de cobertura, así que Visred devuelve todos los niveles que la compañía tenga y las 41 `all_risk` ya estaban en la cotización que el closer presentó dos minutos después. Dos arreglos: el mensaje ahora chequea si hay una cotización resuelta **y vigente** (`Quote::scopeVigente()` — una de anteayer no habilita seguir presentando) y en ese caso le dice al agente que siga; y la tool suma `coverage_codes` para que el pedido completo entre en **una** llamada, persistido en `metadata.niveles_solicitados`. Se eligió el array sobre una fila por nivel porque con filas sueltas no hay forma de distinguir *"quiero los dos"* de *"cambié de opinión"*. **Hallazgo del render, que la suite no podía ver:** partir el bucket C dejaba `gradeLabel` en `null` —la vista decidía por grados distintos, no por labels distintos— y una página que antes tenía título se quedaba sin él; además el escalón decía *"las dos que te recomendé son de niveles distintos"*, que para un par C / C+A contradice la decisión de no mostrarle al cliente una categoría que ninguna aseguradora nombra. Ahora el label se calcula sobre los labels (C y C+A comparten "Terceros Completo") y la leyenda cambia a *"las dos son la misma cobertura, pero una cubre más"*. Verificado renderizado a 375px sobre un par sembrado. Suite **906 verdes** · PHPStan ✅ · Pint ✅ · build ✅. **Los dos prompts se entregan como texto para carga manual** en `/admin/agent-prompts` (`checkout_closer`, `coverage_preference`). Queda abierta la pregunta invertida: **por qué #14 registró una sola preferencia** ante el mismo pedido de dos niveles — fraseo del cliente o schema de la tool, un caso por lado no lo separa. _(rama `feat/grade-terceros-completo-adicionales`)_

- **2026-08-08** — **El closer ofrecía la más barata del nivel, sin las coberturas que el cliente pidió 🎯.** Conversación #14: el cliente pidió comparar terceros completo contra todo riesgo y recibió **Galicia C80 a $73.106 sin granizo** contra **Galicia Todo Riesgo Fr. 4% a $109.655**, con el bullet diferencial atribuyéndole al salto de +$36.549 tanto los daños propios como *"granizo, inundación, cerraduras y parabrisas"*. En la misma cotización había **Triunfo C2 con granizo a $75.878** (+$2.772 sobre la C80) y **Galicia C Clima, misma compañía, a $82.999**. El único diferencial real del par era daños propios; el resto era humo, y empujaba al producto caro con un argumento que el dato no sostiene. **El prompt hacía exactamente lo que dice:** el Paso 1 cierra con *"sin features explícitas → lista vacía (filtrás solo por grade)"*, y el cliente nunca nombró el granizo. **Nadie más que el modelo decide:** `getQuote()` devuelve las 137 alternativas sin filtrar ni ordenar, y `PresentQuoteOptionsTool` solo valida que los ids pertenezcan a la cotización — cero validación de negocio. **Cuatro frentes.** (1) **El pedido del cliente no existía como dato:** `CoveragePreferenceTool` recibe `reasoning` y `WhatsAppAdapter::coveragePreference()` lo tiraba, porque `validatePayload()` devuelve `$validator->validated()` y la clave no estaba en las reglas — lo persistido para #14 era `preference: "D", metadata: NULL`. Ahora la tool toma `coberturas_requeridas` y se guarda en `coverage_preferences.metadata`, columna que **existía desde la migración de febrero, casteada y en `$fillable`, con cero escrituras y cero lecturas**; `getQuote()` se lo recuerda al closer en el mismo turno en que elige. Sin migración. (2) **`normalizedGrade()` clasificaba por substring del nombre del plan y tenía `third_party_complete` en 0 sobre las 813 filas de producción**, ~345 mal (42%). Dos defectos que se potenciaban: buscaba el literal `terceros completo`, que **ninguna de las 7 compañías emite** (verificado contra los `cover.id` reales de `quote_provider_refs.raw_response`: los planes se llaman `c80`, `c-clima`, `c2`, `c-mega`, `sigma`, `premium-max`, y Experta es la única que lo nombra, en singular, `tercero-completo-xl`); y la rama de abajo matcheaba **`rc`**, substring de pa**rc**ial, te**rc**ero, me**rc**antil y Me**rc**osur — de ahí que `c1-robo-e-incendio-total-y-parcial` y `tercero-completo-xl` cayeran en `liability`. Del bucket `liability` de prod (208 filas): 93 con "responsabilidad civil" en el título, **54 con "parcial"**, **40 con "tercero"**. Pasa a clasificar por `features_tags`, que es vocabulario cerrado y ya auditado por `auditarVocabulario()`: `Daños Parciales` **exacto** → `all_risk` (las variantes `por Robo` y `al Amparo del Robo Total` son nivel C, no D); algún tag con `robo`+`parcial` → `third_party_complete`; robo o incendio → `basic`; ninguno → `liability`. **Calibrado contra la cotización #10 antes de escribir el fix**: 47 de 137 cambian, `third_party_complete` pasa de 0 a 43, ninguna `all_risk` se mueve, y las 26 reclasificaciones se revisaron una por una. **El guard de `features_tags` vacío es load-bearing**: Sancor manda "Auto Max 15" y "Garage" sin ninguna feature y sin él caerían a `liability`, o sea afirmar que no cubren robo sin tener el dato. Las 813 filas históricas **no se migran** — aplica hacia adelante, y los docblocks de `QuoteComparisonService` quedan fechados para que se entienda por qué las viejas siguen mal. (3) **`payment_method_id` se descartaba en `mapCover()`**: Visred cotiza el mismo cover una vez por medio de pago y San Cristóbal manda tres (`c-mega`: cbu $100.308, tarjeta $100.308, **cupon $122.971**), así que las filas quedaban indistinguibles y el cupón entraba a la presentación como si fuera otro producto, 22% más caro. Se persiste; **filtrar** queda para después ([#8](https://github.com/andresrom1/workflow-assistant/issues/8)). (4) **Prompts** — el eje 1 pasa de *"coincide"* a **"igual o superior"** (si un todo riesgo cumple y sale menos, se ofrece, pero **se dice**), se agrega *"el precio ordena, NUNCA filtra"*, el matching semántico se extiende a las **familias de tags del proveedor** (el literal `Cristales` no existe en la #10: llegan `Cristales Laterales`, `Cristal de Techo`, `Luneta`, `Parabrisas`, y Triunfo manda `Cristales y Cerraduras` en un solo tag), cada bullet tiene que salir de los tags de **esa** fila, el diferencial del nivel superior solo puede listar lo que **ninguna** alternativa del inferior ofrece, y se prohíbe afirmar equivalencia. **Se instrumentó `descripcion`, que el prompt nunca había nombrado** y es el único campo que habla de límites (`"Adicionales"` vs `"Adicionales con límite."` vs `"Adicionales: cristales, granizo y cerraduras"`). **Verificado contra Visred en vivo que no se pierde nada del response:** el sobre de la task es `{id, status, ready, message, result}` y se guarda `result` entero; `quotation_result` tiene 11 campos, `feature` exactamente 3 (`id`, `name`, `description`) — **no existe `feature_details`** —, `cover.additional_code` vale `"def"` en las 7 compañías, y **ninguno de los 52 paths del schema expone sublímites para vehículos**. Descartado también un caso que parecía falla y no lo era: en la conversación #12 los bullets de Triunfo C8 (cerraduras, ruedas, auxilio mecánico) **son correctos** contra la cotización real — la sospecha venía de comparar contra la base local, que tiene otros datos. Suite **891 verdes** · PHPStan ✅ · Pint ✅. **Los dos prompts se entregan como texto para carga manual** en `/admin/agent-prompts` (`checkout_closer`, `coverage_preference`); hasta cargarlos el runtime sigue con el criterio viejo. _(rama `fix/seleccion-por-features-pedidas`)_

- **2026-08-07** — **El comparador borraba una de las dos opciones que el cliente acababa de recibir 📉.** Reporte sobre `/cotizaciones/p8Ovx6PR0mn1tdgh`: el agente ofreció por WhatsApp Galicia **C80** a $73.106 y Galicia **Todo Riesgo Fr. 4%** a $109.655, y la página no mostraba ninguna comparación. Peor: **la C80 no aparecía en ningún lado** y el plan más barato de toda la página era $109.655. El cliente abría el link y no encontraba la opción que le habían pasado, sin explicación. **Causa:** `buildPublicView()` filtraba a **un solo grado** en su primera línea y todo se construía sobre ese conjunto; con un par cross-grade, `resolverRecomendadas()` buscaba la segunda alternativa dentro de las **ya filtradas**, no la encontraba, y devolvía `null` → sin `comparacion` → en el front `destacadas` vacío y colapsaban todos los `v-if` colgados de él. No era una decisión: el doc del comparador tiene nueve "decisiones ya tomadas" y el filtro por grado no está en ninguna, mientras la rama cross-grade del closer (`CheckoutAgent.md`, "presentá una de cada grade") es prescriptiva. La cotización tenía **137 alternativas** — 68 `basic`, 41 `all_risk`, 28 `liability` — y la página mostraba 41. **Arreglar el `null` no alcanzaba:** los datos muestran **contención**, no diferencia (las 11 coberturas de la C80 están todas dentro de las 21 de Todo Riesgo), así que el panel simétrico habría dejado la columna de la barata diciendo *"Nada que la otra no tenga"* y el bloque de precio *"vas a ahorrar $438.588 al año"*, que cross-grade es falso: no es ahorro, es menos cobertura. **Fix:** el universo visible pasa a ser la **unión de los grados presentados** (`gradesAMostrar()` devuelve lista), el reenganche por `claveVariante` busca en **todas** las alternativas, y cuando los grados difieren **y** la cara contiene a la barata la comparación se arma como **escalón**: la más completa arriba (aunque sea la cara — el orden lo da el precio y no la recomendación, si no la leyenda queda al revés), un bloque con la frase y las coberturas que suma, y la barata abajo con lo que cubre. La frase es *"Si bien la C80 sale $36.549 menos por mes, no lo veas como un ahorro: son estas 7 coberturas menos."* — habla desde el precio bajo porque es la lectura a corregir, y **no** abre con "por $X más" porque eso ya lo dice el agente en `alternative_reason` a pocos centímetros. Sin contención hay tradeoff en dos direcciones y vuelve el panel simétrico con una línea neutra. **Mismo grado no cambia en nada**, garantizado por construcción: el escalón exige grados distintos, así que `/cotizaciones/oPMrXxwnAZZHHgOE` (dos `liability`) sigue con sus dos columnas y su ahorro anual, que ahí **sí** es cierto. **Hallazgo de paso, y por qué la página no imprime el grado:** `normalized_grade` es el `default` de un `match` por substring en `VisredQuotationProvider`, o sea el cajón de lo que no matcheó — en esta sola cotización `basic` juntó B reales, **siete productos C** (`C80`, `C Mega`, `C1`, `C2`, `C8`, `Auto Plus +`, `M PLUS`…) y **una A** que cayó ahí por un typo del proveedor (`Responsablidad` sin la `i`). El agente, que lee las coberturas reales, llama "Terceros Completos" a la misma C80 que el campo marca `basic`. Se deja como señal interna para agrupar y ordenar; `cobertura.label` viaja `null` con dos grados y el copy se calla. **El adapter no se toca** — arreglar el mapeo cambia cómo el agente elige el par y es otro laburo. **Temporal:** `/cotizaciones/{token}/B` sirve la vista anterior para poder mostrar las dos lado a lado; no necesitó código propio (angostar el conjunto de grados a uno reproduce el comportamiento viejo, y el front renderiza la vista anterior sin ninguna rama de legacy). Dos bugs de presentación encontrados en el render y corregidos: el `<title>` salía `"null — …"` sin el guard, y las dos tarjetas del escalón mostraban "Recomendada" porque `esRecomendada()` es true para las dos presentadas. Suite **877 verdes** · PHPStan ✅ · Pint ✅ · build ✅. Verificado a 390px y 1280px, en las cuatro combinaciones de URL × tipo de cotización. _(rama `feat/comparador-cross-grade`)_

- **2026-08-05** — **Cotizábamos por debajo de lo que la compañía cobra 💸.** `VisredQuotationProvider::mapCover()` tomaba el `fee` de la cotización y le aplicaba **encima** el % del catálogo de descuentos, asumiendo que era el premio sin bonificar. **El `fee` ya viene bonificado**: se descontaba dos veces. El error iba en el peor sentido posible — el cliente veía un precio más bajo que el que la compañía después le cobraba. En el test que lo cubría el precio real era 1000 y mostrábamos 850. **Alcance exacto:** solo Triunfo tenía tope > 0 en `visred.max_discount_percent` (el resto cae al `default => 0`, donde la multiplicación no se dispara), así que era la única compañía afectada — y es justo aquella sobre la que hay confirmación de lo que efectivamente cobra. **Fix:** el precio es el `fee` tal cual. La bonificación elegida **no desaparece**: `MaxDiscountPolicy` sigue eligiendo la mayor bajo el tope y su ref sigue viajando como `discount_id` a la emisión (Triunfo la exige: sin ella devuelve 400). Para que nadie la re-multiplique, `mapCover()` ahora recibe `?string $discountRef` en vez del array con el `percent` — el error queda irrepresentable. Se corrigieron las **tres** afirmaciones del `docs/v2/visred-integration/ROADMAP.md` que documentaban el doble descuento como diseño, una de ellas llamándolo "aproximación documentada" (no era aproximado: estaba mal). **Y se documentaron las shapes medidas**, que es lo que faltó en junio y volvió a faltar: la forma real de `field_errors` de DRF (`{"payment": {"credit_card_number": ["…"]}}`, dict anidado, el mensaje colgando del subcampo), el comportamiento de `payment_method_id` en emisión para Triunfo (`tarjeta` → 400 que no cede ante ninguna de ~30 variantes; `tarjeta-cbu` → 200 y después FAILURE con `invalid literal for int()`, crasheando su propio worker con el valor que ellos devuelven; `cbu`/`cupon` emiten) y el catálogo de marcas por compañía. Estaban todas en JSON en un scratchpad que se borra. Suite **865 verdes** · PHPStan ✅ · Pint ✅. _(rama `fix/doble-descuento-fee`)_

- **2026-08-05** — **Las marcas de tarjeta del checkout eran una lista nuestra, y una de ellas no existe 💳.** `cc_brand` viajaba verbatim a `payment.credit_card_brand_id`, que del lado de Visred es una **FK de catálogo**, pero se validaba contra `in:visa,mastercard,amex,naranja,cabal,maestro` — seis valores que escribimos a mano en `CheckoutController` y espejamos a mano en el `<select>`, nunca verificados. La medición contra sandbox (`GET /v1/params/credit-card/`) dice dos cosas: **`maestro` no existe en ningún catálogo**, ni siquiera en el global de 33, así que un cliente que la elegía tenía la emisión rechazada por una opción que le ofrecimos nosotros; y los catálogos **difieren de verdad por compañía** (experta 7, triunfo 9 — con `cmr` y `kadicard`, que nunca ofrecimos —, galicia 13), o sea que ninguna lista fija podía estar bien para todas. **Fix:** `VisredCatalogService::creditCards($companyId)` (mismo molde que `taxConditions()`, reusa `normalize()` sin tocarlo), una entrada de caché por compañía, y el checkout ofrece las de **la compañía de la alternativa que el cliente eligió** — la saca de `quote_alternative_provider_refs.company_id`, que ya estaba poblado. `submit()` valida contra esa misma lista con `Rule::in`. **Dos decisiones que parecen detalles y no lo son.** Los duplicados **no se deduplican**: varias compañías listan `amex` **y** `american-express` con la misma description, son PKs distintos y no está verificado cuál acepta cada una — elegir uno por el cliente sería cambiar un riesgo visible (dos opciones iguales en el select) por uno invisible (la emisión falla y nadie sabe por qué). Y si el catálogo no está disponible se **cae al global** y la validación **falla abierta**: un hipo del endpoint de Visred no puede cortar una venta, y sin lista el comportamiento es el de siempre menos la whitelist inventada. **Detalle de tests:** `CheckoutControllerTest` no tenía **ningún** `Http::fake` (nunca hizo falta porque `submit` no pegaba a Visred); al agregar la consulta de catálogo, un `Http::fake()` catch-all en el `beforeEach` **deja muertos** a los `Http::fake([url => ...])` de cada test — los stubs se acumulan y gana el primero que matchea. Se resuelve con un fake que se evalúa por request contra estado del test. Verificado además contra el sandbox real (triunfo 9 / experta 7 / global 33) y en la página servida. Suite **865 verdes** · PHPStan ✅ · Pint ✅ · build ✅. Queda pendiente el passthrough de `payment_method_id` ([#8](https://github.com/andresrom1/workflow-assistant/issues/8)). *(Corregido el 18-08: acá se lo dio por **bloqueado** porque mandar `tarjeta-cbu` crashea el worker de Visred. El error era del análisis, no del proveedor — `tarjeta-cbu` no es un medio de pago de emisión y no había que mandarlo. Ver la entrada del 18-08.)* _(rama `feat/marcas-tarjeta-desde-catalogo`)_

- **2026-08-04** — **Un lote de facturación se cayó a la mitad y facturó dos veces al mismo cliente 🧾.** El lote 0007 (8 comprobantes, pto vta 3, **AFIP producción**) murió tras autorizar 6. Tres daños distintos, con una sola causa de arranque: el worker en uso mata al proceso hijo a los **60s** (`queue:work --once ... exceeded the timeout of 60 seconds` en el log — es un `queue:listen` sin `--timeout`, **no** el de `composer.json`) y el lote tardaba ~75s. El `$timeout = 600` del job no protege: lo mata el proceso padre, no el worker. Con `retry_after=90` en la conexión `database`, la cola lo reencoló hasta agotar `tries` → `MaxAttemptsExceededException`. **Es la segunda vez que esta misma clase de bug pega** (ver deuda técnica "Queues — worker de IA reclamaba jobs a los 90s", 2026-06-05). **Daño 1, el caro:** el proceso murió entre `autorizar()` y el `update()` del CAE. AFIP tenía el comprobante **18** emitido para Seguros Galicia, la base no se enteró, y al reanudar `ultimoAutorizado()` devolvió 18 → le asignó el **19** a la misma factura. Confirmado por `FECompConsultar`: 18 y 19 con idéntico CUIT `30500000127` e importe `162739.46`, dos CAE distintos. Queda pendiente la **nota de crédito por el 18** (el módulo no emite NC). **Daño 2:** el crash salteó `cerrar()` y `failed()` solo logueaba → lote en `processing` para siempre, con el formulario bloqueado (`v-else`) y el ZIP escondido porque su gate leía `summary`, que era `null`. **Daño 3:** `WithoutOverlapping` sin `expireAfter` dejó el lock `afip-ptovta:3` tomado **24h** (default de `DatabaseLock`); hubo que liberarlo a mano para poder reanudar. **Fix estructural — el número de comprobante es la clave de idempotencia de AFIP** (reenviar `FECAESolicitar` con un número ya autorizado da error 10016, no un duplicado), así que se persiste **antes** de llamar: nueva columna `numero_reservado` + dos índices únicos sobre `(pto_vta, tipo_comprobante, ·)` — la única defensa que no depende de que el código se comporte. Al reintentar, si hay reserva se **reconcilia** con el nuevo `AfipSoapService::consultar()` en vez de asumir hacia adelante: autorizado y coincide receptor+importe → adopta el CAE **sin re-emitir**; autorizado pero ajeno → marca para revisión manual y **nunca pisa el CAE de otro**; no existe → emite con **ese mismo** número (sin hueco en la correlativa); indeterminado → propaga y deja la reserva. Ojo con `consultar()`: AFIP contesta "no existe" con el **error 602**, que es respuesta legítima, así que no puede pasar por `throwOnErrors()` — se compara por código y no por el texto en prosa. **Se separó `AfipRespuestaIndeterminadaException` de `AfipEmisionException`**, que hasta ahora significaban dos cosas incompatibles: rechazo de AFIP (número libre, seguir el lote) vs. respuesta ilegible / SOAP Fault (la factura puede estar emitida). El job captura **solo** el rechazo; todo lo demás propaga para que el reintento reconcilie. **Un job por comprobante** (`EmitInvoice`, ~10s cada uno) encadenado con `Bus::chain` — no `Bus::batch`, que corre en paralelo y la numeración correlativa no lo tolera — cerrando con `CloseInvoiceBatch`, y un `catch()` que lo marca `failed`: el lote siempre llega a un estado terminal. Por durar ~10s **no hizo falta cola nueva ni tocar `retry_after`**, lo que además evita mandar jobs a una cola que ningún worker consuma. **UI:** `POST /facturacion/batches/{id}/resume` retoma solo lo `Pending`, el gate del ZIP se deriva de las facturas y no de `summary`, y el index deja de filtrar por `processing` (un lote `failed` era invisible). Reanudar un lote **sin** pendientes lo cierra en vez de dar error — si no, un lote fallido sin nada por emitir bloqueaba el formulario para siempre. Suite **851 verdes** · PHPStan ✅ · Pint ✅ · build ✅. Los lotes 2, 3 y 4 se auditaron contra AFIP: sin huérfanos. _(rama `main`)_

- **2026-08-03** — **El mensaje de error de Visred lo destruíamos nosotros 🔧.** Una emisión real falló con `400 validation_error` y `field_errors: {"payment": [""]}`: el mail al equipo no decía qué campo rechazó la compañía. **El `[""]` no era de Visred.** `VisredApiException::normalizeFieldErrors()` asumía `dict[str, list[str]]` y mapeaba a `''` todo valor no-escalar — o sea, el dict entero de cualquier serializer anidado. Visred corre Django REST Framework y `payment` / `person_holder` **son** serializers anidados, así que el mensaje real y el nombre del subcampo se perdían siempre. **Ya había pasado dos veces** y quedó documentado como si fuera comportamiento del proveedor (`VisredEmissionProvider::documentTypeIdFor()` y `docs/v2/visred-integration/ROADMAP.md`, ambos con `{person_holder: [""]}`); las dos entradas quedan corregidas — la conclusión operativa se sostiene porque se aisló por A/B contra el sandbox, no leyendo ese payload. **Fix:** aplanado recursivo con notación de puntos (`payment.credit_card_brand_id`), **manteniendo** la shape pública `array<string, list<string>>`. Aplanar en vez de anidar no es cosmético: el blade del mail hace `implode()` sobre el segundo nivel, y las claves con punto son el idioma del validator de Laravel. Tres sutilezas que no se pueden simplificar: el bucket lo decide el **hijo** y no la clave (si no, `{"product_id":["Requerido."]}` daría `product_id.0`); se acumula con `[]=` porque dos ramas pueden caer en el mismo path; y no se puede `array_values()` el recorrido porque en `many=True` el índice original es parte del path (`inspections.1.image_base64`). **Red de seguridad aparte:** `VisredClient` no logueaba **nada** en ningún fallo — ni siquiera importaba `Log` —, así que un 4xx desde documentos, inspecciones, catálogos o el polling de `/tasks/` se perdía entero; ahora hay un `Log::warning` en el único punto de throw, **solo con el bloque `error` de la respuesta** (nunca el request: el body de `emitir/` lleva el número de tarjeta). **Gap de tests cerrado:** el ROADMAP afirmaba que `EmisionFallidaMail` se renderizaba de verdad, pero no había un solo `->render()` en `tests/` — se había hecho a mano. `EmisionFallidaMailTest` automatiza la cadena completa `respuesta DRF → normalizador → job → mailable → blade`. **Y un hallazgo de paso, del mismo tenor:** `phpunit.xml` declaraba un solo testsuite (`Feature`), así que **`tests/Unit` nunca corría** — 120 tests que existían en el repo y solo se ejecutaban si se les pasaba el path a mano, entre ellos `QuoteComparisonServiceTest` y `FranquiciaTest`, que la entrada del 27-jul contaba como verificación del comparador. Se detectó porque el total de la suite no subía al agregar tests nuevos. Testsuite `Unit` agregado; los 120 pasan. Suite **840 verdes** (720 Feature + 120 Unit) · PHPStan ✅ · Pint ✅. Queda pendiente el passthrough de `payment_method_id` ([#8](https://github.com/andresrom1/workflow-assistant/issues/8)): la alternativa que el cliente aceptó se cotizó bajo `tarjeta-cbu` y la emisión manda `tarjeta` hardcodeado. Se despliega aparte a propósito — con el normalizador arreglado, el próximo fallo dice si el rechazo es por ahí o por `credit_card_brand_id`, que viaja verbatim desde una lista hardcodeada nuestra a lo que del lado de Visred es una FK de catálogo nunca verificada. _(rama `fix/visred-field-errors-anidados`)_

- **2026-07-27** — **El comparador de coberturas queda alcanzable ✅.** La vista pública existía pero nadie le mandaba el link al cliente, así que en producción era inalcanzable. **Envío del link:** `PresentQuoteOptionsTool` deja la URL armada en `metadata.pending_public_link` y el orquestador la devuelve junto a los botones; los dos puntos que despachan salida (`ProcessConversationInbox` y `NotifyClientQuoteReady`) mandan el link en un **segundo mensaje encadenado con `Bus::chain`** — aparte y no pegado al de presentación porque los 3 slots de botones de Meta ya están ocupados y un `cta_url` no se mezcla con reply buttons; encadenado y no dos `dispatch()` sueltos porque caen en la misma cola y con más de un worker el link se adelanta al mensaje que le da sentido. El LLM **sigue sin ver la URL** (la deformaría) pero ahora **sí sabe que el mensaje sale**, así puede cerrar con "abajo te paso el detalle" en vez de ignorar algo que el cliente sí ve. **CTA "La quiero":** `POST /cotizaciones/{token}/checkout` abre el checkout directo en vez de mandar al chat con un texto prearmado. Va **sin CSRF a propósito** — la página no tiene autenticación por cookie, así que no hay autoridad ambiente que proteger, y con el guard puesto un link abierto días después revienta con 419 por sesión vencida, que es justo el caso a cubrir. Desde **escritorio** el link del checkout se manda por WhatsApp con un modal que lo explica (el formulario necesita la cámara para las 7 fotos); el destinatario se verifica **antes** de abrir el checkout, y el modal ofrece siempre un botón para escribirnos porque si la ventana de 24hs de Meta está cerrada la API rechaza el mensaje en silencio. **Un solo camino para crear el checkout:** estaba duplicado en `WhatsAppAdapter` y `AgentToolAdapter`; ahora es `QuoteService::crearCheckout()` + `QuoteRepository::marcarCheckoutPendiente()` (único lugar que escribe `checkout_token`, con verificación de unicidad como `ensurePublicToken()`). Dos cambios de comportamiento: **una cotización vencida ya no abre checkout** por ningún canal (los precios valen por el día en que se cotizó), y **`checkout_done` lo escribe el service y no `CheckoutTool`** — única excepción a "el estado lo escribe la tool", porque el checkout también se abre desde la web donde no corre ninguna tool. **Canario del vocabulario:** el diff de la vista es diferencia de conjuntos sin diccionario de sinónimos; cuando el proveedor manda un tag nuevo el diff no se rompe, **miente en silencio**. `QuoteService::auditarVocabulario()` avisa por `tag_desconocido`, `tag_variante` (el mismo escrito distinto — el peor caso) y `tag_compuesto`. **El supuesto de "22 tags" del doc era falso: en la base hay 35**, con pares como `Destrucción Total por accidente` (193) vs `...por Accidente` (48). **Variantes duplicadas resueltas:** lo que distingue las repeticiones con distinto `external_quote_id` es **`payment_method_id`** (`tarjeta-cbu`, `cupon`, `cbu`, `tarjeta`, `todos`), visible en `quote_provider_refs.raw_response` — Triunfo cobra ~5,5% más por cupón, Mercantil cobra igual. La regla actual (la más barata) acierta por coincidencia; modelarlo queda en [#8](https://github.com/andresrom1/workflow-assistant/issues/8). Además: `saveResults` invalida la presentación anterior al recotizar (quedaba apuntando a ids borrados), las factories salen de `database/Factories/` (el autoload no las encontraba en filesystems case-sensitive) y `vite.config.js` deja de hardcodear una IP de LAN. Fuera de scope, como issues: [#7](https://github.com/andresrom1/workflow-assistant/issues/7) vigencia en `CheckoutController`, [#9](https://github.com/andresrom1/workflow-assistant/issues/9) retención de `agent_execution_logs`, [#10](https://github.com/andresrom1/workflow-assistant/issues/10) patente en el checkout. Suite **695 verdes** · PHPStan ✅ · Pint ✅ · build ✅. _(rama `feat/comparador-coberturas`, commits `21bd487`→`25222aa`)_

- **2026-07-26** — **Vista pública de cotizaciones (comparador de coberturas) 🚧.** El agente presenta al cliente **exactamente 2** alternativas por WhatsApp (`PresentQuoteOptionsTool` fuerza `min(2)->max(2)` por el límite de 3 botones de Meta), mientras una cotización real trae entre 24 y 118: todo el detalle de coberturas quedaba en el backend. Nueva ruta pública **`GET /cotizaciones/{token16}`** con las dos recomendadas marcadas y su justificación, el resto agrupado por compañía, y un diff entre las dos. Se validó primero como maqueta hardcodeada, se aprobó, y se portó a datos reales. **Dominio:** `QuoteComparisonService` (glosario canónico desde `full_details`, filtro de alternativas sin coberturas, dedupe a la más barata dentro de compañía+franquicia+coberturas, agrupación con "desde $X", diff por diferencia de conjuntos sobre `features_tags`); `App\Support\Franquicia` extrae porcentaje y mínimo del **título** del producto, que es donde el proveedor manda la franquicia como texto libre en 5 formatos distintos (si no parsea devuelve `null` y la clave de dedupe cae al título crudo, para no colapsar productos distintos en silencio). **Sin diccionario de sinónimos ni LLM en el diff:** se verificó sobre la base entera de producción que el vocabulario es cerrado (22 tags) y que **cada tag tiene una única descripción, idéntica entre las 7 compañías** (~3.700 apariciones, cero excepciones) — el proveedor ya normaliza. Se evaluó y descartó un catálogo canónico con tabla de mapeo. **Vigencia:** `quotes.expires_at` pasa de `now()->addDays(7)` a **fin del día calendario argentino** en UTC (`Quote::endOfBusinessDay()`, +`isVigente()`/`scopeVigente()`); antes ese campo se escribía y **nadie lo comparaba nunca contra `now()`**. **Persistencia de la recomendación:** migración con `public_token` (16 chars, minteado al presentar, idempotente), `recommended_alternative_id`, `presented_alternative_ids`, `presentation_reasons` y `presented_at`; los escribe `PresentQuoteOptionsTool`, que suma al schema `recommended_reason`/`alternative_reason` (obligatorias, truncadas a 600) y dos guards nuevos (`recommended_not_in_set` — antes un id fuera del par no reordenaba nada y quedaba mal atribuido en silencio — y `missing_reason`). Hasta acá la única traza de qué se presentó era `agent_execution_logs.tool_calls`, un log de auditoría sin retención. Prompt de `CheckoutAgent` actualizado con la **deuda documentada** (doble fuente de verdad con el texto del chat, contenido que va a una página pública, razones que se congelan al presentar) y sincronizado a v8. **Privacidad:** la página **no expone patente, DNI, nombre ni teléfono** — props enumeradas a mano, nunca `$snapshot->toArray()`, con test que busca los tres valores en el HTML crudo. Middleware `noindex` (header `X-Robots-Tag` + meta condicional en el blade raíz; **no** se toca `robots.txt`: un `Disallow` impediría el crawl y sin crawl nadie lee el noindex). **UI:** dos layouts, bottom sheets en móvil y master-detail en escritorio, corte por `matchMedia` en 900px; vencida la cotización aparece un banner y el CTA de contratar se reemplaza por un botón deshabilitado. Verificado contra la cotización 30 local: **26 alternativas → 17 planes** tras filtrar las sin coberturas y colapsar los duplicados de Mercantil Andina y San Cristóbal. Suite **673 verdes** · Pint ✅ · build ✅. **Pendientes en [`docs/comparador-coberturas-pendientes.md`](docs/comparador-coberturas-pendientes.md)** — el más importante: **nadie le manda el link al cliente todavía** (quedó fuera de scope), así que la vista es inalcanzable en producción. Además: el CTA "La quiero" tiene que **crear el checkout** en vez de mandar al chat, lo que vuelve obligatorio el guard de vigencia que hoy no existe (`CheckoutController` guarda solo por `status`), falta el canario de tags nuevos, y sigue sin explicarse qué distingue las variantes repetidas que devuelve el proveedor con distinto `external_quote_id`. _(rama `feat/comparador-coberturas`, commits `6ad6e6f`→`c38a2c8`)_

- **2026-07-26** — **Clientes duplicados: toda puerta identifica con `CustomerIdentificationService`.**
  Reporte: tres `Customer` para el mismo teléfono (`+5493516280778`, ids 7, 9 y 10). Diagnóstico: la
  búsqueda no fallaba, **ya no existía**. Hasta el 24-jul `InsuranceOrchestrator::handle()` arrancaba con
  `tryAutoIdentifyByPhone()` → `CustomerIdentificationService::findOrCreate()` →
  `CustomerRepository::findByPhone()`, y linkeaba el cliente existente a la conversación nueva. El commit
  `626ed99` (migración a BSUID) la eliminó a propósito ("se materializa desde la ingesta sin
  deduplicación insegura") y la reemplazó por `syncCustomerIdentifiedState()`, que solo lee el DNI para
  prender un flag; la creación quedó en `ProcessWhatsAppMessage::captureCustomerAttributes()` con un
  `create()` pelado. Las fechas cierran: el cliente 7 (23-jul) es anterior al refactor y fue legítimo;
  los duplicados son todos posteriores. Efecto secundario: `findByPhone()` quedó sin usos.
  **Decisión de dominio (Andrés):** si cualquier dato conocido coincide, es el mismo cliente — el
  teléfono incluido. Fix: (1) `findCustomer()` gana el tipo `ext_user_id`, que resuelve el BSUID por
  `conversations.ext_user_id` **incluidas las archivadas** (el Reset del admin archiva, y por eso el
  usuario que volvía parecía nuevo), y pasa a público; (2) la búsqueda por `dni` compara la identidad
  derivada (`DocumentoIdentidad::clave()`, DNI para físicas / CUIT para jurídicas) contra
  `documento_key` en vez del número crudo, con respaldo exacto para filas viejas; (3) las tres puertas
  que se salteaban el servicio ahora lo usan: ingesta de WhatsApp, `PolicyChainResolver` (que tenía su
  propia copia de la búsqueda por documento) y las dos altas manuales del admin, que además avisan
  "este cliente ya existía"; (4) comando `customers:dedupe` (en seco por defecto, `--apply` para
  ejecutar) para los duplicados que ya están en producción, agrupando por teléfono y por BSUID.
  Se revirtió la aserción del test `does not merge two different bsuids that share a phone`, que
  blindaba la regla vieja. Principio documentado en el CLAUDE.md, junto con la distinción
  servicio (identificar) vs helper (`DocumentoIdentidad`, normalizar/derivar).

- **2026-07-25** — **Las tools vuelven a entrar por `handleToolCall()`: los errores dejan de ser
  invisibles.** Reporte: el cliente dio el DNI por WhatsApp (conversación 10) y el agente contestó
  "Tuve un inconveniente técnico"; `laravel.log` no tenía **ni una línea** de ese turno. El resultado
  real de la tool quedó en `agent_conversation_messages.tool_results`:
  `"Invalid parameters for tool : IdentifyCustomerTool"`, dos veces. Causa de la ceguera: las 7 tools
  llamaban al handler del adapter **directo** (`$adapter->identifyCustomer(...)`), salteándose
  `handleToolCall()`, que es donde vive el `try/catch` con los `Log::warning`/`Log::error`. Sin ese
  catch, la excepción escapa al SDK; Prism mapea cualquier `TypeError`/`InvalidArgumentException` a
  ese texto genérico (`PrismException::invalidParameterInTool()`), lo captura en
  `CallsTools::executeToolCall()` y lo devuelve como resultado de tool **sin loguear**, descartando el
  mensaje original (`getPrevious()` se pierde). O sea: el motivo real nunca se escribió en ningún
  lado. Fix: `handleToolCall(array $payload, string $toolName, ?Conversation $conversation = null)`
  —la conversación ahora llega inyectada desde la tool en vez de re-resolverse por
  `external_conversation_id` (vestigio del camino web), con fallback al lookup viejo para
  `ToolsController`—, se agregaron los arms faltantes (`provide_vehicle_fact`, `revert_stage`), el
  catch pasó de `\Exception` a **`\Throwable`** (un `TypeError` es `Error`, no `Exception`: se escapaba)
  y ambos logs incluyen ahora `tool`, `conversation_id`, `payload`, clase de excepción, archivo:línea
  y trace. Las 7 tools pasan por ahí. Verificado con `ToolErrorVisibilityTest` (7 casos) y sonda
  manual: cada payload malformado devuelve su motivo real
  (`DNI inválido`, `The identifier value field must be a string.`,
  `The selected identifier type is invalid.`, `... field is required.`) en vez de un texto opaco.
  **Nota sobre el incidente:** ninguna de esas cinco validaciones explica el fallo si el payload que
  llegó a la tool fue el que quedó guardado (`{"identifier_type":"dni","identifier_value":"30123727"}`
  → ese caso pasa y persiste el DNI, probado en prod contra la conversación 10). Queda pendiente de
  diagnóstico; con esta instrumentación la próxima vez el motivo sale en una línea de log.
  **Regla:** ninguna tool debe llamar a los handlers del adapter directamente.

- **2026-07-25** — **Migración a DeepSeek v4 + normalización del modelo por agente.** DeepSeek
  deprecó `deepseek-chat` y `deepseek-reasoner` (2026-07-24) en favor de `deepseek-v4-flash` y
  `deepseek-v4-pro`. Se aprovechó para arreglar la inconsistencia de fondo: solo 3 de los 8 agentes
  declaraban modelo (con un string hardcodeado en `#[Model]`), y los otros 5 más los 4 call-sites de
  agente anónimo caían al default del driver, que estaba **hardcodeado en vendor**
  (`DeepSeekProvider::defaultTextModel()` → `'deepseek-chat'`) — no había ningún lugar del repo donde
  leer o cambiar el modelo de la mayoría de la flota. Ahora: `config/ai.php` gana
  `providers.deepseek.models.text.{default,cheapest,smartest}` (env-able, la clave que ya leía el SDK)
  y **los 8 agentes declaran un atributo de tier** — `#[UseSmartestModel]` en `CheckoutAgent` y
  `ConversationAnalyzerAgent`, `#[UseCheapestModel]` en los otros 6. Cero nombres de modelo en `app/`;
  los agentes anónimos absorben el `default` sin tocarlos. Ojo: `#[Model]` **gana** sobre los
  atributos de tier, por eso hubo que borrarlo, no complementarlo. Se preservó el tiering previo
  (flash = ex-chat, pro = ex-reasoner) y el `#[Timeout(360)]` de checkout. Se eliminaron 3 claves de
  config muertas que apuntaban a los modelos deprecados y no tenían consumidor:
  `ai.semantic_analysis.{model,provider}` e `ingesta.extraction_model`. Registrado en
  `docs/configuracion-hardcodeada.md` §5 (resuelve la fila "Modelo por agente", que proponía
  `ai.agent_models.*`; el destino real fue la clave nativa del provider).
- **2026-07-21** — **Fix UX: ancho de burbuja en la recomendación de cotización (WhatsApp).** La
  presentación de las 2 opciones con botones se renderizaba en una burbuja interactiva angosta (ancho
  fijo de WhatsApp para `interactive.type: button`, no configurable) porque todo el texto largo iba en
  `interactive.body.text`. Ahora `SendWhatsAppMessage` parte el texto del agente en su último párrafo:
  el cuerpo (las 2 opciones) sale como mensaje de **texto** full-width y solo la pregunta de cierre
  acompaña a los botones en la burbuja compacta (`splitForButtons()`). Sin `\n\n` no parte (mensajes
  cortos siguen como interactivo único); se conserva el fallback a texto plano si el caption sin partir
  supera 1024. Sin cambios en prompt/service/tool. Nuevo test del camino de split; suite del job 10/10,
  PHPStan 0.
- **2026-07-20** — **Limpieza del residuo pas-mobile en settings + mapa de configuración hardcodeada.**
  Disparado por la decisión de auditar la configuración hardcodeada del sistema para extender la vista
  de Configuración. **Fase 0 (limpieza):** el grupo de settings `poliza_api` (la "API de Emisión" del
  legacy pas-mobile, reemplazada por Visred) no tenía **ningún consumidor** en `app/` — extirpado:
  migración `drop_poliza_api_settings`, bloque `poliza_api` de `config/services.php` (vars
  `POLIZA_API_*` — quedan en el `.env` local, limpieza manual), labels muertos `pas`/`poliza_api` del
  `SettingsController`, y el dashboard hardcodeado de `Admin/Settings/Index.vue` (stat cards +
  `EndpointRow` de la API muerta; la vista soporta `string`/`integer`/`boolean`/`secret`). Además,
  `SystemSettingsSeeder` estaba **huérfano** (nadie lo llamaba) y era la única fuente del grupo
  `checkout` (vivo, 4 consumidores): eliminado y reemplazado por la migración `seed_checkout_settings`,
  que unifica la vía "seed por migración de feature" (`facturacion`, `followup`) e inserta **solo si la
  key no existe** para no pisar valores editados en prod. **Fases 1-2 (inventario):** barrido de 9
  patrones (constantes de clase, `config()` con default inline, `$tries`/`$timeout` de jobs, atributos
  `#[Model]`, URLs, montos, scheduler, lectura de `config/*`, front+seeders) y mapa de referencia
  expandido en `docs/configuracion-hardcodeada.md` — ~80 ítems clasificados por destino (A vista admin /
  B config+env / C invariante de código / P perfil por-PAS), incluyendo **keys fantasma** (consumidas
  por `config()` sin existir en ningún config file: `app.allow_fresh_migrations`,
  `app.tracking_base_url`, `mail.checkout_notifications_to` colgada de `mail.from`) y los gaps de la
  vista (faltan tipos `float`/`json`, labels/íconos de grupo hardcodeados). **Pendiente (triaje con el
  usuario):** qué candidatos A pasan a la vista. Verificación: tests settings+checkout+facturación
  74/74 ✅ · PHPStan 0 · Pint ✅ · build ✅. _(rama `main`, sin commit)_

- **2026-07-20** — **Red de seguridad: ningún Customer sin PAS (nacimiento + borrado).** Reporte:
  un customer terminó el checkout con `pas_id = null`; el mobile lo consume (bloque "TU PRODUCTOR" +
  CTAs Llamar/WhatsApp). Dos causas: (1) la creación real (chat `CustomerIdentificationService::createCustomer`
  → `CustomerRepository::create` y checkout `consolidation->apply`) **nunca** asignaba `pas_id` — solo lo
  hacía el backfill de una vez del `CustomerSeeder`; (2) la FK `customers.pas_id` es `nullOnDelete`, así que
  borrar un PAS ponía en null a todos sus customers (`Admin/UserController::destroy` no reasignaba). Fix en
  tres capas apoyadas en un resolver unificado **`User::defaultPas()`** (config `mango.default_pas_email` con
  fallback al canónico `AdminUserSeeder::EMAIL`): (a) `CustomerRepository::create` asigna el default si no
  viene `pas_id`; (b) evento `deleting` en `User` reasigna los customers del PAS borrado al default antes de
  que dispare el `nullOnDelete`; (c) guard en `UserController::destroy` (403) que impide borrar al PAS por
  default + `DB::transaction`. Unificado también `SiniestroController::defaultPas()` y `SiniestroGuidanceTool`
  para consumir el resolver (elimina la segunda fuente de verdad que devolvía null si faltaba la env).
  Config hardcodeada marcada con `// opcion-de-configuracion` y registrada en
  `docs/configuracion-hardcodeada.md` (insumo del refactor de settings, sin tocar `SystemSetting`).
  Fuera de alcance: vista de perfil por-PAS (teléfono/matrícula) y reasignación manual del customer huérfano
  existente. Tests: `tests/Feature/PasSafetyNetTest.php` (6) + `SeedPasAssignmentTest`/`SiniestroTest`/
  `SiniestroGuidanceToolTest` (14) verdes; PHPStan 0. _(rama `main`)_
- **2026-07-20** — **Fix cotización: `fuel_type_id` volvió al binario `sin-gnc`/`gnc` — RUS recuperado ✅.**
  Reporte del usuario: "`Input should be 'sin-gnc' or 'gnc'` sigue ahí, Galicia y RUS fallan". Causa:
  el mapeo de combustible a ids específicos del catálogo (`nafta`/`diesel`/…), introducido el
  2026-06-08 como "mejora", es una **regresión** — `cotizar/` los acepta pero **Galicia y RUS
  validan por su cuenta río abajo** y solo aceptan el binario, terminando la task en `FAILURE`
  (no un 400). El smoke de junio no lo vio porque midió tasks CREADAS, no estado terminal SUCCESS.
  Confirmado con **sondeo del schema en vivo** (`fuel_type_id` es `string` sin enum; emisión pregunta
  GNC como booleano `has_gnc` → en cotización el campo es la pregunta de GNC, no el combustible) y con
  **verificación live** (control `nafta` vs `sin-gnc`, mismo request): **RUS pasa de FAILURE →
  SUCCESS**. Galicia sigue fallando pero por **otra causa** (`"Field required"` / infra
  `external_service_unavailable`, con `nafta` y con `sin-gnc` por igual), ortogonal al combustible.
  Fix: `VisredQuotationProvider::FUEL_MAP` binario (`gnc`/`con-gnc`→`gnc`; resto→`sin-gnc`;
  desconocido→omite); `insured_amount_fuel` sigue en 1.500.000 cuando GNC; nuevo `logQuoteSummary`
  (resumen por-compañía SUCCESS/FAILURE/PENDING) para no volver a quedar ciego. Tests: reescrito el
  caso que blindaba la regresión. Suite Visred 14/14 + grupo 63/63 · PHPStan 0. Detalle en
  `docs/v2/visred-integration/ROADMAP.md` (D7 + Bitácora 2026-07-20). _(rama `main`)_

- **2026-07-19** — **CustomerIdentifierAgent: UX sin fricción + identidad del asistente.**
  Disparado por revisión de una conversación real (conversation_id=5): el agente atacaba con
  pedido de nombre y DNI apenas el cliente saludaba, sin saber si quería cotizar, y se
  presentó como "Juan, productor asesor" — nombre inventado, no definido en ningún prompt.
  **Fix:** `CustomerIdentifierAgent.md` reescrito — saludo abierto sin pedir datos; nombre+DNI
  se piden **una sola vez**, juntos, y solo cuando el cliente expresó intención de cotizar.
  `DeclineDniTool` ampliada para cerrar el paso tanto ante negativa explícita como ante
  omisión silenciosa (antes solo cubría la negativa, lo que podía trabar al agente
  repreguntando indefinidamente si el cliente ignoraba el DNI sin negarse). El cierre hacia la
  etapa de vehículo ahora pide **los 7 datos del auto juntos** en vez de "marca y modelo": antes
  preguntábamos dos, el cliente contestaba dos y `VehicleIdentifierAgent` le pedía el resto —
  un turno extra de fricción por conversación. Documentada también la salida del caso BSUID
  (`decline_dni` → `missing_customer`): ahí el agente sí pide email o teléfono, único camino
  para avanzar cuando el `wa_id` no es numérico y `tryAutoIdentifyByPhone` no vinculó a nadie.
  Identidad del asistente fijada en `shared_style.md`: "el asistente de MANGO", sin nombre
  propio, prohibido inventarse uno. Alcance acotado a propósito: `identify_customer` no persiste el nombre dicho
  en el chat — el contacto (teléfono/WBID) ya está resuelto por `tryAutoIdentifyByPhone`, y el
  DNI sigue sin ser bloqueante para avanzar. PHPStan 0 nuevos · tests `DeclineDniTool`/
  `DeclineDniFlow`/`IdentifyCustomerTool` verdes. Prompts sincronizados manualmente vía
  `/admin/agent-prompts` (no vía tinker).

- **2026-07-19** — **Diagnóstico: ninguna póliza se puede emitir hoy (DNI placeholder de cotización) — Fase 0 del fix verificada, código pendiente.**
  Disparado por la revisión de una conversación real del piloto (conversation_id=3): el
  checkout se completó, el cliente recibió "¡Listo!" y "tu póliza ya está en proceso de
  emisión" por WhatsApp, pero nunca llegó documentación. Diagnóstico en prod (tinker):
  `EmitirPoliza` había fallado 5 veces con `VisredApiException` — el log solo mostraba
  "Error de validación." (mensaje opaco, `field_errors` nunca se logueaba en el camino de
  emisión). Reproduciendo `PolizaEmisionService::emitir()` a mano, el 400 real es
  `non_field_errors: ["Debes usar el mismo document_number que en la cotización"]`.
  **Causa raíz:** la cotización manda un DNI placeholder (`config('visred.default_holder_dni')`,
  decisión de junio, docs/v2/08 §2.2 y ROADMAP D8) porque el DNI real recién se captura en el
  checkout, bajo el supuesto de que se podía "sobrescribir con el real al emitir". **Ese
  supuesto nunca se verificó y es falso** — Visred exige coincidencia. Además, aunque se saque
  el placeholder, hay un segundo desajuste independiente: la cotización usa `$customer->dni`
  (normalizado por `DocumentoIdentidad` en `Customer::saving`) y la emisión usa `$session->dni`
  (crudo del formulario del checkout, sin normalizar) — mismo cliente, dos formatos, mismo 400.
  **Fase 0 (sonda contra el sandbox) ✅ verificada** — ver detalle en
  `docs/v2/visred-integration/ROADMAP.md` Bitácora 2026-07-19: cotizar **sin** `person_holder`
  (omitido, no placeholder) y emitir sobre ese `quotation_result_id` con un DNI real **funciona**
  (Triunfo, `presale_id 32096`, `policy_number 1822203`). Confirma que la rama "cotizar sin DNI
  si el cliente se niega" del plan es viable.
  **Plan de fix acordado** (fases independientes, detalle completo en el plan de esta sesión):
  Fase 1 — hacer visible el fallo de emisión (loguear `field_errors`, avisar al equipo primero
  y al cliente después por WhatsApp sin pasar por el LLM, normalizar el DNI de la emisión,
  fail-fast en 400, `quotes.status = 'emision_fallida'`). Fase 2 — el checkout deja de afirmar
  que la póliza se emitió (pantalla de éxito + mensaje de WhatsApp), y se corrige que el 500 del
  submit no se le mostraba al cliente. Fase 3 — sacar el placeholder, omitir `person_holder`
  cuando no hay DNI real, guard de coincidencia DNI en el checkout (422 auto-corregible en vez
  de 400 silencioso 10 minutos después), actualizar docs/v2/08. Fase 4 — pedir el DNI en
  `CustomerIdentifierAgent` (explicando el motivo, avanza igual si se niega con 5 de 7
  compañías), sin agregar flag nuevo a `ai_state` (tocaría 12 lugares y corre la numeración de
  `agent_execution_logs.step`). **Código de las Fases 1-4b: aún no implementado**, arranca a
  continuación de esta entrada.

- **2026-07-19** — **Fases 1-4a del fix de emisión — implementadas y verificadas ✅ (código) / ⬜ (Fase 4b: carga del prompt).**
  Continuación de la entrada anterior.
  **Fase 1 — visibilidad del fallo de emisión.** `EmitirPoliza` loguea
  `status`/`error_code`/`field_errors` de `VisredApiException` en vez de solo el mensaje genérico;
  fail-fast (`$this->fail()`) en 400 `validation_error` (determinístico, no tiene sentido
  reintentar 10 minutos). `failed()` avisa primero al equipo (`EmisionFallidaMail`, nuevo, mismo
  patrón que `CheckoutCompletadoMail`) y después al cliente por WhatsApp sin pasar por el LLM
  (`NotifyClientEmissionFailed`, nuevo, idempotente por `Cache::add` igual que
  `SendPolicyDocumentsToClient`); marca `quotes.status = 'emision_fallida'` (antes quedaba en
  `checkout_submitted`, indistinguible de "en vuelo") — con guard: si el quote ya está
  `poliza_emitida` (falló algo POSTERIOR a la emisión, p. ej. persistir un PDF), `failed()` no
  pisa el estado ni le avisa al cliente que falló algo que en realidad tiene emitido.
  Se unificó el destinatario del mail interno a
  `SettingsService::get('checkout.notifications_email', ...)` (existía un setting editable en el
  admin que nadie leía).
  **Descartado en la review:** se había movido los 3 dispatch + el mail **fuera** de la
  `DB::transaction` de `CheckoutController::submit`, bajo el supuesto de que
  `onConnection('database_ai')` era una conexión distinta y un worker podía ver el job antes del
  commit. **El supuesto es falso:** las 4 conexiones de cola tienen `connection => null`, o sea
  usan la misma conexión PDO que la transacción, así que los inserts en `jobs` ya eran atómicos.
  Peor, sacarlos introducía una regresión: un fallo posterior al commit (p. ej. el mail) le
  devolvía 500 al cliente con los datos ya guardados, y el reintento le daba 409. **Revertido** —
  quedan adentro, con un comentario explicando por qué.
  **Fase 2 — el checkout deja de afirmar la emisión.** `Checkout/Success.vue` y
  `NotifyClientCheckoutCompleted` ya no dicen "tu póliza ya está en proceso de emisión" — dicen
  que se está gestionando con la compañía. Se sacó el prop `email` de la pantalla de éxito (no
  hacía nada real: ningún job le manda mail al cliente, WhatsApp es el único canal). Fix aparte:
  `Checkout/Show.vue` no mostraba nada al cliente ante un 500 del submit (el spinner se apagaba
  sin explicación) — ahora sí, con el `message` que ya devuelve el backend.
  **Fase 3 — sin placeholder.** `VisredQuotationProvider::buildRequest` ya no cae a
  `config('visred.default_holder_dni')` (eliminado de `config/visred.php`): manda
  `person_holder` solo si el snapshot tiene DNI real, si no lo omite. Guard de coincidencia en
  `CheckoutController::submit` (422 con `errors.dni`, auto-corregible por el cliente) comparando
  por `DocumentoIdentidad::clave()` — no `normalizar()` — para no bloquear a alguien que dio el
  CUIL en el chat y el DNI a secas en el checkout (misma persona, dígitos distintos tras solo
  normalizar). `docs/v2/08-visred-quote-adapter.md` reescrito con la falsificación fechada.
  **Corrección importante hallada en el camino:** `PolizaEmisionService` ahora manda en la
  emisión el MISMO valor de `RiskSnapshot.dni` que ya viajó en la cotización (no una
  normalización independiente de `CheckoutSession.dni`) — si son dígitos distintos tras
  normalizar (CUIL vs DNI de la misma persona) mandar el del checkout hubiera roto la
  coincidencia igual. Solo cae al DNI del checkout cuando la cotización no llevaba ninguno.
  **Fase 4a — DNI en identificación de cliente (código; SIN flag nuevo en `ai_state`).**
  `InsuranceOrchestrator::tryAutoIdentifyByPhone()` y `IdentifyCustomerTool` solo marcan
  `customer_identified => true` si el customer resultante ya tiene DNI (el backfill de
  nombre/teléfono sigue pasando siempre). Tool nueva `DeclineDniTool` — cierra el paso cuando el
  cliente se niega explícitamente (nunca por inferencia del LLM); con guard: no cierra el paso
  si todavía no hay ningún customer vinculado (conversación solo-BSUID, sin teléfono numérico),
  para no dejar al cliente contra la pared en `identifyVehicle` (`missing_customer`). Registra
  `conversation.metadata.dni_declined_at` (JSON libre, no toca `ai_state`). Validación del DNI
  en `CustomerIdentificationService` ampliada: normaliza antes de validar (acepta guiones/puntos)
  y acepta 11 dígitos (CUIT/CUIL), no solo 7-8.
  **Bug encontrado en la review y corregido — `document_type_id` de 11 dígitos.** Aceptar CUIT/CUIL
  en el chat abrió una vía NUEVA de cotización inemitible: el CUIL llega al snapshot, cotiza bien
  (el request de cotización no lleva tipo de documento) pero la emisión lo rechazaba porque
  `VisredEmissionProvider::buildHolder` hardcodeaba `document_type_id => 'dni'`. Aislado contra el
  sandbox con el mismo `quotation_result_id`: `dni` → rechazado, `cuil` → rechazado, `cuit` →
  aceptado. Fix en el adapter (`documentTypeIdFor()`), que además **arregla un bug latente
  pre-existente**: un cliente con CUIT en `dni` (persona jurídica, o cargado desde checkout/ingesta/
  admin) tampoco podía emitir. Verificado E2E por el adapter real. Se agregó también validación de
  formato al campo `dni` del checkout (7-8 u 11 dígitos), que no tenía ninguna — antes entraba
  cualquier texto y recién reventaba contra Visred.
  **Fase 4b (prompt) — ⬜ pendiente.** El código de `DeclineDniTool` está listo y probado, pero
  el prompt de `CustomerIdentifierAgent` todavía NO pide el DNI (el `.md`/row de `agent_prompts`
  no se tocó a propósito — dos deploys separados, ver diseño de la Fase 4 arriba). Hasta que se
  cargue, el agente sigue sin preguntar por el DNI y el gate de arriba simplemente deja el paso
  abierto sin que nadie lo sepa pedir.
  **Verificación:** suite completa recorrida por bloques (**676 tests, 0 fallos**; la memoria no
  alcanza para un solo proceso — problema de infra pre-existente, no de este cambio). PHPStan
  limpio (baseline regenerado: un entry menos, el `$session->email` que dejó de usarse).
  `EmisionFallidaMail` se renderizó de verdad (`->render()`, ambas variantes con y sin
  `field_errors`) porque `Mail::fake()` NO renderiza el blade y un error de plantilla habría
  pasado los tests. *(Corrección 2026-08-03: ese `render()` se hizo a mano y nunca entró a la
  suite — no había un solo `->render()` en `tests/`. El chequeo quedó automatizado recién ahora,
  en `EmisionFallidaMailTest`.)* La cadena real de identificación (sin mockear el adapter) se cubrió aparte:
  dar `30.123.727` en el chat lo persiste como `30123727` y cierra el paso; identificar por
  email NO lo cierra.
  Nuevos tests: `EmitirPolizaFailureTest`, `NotifyClientEmissionFailedTest`,
  `DeclineDniToolTest`, `IdentifyCustomerToolTest`, +casos en `PolizaEmisionServiceTest`,
  `VisredQuotationProviderTest` (reescritos, ya no codifican el placeholder), `CheckoutControllerTest`
  (guard de DNI, 4 casos), `CustomerIdentificationTest` (normalización + CUIL 11 dígitos),
  `AutoIdentifyBackfillsNameTest` (partido en con-DNI/sin-DNI), `NotifyClientCheckoutCompletedTest`.
  Tripwire `FunnelAnalyticsTest` ("returns all 5 steps") sigue verde — confirma que no se agregó
  ninguna etapa. **Pendiente además de la Fase 4b:** el Paso 0 no llegó a confirmar si la prima
  depende del DNI (queda como pregunta a Visred en `docs/v2/visred-integration/ROADMAP.md`).

- **2026-07-20** — **Fix: checkout fallaba con `customers_dni_unique` por índice único total vs SoftDeletes ✅ (código) / ⬜ (deploy).**
  Síntoma: "No pudimos procesar el checkout" (500 capturado). Causa raíz: los índices únicos de
  `customers.dni` / `.email` eran **totales**, pero `Customer` usa SoftDeletes — una fila borrada
  seguía ocupando el slot del DNI, **invisible para Eloquent** (global scope) y visible para
  Postgres. El guard de clave única de `CustomerConsolidationService` y el `reconcile()` de
  `CustomerMergeService` consultan con el scope activo, así que no veían el conflicto: escribían
  el DNI igual y la transacción entera del submit abortaba. Caso real en prod: customer `id=4`
  soft-deleted a las 03:30 retenía `dni=30123727`; el checkout intentó escribirlo en `id=6` a las
  03:50. Fix: migración `make_customers_unique_keys_partial_on_soft_delete` — índices únicos
  **parciales** (`WHERE deleted_at IS NULL`). Ahora la restricción ve exactamente lo mismo que la
  app y los lookups quedan correctos por construcción; borrar un customer libera su DNI/email.
  Decisión de dominio del usuario: borrado = fuera del espacio de identidad. 2 tests de regresión
  en `CustomerConsolidationTest`; PHPStan 0.

- **2026-07-19** — **Fix: checkout web daba 500 al enviar por permisos de `laravel.log` ✅ (código) / ⬜ (deploy).**
  Síntoma: en el piloto, "confirmar y enviar" del checkout devolvía 500 sin nada en `laravel.log`.
  Causa raíz: los workers de cola corren como **root** (supervisord `user=root`) y php-fpm/nginx como
  **www**. El primer worker que loguea crea `storage/logs/laravel.log` como `root:root 644`, dejándolo
  no-escribible para las peticiones web (www). Cualquier `Log::` en una request web reventaba justo al
  intentar loguear → 500 con el error real ahogado (por eso no había rastro). Confirmado en prod: un
  `chown www:www laravel.log` en caliente hizo que el submit pasara a 200. Fix permanente en
  `.docker/start.sh`: pre-crear el log y `chown -R www:www storage bootstrap/cache` antes de arrancar
  supervisor. **Pendiente: rebuild + `up -d`** (el chown en caliente es efímero; sin redeploy el bug
  reaparece al recrear el contenedor).

- **2026-07-19** — **Fricción de cobertura, post-checkout por WhatsApp y guía de siniestros — review de conversación real #2 ✅ (código) / ⬜ (prompts).**
  Disparado por la revisión de una conversación real del piloto (conversation_id=2): (1) el
  `CoveragePreferenceAgent` encadenaba dos preguntas entre "robo parcial también" y el upsell a
  Todo Riesgo planteado como dicotomía — mucha fricción antes de cotizar. (2) al completar el
  checkout el cliente no recibía nada por WhatsApp (solo se despachaba `EmitirPoliza` + mail
  interno). (3) un siniestro reportado en plena conversación lo manejó el `CheckoutAgent`
  improvisando — llegó a dictaminar cobertura por su cuenta y prometió "te paso con el productor"
  sin mecanismo real detrás.
  **Cambios de código (activos ya):** `WhatsAppOutboundService::sendDocumentMessage()` (envío de
  PDF por link, `type: document`). `NotifyClientCheckoutCompleted` — job de agradecimiento fijo
  despachado en `CheckoutController::submit()` junto a `EmitirPoliza`, sin pasar por el LLM.
  `SendPolicyDocumentsToClient` — envía los `PolicyDocument` `visible_to_client=true` de la
  póliza; idempotente por `poliza_id` (`Cache::add` atómico, se libera si falla o si aún no hay
  documentos); enganchado desde `PolizaEmisionService::emitir()` (tras `storeFromEmission()`) y
  desde `CapturePendingPolicyDocuments` (cuando el reintento captura lo que faltaba).
  `SiniestroGuidanceTool` — tool nueva sin efectos secundarios (no notifica a nadie), resuelve el
  PAS por prelación de 2 niveles (propio del cliente → default de MANGO, mismo criterio que
  `Mobile/SiniestroController` sin su 3er tier de vehículo compartido) y devuelve indicaciones +
  contacto; inyectada en los 5 agentes del embudo vía `InsuranceOrchestrator::resolveAgent()` (un
  siniestro puede reportarse en cualquier etapa).
  **Prompts (⬜ pendiente de carga manual, ver deuda técnica):** reescritura completa de
  `CoveragePreferenceAgent.md` (regla de una sola pregunta + upsell como mención no bloqueante,
  nunca como pregunta/dicotomía) y bloque nuevo `shared_siniestro.md` (cuándo llamar la tool,
  prohibido dictaminar cobertura, prohibido prometer acciones que el sistema no ejecuta) —
  entregados en el chat para que Andrés los cargue en `agent_prompts` + invalide caché.
  Verificación: 32 tests nuevos + ajustes a 2 tests existentes que hubieran pegado a la API real
  de Meta sin `Queue::fake()`/`Http::fake()` de más (el nuevo dispatch de documentos no estaba
  cubierto). Suite completa **594/594** verde, PHPStan 0 errores nuevos. En el camino, PHPStan
  detectó un guard muerto (`$quote->conversation_id === null`, imposible por schema NOT NULL) que
  se sacó en vez de silenciarlo.

- **2026-07-15** — **Review + fixes: backfill de atributos + robustez (rama feat/insurable-asset-model) ✅.**
  Tras la review independiente con 8 ángulos (línea por línea, comportamiento removido, trazado de
  callers, reuso, simplificación, eficiencia, altitud, convenciones): (1) `PolicyChainResolver::backfillMissing` —
  rellena monótonamente (nunca pisa) los atributos del asset que traiga la fuente entrante (resuelve
  la pérdida silenciosa cuando fuentes esparsas crean el asset y después fuentes ricas enriquecen
  metadatos); la resolución de conflictos entre dos valores no vacíos quedó como deuda apuntando a
  `docs/v2/11` (modelo de consolidación con provenance/pesos por fuente, ya existe para Customer).
  (2) `PolicyReferenceService` — `Customer::withTrashed()->findOrFail` en el camino de emisión (el
  cliente puede quedar soft-borrado a mitad de flujo; el borrado solo se bloquea si hay vigente).
  (3) `MobileRiskSeeder::seedVehicleRisk` — autoritativo sobre fixtures (refresca metadata si se
  re-siembra con datos editados; resolveRisk solo rellena faltantes). (4) `InsurableAsset` — método
  `casts()` en vez de propiedad (convención del repo). Verificación: 3 runs completas de suite
  (579/579 verde cada una; los 6 fallos de la 1a fueron pollution transitoria del fatal de memoria
  previo, no regresión de los fixes). Documentación en `docs/v3/05-modelo-insurable-asset.md`
  (nota: backfill mono-tónico y deuda de resolución de conflictos). ROADMAP: fila nueva en deuda
  técnica. **Commits:** `19d4563` (feature) + `3f90668` (fixes). _(rama `feat/insurable-asset-model`)_

- **2026-07-15** — **Modelo `InsurableAsset` — separa identidad del bien de la exposición (Risk), ACORD simplificado ✅.**
  Deuda anotada en la entrada 2026-06-30 (import de reportes: "modelar ramos no-vehiculares") y
  agravada por corridas reales de la ingesta local (AP de Galicia, combinado de Bassi en
  Descargas): el dominio solo modelaba `RiskType::Vehicle` y el `Risk` se llaveaba por
  `metadata->patente` — sin patente no había con qué deduplicar, así que esos contratos morían
  como excepción o se creaban mal tipados. **Modelo nuevo (decisión del usuario, cercano a
  ACORD):** `Customer → InsurableAsset (identidad) → Risk (exposición) → Poliza`. El **Asset**
  es el bien estable y re-identificable, con `natural_key` derivada por tipo
  (`AssetType::naturalKey()`: `vehicle` → patente normalizada; `property`/`person`/`business`/
  `equipment` → `null`, sin clave todavía). El **Risk** queda 1:1 transicional con su asset (no
  hay datos de exposición con consumidor aún). **Regla de dominio explícita:** dos pólizas de AP
  del mismo cliente NUNCA comparten `Risk` — sin clave natural, cada contrato crea su propio
  asset. **Dedup consolidada:** `PolicyChainResolver::resolveRisk(Customer, AssetType, array)`
  pasa a ser el único punto de creación de `Risk` de todo el sistema — migrados los 5 creadores
  que antes duplicaban la lógica (`PolicyReferenceService` en la emisión Visred, `PolizaService`
  en el alta manual del panel, `IngestaConfirmacionService`, `PolicyReportConfirmacionService`,
  y `MobileRiskSeeder` en datos de desarrollo). Migración `insurable_assets` + `risks.asset_id`
  (NOT NULL) con **backfill 1:1** desde el modelo viejo (normalización idéntica a
  `AssetType::naturalKey`); verificado en dev: 135 risks ↔ 135 assets, 0 huérfanos. Las queries
  de dedup exacto pasaron de `metadata->patente` (JSON path) a `natural_key` (columna indexada);
  las búsquedas de texto libre (`ilike`) siguen contra `risk.asset->metadata`. **Fuera de
  alcance, diferido a propósito:** el mapeo producto/ramo → `AssetType` y la materialización de
  filas no-vehiculares del reporte — `PolicyReportImportService::evaluate` sigue devolviendo
  `Exception` para filas sin patente, sin cambios de comportamiento (ver deuda técnica). Detalle
  completo en `docs/v3/05-modelo-insurable-asset.md`. Tests nuevos: `AssetTypeTest` (unit,
  normalización de clave) + `PolicyChainResolverTest` (dedup/no-dedup, hook `saving`). **Suite
  completa 576/576** · PHPStan **0** · Pint ✅. _(rama `feat/insurable-asset-model`, base
  `feature/ingesta-v2-extraccion-llm` — comparte archivos de ingesta con esa rama, se mergean
  juntas)_

- **2026-07-13** — **Ingesta local v2 — extracción server-side con LLM (rama, pendiente evaluación live) 🚧.**
  Problema medido en datos reales: de 290 PDFs escaneados en Descargas, el cliente subió 161
  (el filtro "detectó CUIT de aseguradora ⇒ es póliza" es falso — factura/cotización/denuncia/
  manual también lo llevan); de esos, **145 quedaron pendientes y solo 6 se confirmaron**
  (112/145 sin DNI del tomador, 46 ciegos, San Cristóbal/Experta fallando en la práctica pese
  a estar marcados ✅ en el README del parser v5; endosos/cancelaciones entraban como `poliza`).
  **Decisión:** el cliente deja de extraer campos; el servidor clasifica y extrae con
  **`deepseek-chat`** (texto-first, nunca el PDF como archivo), validando cada campo
  determinísticamente ("validar-o-null") y descartando sola la basura (`descartado_auto`).
  **Validado con smoke test real** contra el corpus (`ingestor/docs/`, 15 PDFs/7 compañías)
  antes de implementar: extracción igual o mejor que el parser v5 en las 7 (mejor en 4),
  clasificación de basura 3/3, costo medido **$0.0002/doc** (98% cache-hit).
  - **Servidor:** `IngestaExtractorAgent` (agente nombrado, `#[Model('deepseek-chat')]` —
    hoy `#[UseCheapestModel]`, ver bitácora 2026-07-25,
    prompt probado en el smoke) + job `ExtractIngestedDocument` (cola `documents`/
    `database_long`, mismo carril que `ExtractCoverageDocumentText`) con validadores
    portados de `parser.py` (patente/DNI-CUIT/fechas/número/alias de compañía). Estados
    nuevos `en_extraccion`/`descartado_auto` en `IngestaStatus` (columna `string`, sin
    migración). Contrato v2 en el endpoint (`schema_version=2`, ya no trae
    `documento/tomador/riesgo/fechas` — eso lo produce el LLM); `IngestaDocumentoService::
    applyExtraction()` reescribe `payload` con el shape v1 para que
    `IngestaPendientesController`/`IngestaConfirmacionService` sigan funcionando **sin
    cambios**. Fix de agrupación de Pendientes: clave `num:{compañía}:{número
    normalizado}` (antes `num:{numero}`, colisionaba entre compañías y se partía por
    diferencias de formato del número). Comando `ingesta:reextraer` (ops: re-despachar un
    doc puntual o los colgados en `en_extraccion` con `--stuck`).
  - **Cliente:** `ingestor/app/v6/` reemplaza `parser.py` (7 extractores regex por
    compañía) por `extract_text.py` (~25 líneas, pdfplumber) — el cliente pasa a ser
    scanner+transportista, sin conocer de pólizas. Único filtro que queda del lado
    cliente: tamaño (`max_size_mb=15`). `app/v5/` queda intacta en disco como referencia
    (el rollback real requiere revertir también el servidor, que ya no acepta
    `schema_version=1`). `ingestor/` no tiene git propio — los archivos quedan en disco,
    sin commit.
  - **Tests:** `PolicyDocumentIngestaTest` reescrito a v2 (11), `ExtractIngestedDocumentTest`
    nuevo (10 — fixtures con respuestas *reales* de deepseek-chat del smoke test,
    validadores, degradación sin texto/LLM caído, E2E endpoint→job→confirm→materialización),
    `IngestaPendientesGroupingTest` nuevo (3). Un assert de `IngestaConfirmacionTest`
    actualizado (la key de agrupación cambió de formato, cambio intencional de esta rama).
    Suite completa **571/571** verde (el entorno tiene un techo de memoria de 128M
    preexistente que no alcanza para correr toda la suite en un proceso — se corrió con
    `--memory-limit` elevado, igual que quedó documentado en la entrada 2026-07-06). PHPStan
    **0** errores nuevos.
  - **Pendiente antes de mergear a `main`:** evaluación live vía **ngrok** (decisión del
    usuario: no pasar a producción todavía) — correr `app/v6/` contra el corpus real +
    verificar `/ingesta-pendientes` + confirmar un contrato end-to-end; recién después
    re-apuntar `config.yaml` a `mangobroker.com.ar` y mergear. Backlog de los 145
    pendientes v1: no se re-procesan automáticamente (sin `payload.texto`, dedup por hash
    los sigue reconociendo como ya subidos) — se triage-an a mano en la UI existente.
    _(rama `feature/ingesta-v2-extraccion-llm`, sin mergear)_

- **2026-07-09** — **Fix génesis — el PAS asignado al cliente venía sin teléfono (crash de siniestro en mobile) ✅.** Tras el deploy a GCP con BD en blanco (solo seed), el mobile crasheaba al hacer swipe en Siniestros: el PAS resuelto tenía `metadata` sin `phone`, y `ClaimReport.fromJson` (mango-mobile) castea `phone as String` no-nullable. Causa de raíz en el seed: (1) el PAS era un user aparte (`MobilePasSeeder`) del admin, con `metadata` que podía quedar incompleta; (2) `AdminUserSeeder`/`MobilePasSeeder` usaban `firstOrCreate` → si el user ya existía (logueó antes del seed) **no** rellenaban `metadata`. Decisión (usuario): **el admin es un superset del PAS** (mayor jerarquía). Cambios: `User::isPas()`/`scopePas()` reconocen `role=admin` como PAS; `AdminUserSeeder` idempotente (`firstOrNew` + fuerza `metadata` matrícula/teléfono, preserva password) y expone `AdminUserSeeder::EMAIL` como fuente de verdad; `CustomerSeeder` asigna ese admin a las fixtures **y** backfillea todo customer con `pas_id` null; **`MobilePasSeeder` eliminado**; `SiniestroController::defaultPas()` y el dropdown de `Customers/Edit` usan el scope `pas()`. Perfil de PAS por default: matrícula `97072`, teléfono `+5493516280778` (env-overridable). Test `SeedPasAssignmentTest` (4 casos: admin-es-PAS con perfil, todos los customers con PAS, backfill, idempotencia del metadata). Suite mobile PAS **27/27** · PHPStan **0**. _(rama `main`)_

- **2026-07-07** — **Deprecación de ChatKit + retiro de Laravel Reverb ✅.** OpenAI anunció el sunset de **Agent Builder** (a más tardar **30-nov-2026**), así que el chat web sobre **OpenAI ChatKit** deja de tener continuidad. Consecuencias inmediatas: (1) el asistente opera **solo por WhatsApp** por ahora; (2) la **landing pública de marca (MANGO)** ya vive en este proyecto (`/` → `Landing/Index.vue`); (3) **Reverb retirado** — sin cliente web no hay quién consuma broadcasts. Cambios: `ApiQuoteResolution` ya no ramifica por canal (la entrega asíncrona de cotizaciones va **siempre** por el pipeline de jobs de WhatsApp — `NotifyClientQuoteReady` → `SendWhatsAppMessage`); eliminados el evento `QuoteProcessed` (`ShouldBroadcast`), `routes/channels.php` (+ el `channels:` de `bootstrap/app.php`), `config/reverb.php`, la conexión `reverb` de `config/broadcasting.php`, `resources/js/echo.js` (+ import en `bootstrap.js`), el `[program:reverb]` de `.docker/start.sh` y `broadcasting/*` de `config/cors.php`. Deps removidas: `laravel/reverb` (composer), `laravel-echo` + `pusher-js` (npm). Doc actualizada (`README.md`, `docs/README.md`, este ROADMAP). Test `VisredQuotationProviderTest` migrado de `Event::assertDispatched(QuoteProcessed)` a `Queue::assertPushed(NotifyClientQuoteReady)`. Suite Visred **14/14** · PHPStan **0** · build ✅. **Nota:** la deprecación del código del adapter ChatKit (`OpenAI\AgentToolAdapter`, tools HTTP `web-chat/v1`) es una cirugía posterior — por ahora queda en su lugar, marcado como canal deprecado en la doc. _(rama `main`)_

- **2026-07-07** — **Pulido del `CoveragePreferenceAgent` — CASO 2 (cubrir robo) ✅.** Diagnóstico sobre el mensaje #532 (conversación #43): el cliente pidió "algo q cubra robo" (#531) y el agente respondió *"¿te preocupa solo que se roben el auto entero, o también si te afanan una rueda o te rompen un cristal?"*. Dos fallas: (1) terminología incorrecta — "el auto entero" en vez de **Robo Total**; (2) arrancaba la pregunta por cristales sin validar primero el robo pedido, leyéndose como si ignorara la intención del cliente (contradice la regla "validá primero, después expandí" y el feedback de `notes_agent_ux_feedback.md`). Fix: reescrito el CASO 2 en `CoveragePreferenceAgent.md` para validar el robo primero y desambiguar B (**Robo Total**) vs C (suma **Robo Parcial** + cristales) con nomenclatura correcta; prohibición explícita de decir "el auto entero". Sincronizado el row activo (`agent_prompts` id 21, `coverage_preference`) desde el `.md` + `Cache::forget`. _(rama `main`)_

- **2026-07-07** — **Fix — el año del vehículo no se actualizaba al re-usar una patente (callejón sin salida en cotización) ✅.** Diagnóstico sobre la conversación #42: cuando la misma patente se re-apunta a otro auto (`AB413BS`: Peugeot 2008 2017 → Fiat Cronos 2021 GNC), `VehicleIdentificationService::updateVehicle` actualizaba marca/modelo/versión/CP/combustible pero **congelaba el `year`** a propósito ("integridad temporal"). El año viejo (2017) se filtraba al `VisredQuotabilityResolver`, que hace **match exacto por año** en el árbol de catálogo: Fiat 2017 no tiene grupo "Cronos" (salió en 2018) → `notQuotable` en el paso de `groups/`, **antes** de llegar a versiones → derivación innecesaria a un PAS humano. Verificado contra el catálogo Visred sandbox: Fiat 2021 → Cronos → existe "1.3 ATTRACTIVE". Fix: `year` ahora se actualiza como los demás campos (el auto declarado ahora es el que vale; congelar solo el año era incoherente con actualizar marca/modelo). Test de regresión `VehicleUpdateYearTest`. **Segundo fix (mismo diagnóstico):** el debounce del inbox de 2s no agrupó dos inbounds a ~5s → doble respuesta descoordinada (521/522 pisándose). Se centralizó la ventana en `config('whatsapp.inbox_debounce_seconds')` (default **8s**, `WHATSAPP_INBOX_DEBOUNCE_SECONDS`) y se aplica en los dos dispatch de `ProcessConversationInbox` (texto en `ProcessWhatsAppMessage`, media en `ProcessMediaAttachment`). Suite vehículo/quotability **65/65** + jobs de inbox **24/24** verdes. _(rama `main`)_

- **2026-07-06** — **Mejoras UX de cotización por WhatsApp + admin panel (8 frentes) ✅.** Ronda completa sobre el flujo conversacional y el panel, sin tocar el stack de `VehicleIdentifierAgent` (intocable). Resumen por frente:
  1. **Fix — auto-identificación sin nombre:** `InsuranceOrchestrator::tryAutoIdentifyByPhone()` backfillea el nombre del `Customer` desde `sender_name` del primer inbound apenas se auto-identifica por teléfono, para que el saludo del primer turno ya sea personal (antes corría con customer anónimo).
  2. **Cierre de la rama `NeedsFact` del resolver Visred:** cuando el catálogo no puede decidir entre versiones ("1.6 ALLURE" vs "1.6 ALLURE TIPTRONIC") y el bot pregunta el dato faltante (transmisión, etc.), la respuesta del cliente **se perdía** — llegaba a `CoveragePreferenceAgent` sin forma de reingresarla al dominio, y el cliente esperaba una cotización que nunca arrancaba. Fix: `WhatsAppAdapter::onNeedsFact()` marca `metadata.pending_vehicle_fact`; tool nueva **`provide_vehicle_fact`** (`ProvideVehicleFactTool` + `WhatsAppAdapter::provideVehicleFact()`) enriquece `Vehicle.version` con la respuesta y reintenta el gate de `Quotability`; `coveragePreference()` deja de prometer "la oferta será procesada en breve" cuando no hay quote en marcha (mensaje honesto según haya o no un fact pendiente).
  3. **Retroceso de etapa (`revert_to_stage`):** tool nueva en `CoveragePreferenceAgent`/`QuoteAgent`/`CheckoutAgent` (no en Customer/VehicleIdentifier) que invalida en cascada los flags de `ai_state` desde la etapa corregida en adelante (`customer`→las 5, `vehicle`→4, `coverage`→3) y expira (`QuoteService::expireOpenQuotes`) la cotización en curso. Guard en `NotifyClientQuoteReady` para no presentar una quote `expired` tras un revert. Sección "Cambio de rumbo" agregada a los 3 prompts.
  4. **Takeover humano:** `Conversation::isAiPaused()`/`setAiPaused()` (flag en `metadata`, fuera de `ai_state`); `ProcessConversationInbox` saltea la IA si está pausada (marca `processed_at` igual, para no perder los mensajes) y al reanudar antepone un resumen de lo conversado durante la pausa al próximo prompt del LLM. 3 acciones nuevas en `ConversationController` (`pauseAi`/`resumeAi`/`sendManualMessage`, este último despacha por el pipeline outbound existente con `agent_name='human'`). UI en `Conversations/Show.vue`: badge "IA pausada", modal de confirmación, composer manual, burbujas diferenciadas para mensajes del asesor.
  5. **Refresh automático de `Conversations/Show`:** `usePoll` de Inertia (5s, sin Reverb) sobre `messages`/`executions`/`conversation`, auto-stop si la conversación pasa a `completed`/`archived` (mismo patrón que `Facturacion/Index.vue`).
  6. **Seguimiento de conversaciones estancadas:** command `conversations:follow-up-stalled` (schedule horario, `08:00–20:00` hora Argentina) que manda un nudge fijo (sin LLM) por conversación con progreso parcial cuya ventana de 24h de WhatsApp está por cerrar en la próxima corrida hábil — un solo envío por conversación, nada si la ventana ya cerró (sin templates, por decisión explícita). Setting `followup.enabled` (grupo `followup`, editable en `/admin/settings`).
  7. **Botones interactivos en el cierre de venta:** `MessageType::Interactive` nuevo; el webhook/`ProcessWhatsAppMessage` aceptan taps de botón como texto normal (título del botón = contenido). `PresentQuoteOptionsTool` arma los botones **desde `quote_alternatives` (dominio)** — el LLM nunca escribe título/precio de botón — y los deja en `metadata.pending_interactive`; el orquestador los levanta al retornar (`buttons` en el shape de `handle()`) y `SendWhatsAppMessage` los adjunta al mensaje (`WhatsAppOutboundService::sendInteractiveButtons`, bypass del modality decider/TTS). Presentación: 2 opciones + botón "Tengo una pregunta", con **recomendación anclada** en el texto (ancorada en investigación de UX: choice overload + alternative close + práctica de venta de seguros — liderar con una recomendación convierte más que presentar 2 opciones con peso igual).
  8. **Analytics — conversión por versión de prompt + diff de versiones:** `AnalyticsRepository::funnelByPromptVersion()` (mismo criterio JSONB que `completedPerStep()`) agrupa entered/completed/conversión por `agent_prompt_id`; desglose expandible por step en `Funnel.vue`. Diff entre versión y su `parent_version_id` en `AgentPrompts/Show.vue` (`diffLines` de `diff`, líneas verdes/rojas sobre el design system).
  **Tests:** ~70 tests nuevos (unit + feature) cubriendo los 8 frentes, suite completa **559 tests verdes** (corrida en chunks — el entorno tiene un techo de memoria de 128M preexistente que no alcanza para correr toda la suite en un solo proceso, confirmado igual en `main` sin estos cambios). PHPStan **0** errores nuevos · Pint ✅ · build ✅. Verificado en preview: takeover (pausar/reanudar, burbuja "Asesor", poll), funnel por versión de prompt y diff de versiones (con datos reales de logs/prompts sembrados y limpiados post-verificación). **Hallazgo de entorno documentado:** `Carbon::setTestNow()` con una timezone no-UTC desfasa 3hs los casts `datetime` de Eloquent al releer de DB en tests — los tests de `follow-up-stalled` fijan el testNow siempre en UTC (con el horario Argentina equivalente anotado en comentario). **Pendiente:** aprobar en Meta el uso de mensajes interactivos si no estaba habilitado; revisar el tono de los 4 textos fijos de seguimiento con Andrés antes de activar el schedule en prod (`followup.enabled`). _(rama `main`)_

- **2026-07-06** — **Facturación: PDF al vuelo (sin persistir), detalle de lote, peso y nombre corregidos ✅.**
  Cuatro fixes sobre feedback de uso real:
  1. **Nombre de archivo:** `FC-{ptovta}-{numero}.pdf` (ej. `FC-3-00000008.pdf`), en `InvoicePdfService::filename()`.
  2. **Sin persistencia:** el PDF es 100% derivable de la `Invoice` (snapshot inmutable) + config del emisor, así que se genera al vuelo en cada descarga en vez de guardarse en disco. Se eliminó `invoices.pdf_path` (migración `2026_07_06_000002`), el evento/listener `InvoiceAuthorized`→`GenerateInvoicePdf` (su único propósito era persistir el PDF al autorizar; sin destino ya no tenía sentido) y su registro en `AppServiceProvider`. El job de emisión ya no dispara esos eventos. Los PDFs viejos en `storage/app/private/invoices/` se borraron (huérfanos, regenerables al vuelo desde la DB — los datos reales de las 9 facturas del go-live están intactos en la tabla `invoices`, nada de eso se perdió).
  3. **Detalle de lote:** pantalla nueva `Facturacion/BatchShow.vue` (`GET /admin/facturacion/batches/{id}`) con los datos del lote, resumen y tabla completa de facturas (compañía, importe, N° comprobante, CAE, vto., estado, y descarga de PDF por fila). El link "Detalle" en "Lotes recientes" de `Facturacion/Index.vue` lleva ahí; el botón "ZIP" se mantiene como atajo rápido. Nueva ruta de descarga individual `GET /admin/facturacion/invoices/{invoice}/pdf`.
  4. **Peso del PDF:** de ~917KB a ~62KB (15x menos). Causa: `barryvdh/laravel-dompdf` trae `enable_font_subsetting=false` por default → embebe la tipografía DejaVu completa en cada PDF. Fix: `->setOption('enable_font_subsetting', true)` en `InvoicePdfService` antes de `->output()`.
  24 tests verdes (agregados: detalle de lote, descarga individual 200/404, ZIP con generación al vuelo y 404 sin autorizadas, nombre/peso del PDF) · PHPStan 0 · Pint · build OK. Verificado en preview: render del PDF real (pdf.js) con header intacto, detalle de lote sin errores de consola, descarga individual `fetch` confirmando `FC-3-00000009.pdf` / 61.6KB / 200.

- **2026-07-06** — **Facturación EN PRODUCCIÓN — go-live real ✅.** Se emitió la facturación mensual real de comisiones por la pantalla: **9 Facturas C autorizadas, 0 rechazos, total $1.912.342,05** (comprobantes 0003-00000001 a 0003-00000009, a Triunfo/Galicia/Sancor/San Cristóbal/Río Uruguay/etc.). Setup de producción: certificado de producción (`storage/app/certs/afip-prod.crt`, emisor `CN=Computadores O=AFIP`, matchea `afip.key`), servicio `wsfe` delegado y **punto de venta 3 tipo Web Services** (separado del PV 2 de Comprobantes en Línea manual). Config: `config/afip.php` resuelve el cert por entorno (homo→`afip.crt` / prod→`afip-prod.crt`); `.env` `AFIP_HOMOLOGACION=false`; punto de venta en el setting del emisor. Fix de robustez: la caché del TA de WSAA ahora es por-entorno (`afip:ta:{homo|prod}:{cuit}`) para no reusar el token de un entorno en otro. Validado con `php artisan afip:ping` (read-only) antes de emitir. **Gaps conocidos pendientes:** sin notas de crédito/anulación, sin recuperación de lote trabado desde la UI.

- **2026-07-06** — **Facturación: validada contra AFIP homologación + PDF con formato oficial ✅.** Circuito completo probado emitiendo una Factura C real contra homologación (`wswhomo`): WSAA + certificado + servicio `wsfe` autorizado + `FECAESolicitar` → **CAE real**. Certificado generado con `openssl` (clave + CSR en `storage/app/certs/`, subido al WSASS de homologación por Andrés). Comando nuevo `php artisan afip:ping` (smoke test de conexión/firma vía `FECompUltimoAutorizado`, sin emitir). **PDF rehecho al formato oficial de AFIP** (`resources/views/invoices/factura-c.blade.php`): banda ORIGINAL/DUPLICADO/TRIPLICADO (una copia por hoja, `page-break-after`), encabezado emisor|C|comprobante con IIBB e inicio de actividades, banda de período facturado, bloque del receptor (CUIT, razón social, condición IVA nombre completo, domicilio, condición de venta), tabla de 8 columnas (Código/Producto/Cantidad/U.Medida/P.Unit/%Bonif/Imp.Bonif/Subtotal) y totales. Se reemplazó el encabezado con `position:absolute` (que dompdf encimaba) por tabla de 3 columnas. Campo nuevo: `billing_companies.domicilio` + snapshot `invoices.receptor_domicilio`, cableado en la pantalla de configuración (alta/edición). 20 tests verdes · PHPStan 0 · build OK.

- **2026-07-05** — **Facturación electrónica de comisiones (ARCA/AFIP, Factura C) ✅.** Módulo nuevo, aislado y **solo-admin** (`/admin/facturacion`), sin relación con el flujo conversacional. Emite mensualmente las Facturas C (tipo 11, concepto Servicios) del productor hacia un padrón propio de compañías (`billing_companies` — NO el catálogo Visred; incluye ajenas como Cooperación/LPS). **AFIP local (decisión del usuario):** firma WSAA con `openssl_pkcs7_sign` + WSFEv1 por **SOAP crudo sobre HTTP** (`Http` de Laravel, sin `ext-soap` ni SDK/relay de terceros), todo aislado en `AfipSoapService` (devuelve arrays tipados, sin DTO). TA cacheado 12h; numeración vía `FECompUltimoAutorizado`; `CondicionIVAReceptorId` (RI=1, RG 5616). **Async sin Reverb:** `POST /facturacion` crea `InvoiceBatch` + `Invoice` en `Pending` (snapshot inmutable del receptor) y despacha `EmitInvoiceBatch` (cola, `WithoutOverlapping` por punto de venta = 2). El job emite una por una: si AFIP rechaza una, la marca `Rejected` con el error y **continúa**; por cada respuesta emite `InvoiceAuthorized`/`InvoiceRejected`, y el listener `GenerateInvoicePdf` (síncrono) arma el PDF reglamentario (dompdf + **QR RG 4892** con `endroid/qr-code` en SvgWriter — sin `ext-gd`, vía `phenx/php-svg-lib`) en `storage/app/private/invoices/{YYYY-MM}/`. Descarga en `.zip` (`ZipArchive`). **Front Inertia** (`Facturacion/Index.vue`): datos comunes del lote (código ej. `0006`, producto/servicio, período, vto), compañías **tildadas por defecto** (destildar para hacer 2–3 lotes/mes), input de monto formateado es-AR (envía número plano), total en vivo, aviso de duplicado por código, validación por fila, modal de confirmación, y **polling** (`usePoll`) hasta que el lote cierra. Deps nuevas: `barryvdh/laravel-dompdf`, `endroid/qr-code`, `phenx/php-svg-lib`. Config `config/afip.php` (+ `.env` `AFIP_*`); certificados en `storage/app/certs/` (gitignored). Tests: 11 verdes (store/validación/acceso-admin/download/render + job éxito-y-rechazo-continúa + listener PDF + unit payload/QR). PHPStan 0 · Pint · build OK · verificado en preview (render + reactividad es-AR). **Pendiente de Andrés:** cargar cert de homologación (el CUIT/punto de venta y demás datos del emisor ya se autogestionan desde la UI). Arranca en `AFIP_HOMOLOGACION=true`.

- **2026-07-05** — **Configuración de facturación autogestionable ✅** (misma tanda). Pantalla solo-admin `/admin/facturacion/configuracion` (`Facturacion/Configuracion.vue`, link ⚙ desde la página principal) con dos secciones: **datos del emisor** (razón social, CUIT, punto de venta, condición IVA, subtítulo, domicilio, ingresos brutos, inicio de actividades) y **ABM de compañías** (`billing_companies`: alta/edición/baja, con validación de CUIT y toggle `activo`). Los datos del emisor viven en el grupo de settings `facturacion` (`SystemSetting`, sembrado por migración, editado con `SettingsService::saveGroup`) y se leen en runtime vía el servicio nuevo `App\Services\Facturacion\Emisor` (fallback a `config('afip.*')`): ahora `AfipSoapService` toma el CUIT y el controller/PDF el punto de venta y los datos de cabecera desde ahí, en vez de hardcodearlos. Borrar una compañía con facturas emitidas la **desactiva** (registro contable inmutable) en vez de borrarla. 9 tests nuevos (render, guardado de emisor + round-trip por `Emisor`, validación de CUIT, ABM de compañías, desactivación con historial, acceso admin) → **20 verdes** en el módulo. PHPStan 0 · build OK · verificado en preview (render sin errores + guardado end-to-end persistido en DB). El `BillingCompanySeeder` queda como atajo opcional; la carga normal es por la pantalla.

- **2026-07-04** — **Landing pública de MANGO en `/` ✅.** La raíz deja de servir el `welcome.blade.php` stock de Laravel y pasa a una página Inertia de marca (`Landing/Index.vue`, agregada a `LAYOUT_EXCLUDED` en `app.js` — standalone, sin AppLayout) construida sobre el design system Mango existente (tokens `--mg-*`, `MangoLayout`, `MangoLogo`, clases `mg-btn-*`/`mg-card`/`mg-display`). Contenido según el brandbook (`mango-brandbook_1.docx`): hero con el claim "IA que cotiza. Humano que te cuida.", sección **modelo invertido** (IA 24/7 antes de comprar / PAS propio después), grilla con **6 features reales de mango-mobile** (spec v3: pólizas a mano, documentos offline, botón de siniestro→PAS, alertas de granizo SMN, emergencias con ubicación en vivo, vehículo compartido), sección **"Tu PAS: te conviene conocerlo"** (incentivo explícito al contacto con el productor) y cierre naranja. **Dos CTAs** (cotizar por WhatsApp / descargar la app) con toggle `HERO_CTA` en el `<script setup>` para evaluar en el grupo cuál queda como primaria (A/B real fuera de scope). Config nueva: `whatsapp.public_number` (arma el link `wa.me` con texto precargado) y `whatsapp.app_download_url` (`MANGO_APP_DOWNLOAD_URL`); con `null` los botones quedan placeholder. **Segunda vuelta (feedback "insulsa"):** toggle **claro/oscuro** en el header (misma mecánica que el admin: `pas-theme` en localStorage + `data-theme`); link **"Ingresar"** al backoffice (header + footer); identidad MANGO explícita — sección **"¿Por qué Mango?"** (doble lectura fruta/plata del brandbook + el test de boca a boca como burbujas de chat), **mockup del chat del asistente** en el hero, isotipo fruta como marca de agua en hero/nombre/cierre, y cierre **"Te cuidamos el mango."** Fix colateral: `<title inertia>` en `app.blade.php` (el `<Head>` de Inertia no podía manejar el título). Verificado en preview: light/noir, desktop/mobile, toggle funcional. Tests `LandingPageTest` **2/2** · PHPStan **0** · Pint ✅ · build ✅. **Tercera vuelta (feedback "muy estática"):** se agregó movimiento — chat del hero **animado en loop** (burbujas que aparecen + indicador "escribiendo…" + reset), **test de boca a boca** que se teclea solo al entrar en viewport, **reveals fade-up con stagger** en todas las secciones (directiva `v-reveal`), frutas de fondo **flotando**, punto de estado verde **latiendo**, y **hover** con lift en las cards (ícono que escala/rota). Respeta `prefers-reduced-motion` (muestra todo estático). Los reveals NO dependen del IntersectionObserver: usan chequeo por `getBoundingClientRect` en mount (doble-rAF) + fallback en scroll/resize, así el contenido nunca queda atascado en opacity:0 si el observer no corre. Nota de verificación: el renderer del preview MCP es offscreen (viewport 0×0, transiciones CSS congeladas, screenshots con timeout) — se verificó la lógica por DOM (los 38 reveals reciben `reveal-in` al scrollear; el chat cicla; el boca-a-boca dispara), el fade visual se ve en un navegador real. Fix de entorno: se removió un `public/hot` de Vite obsoleto que apuntaba a un dev server muerto (el SPA no montaba); con `composer run dev` se regenera. **Cuarta vuelta (feedback):** (1) **eliminada** la sección "¿Por qué Mango?" (análisis interno de marca —fruta/plata, test de boca a boca— que no le aporta al cliente); (2) la sección del PAS se reconvirtió en **"Postventa 100% humana"** con foco explícito en el diferencial: lista **tachada animada** de lo que NUNCA vas a encontrar (un 0800 en espera, un formulario que nadie contesta, un bot) + bloque "en su lugar, una persona real" + **chat de WhatsApp animado** con la productora (Martina) manejando un siniestro en vivo; (3) **cierre rehecho** para que pegue — headline gigante "Te cuidamos el mango" + línea con **palabra rotativa** ("cuando pasa un choque/robo/duda/granizo, te atiende una persona") + segunda fruta flotando + **shine** en el CTA; (4) **+dinámica**: tachado que se dibuja al entrar en viewport, chat de la productora one-shot on-view, palabra rotativa cada 2s (via `:key` remount, sincronizado), brillo del CTA. Todo respeta `prefers-reduced-motion`. Verificado por DOM en preview: secciones removidas ausentes, chat de Martina completa sus 4 mensajes, tachado dispara, rotador cicla por las 4 palabras. **Quinta vuelta (feedback):** el mango de fondo pasó de un bob vertical simple a movimiento orgánico (traslación X/Y + balanceo/rotación); el mango de fondo pasó a movimiento orgánico (traslación X/Y + balanceo). **Sexta vuelta (corrección de "glow"):** el usuario aclaró que quería un **glow de borde tipo prompt de IA nativo de Android** en las cards de chat, NO manchas en el background. Se removieron los glows de fondo y se agregó un **borde animado** a las dos cards de chat (asistente IA del hero + Martina): un cuadrado con `conic-gradient` mango que **gira** (`transform: rotate`, no `@property` — más robusto y verificable) detrás de una superficie interior opaca (`.ai-glow-body`) que lo tapa y deja ver solo un anillo fino girando en el borde (wrapper `.ai-glow` con `overflow:hidden` + `padding:1.5px` + `isolation:isolate`). Nota: el keyframe necesita `from`/`to` explícitos — el renderer no resuelve el `from` implícito; y las animaciones (transform) son intermitentes en el preview offscreen (a veces congela todas por igual, fruta incluida), así que se verificó la **estructura** por DOM (body opaco z-1 sobre anillo z-0, conic-gradient presente, borde fino) y el giro se ve en un navegador real. **Séptima vuelta:** dinámica muy sutil para las otras cards — glow mango que "respira" (keyframe box-shadow transparente→glow, mismo patrón que el `pulse-dot`) en los tiles de ícono de las 6 feature cards, **escalonado** por índice (`--breathe-delay` 0/0.6/1.2/1.8/2.4/3s) para que no laten a la vez. Respeta `prefers-reduced-motion`. _(rama `main`)_
- **2026-07-01** — **Identidad de cliente — clave canónica (DNI/CUIT/CUIL) + lookup "cliente existente" ✅.** La identidad del cliente vivía en `customers.dni` (único) con **match exacto y sin normalizar**: el mismo número escrito distinto (`30-71784318-1` vs `30717843181`) o la misma persona cargada una vez por **DNI** y otra por **CUIL/CUIT** quedaban como clientes distintos. **Regla de dominio (AFIP), agnóstica de proveedor:** persona física → identidad = **DNI** (su CUIL, y su CUIT si es responsable inscripto, tienen formato `PP-DNI-V` y **contienen el DNI**; prefijos 20/23/24/27); persona jurídica → solo **CUIT** (prefijos 30/33/34), el medio **no** es DNI. **Helper `app/Support/DocumentoIdentidad.php`** (dominio puro, estático): `normalizar()` (solo dígitos), `clave()` (DNI para físicas —extrae el medio del CUIL/CUIT y saca el cero de relleno— / CUIT completo para jurídicas), `inferirTipoPersona()` (por prefijo). **Persistencia:** migración agrega `customers.documento_key` (index **no-único** a propósito: la fusión de convergencias la hace `CustomerMergeService` en el próximo alta, sin romper la migración) + backfill que **normaliza `dni` in-place** (solo-dígitos) y calcula la clave (pre-chequeado: 0 colisiones). Hook `saving` en el modelo `Customer` recalcula `dni` normalizado + `documento_key` desde `dni`+`document_type_id`+`person_type_id` en **todo alta** (checkout/chat/ingesta) — única fuente de verdad. **Dedup por clave (reusa la maquinaria):** `CustomerRepository::findByDocumentoKey`; `CustomerMergeService::reconcile` acepta `documento_key`; `PolicyChainResolver::resolveCustomer($dni, $names, $documentType, $personType)` busca por clave y setea tipo-doc/persona al crear. **`IngestaConfirmacionService` pasa el tipo** (de overrides o payload `tomador.documento_tipo`/`tipo_persona`); `PolicyReportConfirmacionService` sigue igual (infiere por prefijo). **No toca la emisión Visred:** el holder emitido sale de `CheckoutSession` (`PolizaEmisionService`→`$session->dni`), no de `Customer` — verificado. **UI (modal de confirmar contrato):** selects **Tipo de documento** [DNI|CUIT|CUIL] y **Tipo de persona** [Física|Jurídica] (pre-cargados del parser + inferencia por prefijo); endpoint `GET ingesta-pendientes/buscar-cliente` → chip **"Cliente existente: {nombre}"** (verde, autocompleta Nombre/Apellido si están vacíos) / **"Cliente nuevo"**, lookup **debounced 400ms** por la misma clave que usará el confirm (truthful). Tests: `DocumentoIdentidadTest` **11/11** (unit) + `IngestaConfirmacionTest` **21/21** (dedup DNI↔CUIL misma persona → 1 cliente, jurídica por CUIT, lookup existe/nuevo). Suite customer+checkout+report+merge **110/110** · PHPStan **0** · Pint ✅ · build ✅. **Pendiente:** revisar colisiones de `documento_key` preexistentes si se cargan datos masivos (hoy 0). _(rama `main`)_
- **2026-07-01** — **Ingesta local — mejoras UX de la cola de Pendientes ✅.** Cinco mejoras sobre `/ingesta-pendientes` (la revisión funcionaba pero era floja). **(A) Fix de inferencia de estado (`IngestaConfirmacionService::resolveEstado`):** comparación por **fecha** contra `today()` (no `now()`) — corrige off-by-one que marcaba **Vencida el mismo día del vencimiento**; ahora lee `vigencia_desde` → **Emitida** si aún no rige; fallback Emitida sin `vigencia_hasta`. Además `resolvePoliza`/`resolveEstado` prefieren el **override** de `vigencia_desde/hasta` sobre el payload (el admin ya puede corregir fechas en el form). **(B) Dropdown de estados restringido:** `PolizaEstado::paraIngesta()` (solo Vigente/Emitida/Vencida/Anulada — excluye `Programada`/`EnProceso`/`NoVigente`, que solo salen de reportes de cartera); el `index` y la validación (`Rule::in`) usan ese set. **(C) Confirmar/descartar por contrato:** la unidad de trabajo pasa a ser la **tarjeta (contrato)**, no el doc suelto. `confirmContrato(ids, overrides)` resuelve la póliza **una sola vez** (reusa `resolvePoliza`/`attachDocument`, principal = póliza-con-número) y adjunta **todos** los docs del grupo; `discardContrato(ids)` descarta el contrato entero; se conserva `discard` por doc para **quitar basura suelta** de un contrato bueno. Rutas `ingesta-pendientes.{confirmar,descartar}-contrato`. **(D) Index enriquecido:** helper `mergeContrato()` (primer no-nulo across docs) arma `prefill` (form) y `resumen` (tarjeta: tomador, DNI/CUIT, dominio, vehículo marca/modelo/año, vigencia); `campos_faltantes`+`faltantes_count` (badge), **`key` estable** por grupo (reemplaza el `:key` por índice — de paso mata el glitch de re-render que se veía al descartar), orden por faltantes primero, `preview_url` firmada con **`ResponseContentDisposition=inline`** (+ TTL 30min) para que el iframe **muestre** el PDF en vez de descargarlo. **(E) Rediseño del modal a dos paneles (`PendientesIngesta.vue`):** `max-w-6xl h-[90vh]` en grid — izquierda **visor con tabs** por tipo de doc + `<iframe>`, derecha **form de contrato** con `vigencia_desde/hasta` agregados, campos faltantes **resaltados** + auto-focus al primero; tarjeta con resumen enriquecido + badge de completitud. Tests `IngestaConfirmacionTest` **17/17** (4 de fechas + confirmar/descartar contrato + index con key/prefill/resumen). Suite ingesta+policy-docs **47/47** · PHPStan **0** · Pint ✅ · build ✅. **Pendiente (2da tanda):** manejar el choque "ya hay una vigente" al confirmar histórico; señal "este contrato ya existe" antes de adjuntar. _(rama `main`)_
- **2026-06-30** — **Import de reportes de cartera (origen Portal Visred) ✅.** Tercera fuente de mantenimiento de cartera, **independiente** de la ingesta de PDFs y de la emisión Visred: el productor sube un **"Listado de Pólizas"** (xlsx, snapshot completo) al panel y se reconcilia la cartera. Aporta el *esqueleto* `Customer→Risk→Poliza` + **estados** + **vencimientos**; **no** crea `PolicyDocument` (el reporte no trae PDFs). Las fuentes **convergen sólo en la `Poliza` por `company+numero`** (principio de desacople; ningún importador conoce al otro). **Origen como seam (anti-colisión):** enum `ReporteOrigen` (`portal_visred`) elige el parser vía `ReportParserFactory`; agregar un origen futuro (reporte de una compañía, otra fuente) = un parser + una línea, sin tocar los demás. El origen es **provenance** (`polizas.metadata.origen_reporte`), nunca clave de dominio. **Estados:** `PolizaEstado` ampliado con `Programada`/`EnProceso`/`NoVigente` (+`label()`); mapeo del reporte Vigente→Vigente, Anulada→Anulada, Programada→Programada, En-Proceso→EnProceso, **No-Vigente→NoVigente** (paraguas; `Vencida` es subcaso que el reporte no distingue, la decide la lógica de vencimientos). **Premio/Costo se descartan** (no hay campo `premio`; ninguno es la cuota). **Parseo:** `openspout` (dep nueva, read-only); `PortalVisredReportParser` mapea por **header normalizado** (sin acentos/guiones), col "CUIT"=DNI, descarta ID/Productor/Premio/Costo. **Confirmación por lote (modo arranque):** `PolicyReportImportService::stage()` hashea el archivo (idempotente — re-subir devuelve el mismo lote), parsea y crea `policy_report_batches` + `policy_report_rows` con cada fila evaluada en **dry-run** (`ReporteRowAccion`: create/update_estado/noop/exception) + `summary`, **sin escribir dominio**. `PolicyReportConfirmacionService::confirm()` materializa en transacción re-derivando el match por `company+numero` (idempotente), reconcilia estado/vigencia + `last_synced_at`; `discard()` no crea nada. Reusa `IngestaStatus` (pendiente/confirmado/descartado). **Refactor:** se extrajo el find-or-create `Customer→Risk` de `IngestaConfirmacionService` a **`PolicyChainResolver`** (compartido, con dedup vía `CustomerMergeService`) — la ingesta de PDFs sigue verde (12/12). **Alcance acotado (decisión):** el dominio sólo modela `RiskType::Vehicle` y el Risk se llavea por patente → **filas sin patente (ramos Vida/Caución/Comercio) → `exception`** (visibles en el diff, reprocesables cuando se extienda el dominio); se materializan sólo las vehiculares. **Invariante** "una vigente por Risk" respetada marcando excepción (import no destructivo). **UI:** `PolicyReportImportController` + rutas `reporte-cartera.*` + página `ReporteCartera/Index.vue` (subida + resumen del lote + diff de filas + modales inline, sin `window.confirm`) + NavItem "Reporte de cartera". Tests `PolicyReportImportTest` **11/11** (stage/confirm/idempotencia/dedup/update_estado/mapeo de estados/exception sin patente/invariante/controller). PHPStan **0** · Pint ✅. **Pendiente:** modelar ramos no-vehiculares (extender `RiskType` + clave de dedup sin patente); enriquecer phone/email del cliente; canonicalizar nombres de compañía entre fuentes; build frontend (`npm run build`). _(rama `main`)_
- **2026-06-25** — **Ingesta local de documentos — lado server completo (Fases 1–3) ✅.** Implementado el endpoint y la lógica de alta que la entrada 2026-06-24 dejó en diseño. Contrato verificado campo-a-campo contra el ingestor real (`uploader.py` transporte + `parser.py::_build` forma + `main.py:132` inyecta el `hash_sha256`). **Fase 1 — endpoint:** ruta `POST /api/ingesta/documentos` con `auth:sanctum`; `User` usa `HasApiTokens`; comando `php artisan ingesta:token {email}` emite el PAT para `SANCTUM_TOKEN` del cliente. `PolicyDocumentIngestaController` valida el multipart (`metadata` string + `file` `mimetypes:application/pdf` `max:51200`), decodifica el JSON y aplica **minimizar-422** (§2-bis): sólo rechaza lo improcesable (envoltorio roto, sin `hash_sha256`, `schema_version≠1`); un contrato incompleto (sin número/tomador, caso Mercantil/Experta) **degrada a Pendientes, no rechaza**; `kind` desconocido → `Otro`. **Fase 2 — staging + dedup + R2:** tabla **`ingested_documents`** (`hash_sha256` **único** + columnas denormalizadas `kind`/`compania`/`numero_poliza`/`documento_numero`/`patente`/`status` + **`payload` JSONB** crudo + `poliza_id`/`policy_document_id` nullable), enum **`IngestaStatus`** (pendiente/confirmado/descartado), modelo + factory. `IngestaDocumentoService::stage()`: **dedup por hash** (reenvío → fila existente, no re-sube), sube el PDF a **R2** (`ingesta/{hash}.pdf`), crea la fila `pendiente`. Controller: duplicado → **200** idempotente, alta nueva → **201**. **Fase 3 — confirmación humana + materialización:** `PolicyDocumentSource::LocalIngesta`. `IngestaConfirmacionService::confirm($doc, $overrides)` materializa en transacción `Customer→Risk→Poliza→PolicyDocument` — cliente **vía dedup** (`CustomerRepository::findByDni` + `CustomerMergeService::reconcile`, nunca `customers` directo), Risk por `metadata->patente`, **Poliza find-or-create por `company+numero`** (documentos del mismo contrato se acumulan; tarjeta sin número se adjunta por **fallback de patente**), marca `metadata.origen='ingesta_local'`, **estado inferido** de las fechas, `PolicyDocument source=local_ingesta` reusando el PDF en R2; `discard()` no crea nada. **Renovación:** `contrato_anterior_id` se **infiere por patente+compañía** (`sugerirContratoAnterior()`) pero se presenta como **sugerencia que el humano confirma** (modo de arranque; se elimina en fase posterior); al vincular, la anterior pasa a `vencida`. **UI:** `IngestaPendientesController` (agrupa pendientes por `numero_poliza`/`patente` + preview R2 + sugerencia) + rutas web `ingesta-pendientes.*` + página `PolicyDocuments/PendientesIngesta.vue` (modales inline, sin `window.confirm`) + NavItem "Ingesta". **Docs:** `docs/v3/04` actualizado (estado IMPLEMENTADO, §6 mapa de archivos, §7 preguntas resueltas) + índice `docs/README.md` + **`ingestor/app/v5/SERVIDOR.md`** (contraparte server documentada en el repo del cliente, las dos puntas). Tests `PolicyDocumentIngestaTest` **10/10** + `IngestaConfirmacionTest` **12/12**. **Suite completa 459/459** · PHPStan **0** · Pint ✅ · build ✅. **Pendiente:** modo automático (quitar confirmación manual cuando el parser demuestre acierto consistente); cleanup de PDFs descartados en R2. _(rama `documentation-management`)_
- **2026-06-24** — **Ingesta local de documentos — cliente Python listo (parser v5) + diseño server documentado.** Feature nuevo: un script local (`ingestor/`, repo aparte) vigila la carpeta de Descargas del productor, detecta PDFs de pólizas, extrae datos de forma **determinística (sin LLM, sin costo)** y los manda —**JSON contrato + PDF**, multipart— a un endpoint de `workflow-assistant`, que dará de alta lo que falte (`Customer→Risk→Poliza→PolicyDocument` vía dedup) y encolará a **Pendientes** para confirmación manual. **Lado cliente HECHO:** parser **v5** (`ingestor/app/v5/`, doc en `ingestor/README.md`) tras iterar v1→v5 contra un corpus real de 7 aseguradoras (15 PDFs en `ingestor/docs/`). Estrategia ganadora: **pdfplumber + extractor por compañía + validar-o-`null`** (se descartó la línea DeepSeek v3/v3.1 con PyMuPDF + aplanar texto + regex genérica: capturaba miles de chars de basura). Detección de compañía por **CUIT con límites** (no substring); si matchea >1 alias → `None` (la tarjeta verde Mercosur lista varias aseguradoras). Resultado: **5/7 contratos con datos clave completos** (Sancor, Río Uruguay, San Cristóbal, Triunfo, Galicia); Mercantil parcial; Experta necesita OCR. **Insight de dominio:** la **patente suele estar en la tarjeta, no en el frente** → el server debe unir documentos del mismo contrato por **`numero_poliza` con `patente` como fallback**. **Lado server PENDIENTE** (diseño escrito): endpoint multipart con Sanctum, find-or-create delegando el alta de cliente a la dedup (`docs/v2/12`), nuevo `PolicyDocumentSource::LocalIngesta`, marca de origen en `polizas.metadata`, dedup de PDF por `hash_sha256`, encolado a Pendientes. Doc canónica **`docs/v3/04-ingesta-local-documentos.md`** (+ índice `docs/README.md`). **Sin código en workflow-assistant todavía** (solo documentación; lo construido va en la entrada 2026-06-25). _(rama `documentation-management`)_
- **2026-06-22** — **Centro de mantenimiento de cartera (`/mantenimiento-cartera`) ✅.** Cola **única accionable** que fusiona las dos fuentes de trabajo derivadas — "registrar una renovación que hizo la compañía" y "cargar un documento faltante" — en un solo listado priorizado por urgencia (un eje, menor = más urgente). **Todo derivado, sin tabla de tareas:** la cola se auto-vacía por hechos de dominio. **Insight clave — clearing por sucesora (renovación o cambio de compañía), agnóstico del origen:** una póliza vieja cae de la cola cuando existe otra que la referencia por `contrato_anterior_id`, sin importar si fue una renovación o un cambio de compañía. **Datos:** migración evoluciona el back-ref `contrato_anterior_ref` (string) → **`contrato_anterior_id`** (FK self, `nullOnDelete`) + agrega **`periodo_corto`** (bool, la excepción que no se renueva — p. ej. AP por días) y **`no_renovar_at`** (timestamp, descarte honesto sin anular — la póliza queda vencida pero sale de la cola). Migración limpia (0 filas con back-ref). **Modelo `Poliza`:** relaciones self `contratoAnterior()`/`sucesoras()`; **`esRenovable()`** (estructural, sin ventana — gatea el botón "Renovar": permite renovar una **vencida sin sucesora** —caso escalado— y bloquea la doble renovación); **`scopeARenovar()`** (estructural + ventana 45d — arma la cola); **`scopeDocumentacionIncompleta()`** (extrae el `whereRaw` de conteo de kinds que vivía inline en `pendientes()`, ahora compartido). **`PolizaService::renovar()`** cambia el guard "debe ser vigente" por `esRenovable()` y setea `contrato_anterior_id`. **Acción "No renovar"** (`polizas.descartar-renovacion` POST/DELETE) con modal inline; reactivable desde `Edit.vue`. **`MantenimientoCarteraController`** (`__invoke`) arma un **reporte operativo por póliza** (una fila por póliza, no dos): cada fila es un **checklist accionable** que fusiona los documentos esperados (todos con estado ✓/☐) + la **renovación como un ítem más** (derivada de `vigencia`, niveles `al_dia`/`vence_pronto`/`vencida`/`sin_vigencia`/`no_renueva`/`renovada`/`descartada`). Una póliza aparece si le **falta ≥1 documento** _o_ tiene **renovación pendiente** (dentro de ventana o vencida); urgencia = el eje más urgente entre el deadline de renovación y la criticidad del peor faltante. **Ventana de renovación seleccionable** (30/60/90, default 30, como Vencimientos); la línea de renovación solo se muestra al acercarse el vencimiento (estado `al_dia`/fuera de ventana no se renderiza, para no ensuciar con eventos lejanos). Paginado en memoria (`LengthAwarePaginator`). Vista `MantenimientoCartera/Index.vue` (card-checklist por póliza: contador `N/M`, ítems de doc con ✓/☐ donde cada **faltante linkea a su carga con el `kind` preseleccionado**, renovación con ícono por nivel + "Renovar"/"No renovar" cuando es accionable, modal inline) + NavItem "Mantenimiento de cartera" (grupo Principal). **Acción #2 (carga directa):** `PolicyDocumentController::show` acepta `?kind=` (valida contra el enum) y `Show.vue` preselecciona el tipo en el form de subida. `Edit.vue`: botón "Renovar" gateado por `es_renovable`; `PolizaFields.vue` con checkbox "Período corto". Las vistas `/documentacion-pendiente` y `/polizas/vencimientos` quedan intactas. Tests: `MantenimientoCarteraTest` (esRenovable, scopeARenovar, reporte/orden, exclusiones, fila única combinada, ventana seleccionable) + `PolicyDocumentControllerTest` (preselección de `kind`) + `PolizaControllerTest` (renovación con `contrato_anterior_id`, vencida-sin-sucesora escalada, doble renovación rechazada, período corto, descartar/reactivar). **Suite 437/437** · PHPStan **0** · Pint ✅ · build ✅. _(rama `documentation-management`)_
- **2026-06-21** — **Mantenimiento de documentación de póliza — Panel "Documentación pendiente" (checklist global) ✅.** Vista dedicada de **desviaciones** (vistazo diario): lista las pólizas **activas (vigentes + emitidas)** a las que les falta ≥1 documento esperado, con el detalle de **qué falta** (chips rojos) + completitud `N/M` + CTA "Gestionar documentos". Excluye vencidas (de esas no se chasea nada). `PolicyDocumentController::pendientes` (filtra por estado + subquery `count(distinct kind) < esperados`, reusa `PolicyDocumentKind::expectedForActivePolicy()`); ruta propia `/documentacion-pendiente` (evita el doble-highlight de `startsWith('/policy-documents')`); vista `PolicyDocuments/Pendientes.vue` **paginada** (`paginate()->through()` + selector 15/25/50 + componente `Pagination`); **ítem nuevo en el nav** ("Doc. pendiente", grupo Principal, ícono de alerta). Tests `PolicyDocumentControllerTest` (incluye vigente/emitida incompletas, excluye completa y vencida; paginación con `per_page`). PHPStan **0** · Pint ✅ · build ✅. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-22** — **WhatsApp identidad — captura de BSUID + teléfono y respuesta por BSUID ✅** (cierra dos ítems del workstream Meta-readiness listado el 2026-06-20: "parser tolerante a `wa_id` ausente" y "envío por `recipient`"). Desde abril 2026 Meta inyecta el **BSUID** (`contact.user_id`) en **todos** los webhooks y el **teléfono** (`contact.wa_id`) pasó a ser **opcional** (falta en usuarios username-only sin ventana de 30d / Contact Book — ver doc Meta §4). **Antes:** el webhook **descartaba** el mensaje si faltaba `wa_id` (`if ($waId && …)`) y el teléfono se tipaba `string` no-nullable por todo el pipeline → se perdía el BSUID. **Fix (mínimo, sin tocar DB):** (1) **`WhatsAppWebhookController`** despacha si hay `messageId` y (teléfono **o** BSUID). (2) **`ProcessWhatsAppMessage`** `?string $waId`; al **crear** la conversación sin teléfono usa el BSUID como `external_conversation_id` (el dual-key `findByExtUserId` ya existía); el BSUID sigue en `ext_user_id`, el teléfono en `messages.sender_phone` (ya nullable). (3) **`ProcessConversationInbox`**/(4)**`ProcessMediaAttachment`** `?string $waId`. (5) **`SendWhatsAppMessage`** lleva `phone` + `bsuid` por separado. (6) **`WhatsAppOutboundService`** — helper `recipientPayload(phone, bsuid)` **sin regex**: si hay teléfono → `to`, si no → `recipient` (BSUID, doc §10) en `sendMessage`/`sendTypingIndicator`/`sendAudioMessage`; `sendTemplate` queda en `to` (evita la prohibición de BSUID en AUTH, §8). (7) **`NotifyClientQuoteReady`** deriva teléfono/BSUID de la conversación. **`InsuranceOrchestrator` sin cambios** (su auto-id por teléfono ya omite el BSUID alfanumérico). El BSUID **no** va en `customers` (identidad = dni/email/phone). Tests nuevos: ingesta solo-BSUID + ambos presentes (`ProcessWhatsAppMessageIngestionTest`), destinatario BSUID en outbound (`ProcessConversationInboxTest`), `to` vs `recipient` por null-check (`WhatsAppOutboundServiceTest`). **Suite completa 428/428** · PHPStan **0** · Pint ✅. _(rama `feat/whatsapp-bsuid-capture`)_
- **2026-06-21** — **Mantenimiento de documentación de póliza — Fase 5: notificación push de documento nuevo ✅.** Al cargar un documento (`PolicyDocumentService::storeAdminUpload`) de una póliza **vigente**, se avisa al asegurado por push. **`PolicyDocumentNotifier`** (agnóstico de canal) resuelve la(s) `MobileAccount` del titular por email (mismo seam que `resolveCustomer`, case-insensitive) y encola un **`PublishDocumentAvailable`** por cuenta; **`DocumentAvailablePublisher`** (espeja `FcmTopicPublisher`) publica data-only (`type=policy_document` + `poliza_id`/`kind`) al **topic por cuenta** `account-{id}` (reusa la infra FCM; sin token por dispositivo). **Best-effort:** la resolución/encolado va en try/catch (un fallo no rompe la carga) y el job traga errores de FCM. No se encola si la póliza no está vigente o el titular no tiene cuenta. **Contraparte Flutter (mango-mobile, pendiente):** suscribirse al topic `account-{id}` al autenticarse + manejar `type=policy_document` (notificación local + refresco del Home), reusando `weather_messaging`/`acp_local_notifier`. Tests `PolicyDocumentControllerTest` (encola para titular con cuenta; no encola si vencida / sin cuenta). **Cierre de contrato:** `Mobile/AuthController` ahora expone `user.id` en `/auth/session` (la app lo necesita para suscribirse al topic `account-{id}`); nota de seguridad documentada (topic adivinable → mitiga que el push es data-only, sin contenido; hardening = token opaco por cuenta si hiciera falta). Doc canónica del contrato backend↔Flutter en **`docs/v3/03-entrega-documentacion-app.md`** (API mobile, push FCM, qué falta en Flutter); índice `docs/README.md` marca 01/02 como diseño histórico descartado y 03 como lo implementado. **Suite completa 423/423** · PHPStan **0** · Pint ✅. **Feature backend completo (Fases 1–5); contraparte Flutter pendiente (doc 03 §4).** _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-21** — **Mantenimiento de documentación de póliza — Fase 4: checklist de completitud ✅.** `PolicyDocumentKind::expectedForActivePolicy()` define el set esperado de una vigente (**Póliza + Cédula de circulación + Certificado** — supuesto de dominio fijo, a confirmar). `PolicyDocumentController::show` expone `checklist` (presente/faltante por kind) → card "Documentación esperada" en `Show.vue` con contador `N/M` y faltantes marcados; `index` expone `doc_presentes`/`doc_esperados` (eager-load `documents:id,poliza_id,kind`) → indicador `N/M esperados` que reemplaza el sub-texto "visibles" (redundante con la regla todo-de-la-vigente). Tests `PolicyDocumentControllerTest` (checklist en show; completitud en index). PHPStan **0** · Pint ✅ · build ✅. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-21** — **Mantenimiento de documentación de póliza — Fase 3: cola de vencimientos ✅.** `PolizaController::vencimientos` lista las pólizas **vigentes** con `vigencia` dentro de los próximos N días (default 30, selector 15/30/60/90) ordenadas por urgencia — derivado de `vigencia`, **sin persistir** nada ni barrido nocturno. Excluye las vencidas y las vigentes **sin** `vigencia` (default conservador: no se inventa fecha). Ruta `polizas.vencimientos`; vista `Polizas/Vencimientos.vue` (badge de urgencia por días restantes — rojo ≤7d, ámbar ≤30d — + CTAs "Renovar"/"Documentos"); acceso desde el header de `Polizas/Index.vue`. Test `PolizaControllerTest` (solo vigentes en ventana; excluye vencida y sin-vigencia). PHPStan **0** · Pint ✅ · build ✅. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-21** — **Mantenimiento de documentación de póliza — Fase 2: alta externa + renovación + organización ✅.** (1) **Renovación:** `PolizaService::renovar($anterior, $nuevaData)` abre una Poliza **nueva** sobre el **mismo Risk** (número nuevo) con `contrato_anterior_ref = $anterior->numero`, marca la anterior **vencida** y la nueva **vigente**, en una transacción — preserva "una vigente por Risk" por construcción; rechaza renovar una no-vigente. `PolizaController::renovarForm`/`renovar` + rutas `polizas.renovar.form`/`polizas.renovar`; vista `Polizas/Renovar.vue` (arrastra compañía/cobertura/suma de la anterior, número/vigencia en blanco) + botón "Renovar" en `Edit.vue` (solo vigentes); `PolizaFields.vue` con flag `showEstado` (oculta el estado en renovación, lo fuerza el servicio). (2) **Alta de pólizas externas:** ya cubierta por el CRUD manual existente (`createManual`, con `vigencia`/`estado`) — sin código nuevo. (3) **Organización vigente/histórica:** badge de estado en `PolicyDocuments/{Index,Show}.vue` (Vigente verde vs Histórica gris). (4) **Seed de vigencia:** `PolicyReferenceService::materialize` setea `vigencia = emisión + 1 año` para que las emitidas aparezcan en la detección (Fase 3). Tests `PolizaControllerTest` (renovarForm, renovación con back-ref + anterior vencida, rechazo no-vigente) + `PolicyReferenceServiceTest` (seed). Suite tocada **52/52**. PHPStan **0** · Pint ✅ · build ✅. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-21** — **Mantenimiento de documentación de póliza — Fase 1: regla "todo lo de la vigente" + back-ref ✅.** Tras una sesión de diseño que **acotó v3** (el modelo de transacciones/fold/extractor se descartó: la reconstrucción de estado a fecha pasada no la consume nadie en MANGO y la historia ya vive en Visred + la compañía + la pila de PDFs; ver plan `necesito-un-plan-de-crispy-liskov.md`), el objetivo quedó en **cargar y mantener la documentación** de la póliza y entregarla a Mango. **Fase 1:** (1) regla de visibilidad **"todo lo de la vigente"** — `Mobile/PolizasController::documentos` entrega **todos** los documentos de la póliza **vigente** (gate por `estado`) y **ninguno** de las vencidas; se retiró el filtro/​toggle `visible_to_client` por documento (la columna queda, default `true`, sin UI). (2) `polizaPayload` expone **`tiene_documentos`** (vía `withCount('documents')`) para que el Home decida si ofrecer la descarga. (3) Migración **`contrato_anterior_ref`** en `polizas` (back-ref de renovación, base de la Fase 2). (4) Admin: `PolicyDocuments/Show.vue` sin toggle de visibilidad; `toggleVisibility` (controller + ruta) eliminado; `store` ya no recibe `visible_to_client` (persiste `true`). **Contraparte Flutter (mango-mobile, pendiente):** acción "Descargar documentación" en `PolicyCard` que consume `/polizas/{id}/documentos`, condicional a `tiene_documentos`. Tests `Mobile/PolizasTest` (todo-de-la-vigente, vencida sin docs, `tiene_documentos`) + `PolicyDocumentControllerTest` (sin toggle) — **24/24**. PHPStan **0** · Pint ✅. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-20** — **v3 — Modelo de mantenimiento de cartera + endosos (diseño, gate) escrito.** Nueva carpeta `docs/v3/` (diseño post-emisión, no implementado) con 2 docs: **`01-modelo-mantenimiento-cartera-endosos.md`** (modelo de dominio `Risk → Poliza (transacción 0) → Endoso`; jerarquía temporal término-cobertura/facturación/plan-de-pago; ejes de estado ortogonales — A: Emitida/Vigente/Vencida **computado** + Anulada/Rescindida por endoso terminal · B: suspensión por falta de pago **parqueada** sin señal de cobranza; **auto-transición se computa, no se persiste**; taxonomía de endosos append-only plegados por `fecha_inicio` retroactiva; frontera invariante/variable) y **`02-extractor-documentos-poliza.md`** (el "punto 6": documento → clasificar→extraer→**confirmar** humano-en-el-loop → hechos de dominio; seed computado vs hecho extraído; un solo extractor para captura-emisión + carga-manual; mapper por compañía; agnóstico de canal). **Validado contra 4 compañías** como casos testigo (San Cristóbal, Triunfo, Río Uruguay, Sancor) — 3 modelos de facturación (refacturación mensual · premio único en cuotas · semestral), 2 duraciones de término (anual/semestral), un endoso modificatorio y uno terminal. **Reconciliación ✅ hecha:** invierte la premisa on-demand de `docs/v2/10` §4/§5 (no existe endpoint de consulta por `policy_number` → cartera manual); correcciones v3 anotadas en `docs/v2/10` §1/§4/§5 y en la memoria `feedback_modelo_agnostico_proveedor`. Índice `docs/README.md` actualizado con sección v3. **Decisiones de diseño §13 cerradas:** cadencia = hecho por póliza (extraído del documento / derivado de períodos, no config por compañía); eje B (suspensión) = diferido explícito con condición de reingreso (señal de cobranza); estado computado = decidido, la migración del código persistido→computado es delta de implementación; back-ref de renovación normalizado a `contrato_anterior_ref`; umbral de extracción (doc 02) = default conservador (siempre confirma el operador). **Refinamiento de modelo (2026-06-21):** se aplanó a **corriente de transacciones uniforme** — `Risk → Poliza (identidad liviana) → Transacción (corriente)`; "endoso" pasa a ser un **`tipo`** de transacción (no entidad aparte); la **Transacción (operación)** es el concepto unificador (emisión/renovación abren Poliza, las demás la modifican); estado = **fold** de la corriente con **dos semánticas** (atributos = última que tocó el campo por `fecha_inicio`; montos = la transacción cuyo período contiene el corte); "vigente" es del contrato, no de la transacción. Motivado por mínima fricción de carga (la refacturación —la transacción más frecuente— se carga como una fila uniforme). **Sin código** (model-first, gate de revisión). _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-20** — **Dedup/merge de Clientes — procedimiento de identificación implementado ✅** (cierra la "implementación pendiente" de la entrada del 2026-06-20 abajo). Tras analizar el flujo de registro completo (4 fuentes — WhatsApp/Checkout/App/Admin — × órdenes temporales; plan `analiza-acabadamente-el-problema-lively-fern.md`), se aplicó: **(A) `reconcile()` solo por claves fuertes** (`dni`/`email`, únicas) — se quitó `phone` del lookup (en el checkout dni+email son obligatorios y ya cubren todo duplicado de la **misma** persona; `phone` no es único → solo podía fusionar **dos personas distintas**, p. ej. número reasignado). **(B) Survivorship por campo** — nuevo `CustomerConsolidationService::mergeFields()`: por campo gana la fuente más confiable (**admin > checkout > chat**), desempata el `at` más reciente, **preserva la provenance ganadora** y audita cambios + descartes; reemplaza el viejo "rellenar huecos + `auditConflicts`". **(C) Árbol create/enrich/merge en el chat** — `CustomerIdentificationService::resolveForConversation()` (agnóstico de canal, lo usan `WhatsAppAdapter` y `AgentToolAdapter`): identificador de **otra** fila → **merge**; identificador que **nadie** posee y la conversación ya tiene tomador → **enriquecer** esa fila (no crear) — elimina el `create+repoint` que dejaba filas huérfanas (la fábrica de duplicados). **(D) `wbid` era un mislabel de DNI** — confirmado con la doc de Meta (Abril 2026): el `wbid`/BSUID real es un identificador **de conversación** (`conversations.ext_user_id`, dual-key ya implementado), **no** del tomador. El tool de cliente ahora ofrece `email|phone|`**`dni`**; arreglado el **crash triple** (`validateIdentifier`/`findCustomer`/`createCustomer` ahora manejan `dni` + `default`); eliminado `CustomerRepository::findByType` (query a columna arbitraria, sin uso). **(E) `MobileAccount::resolveCustomer()` case-insensitive** (`LOWER(email)`) — un casing distinto del OAuth dejaba al titular sin ver sus pólizas. **Tests:** 6 nuevos en `CustomerMergeTest` (reproduce el bug #4, chat-enrich/chat-merge, número reasignado **no** mergea, survivorship admin-protegido + recencia). **Fuera de scope** (workstream Meta-readiness, `docs/v2/12` §6.3, doc nuevo de Meta): cert TLS DigiCert→Meta, parser tolerante a `wa_id` ausente (username-only), `business_username_update`, envío por `recipient`, Portfolio Pacing. PHPStan **0** · Pint ✅ · suite **413/413**. Docs `docs/v2/12` §6–7 actualizado. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-19** — **Fix — pólizas emitidas quedaban SIN documentación + captura diferida de documentos.** Síntoma real (checkout quote 25 → póliza Triunfo 1822203): emisión exitosa pero `policy_documents` vacío y warning `[VisredDocument] documento sin result.url {download-poliza}`. **Causa:** `VisredDocumentService::fetchOne` asumía `POST /v1/documents/` **síncrono** (esperaba `result.url` en el primer POST), pero la compañía genera el PDF **async** — recién emitida la póliza todavía se está generando, así que la URL viene vacía. Hacía **un solo intento, sin polling** → devolvía `null` → `documents` vacío → cero `policy_documents`. Y como el `presale_id` muere en el adapter y no se persistía nada, no había forma de recuperar las descargas después (presale caduco). Solo se intentaba `download-poliza` (certificado/tarjeta de circulación ni figuraban en config). **Fix (Completo):** (1) la captura hace **un intento** por llamada (no poll-ea inline — no bloquea el job de emisión). (2) **Captura diferida:** `capture()` separa listos vs pendientes; `emit()` reporta `pending_documents` (token **opaco** = presale + `kind` faltantes) en el shape neutro; `PolizaEmisionService` persiste una **`poliza_provider_refs`** (misma convención `*_provider_refs` que `quote_alternative_provider_refs` — token aislado, el dominio no lo interpreta) y encola **`CapturePendingPolicyDocuments`**, que re-pide los PDFs **1 vez por minuto durante ~10 min** (delay/backoff `visred.document_retry_*`=60s, 10 tries; se re-encola con delay, no bloquea worker) y borra la ref al terminar o al agotar la ventana. Nuevo método de puerto `EmissionProvider::capturePendingDocuments()`. (3) **Catálogo ampliado** (`visred.document_task_types`): + `download-certificate`→`certificado` (nuevo `PolicyDocumentKind::Certificado`) y + `download-circulation-card`→`circulation-card`. Supera el supuesto "síncrono" de la entrada 2026-06-15. Migración `poliza_provider_refs` + modelo + relación `Poliza::providerRef()`. 9 tests nuevos (intento único/split/captura diferida en `VisredEmissionProviderTest`, encolado en `PolizaEmisionServiceTest`, job en `CapturePendingPolicyDocumentsTest`). PHPStan **0** · Pint ✅ · suite **403/403**. La póliza 1822203 ya emitida queda igual sin recuperación automática (presale caduco) → carga manual admin. Docs: `docs/v2/10` §5. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-20** — **Dedup/merge de Clientes — data-fix aplicado + plan corregido (`docs/v2/12`).** Caso real reportado: checkout #4 (quote 25) emitió la **póliza 1822203** bajo `customer #1` (fila creada por WhatsApp, solo teléfono) mientras la app móvil identifica al titular **por email** (`MobileAccount::resolveCustomer()`, `customers.email` único) → resolvía `customer #4` (fila con email/DNI, `mobile_account` Firebase) → la app no veía la póliza. El log lo delató: `CustomerConsolidation: clave única en conflicto` para `dni`/`email` (owned_by #4, intentado sobre #1). **Data-fix:** merge manual **#1 → #4** (sobrevive #4: enriquecido con el domicilio/nacimiento/sexo/cond.fiscal/`field_sources` del checkout que habían caído en #1; repuntadas FKs `conversations`/`customer_audits`/`risk_snapshots`/`risks`/`vehicles`; #1 borrado). Verificado: la 1822203 quedó bajo #4 y `resolveCustomer()` la levanta. **Corrección de diseño (supera la entrada del 2026-06-19 abajo):** `reconcile()` debe reconciliar **solo por claves fuertes (`dni`/`email`, únicas), NO por teléfono** — `phone` no es único (compartido en familia) y `findByPhone()->first()` puede fusionar **dos personas distintas**. Con eso el "conflicto de identidad fuerte" prácticamente desaparece y el guard queda como red de seguridad mínima ante email reciclado/dato sucio. Doc autocontenido nuevo **`docs/v2/12-deduplicacion-merge-clientes.md`** (causa raíz, data-fix, `Merge` vs `Consolidation`, decisión claves-fuertes, plan pendiente: sacar teléfono de `reconcile`, source `'merge'` en consolidation, fix `UnhandledMatchError` en `createCustomer` para `wbid`/`dni`, merge cross-key en `WhatsAppAdapter::identifyCustomer`, tests). **Implementación pendiente** (se retoma en ventana nueva). _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-19** — **`CustomerMergeService` — fusión de identidades duplicadas en convergencia ✅.** Causa raíz del crash anterior: el duplicado es **estructural**, no un error de carga. Cada puerta de entrada crea un `Customer` atado a **una sola clave** y nunca se cruzan — WhatsApp por **teléfono** (`CustomerIdentificationService`), app mobile por **email** (`MobileAccount`, resuelve lazy por `customers.email`), y el checkout es donde por primera vez conviven teléfono+email+DNI. Nuevo servicio **agnóstico de canal** `CustomerMergeService` (complementa a `CustomerConsolidationService`: ese reconcilia **campos** dentro de una fila, este reconcilia **dos filas** en una). `reconcile(survivor, {dni,email,phone})` busca filas que ya posean cualquiera de esos identificadores y fusiona cada una (maneja DNI en una fila + email en otra); `merge(survivor, loser)` en transacción: (1) audita conflictos (gana el survivor, se deja rastro del valor descartado), (2) repunta las **6** columnas `customer_id` (`conversations`+quotes, `vehicles`, `risks`+pólizas, `risk_snapshots`, `customer_audits`, `mobile_accounts`) perdedor→survivor, (3) **`forceDelete()`** del perdedor (hard delete: el índice único de dni/email **no es parcial**, un soft-delete retendría el slot), (4) rellena los vacíos del survivor con los del perdedor vía `apply()` con source nuevo **`merge`** (rellena-solo-vacíos, igual que `chat`). **Dirección:** survivor = customer de la conversación en curso (la quote/snapshot/session activos cuelgan de ahí). Enganchado en `CheckoutController::submit` **antes** de la consolidación. El guard de clave única queda como red de seguridad (defensa en profundidad). 5 tests nuevos (`CustomerMergeTest`). PHPStan **0** · batería de cliente **28/28**. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-19** — **Fix — el checkout reventaba al consolidar un DNI/email ya usado por otro `Customer`.** `CustomerConsolidationService::apply()` escribía las claves de negocio únicas (`dni`, `email`) sobre el `Customer` de la conversación **sin chequear** que el valor ya perteneciera a otra fila → `UniqueConstraintViolationException` (`customers_dni_unique`) que **abortaba toda la transacción del checkout** (se perdía session, transición de fotos, dispatch de emisión y mail). Caso real: el customer de la conversación (placeholder con toda la actividad — convs/vehículos/snapshots — pero `dni=null`) declaraba en checkout un DNI/email que ya era de **otro** customer creado por CRUD admin. **Fix mínimo agnóstico de canal:** nueva const `UNIQUE_FIELDS=['dni','email']`; antes de escribir una de esas claves, si otra fila ya la posee (`whereKeyNot`), se **saltea el campo** (el resto de los datos sí consolidan) y se **loguea el conflicto de identidad** con ambos `customer_id`. No se auto-mergea (dirección ambigua: el placeholder tiene la actividad, el otro tiene el DNI → **merge manual**, deuda señalada). El camino admin ya validaba con `Rule::unique`; este cierra el hueco del checkout. 2 tests nuevos en `CustomerConsolidationTest` (colisión de DNI y de email). PHPStan **0** · suite del archivo **7/7**. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-17** — **CRUD de Clientes (sección propia) + consolidación del `Customer` como fuente de verdad ✅.** Nueva sección `/customers` (independiente de "Conversaciones", que queda intacta) con detalle rico para el productor: resumen de cartera (KPIs), vencimientos/alertas (cuota + renovación por urgencia), vehículos, pólizas (+acceso a sus docs), cotizaciones, conversaciones, **datos de la última declaración de checkout (read-only)**, **divergencias** (con resolución 1-clic), timeline y notas. **Integridad de datos:** el `Customer` pasa a ser el registro canónico — el checkout y el chat **sincronizan de vuelta** vía `CustomerConsolidationService` (agnóstico de canal). Modelo de pesos por fuente (**checkout ≈ admin > chat**): admin manual siempre aplica y se audita; checkout (declaración jurada) rellena vacíos y pisa lo de origen chat, pero **no pisa lo curado por admin → marca divergencia**; chat solo rellena vacíos. Provenance por campo en `metadata.field_sources` + **audit log** (`customer_audits`). **Nombre partido** en `first_name`/`last_name` (migración; `name` sincronizada por compat). Campos canónicos alineados con el `person_holder` de Visred + domicilio del tomador. **Domicilio:** Visred toma un solo `address`; el dominio modela tomador (en `Customer`) y guarda (en `vehicle`), y el **mapeo vive en el adapter** — `address.zip_code` emitido = **CP de guarda del vehículo** (el que tarifa), regla confinada a `VisredEmissionProvider` (multi-provider ready). Hooks: `CheckoutController::submit` (source checkout) y `CustomerController` (source admin). Análisis de dominio completo en **`docs/v2/11`**. Tests nuevos (`CustomerConsolidationTest` + `CustomerCrudTest` portado a `customers.*` + caso zip de guarda en `VisredEmissionProviderTest`). Side-effect positivo: las anotaciones `@property`/generics de `Customer`/`Conversation`/`Quote` redujeron el **baseline PHPStan de 143 → 42**. PHPStan **0** · build ✅ · suite **392/392**. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-16** — **Index de "Docs Pólizas" diferenciado del de "Pólizas".** El index de documentos era un clon visual del listado de pólizas (mismo buscador + tabla), solo cambiaba el destino de la fila. Se conservan ambas vistas (decisión del usuario) pero el de Docs pasa a ser **documentación-céntrico**: pills de filtro **Todas / Con documentos / Sin documentos** (`Poliza::has/doesntHave('documents')`), orden por **última carga de documento** (subquery `max(captured_at) desc nulls last`, sin docs al final) y columna **"Último documento"** (kind + fecha, o badge "Sin documentos") en vez de cobertura/estado/vigencia; badge de cantidad con sub-línea "N visibles". Backend: relación `Poliza::latestDocument()` (`latestOfMany('captured_at')`) + `withCount('documents as visible_documents_count')`; el index expone `last_kind`/`last_document_at`/`visible_count`. `@property-read` para los counts agregados (Larastan no resuelve aliases custom). 2 tests nuevos de filtro (`PolicyDocumentControllerTest` 8 → 10). PHPStan **0** · suite del archivo verde. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-16** — **Vista "Clientes" renombrada a "Conversaciones" (panel admin).** Rename completo de la vista de CRUD de clientes a pedido del usuario, manteniendo intacto el modelo de dominio: `Customer`, tabla `customers`, param de ruta `{customer}` y FKs `customer_id` **sin tocar**. Cambios: URLs `/customers/*` → `/conversations/*`; route names `customers.*` → `conversations.*`; `CustomerController` → `ConversationController` (namespace `App\Http\Controllers`); carpeta Inertia `Customers/` → `Conversations/`; nav "Clientes" → "Conversaciones" + título de la página. **Colisión resuelta:** ya existía `Admin\ConversationController` (auditoría de chats, modelo `Conversation` real) — para evitar el choque de `use`, el nuevo controller se importa aliased en `routes/web.php` (`ConversationController as CustomerConversationController`). Actualizados: auth redirects (`AuthenticatedSessionController`/`VerifyEmailController`), tests (`CustomerCrudTest`, `AuthenticationTest`, `EmailVerificationTest`), cross-links de Pólizas, blades legacy, `phpstan-baseline.neon` (path del controller) y docs v2 (#02, #05). _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-15** — **CRUD de clientes y pólizas (admin panel) ✅.** Hasta ahora el admin podía cargar documentos a pólizas existentes pero no crear un cliente ni una póliza. Cadena de dominio `Customer → Risk → Poliza` (ambas FK NOT NULL), sin migración (todas las columnas ya existían). **Clientes:** `CustomerController` completa los métodos vacíos (`create/store/edit/update/destroy`) reusando `CustomerRepository` (normaliza email/teléfono, marca `is_anonymous`); alta exige ≥1 identificador (DNI/email/teléfono), `dni`/`email` únicos; baja = soft-delete **bloqueado si el cliente tiene una póliza vigente** (vía `risks.polizas`); `show()` ahora lista las pólizas del cliente. `Customers/{Create,Edit}.vue` + botón "Nuevo cliente" en Index + acciones Editar/Eliminar (modal inline) y sección Pólizas + "Agregar póliza" en Show. **Pólizas:** sección propia (nav "Pólizas") con `PolizaController` (resource sin `show` — el "show" es el doc-manager existente) + atajo "Agregar póliza" desde el cliente (`?customer={id}`). Alta **search-first** del cliente (Inertia GET, mismo UX que Docs Pólizas) → selector de Risk **reusar-existente o crear-nuevo inline** (patente/marca/modelo/versión/año/...) → campos de póliza. Lógica en `PolizaService` (agnóstico de canal, sin DTOs): resuelve el Risk (valida pertenencia al cliente), aplica el constraint de dominio **"una sola póliza `vigente` por Risk"** (en código — `ValidationException` clara, no auto-degrada la vigente) tanto al crear como al editar. `Polizas/Edit.vue` cross-linkea a "Gestionar documentos" (`policy-documents.show`). Componente compartido `Polizas/PolizaFields.vue`. 18 tests nuevos (`PolizaControllerTest` 11 + `CustomerCrudTest` 7). PHPStan **0** · Pint ✅ · build ✅ · suite **382/382**. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-15** — **Gestión manual de documentos de póliza (admin panel) ✅.** Cierra la deuda abierta el mismo día: post-emisión (renovaciones/endosos/correcciones) NO pasa por Visred (no hay `presale_id`), así que esos documentos entran por carga manual del admin. Nueva sección "Docs Pólizas" (grupo Principal del nav): `PolicyDocuments/Index.vue` busca la `Poliza` (por `numero`/patente/cliente, `ilike` + JSON `metadata->patente`) y `Show.vue` es su gestor — subir (PDF o imagen, `max:51200`), togglear `visible_to_client` (un doc `admin_upload` visible aparece de inmediato en `GET /polizas/{id}/documentos` de mango), y eliminar con modal inline (borra el archivo en R2 vía `DeleteOrphanPhoto`). Backend: `PolicyDocumentController` (sin DTOs, arrays/Eloquent directo), `PolicyDocumentService::storeAdminUpload` (**siempre `create()`** — los documentos se acumulan al contrato, no se reemplazan; path `policy-documents/{id}/{kind}-{uuid}.{ext}`). **Modelado:** `kind`/`source` pasan a enums tipados (`PolicyDocumentKind`/`PolicyDocumentSource`) — única fuente de verdad cross mobile/admin/Visred; migración agrega `original_filename` + `label` (nullable) para distinguir varios docs del mismo `kind`. Factories nuevos `PolicyDocument`/`Poliza`/`Risk`. La emisión queda intacta (su `updateOrCreate` solo "pisa" en un retry idempotente del mismo snapshot). PHPStan **0** · Pint ✅ · suite **364/364**. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-15** — **Corrección — `presale_id` confinado al adapter + documentos de póliza persistidos.** `presale_id` (dato de Visred, solo vive en el ciclo de emisión) estaba embebido como handle durable en `polizas` (tabla que lee mango-mobile) y paseado por el shape neutro y el dominio. **Sacado:** nueva migración dropea `polizas.presale_id`; `EmissionProvider::emit()` ya no lo expone (SUCCESS se decide por `status`); la inspección post-emisión pasa a ser **interna del adapter** (`uploadInspection` removido del puerto, `presale_id` no sale de `VisredEmissionProvider`). La referencia durable es `policy_number` + `company_id`. **Documentos — el túnel queda caduco:** el adapter captura los oficiales dentro de `emit()` (con el presale vivo, `POST /v1/documents/` síncrono → `result.url` pre-firmada → bytes) y los devuelve como blobs neutros; `PolicyDocumentService` los persiste en R2 (`PolicyDocument`, `source=visred_emission`, `visible_to_client`). Nuevo `GET /polizas/{id}/documentos` (URL R2 firmada/expirable, authz propio/compartido). **Renovación = sin Visred, sin presale** (ocurre en la compañía) → renovaciones/endosos van por **carga manual del admin (deuda)**. PHPStan **0** · tests emisión/referencia/adapter/documentos **34/34**. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-07** — **Visred — V2-3: `VisredQuotationProvider` + gate de quotability ✅.** Diseño **aplanado** (a pedido del usuario: "más llano, sin exceso de ingeniería"). **Gate (resolver real):** puerto agnóstico `Quotability::check(Vehicle): QuotabilityResult` — lo único que ve el canal; `identify_vehicle` NO conoce el catálogo, solo el tri-estado `Quotable`/`NeedsFact`/`NotQuotable`. `VisredQuotabilityResolver` (impl Visred) navega el árbol de 5 GETs (rutas singulares, shapes heterogéneos → `{ref,label}`), desambigua (Tier 1a igualdad exacta = candado del re-cotizar → Tier 1b subconjunto léxico → Tier 2 LLM `DisambiguationAgent`, parsea `version_name`, fakeable), beam por groups; error de Visred → rama honesta `NotQuotable`; refina `Vehicle.version` (dominio). Gate en `WhatsAppAdapter::identifyVehicle` (sirve WhatsApp + web-chat): Quotable→crea Quote + token en `risk_provider_refs`; NeedsFact→pide hecho de dominio; NotQuotable→rama honesta. Token/proveedor **nunca** en `ai_state` ni en mensajes (test lo verifica). Migración nueva `risk_provider_refs` (genérica: `risk_snapshot_id`+`provider`+`external_vehicle_ref`, unique); relación `RiskSnapshot::providerRefs()`; **sin** columna de proveedor en el dominio. **Cotización:** `VisredQuotationProvider implements QuotationProvider` lee el token, `cotizar/`→polling acotado (`Sleep`, tolerante a FAILURE parcial y budget agotado), aplana company→covers, `normalized_grade` solo en el adapter; firma `generateAlternatives(RiskSnapshot): array` **intacta**. **Decisiones:** "resolver real siempre" en producción (bind `Quotability→VisredQuotabilityResolver`, independiente del seam de cotización que sigue en `QuotingEngine` — V2-4); en tests `TestCase` bindea `StubQuotability` por defecto, los tests de quotability usan el resolver real con `Http::fake`+`DisambiguationAgent::fake`. `quotation_result_id` por-alternativa **diferido a V2-5** (lo consume la emisión; hoy cada alt lo lleva en `external_quote_id`, `saveResults` intacto). 14 tests nuevos (6 resolver + 3 gate + 5 cotización). Pint ✅ · PHPStan **0** ✅ · suite **302/302**. Env renombradas `MANGO_QUOTATION_PROVIDER`/`MANGO_EMISSION_PROVIDER` → `QUOTATION_PROVIDER`/`EMISSION_PROVIDER`. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-07** — **Visred — V2-2: `VisredClient` (HTTP + JWT) ✅.** Cliente agnóstico de proveedor que espeja el patrón de `WhatsAppOutboundService`. `app/Services/Visred/VisredClient.php`: `get()`/`post()` autenticados, login (`/v1/accounts/token/`) + refresh (`/refresh/`) con tokens cacheados **server-side** (Cache, no en estado de instancia → sirve transient), **refresh-on-401** (refresca y reintenta una vez; si el refresh falla, re-login con credenciales de servicio), `Authorization: Bearer` + `Accept: application/json`, retry transitorio (red/5xx/429, **no** 4xx), `X-Mock-Scenario` **solo** si `config('visred.sandbox')`. `app/Exceptions/Visred/VisredApiException.php`: normaliza el envelope `{success:false,error:{message,code,field_errors}}` a excepción tipada (`status`/`code`/`field_errors`) — capa anticorrupción, el dominio nunca la ve (ADR-001); falla de red → `503`/`external_service_unavailable`. `config/visred.php` espeja `config/whatsapp.php` (`quotation_provider`/`emission_provider` default `mock`, `base_url`, creds, `timeout`, `poll_budget`/`poll_interval`, `sandbox`); claves `VISRED_*`/`MANGO_*` en `.env.example` (placeholders). 14 tests herméticos `Http::fake` + fixtures `tests/Fixtures/Visred/` (login, refresh, 401→refresh→reintento, re-login, 400/401/403/404/409/503, field_errors, gating `X-Mock-Scenario`, falla de red) + **smoke-test REAL** vía `tinker` contra el sandbox (login + refresh OK, sin imprimir secretos). Pint ✅ · PHPStan **0** ✅ · suite **288/288** (882 assertions). El seam de selección sigue en `QuotingEngine` (mock) — el flip es V2-4. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-07** — **Visred — auth real verificada + docs de plan dedicados.** Smoke-test `POST /v1/accounts/token/` contra el sandbox → `200` con `access`+`refresh` (base URL corregido a `https://sandbox-api.visred.com.ar`; el host `sandbox.visred.com` no resolvía). Credenciales en `.env` (no commiteadas), egress OK. Creados `docs/v2/visred-integration/PLAN.md` (copia canónica del plan en el repo) + `docs/v2/visred-integration/ROADMAP.md` (tracking específico del plan). V2-2 desbloqueada. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-06** — **Visred — Fase 1: puerto `QuotationProvider` extraído** (refactor sin cambio de comportamiento). Nuevo `app/Contracts/QuotationProvider.php` (firma neutra `generateAlternatives(RiskSnapshot): array{task_id,status,raw,parsed_alternatives}`); `QuotingEngine implements QuotationProvider`; `ApiQuoteResolution` pasa a depender del puerto (no del concreto); bind `QuotationProvider → QuotingEngine` (mock, default) en `AppServiceProvider` — el switch por config (`mock|visred`) llega en Fase 4. PHPStan 0 ✅ · Pint ✅ · suite **274/274** (845 assertions) verde. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-06** — **Visred — Fase 0 (modelo de dominio, gate) escrita.** `docs/v2/10-modelo-dominio-cotizacion-emision.md` (+ índice #10 en `docs/README.md`): modelo **agnóstico de proveedor** para cotización + emisión (Visred = un Adapter detrás de los puertos `QuotationProvider`/`EmissionProvider`; admite 2º proveedor sin tocar el dominio). Decisiones de la sesión de plan: **límites de contexto** (la compañía vía Visred es el *system of record* de póliza/endosos/estado/documentos — on-demand, última versión; MANGO es *engagement*), **referencia mínima + on-demand** con cache-aside (~2h) para la cartera (sin proyección autoritativa ni store de endosos), **`Risk` = identidad + estado actual sin versionar** (la historia comercial vive en la cadena inmutable de `risk_snapshots`), y propuesta de **repurposear `polizas`** a tabla de referencia (coordinar con mango-mobile). Credenciales sandbox **disponibles** → E2E real en scope. Implementación (Fases 1+: `VisredClient`, adapters, config seam, emisión) **supeditada a la aprobación de este doc**. **En revisión.** _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-05** — **V2-7 — Envío real de WhatsApp verificado + racional del seam documentado.** Con `WHATSAPP_DISPATCH_DRIVER=cloud` (estaba sin setear → caía al default `log`, no-op) se confirmó el flujo completo end-to-end: app "Estoy bien, pero pasó algo" (Estado 1) → `EmergencyController` → `CloudApiWhatsAppDispatcher` → encola `SendWhatsAppTemplate` → worker → `WhatsAppOutboundService::sendTemplate()` → Meta → webhooks de status `sent` y `delivered`. Confirma template `mango_emergencia_estoy_bien` aprobado en Meta, worker `whatsapp-outbound` corriendo y credenciales válidas. La env var vive en `.env` local (no se commitea): para prod, MANGO debe setearla en el deploy. **Documentado el racional contundente** de por qué el seam `log`/`cloud` aplica solo a los avisos por **template** y NO a las respuestas conversacionales (`sendMessage`): son dos superficies de API distintas con reglas de consentimiento opuestas (ventana de 24h de Meta); el kill-switch va en la superficie que puede alcanzar a un destinatario **no-consentido** (solo los templates pueden) — `docs/v2/06-adapters.md` nueva sección + resumen por canal + estado actualizado en `docs/v1/deuda-whatsapp-dispatch.md`. Un futuro corte global "no hablar con Meta" iría en `WhatsAppOutboundService` (transporte), ortogonal a este seam. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-05** — **Fix — PAS por default no resolvía (mismatch de env key).** La directiva "el PAS de último recurso soy yo (Andrés)" no se cumplía: `config/mango.php` leía `MANGO_DEFAULT_PAS_EMAIL` (inexistente en `.env`) mientras el `.env` define `MANGO_DEV_PAS_EMAIL`. Resultado: `SiniestroController::defaultPas()` devolvía `null` y el aviso de siniestro caía al `422 NO_PAS_ASSIGNED` en vez del Tier 3. Fix de una línea: `default_pas_email` ahora cae a `env('MANGO_DEV_PAS_EMAIL')` cuando no está la clave específica. Verificado end-to-end (resuelve a `andresrom@gmail.com` → User PAS real `Andrés / +5493516280778`). El path defensivo `422` se conserva intacto (se ejercita con `config(['mango.default_pas_email' => null])` en `SiniestroTest`). `SiniestroTest` 6/6 ✅. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-05** — **V2-7 — Dispatch de WhatsApp cableado.** Puerto `WhatsAppDispatcher` (`app/Contracts/`) con seam por config `whatsapp.dispatch_driver`: `CloudApiWhatsAppDispatcher` (encola `SendWhatsAppTemplate` en `whatsapp-outbound`) y `LogWhatsAppDispatcher` (no-op, default local/testing). Nuevo `WhatsAppOutboundService::sendTemplate()` (reusa `post()` con retry + manejo 131048). `config/whatsapp.php` con nombres/idioma de los 3 templates por env (placeholders hasta aprobación Meta). Los 3 TODO reemplazados: siniestro→PAS (nombre+contacto del cliente), emergencia Estado 1 (link Google Maps estático), Estado 2 (`tracking_url` público, **nunca** el `update_secret`). Contratos mobile sin cambios. 7 tests nuevos en `EmergencyDispatchTest` + suite emergencia/siniestro verde (33 ✅). Pint ✅ · PHPStan 0 ✅. **Go-live pendiente de MANGO:** templates aprobados en Meta + `WHATSAPP_DISPATCH_DRIVER=cloud`. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-02** — Reorganización de documentación: creado `docs/v2/` canónico (8 docs: schema, endpoints, services, repos, controllers, adapters, consolidado v1, Visred adapter) + índice `docs/README.md`. `docs/v1/` pasa a archivo histórico. Creado este ROADMAP. _(commit `d9fbe8e`)_
- **2026-06-02** — Rama `cirugia-v2` creada desde `main` (será la futura `main`). **Extirpación de `pas_mobile`** (V2-6 parcial): eliminados `MobileAppQuoteResolution`, `QuoteOfferedToPas`, `FallbackToApiListener`, `QuoteWebhookController` + ruta, `MobileSyncLog`, `OpportunityTestDataSeeder`; nueva migración dropea tabla `mobile_sync_logs` y columnas `mobile_*`/`sent_to_mobile_at`/`expected_resolution_at` de `quotes`; limpiados config/services `mobile_app`, settings seeder, `SettingsController`, vistas (badges PAS, card endpoint). `QuoteService` colapsado a estrategia única `api`; adapters llaman `resolveQuote()` sin estrategia. Verificado n8n = stub muerto (puente real = `WhatsAppWebhookController`). Pint ✅ · PHPStan 0 ✅ · suite idéntica a baseline (24 fallos pre-existentes ajenos). _(commit `1d45c7c`; docs reorg en `d9fbe8e`)_
- **2026-06-02** — Limpieza residual: `STYLE_GUIDE.md` y skill `frontend-style-guide` → v2.3, removidos los badges `offered_pas`/`rejected_pas` del snippet `statusClass`.
- **2026-06-02** — Borrado stub `app/Adapters/n8n-whatsapp/` (dead, lanzaba excepción) + todas sus referencias en `CLAUDE.md`, `README.md` (×2, reescritas a Cloud API nativa) y `docs/v2/06-adapters.md` (×2). **V2-6 completa.**
- **2026-06-03** — **Barrido de residuo legacy en jobs/adapters** (auditoría jobs/queues/controllers/services). Borrados 2 jobs muertos del viejo flujo async de proveedores PAS: `RequestQuotesFromProviders` (huérfano, no se despachaba; su rol —resolución async— hoy es sincrónico vía `tryResolveQuoteById → resolveQuote`) y `RecieveQuotesFromProviders` (stub vacío, andamio del receipt `offered_pas`). Reescrito copy stale "Oferta enviada a los productores" → "Cotización procesada; preparando las alternativas…" en ambos adapters (`AgentToolAdapter`, `WhatsAppAdapter`). Corregidos docblocks "resolución Mobile" → "vía API" en los 2 adapters + `AIProviderAdapterInterface`. README: la sección "Cotización Asíncrona / `RequestQuotesFromProviders`" reescrita a `ApiQuoteResolution`/`NotifyClientQuoteReady`. Auditado: **cero acoplamiento import/runtime restante** con `pas_mobile`/`pas-web` (lo que matchea —`Mobile/`, `auth:mobile`, `mobile_account_id`, "PAS"/productor, `external_quote_id`— es mango-mobile vigente + dominio de negocio + ADR-001, no legacy). PHPStan 0 ✅. _(commit `405126b` — barrido junto al feat de tracking)_
- **2026-06-03** — **Triage de suite**: confirmado que los 24 tests rojos son **pre-existentes y ajenos** a la limpieza legacy (verificado vía `git stash` — `IdentifyVehicleTest` da idéntico 9F/7P con y sin los cambios; total se mantiene en 24 = baseline). Causa raíz: tests de integración pegan contra AI/HTTP real sin credenciales en entorno de test. Promovidos a **deuda activa "en arreglo"** (ver tabla) — se atacan en sesión aparte fakeando el borde AI/WhatsApp. _(commit `405126b`)_
- **2026-06-03** — **Retiro del residuo final del flujo PAS opportunities** (cierre de V2-6). Eliminado el job `CheckQuoteAcceptance` (`app/Jobs/`) — watchdog cuyo propósito original era forzar el fallback a API cuando ningún PAS aceptaba la oferta dentro del timeout (`offered_pas` → `rejected_pas`); tras extirpar esos estados quedó manejando solo `pending`, sin razón de ser en el flujo WhatsApp-only. Removido su dispatch en `QuoteService::createPendingQuote` y el setting `pas.opportunity_timeout_minutes` (seeder + `SettingsController` + stat card en `Settings/Index.vue`); nueva migración `drop_pas_opportunity_timeout_setting` borra el row de `system_settings`. La resolución sincrónica (`coveragePreference → tryResolveQuoteById → resolveQuote`) y `ApiQuoteResolution` se conservan intactas. Efecto: una quote que nunca alcanza el paso de cobertura queda en `pending` (no se auto-resuelve) — comportamiento **aceptado y deliberado** en el modelo WhatsApp: si el cliente no elige cobertura no hay nada que cotizar, y la resolución real ocurre vía la estrategia `api`. El ciclo de vida de estados de la quote (incluido el destino de las que quedan en `pending`) se va a **rediseñar junto con el polling de `TaskList` de Visred** (ver V2-3) — ese será el momento de definir si `pending` necesita un estado terminal o expiración. Pint ✅ · PHPStan 0 ✅ · tests Quote verdes. _(commit `405126b`)_
- **2026-06-03** — **Suite a verde: 24 → 0 fallos (264/264, 809 assertions).** Los "24 rojos pre-existentes" no eran un único problema de "AI sin mock" como se creía en el triage del 2026-06-03; al diagnosticarlos uno por uno resultaron **5 causas raíz distintas**, ninguna de lógica de negocio:
  1. **`ProcessConversationInbox` `TypeError` (4 tests).** Los mocks de `InsuranceOrchestrator::handle()` devolvían `['text', 'agent']` sin la clave `execution_log_ids`; el job hace `end($reply['execution_log_ids'])` ([ProcessConversationInbox.php:71](app/Jobs/ProcessConversationInbox.php:71)) → `null` → TypeError. **No se agregó defensa `?? []`**: el orquestador real **siempre** retorna la clave (contrato `array{text, agent, execution_log_ids: int[]}`; ambos `return` la incluyen y el path de error hace `throw`), así que era mock incompleto, no bug de prod. Fix = completar los 4 mocks.
  2. **`SendWhatsAppMessage` Mockery mismatch (3 tests).** Prod pasa `config('ai.default')` (`'deepseek'`) como último arg a `sendMessage`/`sendAudioMessage`; las expectativas `->with(...)` tenían un arg de menos. Fix = agregar `config('ai.default')`.
  3. **`IdentifyVehicle` 500 (varios).** El test mandaba `sessionUuid => 'test-session-uuid-vehicle'`, no es UUID válido y `quotes.session_uuid` es columna `uuid` de Postgres → `SQLSTATE[22P02]` → `server_error`/500. Fix = `Str::uuid()` en el `beforeEach`.
  4. **`ConversationShow` orden no-determinista (1 test).** `created_at` no está en `$fillable` de `Message`, el `create([... 'created_at' => ...])` del test se ignora y ambos mensajes quedan con el mismo timestamp; `orderBy('created_at')` sin desempate devolvía orden arbitrario. Fix de prod = `->orderBy('id')` como desempate determinista en `ConversationController::show`.
  5. **`CheckoutAgentPrompt` — archivo de prod destruido (ver abajo).**
  Cambios de prod: **2** — el desempate `orderBy('id')` y la **recuperación de `CheckoutAgent.md`**. El resto, fixes de test. Pint ✅ · PHPStan 0 ✅. _(commit `405126b`)_
- **2026-06-03** — **🔴 Bug de fondo descubierto: `AgentPromptController` escribe prompts `.md` versionados desde un request HTTP, y eso destruyó `CheckoutAgent.md` en el repo.**
  - **Síntoma.** `tests/Feature/AI/CheckoutAgentPromptTest` fallaba afirmando que el prompt no contenía `check_coverage_rule`, `Fallback contrastado`, etc. El archivo en disco era el string `"promoted draft content sufficiently long to pass validation"` (1 línea), y `git show HEAD:...CheckoutAgent.md` confirmó que **el stub estaba commiteado** (entró en `b2e70e9`). El prompt real (203 líneas) se recuperó de `1504224` y se verificó contra todas las assertions.
  - **Causa raíz.** [`AgentPromptController::promoteDraft()`](app/Http/Controllers/Admin/AgentPromptController.php:153) y `::store()` llaman a `writePromptFile()` ([:324](app/Http/Controllers/Admin/AgentPromptController.php:324)), que hace `file_put_contents(resource_path('prompts/agents/{File}.md'), $content)` — una escritura cruda a un archivo **bajo control de versiones** disparada por una acción HTTP autenticada. `AgentPromptDraftFlowTest` ("promotes a draft to active") ejercita el controller real promoviendo un draft de `checkout_closer` con contenido stub → pisa `CheckoutAgent.md`. Como no había restore, el stub quedó en el working tree y se commiteó. Por eso el test estuvo rojo "desde siempre".
  - **Por qué es un problema más allá del test (4 puntos):**
    1. **Duplicación de fuente de verdad.** Según `CLAUDE.md`, el runtime sirve los prompts desde la tabla `agent_prompts` (`AgentPrompt::activeFor($key)`, cacheado con `rememberForever`); el `.md` es **solo fallback/seed**. Escribir el `.md` en cada promote es un side-effect de "sync al repo" que no es atómico con el write a la DB.
    2. **Mutación de artefactos en runtime.** En producción, una acción de un admin reescribe un archivo del *deploy* — no sobrevive a un redeploy, y genera drift entre entornos y entre disco↔git.
    3. **Sin seam fakeable.** `file_put_contents` a un `resource_path` absoluto no lo intercepta `Storage::fake()`; el único blindaje posible desde el test es backup/restore manual (lo que se aplicó como mitigación interina).
    4. **Sin atomicidad ni lock.** Un write fallido deja el prompt a medio escribir; no hay bloqueo ante promotes concurrentes.
  - **Mitigación aplicada (interina).** `AgentPromptDraftFlowTest` ahora respalda el `.md` real en `beforeEach` y lo restaura en `afterEach`, para que el stub de los tests no vuelva a filtrarse al repo. Cierra la hemorragia pero **no** arregla la causa.
  - **Propuesta de fondo — dos opciones:**
    - **Solución A (mínima, recomendada): eliminar la escritura del archivo.** La DB ya es la fuente de verdad y está cacheada; el `.md` es fallback documentado. `promoteDraft`/`store` no deberían escribir el archivo: se borra `writePromptFile()`. La sincronización repo→`.md` (para devs / versionado) pasa a ser un comando artisan explícito y opt-in (p.ej. `prompts:export`, reutilizando el snippet de sync ya documentado en `CLAUDE.md`), corrido a propósito, no como efecto colateral de un HTTP. *Pros:* elimina el vector de pérdida de datos, no muta archivos trackeados en runtime, sin drift entre entornos, tests sin necesidad de blindaje. *Contras:* el `.md` del repo deja de auto-actualizarse cuando un admin promueve en prod (aceptable: ya estaba derivando y la DB es canónica).
    - **Solución B (si se quiere conservar el espejo en archivo): escribir vía `Storage` a un disco no-versionado y fakeable.** Definir un disco `prompts` (p.ej. `storage/app/prompts`, o R2 como el resto de media) y reemplazar `file_put_contents(resource_path(...))` por `Storage::disk('prompts')->put(...)`; la lectura cae a ese disco y luego al seed del repo. *Pros:* mantiene el "persistir una copia"; `Storage::fake('prompts')` hace los tests herméticos; nunca toca archivos bajo git; vive en una ubicación realmente escribible. *Contras:* más piezas; el seed `.md` y las copias vivas pueden divergir (pero esa divergencia ya es inocua y fuera de git).
  - **Recomendación:** **Solución A** — alinea con el diseño documentado ("DB = fuente de verdad, `.md` = fallback") y remueve el modo de falla de raíz con el menor código. Reservar B solo si producto quiere que el panel admin empuje cambios de prompt de vuelta a un artefacto desplegable. _(descubrimiento; recuperación de `CheckoutAgent.md` en commit `405126b`)_
- **2026-06-03** — **Solución A aplicada: eliminado el clobber de prompts `.md` desde HTTP.** Borrado `AgentPromptController::writePromptFile()` y sus dos llamadas (`promoteDraft` [:153], `store` [:197]). `promoteDraft`/`store` ya **no** escriben ningún archivo: la DB (`agent_prompts`, cacheada con `rememberForever`) es la única fuente de verdad en runtime y el `.md` queda como seed/fallback (lo sigue cargando `createDraft` cuando no hay versión activa, vía `resolvePromptPath`). Removida la mitigación interina del test (`beforeEach` backup + `afterEach` restore del `.md`) — ya no hay escritura a disco que blindar — y agregado un **header de HISTORIA** en `AgentPromptDraftFlowTest` que narra el incidente (clobber → stub commiteado en `b2e70e9` → recuperado de `1504224`) para que nadie reintroduzca escrituras a `resource_path()` desde un controller. La dirección `.md → DB` sigue disponible vía el comando existente `agent:sync-prompts`; **no** se agregó `prompts:export` (DB→`.md`) — opt-in para más adelante si se quiere versionar cambios hechos desde el panel. `AgentPromptDraftFlowTest` (15) + `CheckoutAgentPromptTest` (7) = 22 verdes. _(commit `826b482`)_
- **2026-06-02** — **Extirpación de `pas-web`.** Auditoría exhaustiva (`pas-web`, `pas_web`, `PasWeb`, `PAS_WEB`, `external_quote_id`, `quote_provider_refs`, `poliza_api`): **cero huella en código, config y `.env`** de `workflow-assistant` — ya estaba desacoplado de facto (la estrategia `api` usa `QuotingEngine` mock, no pas-web; `quote_provider_refs`/`poliza_api` son genéricos de proveedor). Todas las menciones vivían en docs: actualizadas las convenciones de canal y narrativas en `docs/README.md` y `docs/v2/01`–`07` a `legacy, extirpado`, para `pas-web` **y** `pas_mobile` (criterio unificado: ambos permanecen documentados, marcados legacy + extirpado). `docs/v1/adr/001-quote-provider-refs.md` se deja **intacta** (archivo histórico; el bug ocurrió con pas-web y el invariante sigue vigente para Visred). _(commit `48e2b90`)_
- **2026-06-05** — **Auditoría de arquitectura de queues.** Mapeado job→conexión→cola→worker de los 12 jobs. Tres hallazgos:
  - **🔴 H1 (resuelto).** El worker de IA corría `queue:listen --queue=whatsapp-ai,whatsapp-outbound,default --timeout=180` **sin argumento de conexión** → usaba la default `database` (retry_after=90), anulando la conexión `database_ai` (retry_after=200) que existe justamente para tolerar LLM lento. Como ambas conexiones comparten la tabla `jobs` y el reclaim usa el `retry_after` de la conexión *que consume*, todo job de `whatsapp-ai` que tardara 90–180s era reclamado mientras corría → **doble llamada al LLM + doble respuesta WhatsApp**. `WithoutOverlapping` no protege (serializa el despacho, no el reclaim del driver). Fix en `run.md`: worker de IA ahora `queue:listen database_ai --queue=whatsapp-ai --timeout=180`, separado del liviano (`whatsapp-outbound,default`) y del de media. Tres procesos, uno por conexión (un worker bindea una sola conexión).
  - **⏸️ H2 (latente, statu quo).** Cola `semantic-analysis` sin consumidor. Es Tier 2 de QA: `AnalyzeConversationSemanticsJob` corre DESPUÉS de responder, escribe flags en `conversations.semantic_analysis` que solo lee el panel admin — **no** afecta el chat en vivo (el orquestador no las lee). Hoy apagado por flag → no acumula. Decisión: dejar apagado durante cirugía v2; si se quiere QA, cablear al worker `database_ai`; si se quiere impacto en vivo, es rediseño (que el orquestador reaccione a `semantic_loop`/`context_loss`).
  - **✅ H3 (resuelto, opción A).** `ExtractCoverageDocumentText` (timeout=300) salió de la cola `default` (retry_after=90). Nueva conexión `database_long` en `config/queue.php` (retry_after=360 > timeout=300, cola `documents`); el job ahora `onConnection('database_long')`; worker dedicado en `run.md` (`queue:listen database_long --tries=2 --timeout=300`), ocioso casi siempre (solo corre al actualizar un manual de producto, admin-triggered). Lo saca del hot-path de WhatsApp. NO corrompía pgvector (chunking borra antes de insertar; [ChunkAndEmbedService.php:47](app/Services/ChunkAndEmbedService.php)).
  - **⬜ H5 (documentado).** Prod: `run.md` es cheatsheet de dev con `queue:listen` lanzado a mano. En prod usar `queue:work` bajo supervisor que reinicie procesos caídos (ver tabla de deuda). El modo de falla "worker muere → cola frena en silencio" es real (pasó con `semantic-analysis`). 4 conexiones a supervisar.

  Acoplamiento de los jobs de análisis (referencia): `AnalyzeConversationHealthJob` (Tier 1, **sin IA** — heurísticas: loops por hash de mensajes, stuck≥5 turns, tool_errors, abandoned 24h, long≥20 msgs) corre en cola `default` y SÍ se consume; escribe `conversations.flags`. `AnalyzeConversationSemanticsJob` (Tier 2, **con IA**) es el huérfano de H2. Ambos se despachan desde `ProcessConversationInbox` tras responder y solo alimentan el panel admin. _(commit `3c20d26`)_
- **2026-06-05** — **Doc de arquitectura de queues** creado: `docs/v2/09-queues-architecture.md` (modelo conexión/cola/worker, invariante `retry_after > timeout`, incidente H1, tabla de conexiones, inventario de 12 jobs, topología de 4 workers, pipeline de inbox de 3 etapas, Tier 1/2 de observabilidad, deuda H2/H5). Agregado al índice canónico en `docs/README.md` (#09). _(commit `3c20d26`)_
