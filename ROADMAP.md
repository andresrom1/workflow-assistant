# workflow-assistant — Roadmap de implementación

> **Fuente de verdad de "dónde estamos"** en el backend. Trackea ejecución, no decisiones de producto.
> El detalle de arquitectura vive en [`docs/v2/`](docs/README.md). La app cliente tiene su propio roadmap en `mango-mobile/ROADMAP.md`.
>
> **Protocolo de mantenimiento (Claude):** cada vez que hagamos un cambio, actualizar este archivo en el mismo turno —
> mover el estado de la fase, completar fecha + commit/PR, y agregar una línea a la **Bitácora**. Si un cambio
> abre o salda deuda, reflejarlo en la tabla de deuda.

**Leyenda:** ⬜ Sin empezar · 🚧 En progreso · ✅ Completa · ⏸️ Bloqueada · ❌ Descartada

---

## Estado actual (snapshot)

### Base ya construida (v1 → operativa, mayormente con mocks)

| Área | Estado | Nota |
|------|--------|------|
| Flujo de cotización (chat web ChatKit) | ✅ | identify customer/vehicle → coverage → quote → checkout. Adapter `OpenAI\AgentToolAdapter`. |
| Flujo de cotización (WhatsApp) | ✅ | Orquestador + sub-agentes, pipeline de inbox de 3 etapas. Adapter `AIProviders\WhatsAppAdapter`. |
| Integración WhatsApp Cloud API | ✅ | Webhooks in, `WhatsAppOutboundService`, media STT/TTS, idempotencia por `wamid`. |
| Motor de cotización | 🚧 | `QuotingEngine` **es mock** (`sleep(30)` + catálogo simulado). Seam para Visred. |
| RAG coberturas (pgvector) | ✅ | Dos agentes (frontal + experto), `ChunkAndEmbedService`, `SearchCompanyDocumentationTool`. |
| Checkout web + fotos R2 | ✅ | Token opaco, 7 fotos inspección, tarjeta cifrada, auditoría admin. |
| API Mobile (mango-mobile) | ✅ | auth, pólizas, siniestro, emergencia, shared-risks. Guard `auth:mobile`. |
| Panel admin / observabilidad | ✅ | Conversaciones, prompts versionados, analytics funnel, Studio, settings, usuarios. |
| Aislamiento de datos de proveedor | ✅ | ADR-001 — `quote_provider_refs`. Vigente. |
| Documentación v2 | ✅ | `docs/v2/` canónico + índice `docs/README.md` (2026-06-02). |

---

## Cirugía v2 — forward roadmap

> Objetivo: reemplazar el rol de `pas_mobile`/`pas-web` (cotización manual + fallback) por **Visred**
> (cotiza y emite sin intervención humana), y retirar lo legacy. Diseño base en
> [`docs/v2/08-visred-quote-adapter.md`](docs/v2/08-visred-quote-adapter.md).

| Fase | Tema | Estado | Detalle |
|------|------|--------|---------|
| V2-0 | Reorden + doc de arquitectura | ✅ | docs/v2 + ROADMAP (2026-06-02). |
| V2-M | Modelo de dominio (agnóstico) — **gate** | ✅ | `docs/v2/10` (2026-06-06). Límites de contexto (compañía = system of record), puertos `QuotationProvider`/`EmissionProvider`, referencia mínima + on-demand, `Risk` sin versionar, repurpose de `polizas`. **Aprobado por el usuario** (2026-06-06). |
| V2-1 | Extraer `QuotationProvider` | ✅ | `app/Contracts/QuotationProvider.php`; `QuotingEngine implements QuotationProvider`; `ApiQuoteResolution` depende del puerto; bind `→ QuotingEngine` (mock, default) en `AppServiceProvider`. Sin cambio de comportamiento. PHPStan 0, suite 274/274 (2026-06-06). |
| V2-2 | `VisredClient` (HTTP + JWT) | ✅ | `app/Services/Visred/VisredClient.php` + `app/Exceptions/Visred/VisredApiException.php` + `config/visred.php`. login/refresh cacheado (Cache server-side), refresh-on-401 (1 reintento → re-login si falla), Bearer+Accept, retry transitorio, `X-Mock-Scenario` solo sandbox, envelope de error normalizado. 14 tests `Http::fake` + smoke-test live (login+refresh). PHPStan 0 · suite 288/288 (2026-06-07). Detalle en `docs/v2/visred-integration/`. |
| V2-3 | `VisredQuotationProvider` + gate de quotability | ✅ | **Cotización:** `VisredQuotationProvider` lee el token de `risk_provider_refs`, `cotizar/`→polling acotado (tolerante a parcial/budget), aplana company→covers→`parsed_alternatives`, `cover`→`normalized_grade` (solo en el adapter). Firma `generateAlternatives` intacta. **Gate (resolver real):** puerto agnóstico `Quotability` + `VisredQuotabilityResolver` (árbol de 5 GETs + desambiguación Tier 1 léxico / Tier 2 `DisambiguationAgent` LLM) corre en identify-vehicle; tri-estado `QuotabilityResult`; refina `Vehicle.version` + token → tabla genérica `risk_provider_refs`. NLU intocable. **`quotation_result_id` por-alternativa diferido a V2-5.** PHPStan 0 · suite **302/302** (2026-06-07). Detalle en `docs/v2/visred-integration/`. |
| V2-4 | Config seam (`QUOTATION_PROVIDER`) | ⬜ **próxima** | Bind condicional `mock`/`visred` de `QuotationProvider` en `AppServiceProvider` (lee `config('visred.quotation_provider')`, espeja `WhatsAppDispatcher`). Flip por env + E2E real contra sandbox. El gate de quotability **ya** corre real. |
| V2-5 | Emisión Visred | ⬜ | `PolizaEmisionService` (hoy skeleton) → emisión real (`emitir/` → polling → `presale_id`/`policy_number`) + descarga de documento. |
| V2-6 | Retiro de legacy PAS | ✅ | **pas_mobile extirpado** (rama `prepare-proyect-for-API-refactor`): `MobileAppQuoteResolution`, `QuoteOfferedToPas`, `FallbackToApiListener`, `QuoteWebhookController` + ruta `/webhooks/quote-update`, `MobileSyncLog` + columnas `mobile_*` de `quotes`, settings/config `mobile_app.*`, badges `offered_pas`/`rejected_pas`, y stub `n8n-whatsapp/`. `QuoteService` colapsado a estrategia única (`api`). Puente WhatsApp real = `WhatsAppWebhookController` (Cloud API nativa, sin n8n). **pas-web también extirpado:** auditado cero huella en código/config/`.env` de `workflow-assistant` (ya estaba desacoplado de facto); queda solo en docs marcado `legacy, extirpado`. ADR-001 (docs/v1, histórico) se conserva intacta — el invariante `quote_provider_refs` sigue vigente para Visred. **Residuo final retirado (2026-06-03):** job `CheckQuoteAcceptance` (watchdog de timeout que originalmente esperaba la aceptación del PAS) + setting `pas.opportunity_timeout_minutes`. La resolución sincrónica vía `coveragePreference → resolveQuote` se conserva. |
| V2-7 | Deuda WhatsApp dispatch | ✅ | **Código completo** (2026-06-05). Puerto `WhatsAppDispatcher` (seam por `whatsapp.dispatch_driver`: `cloud` vía Job `SendWhatsAppTemplate` / `log` no-op). `WhatsAppOutboundService::sendTemplate()`. Los 3 TODO cableados (siniestro al PAS, emergencia Estado 1 con Google Maps, Estado 2 con `tracking_url` — nunca el `update_secret`). Tests en `EmergencyDispatchTest`. **Go-live pendiente de MANGO:** aprobar los 3 templates en Meta + setear nombres reales y `WHATSAPP_DISPATCH_DRIVER=cloud` en `.env` (hoy default `log`). |

---

## Deuda técnica abierta

| Deuda | Ubicación | Estado |
|-------|-----------|--------|
| Dispatch WhatsApp — Siniestro → PAS | `Mobile/SiniestroController.php` | ✅ cableado (V2-7). Pendiente template Meta + `driver=cloud`. |
| Dispatch WhatsApp — Emergencia Estado 1 | `Mobile/EmergencyController.php` | ✅ cableado (V2-7). Pendiente template Meta + `driver=cloud`. |
| Dispatch WhatsApp — Emergencia Estado 2 | `Mobile/EmergencyController.php` | ✅ cableado (V2-7). Pendiente template Meta + `driver=cloud`. |
| `QuotingEngine` mockeado (`sleep(30)`) | `Services/QuotingEngine.php` | 🚧 se resuelve en V2-1→V2-4 |
| `PolizaEmisionService` skeleton | `Services/PolizaEmisionService.php` | ⬜ se resuelve en V2-5 |
| **Gestión manual de documentos de póliza (admin panel)** | `PolicyDocumentController` + `PolicyDocuments/{Index,Show}.vue` | ✅ resuelto 2026-06-15 — buscador de pólizas → gestor de documentos (subir PDF/imagen `source=admin_upload`, togglear `visible_to_client`, eliminar con borrado en R2). Ver Bitácora |
| **CRUD de clientes y pólizas (admin panel)** | `ConversationController` (ex-`CustomerController`) + `PolizaController` + `PolizaService` + `Conversations/{Create,Edit}.vue` + `Polizas/{Index,Create,Edit,PolizaFields}.vue` | ✅ resuelto 2026-06-15 — alta/edición/baja de clientes (soft-delete bloqueado si hay póliza vigente) y de pólizas (sección propia "Pólizas" + atajo desde el cliente; Risk reusar-o-crear; constraint "una vigente por Risk" en `PolizaService`). Renombrada la vista a "Conversaciones" el 2026-06-16. Ver Bitácora |
| Credenciales sandbox Visred | — externo | ✅ resueltas (2026-06-07) — login 200 contra `https://sandbox-api.visred.com.ar`; viven en `.env` (no commiteadas) |
| 24 tests rojos pre-existentes | `ProcessConversationInboxTest`, `ProcessMediaAttachmentTest`, `IdentifyVehicleTest`, `AI/CheckoutAgentPromptTest`, `Admin/ConversationShowTest`, `Jobs/SendWhatsAppMessageTest`, `SendWhatsAppMessageTest` | ✅ resuelto 2026-06-03 — suite 264/264 (ver Bitácora). Causa real ≠ "AI sin mock": eran mocks incompletos + dato UUID inválido + test pollution + orden no-determinista |
| **Escritura de prompts `.md` desde request HTTP** (clobber de archivos versionados) | `AgentPromptController::writePromptFile()` (`promoteDraft`/`store`) | ✅ resuelto 2026-06-03 — **Solución A**: eliminado `writePromptFile()` y sus 2 llamadas; la DB (`agent_prompts`, cacheada) queda como única fuente de verdad, el `.md` es seed/fallback. Test sin blindaje (ya no escribe disco); historia documentada en el header de `AgentPromptDraftFlowTest`. Ver Bitácora 2026-06-03 |
| **Queues — worker de IA reclamaba jobs a los 90s** (doble llamada al LLM / doble respuesta) | `run.md` (worker), `config/queue.php` (`database_ai`) | ✅ resuelto 2026-06-05 — el worker corría `queue:listen` sin conexión → usaba `database` (retry_after=90) con `--timeout=180`, anulando la conexión `database_ai` (retry_after=200) que existe para esto. Fix: worker de IA ahora bindea `database_ai`, separado del liviano. Ver Bitácora |
| **Queue `semantic-analysis` sin consumidor** (Tier 2 QA) | `AnalyzeConversationSemanticsJob`, workers | ⏸️ latente — hoy apagado (`AI_SEMANTIC_ANALYSIS_ENABLED=false`), no acumula. Es auditoría offline (panel admin), NO afecta el chat en vivo. Statu quo decidido: dejar apagado durante cirugía v2. **⚠️ Antes de encenderlo hay que revisar la arquitectura:** (1) cablear la cola `semantic-analysis` a un worker (sin esto los jobs caen en el vacío), (2) confirmar conexión `database_ai`, (3) decidir si el resultado solo alimenta el panel (QA offline) o si se rediseña para impactar el chat en vivo (orquestador reacciona a `semantic_loop`/`context_loss`) |
| **`ExtractCoverageDocumentText` (timeout=300) en cola `default`** (retry_after=90 → reclaim) | `app/Jobs/ExtractCoverageDocumentText.php` | ✅ resuelto 2026-06-05 — **Opción A aplicada**: nueva conexión `database_long` (retry_after=360 > timeout=300) + cola `documents` con worker dedicado (`queue:listen database_long`). El job ahora `onConnection('database_long')`; sacado del hot-path de WhatsApp. No corrompía pgvector (chunking borra antes de insertar) |
| **Prod: `queue:listen` a mano sin supervisor** (Hallazgo 5) | `run.md`, deploy | ⬜ `run.md` es cheatsheet de **dev**. En prod: (1) `queue:work` (no `listen` — `listen` rebootea el framework por job) con `--max-time`/`--max-jobs`, (2) bajo supervisor (supervisord/systemd) que reinicie procesos caídos. Modo de falla actual: un worker muere y su cola se frena en silencio (es lo que pasó con `semantic-analysis`). 4 conexiones/colas a supervisar: `database_ai`(whatsapp-ai), `database`(whatsapp-outbound,default), `database_media`(media), `database_long`(documents) |
| **Quotes en `pending` sin estado terminal** (abandono pre-cobertura) | `QuoteService::createPendingQuote`, enum `status` en `Quote` | ⬜ a implementar — tras borrar `CheckQuoteAcceptance` nada expira los `pending` abandonados (cliente que crea la quote pero nunca elige cobertura). **Regla decidida:** un `pending` expira (`status = 'expired'`) **al día siguiente de su creación a las 00:00hs**. Implementación futura: command programado (`schedule`) que barre `pending` con `created_at < hoy 00:00` → `expired`. Se alinea con el rediseño del ciclo de vida de la quote en V2-3 (polling `TaskList` Visred) |

---

## Bitácora

> Entrada por cada cambio relevante. Formato: `fecha — qué — commit/PR`.

- **2026-06-22** — **WhatsApp identidad — captura de BSUID + teléfono y respuesta por BSUID ✅** (cierra dos ítems del workstream Meta-readiness listado el 2026-06-20: "parser tolerante a `wa_id` ausente" y "envío por `recipient`"). Desde abril 2026 Meta inyecta el **BSUID** (`contact.user_id`) en **todos** los webhooks y el **teléfono** (`contact.wa_id`) pasó a ser **opcional** (falta en usuarios username-only sin ventana de 30d / Contact Book — ver doc Meta §4). **Antes:** el webhook **descartaba** el mensaje si faltaba `wa_id` (`if ($waId && …)`) y el teléfono se tipaba `string` no-nullable por todo el pipeline → se perdía el BSUID. **Fix (mínimo, sin tocar DB):** (1) **`WhatsAppWebhookController`** despacha si hay `messageId` y (teléfono **o** BSUID). (2) **`ProcessWhatsAppMessage`** `?string $waId`; al **crear** la conversación sin teléfono usa el BSUID como `external_conversation_id` (el dual-key `findByExtUserId` ya existía); el BSUID sigue en `ext_user_id`, el teléfono en `messages.sender_phone` (ya nullable). (3) **`ProcessConversationInbox`**/(4)**`ProcessMediaAttachment`** `?string $waId`. (5) **`SendWhatsAppMessage`** lleva `phone` + `bsuid` por separado. (6) **`WhatsAppOutboundService`** — helper `recipientPayload(phone, bsuid)` **sin regex**: si hay teléfono → `to`, si no → `recipient` (BSUID, doc §10) en `sendMessage`/`sendTypingIndicator`/`sendAudioMessage`; `sendTemplate` queda en `to` (evita la prohibición de BSUID en AUTH, §8). (7) **`NotifyClientQuoteReady`** deriva teléfono/BSUID de la conversación. **`InsuranceOrchestrator` sin cambios** (su auto-id por teléfono ya omite el BSUID alfanumérico). El BSUID **no** va en `customers` (identidad = dni/email/phone). Tests nuevos: ingesta solo-BSUID + ambos presentes (`ProcessWhatsAppMessageIngestionTest`), destinatario BSUID en outbound (`ProcessConversationInboxTest`), `to` vs `recipient` por null-check (`WhatsAppOutboundServiceTest`). **Suite completa 428/428** · PHPStan **0** · Pint ✅. _(rama `feat/whatsapp-bsuid-capture`)_
- **2026-06-21** — **Mantenimiento de documentación de póliza — Fase 5: notificación push de documento nuevo ✅.** Al cargar un documento (`PolicyDocumentService::storeAdminUpload`) de una póliza **vigente**, se avisa al asegurado por push. **`PolicyDocumentNotifier`** (agnóstico de canal) resuelve la(s) `MobileAccount` del titular por email (mismo seam que `resolveCustomer`, case-insensitive) y encola un **`PublishDocumentAvailable`** por cuenta; **`DocumentAvailablePublisher`** (espeja `FcmTopicPublisher`) publica data-only (`type=policy_document` + `poliza_id`/`kind`) al **topic por cuenta** `account-{id}` (reusa la infra FCM; sin token por dispositivo). **Best-effort:** la resolución/encolado va en try/catch (un fallo no rompe la carga) y el job traga errores de FCM. No se encola si la póliza no está vigente o el titular no tiene cuenta. **Contraparte Flutter (mango-mobile, pendiente):** suscribirse al topic `account-{id}` al autenticarse + manejar `type=policy_document` (notificación local + refresco del Home), reusando `weather_messaging`/`acp_local_notifier`. Tests `PolicyDocumentControllerTest` (encola para titular con cuenta; no encola si vencida / sin cuenta). **Cierre de contrato:** `Mobile/AuthController` ahora expone `user.id` en `/auth/session` (la app lo necesita para suscribirse al topic `account-{id}`); nota de seguridad documentada (topic adivinable → mitiga que el push es data-only, sin contenido; hardening = token opaco por cuenta si hiciera falta). Doc canónica del contrato backend↔Flutter en **`docs/v3/03-entrega-documentacion-app.md`** (API mobile, push FCM, qué falta en Flutter); índice `docs/README.md` marca 01/02 como diseño histórico descartado y 03 como lo implementado. **Suite completa 423/423** · PHPStan **0** · Pint ✅. **Feature backend completo (Fases 1–5); contraparte Flutter pendiente (doc 03 §4).** _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-21** — **Mantenimiento de documentación de póliza — Fase 4: checklist de completitud ✅.** `PolicyDocumentKind::expectedForActivePolicy()` define el set esperado de una vigente (**Póliza + Cédula de circulación + Certificado** — supuesto de dominio fijo, a confirmar). `PolicyDocumentController::show` expone `checklist` (presente/faltante por kind) → card "Documentación esperada" en `Show.vue` con contador `N/M` y faltantes marcados; `index` expone `doc_presentes`/`doc_esperados` (eager-load `documents:id,poliza_id,kind`) → indicador `N/M esperados` que reemplaza el sub-texto "visibles" (redundante con la regla todo-de-la-vigente). Tests `PolicyDocumentControllerTest` (checklist en show; completitud en index). PHPStan **0** · Pint ✅ · build ✅. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-21** — **Mantenimiento de documentación de póliza — Fase 3: cola de vencimientos ✅.** `PolizaController::vencimientos` lista las pólizas **vigentes** con `vigencia` dentro de los próximos N días (default 30, selector 15/30/60/90) ordenadas por urgencia — derivado de `vigencia`, **sin persistir** nada ni barrido nocturno. Excluye las vencidas y las vigentes **sin** `vigencia` (default conservador: no se inventa fecha). Ruta `polizas.vencimientos`; vista `Polizas/Vencimientos.vue` (badge de urgencia por días restantes — rojo ≤7d, ámbar ≤30d — + CTAs "Renovar"/"Documentos"); acceso desde el header de `Polizas/Index.vue`. Test `PolizaControllerTest` (solo vigentes en ventana; excluye vencida y sin-vigencia). PHPStan **0** · Pint ✅ · build ✅. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-21** — **Mantenimiento de documentación de póliza — Fase 2: alta externa + renovación + organización ✅.** (1) **Renovación:** `PolizaService::renovar($anterior, $nuevaData)` abre una Poliza **nueva** sobre el **mismo Risk** (número nuevo) con `contrato_anterior_ref = $anterior->numero`, marca la anterior **vencida** y la nueva **vigente**, en una transacción — preserva "una vigente por Risk" por construcción; rechaza renovar una no-vigente. `PolizaController::renovarForm`/`renovar` + rutas `polizas.renovar.form`/`polizas.renovar`; vista `Polizas/Renovar.vue` (arrastra compañía/cobertura/suma de la anterior, número/vigencia en blanco) + botón "Renovar" en `Edit.vue` (solo vigentes); `PolizaFields.vue` con flag `showEstado` (oculta el estado en renovación, lo fuerza el servicio). (2) **Alta de pólizas externas:** ya cubierta por el CRUD manual existente (`createManual`, con `vigencia`/`estado`) — sin código nuevo. (3) **Organización vigente/histórica:** badge de estado en `PolicyDocuments/{Index,Show}.vue` (Vigente verde vs Histórica gris). (4) **Seed de vigencia:** `PolicyReferenceService::materialize` setea `vigencia = emisión + 1 año` para que las emitidas aparezcan en la detección (Fase 3). Tests `PolizaControllerTest` (renovarForm, renovación con back-ref + anterior vencida, rechazo no-vigente) + `PolicyReferenceServiceTest` (seed). Suite tocada **52/52**. PHPStan **0** · Pint ✅ · build ✅. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-21** — **Mantenimiento de documentación de póliza — Fase 1: regla "todo lo de la vigente" + back-ref ✅.** Tras una sesión de diseño que **acotó v3** (el modelo de transacciones/fold/extractor se descartó: la reconstrucción de estado a fecha pasada no la consume nadie en MANGO y la historia ya vive en Visred + la compañía + la pila de PDFs; ver plan `necesito-un-plan-de-crispy-liskov.md`), el objetivo quedó en **cargar y mantener la documentación** de la póliza y entregarla a Mango. **Fase 1:** (1) regla de visibilidad **"todo lo de la vigente"** — `Mobile/PolizasController::documentos` entrega **todos** los documentos de la póliza **vigente** (gate por `estado`) y **ninguno** de las vencidas; se retiró el filtro/​toggle `visible_to_client` por documento (la columna queda, default `true`, sin UI). (2) `polizaPayload` expone **`tiene_documentos`** (vía `withCount('documents')`) para que el Home decida si ofrecer la descarga. (3) Migración **`contrato_anterior_ref`** en `polizas` (back-ref de renovación, base de la Fase 2). (4) Admin: `PolicyDocuments/Show.vue` sin toggle de visibilidad; `toggleVisibility` (controller + ruta) eliminado; `store` ya no recibe `visible_to_client` (persiste `true`). **Contraparte Flutter (mango-mobile, pendiente):** acción "Descargar documentación" en `PolicyCard` que consume `/polizas/{id}/documentos`, condicional a `tiene_documentos`. Tests `Mobile/PolizasTest` (todo-de-la-vigente, vencida sin docs, `tiene_documentos`) + `PolicyDocumentControllerTest` (sin toggle) — **24/24**. PHPStan **0** · Pint ✅. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-20** — **v3 — Modelo de mantenimiento de cartera + endosos (diseño, gate) escrito.** Nueva carpeta `docs/v3/` (diseño post-emisión, no implementado) con 2 docs: **`01-modelo-mantenimiento-cartera-endosos.md`** (modelo de dominio `Risk → Poliza (transacción 0) → Endoso`; jerarquía temporal término-cobertura/facturación/plan-de-pago; ejes de estado ortogonales — A: Emitida/Vigente/Vencida **computado** + Anulada/Rescindida por endoso terminal · B: suspensión por falta de pago **parqueada** sin señal de cobranza; **auto-transición se computa, no se persiste**; taxonomía de endosos append-only plegados por `fecha_inicio` retroactiva; frontera invariante/variable) y **`02-extractor-documentos-poliza.md`** (el "punto 6": documento → clasificar→extraer→**confirmar** humano-en-el-loop → hechos de dominio; seed computado vs hecho extraído; un solo extractor para captura-emisión + carga-manual; mapper por compañía; agnóstico de canal). **Validado contra 4 compañías** como casos testigo (San Cristóbal, Triunfo, Río Uruguay, Sancor) — 3 modelos de facturación (refacturación mensual · premio único en cuotas · semestral), 2 duraciones de término (anual/semestral), un endoso modificatorio y uno terminal. **Reconciliación ✅ hecha:** invierte la premisa on-demand de `docs/v2/10` §4/§5 (no existe endpoint de consulta por `policy_number` → cartera manual); correcciones v3 anotadas en `docs/v2/10` §1/§4/§5 y en la memoria `feedback_modelo_agnostico_proveedor`. Índice `docs/README.md` actualizado con sección v3. **Decisiones de diseño §13 cerradas:** cadencia = hecho por póliza (extraído del documento / derivado de períodos, no config por compañía); eje B (suspensión) = diferido explícito con condición de reingreso (señal de cobranza); estado computado = decidido, la migración del código persistido→computado es delta de implementación; back-ref de renovación normalizado a `contrato_anterior_ref`; umbral de extracción (doc 02) = default conservador (siempre confirma el operador). **Refinamiento de modelo (2026-06-21):** se aplanó a **corriente de transacciones uniforme** — `Risk → Poliza (identidad liviana) → Transacción (corriente)`; "endoso" pasa a ser un **`tipo`** de transacción (no entidad aparte); la **Transacción (operación)** es el concepto unificador (emisión/renovación abren Poliza, las demás la modifican); estado = **fold** de la corriente con **dos semánticas** (atributos = última que tocó el campo por `fecha_inicio`; montos = la transacción cuyo período contiene el corte); "vigente" es del contrato, no de la transacción. Motivado por mínima fricción de carga (la refacturación —la transacción más frecuente— se carga como una fila uniforme). **Sin código** (model-first, gate de revisión). _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-20** — **Dedup/merge de Clientes — procedimiento de identificación implementado ✅** (cierra la "implementación pendiente" de la entrada del 2026-06-20 abajo). Tras analizar el flujo de registro completo (4 fuentes — WhatsApp/Checkout/App/Admin — × órdenes temporales; plan `analiza-acabadamente-el-problema-lively-fern.md`), se aplicó: **(A) `reconcile()` solo por claves fuertes** (`dni`/`email`, únicas) — se quitó `phone` del lookup (en el checkout dni+email son obligatorios y ya cubren todo duplicado de la **misma** persona; `phone` no es único → solo podía fusionar **dos personas distintas**, p. ej. número reasignado). **(B) Survivorship por campo** — nuevo `CustomerConsolidationService::mergeFields()`: por campo gana la fuente más confiable (**admin > checkout > chat**), desempata el `at` más reciente, **preserva la provenance ganadora** y audita cambios + descartes; reemplaza el viejo "rellenar huecos + `auditConflicts`". **(C) Árbol create/enrich/merge en el chat** — `CustomerIdentificationService::resolveForConversation()` (agnóstico de canal, lo usan `WhatsAppAdapter` y `AgentToolAdapter`): identificador de **otra** fila → **merge**; identificador que **nadie** posee y la conversación ya tiene tomador → **enriquecer** esa fila (no crear) — elimina el `create+repoint` que dejaba filas huérfanas (la fábrica de duplicados). **(D) `wbid` era un mislabel de DNI** — confirmado con la doc de Meta (Abril 2026): el `wbid`/BSUID real es un identificador **de conversación** (`conversations.ext_user_id`, dual-key ya implementado), **no** del tomador. El tool de cliente ahora ofrece `email|phone|`**`dni`**; arreglado el **crash triple** (`validateIdentifier`/`findCustomer`/`createCustomer` ahora manejan `dni` + `default`); eliminado `CustomerRepository::findByType` (query a columna arbitraria, sin uso). **(E) `MobileAccount::resolveCustomer()` case-insensitive** (`LOWER(email)`) — un casing distinto del OAuth dejaba al titular sin ver sus pólizas. **Tests:** 6 nuevos en `CustomerMergeTest` (reproduce el bug #4, chat-enrich/chat-merge, número reasignado **no** mergea, survivorship admin-protegido + recencia). **Fuera de scope** (workstream Meta-readiness, `docs/v2/12` §6.3, doc nuevo de Meta): cert TLS DigiCert→Meta, parser tolerante a `wa_id` ausente (username-only), `business_username_update`, envío por `recipient`, Portfolio Pacing. PHPStan **0** · Pint ✅ · suite **413/413**. Docs `docs/v2/12` §6–7 actualizado. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-19** — **Fix — pólizas emitidas quedaban SIN documentación + captura diferida de documentos.** Síntoma real (checkout quote 25 → póliza Triunfo 1822203): emisión exitosa pero `policy_documents` vacío y warning `[VisredDocument] documento sin result.url {download-poliza}`. **Causa:** `VisredDocumentService::fetchOne` asumía `POST /v1/documents/` **síncrono** (esperaba `result.url` en el primer POST), pero la compañía genera el PDF **async** — recién emitida la póliza todavía se está generando, así que la URL viene vacía. Hacía **un solo intento, sin polling** → devolvía `null` → `documents` vacío → cero `policy_documents`. Y como el `presale_id` muere en el adapter y no se persistía nada, no había forma de recuperar las descargas después (presale caduco). Solo se intentaba `download-poliza` (certificado/tarjeta de circulación ni figuraban en config). **Fix (Completo):** (1) la captura hace **un intento** por llamada (no poll-ea inline — no bloquea el job de emisión). (2) **Captura diferida:** `capture()` separa listos vs pendientes; `emit()` reporta `pending_documents` (token **opaco** = presale + `kind` faltantes) en el shape neutro; `PolizaEmisionService` persiste una **`poliza_provider_refs`** (misma convención `*_provider_refs` que `quote_alternative_provider_refs` — token aislado, el dominio no lo interpreta) y encola **`CapturePendingPolicyDocuments`**, que re-pide los PDFs **1 vez por minuto durante ~10 min** (delay/backoff `visred.document_retry_*`=60s, 10 tries; se re-encola con delay, no bloquea worker) y borra la ref al terminar o al agotar la ventana. Nuevo método de puerto `EmissionProvider::capturePendingDocuments()`. (3) **Catálogo ampliado** (`visred.document_task_types`): + `download-certificate`→`certificado` (nuevo `PolicyDocumentKind::Certificado`) y + `download-circulation-card`→`circulation-card`. Supera el supuesto "síncrono" de la entrada 2026-06-15. Migración `poliza_provider_refs` + modelo + relación `Poliza::providerRef()`. 9 tests nuevos (intento único/split/captura diferida en `VisredEmissionProviderTest`, encolado en `PolizaEmisionServiceTest`, job en `CapturePendingPolicyDocumentsTest`). PHPStan **0** · Pint ✅ · suite **403/403**. La póliza 1822203 ya emitida queda igual sin recuperación automática (presale caduco) → carga manual admin. Docs: `docs/v2/10` §5. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-20** — **Dedup/merge de Clientes — data-fix aplicado + plan corregido (`docs/v2/12`).** Caso real reportado: checkout #4 (quote 25) emitió la **póliza 1822203** bajo `customer #1` (fila creada por WhatsApp, solo teléfono) mientras la app móvil identifica al titular **por email** (`MobileAccount::resolveCustomer()`, `customers.email` único) → resolvía `customer #4` (fila con email/DNI, `mobile_account` Firebase) → la app no veía la póliza. El log lo delató: `CustomerConsolidation: clave única en conflicto` para `dni`/`email` (owned_by #4, intentado sobre #1). **Data-fix:** merge manual **#1 → #4** (sobrevive #4: enriquecido con el domicilio/nacimiento/sexo/cond.fiscal/`field_sources` del checkout que habían caído en #1; repuntadas FKs `conversations`/`customer_audits`/`risk_snapshots`/`risks`/`vehicles`; #1 borrado). Verificado: la 1822203 quedó bajo #4 y `resolveCustomer()` la levanta. **Corrección de diseño (supera la entrada del 2026-06-19 abajo):** `reconcile()` debe reconciliar **solo por claves fuertes (`dni`/`email`, únicas), NO por teléfono** — `phone` no es único (compartido en familia) y `findByPhone()->first()` puede fusionar **dos personas distintas**. Con eso el "conflicto de identidad fuerte" prácticamente desaparece y el guard queda como red de seguridad mínima ante email reciclado/dato sucio. Doc autocontenido nuevo **`docs/v2/12-deduplicacion-merge-clientes.md`** (causa raíz, data-fix, `Merge` vs `Consolidation`, decisión claves-fuertes, plan pendiente: sacar teléfono de `reconcile`, source `'merge'` en consolidation, fix `UnhandledMatchError` en `createCustomer` para `wbid`/`dni`, merge cross-key en `WhatsAppAdapter::identifyCustomer`, tests). **Implementación pendiente** (se retoma en ventana nueva). _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-19** — **`CustomerMergeService` — fusión de identidades duplicadas en convergencia ✅.** Causa raíz del crash anterior: el duplicado es **estructural**, no un error de carga. Cada puerta de entrada crea un `Customer` atado a **una sola clave** y nunca se cruzan — WhatsApp por **teléfono** (`CustomerIdentificationService`), app mobile por **email** (`MobileAccount`, resuelve lazy por `customers.email`), y el checkout es donde por primera vez conviven teléfono+email+DNI. Nuevo servicio **agnóstico de canal** `CustomerMergeService` (complementa a `CustomerConsolidationService`: ese reconcilia **campos** dentro de una fila, este reconcilia **dos filas** en una). `reconcile(survivor, {dni,email,phone})` busca filas que ya posean cualquiera de esos identificadores y fusiona cada una (maneja DNI en una fila + email en otra); `merge(survivor, loser)` en transacción: (1) audita conflictos (gana el survivor, se deja rastro del valor descartado), (2) repunta las **6** columnas `customer_id` (`conversations`+quotes, `vehicles`, `risks`+pólizas, `risk_snapshots`, `customer_audits`, `mobile_accounts`) perdedor→survivor, (3) **`forceDelete()`** del perdedor (hard delete: el índice único de dni/email **no es parcial**, un soft-delete retendría el slot), (4) rellena los vacíos del survivor con los del perdedor vía `apply()` con source nuevo **`merge`** (rellena-solo-vacíos, igual que `chat`). **Dirección:** survivor = customer de la conversación en curso (la quote/snapshot/session activos cuelgan de ahí). Enganchado en `CheckoutController::submit` **antes** de la consolidación. El guard de clave única queda como red de seguridad (defensa en profundidad). 5 tests nuevos (`CustomerMergeTest`). PHPStan **0** · batería de cliente **28/28**. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-19** — **Fix — el checkout reventaba al consolidar un DNI/email ya usado por otro `Customer`.** `CustomerConsolidationService::apply()` escribía las claves de negocio únicas (`dni`, `email`) sobre el `Customer` de la conversación **sin chequear** que el valor ya perteneciera a otra fila → `UniqueConstraintViolationException` (`customers_dni_unique`) que **abortaba toda la transacción del checkout** (se perdía session, transición de fotos, dispatch de emisión y mail). Caso real: el customer de la conversación (placeholder con toda la actividad — convs/vehículos/snapshots — pero `dni=null`) declaraba en checkout un DNI/email que ya era de **otro** customer creado por CRUD admin. **Fix mínimo agnóstico de canal:** nueva const `UNIQUE_FIELDS=['dni','email']`; antes de escribir una de esas claves, si otra fila ya la posee (`whereKeyNot`), se **saltea el campo** (el resto de los datos sí consolidan) y se **loguea el conflicto de identidad** con ambos `customer_id`. No se auto-mergea (dirección ambigua: el placeholder tiene la actividad, el otro tiene el DNI → **merge manual**, deuda señalada). El camino admin ya validaba con `Rule::unique`; este cierra el hueco del checkout. 2 tests nuevos en `CustomerConsolidationTest` (colisión de DNI y de email). PHPStan **0** · suite del archivo **7/7**. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-17** — **CRUD de Clientes (sección propia) + consolidación del `Customer` como fuente de verdad ✅.** Nueva sección `/customers` (independiente de "Conversaciones", que queda intacta) con detalle rico para el productor: resumen de cartera (KPIs), vencimientos/alertas (cuota + renovación por urgencia), vehículos, pólizas (+acceso a sus docs), cotizaciones, conversaciones, **datos de la última declaración de checkout (read-only)**, **divergencias** (con resolución 1-clic), timeline y notas. **Integridad de datos:** el `Customer` pasa a ser el registro canónico — el checkout y el chat **sincronizan de vuelta** vía `CustomerConsolidationService` (agnóstico de canal). Modelo de pesos por fuente (**checkout ≈ admin > chat**): admin manual siempre aplica y se audita; checkout (declaración jurada) rellena vacíos y pisa lo de origen chat, pero **no pisa lo curado por admin → marca divergencia**; chat solo rellena vacíos. Provenance por campo en `metadata.field_sources` + **audit log** (`customer_audits`). **Nombre partido** en `first_name`/`last_name` (migración; `name` sincronizada por compat). Campos canónicos alineados con el `person_holder` de Visred + domicilio del tomador. **Domicilio:** Visred toma un solo `address`; el dominio modela tomador (en `Customer`) y guarda (en `vehicle`), y el **mapeo vive en el adapter** — `address.zip_code` emitido = **CP de guarda del vehículo** (el que tarifa), regla confinada a `VisredEmissionProvider` (multi-provider ready). Hooks: `CheckoutController::submit` (source checkout) y `CustomerController` (source admin). Análisis de dominio completo en **`docs/v2/11`**. Tests nuevos (`CustomerConsolidationTest` + `CustomerCrudTest` portado a `customers.*` + caso zip de guarda en `VisredEmissionProviderTest`). Side-effect positivo: las anotaciones `@property`/generics de `Customer`/`Conversation`/`Quote` redujeron el **baseline PHPStan de 143 → 42**. PHPStan **0** · build ✅ · suite **392/392**. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-16** — **Index de "Docs Pólizas" diferenciado del de "Pólizas".** El index de documentos era un clon visual del listado de pólizas (mismo buscador + tabla), solo cambiaba el destino de la fila. Se conservan ambas vistas (decisión del usuario) pero el de Docs pasa a ser **documentación-céntrico**: pills de filtro **Todas / Con documentos / Sin documentos** (`Poliza::has/doesntHave('documents')`), orden por **última carga de documento** (subquery `max(captured_at) desc nulls last`, sin docs al final) y columna **"Último documento"** (kind + fecha, o badge "Sin documentos") en vez de cobertura/estado/vigencia; badge de cantidad con sub-línea "N visibles". Backend: relación `Poliza::latestDocument()` (`latestOfMany('captured_at')`) + `withCount('documents as visible_documents_count')`; el index expone `last_kind`/`last_document_at`/`visible_count`. `@property-read` para los counts agregados (Larastan no resuelve aliases custom). 2 tests nuevos de filtro (`PolicyDocumentControllerTest` 8 → 10). PHPStan **0** · suite del archivo verde. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-16** — **Vista "Clientes" renombrada a "Conversaciones" (panel admin).** Rename completo de la vista de CRUD de clientes a pedido del usuario, manteniendo intacto el modelo de dominio: `Customer`, tabla `customers`, param de ruta `{customer}` y FKs `customer_id` **sin tocar**. Cambios: URLs `/customers/*` → `/conversations/*`; route names `customers.*` → `conversations.*`; `CustomerController` → `ConversationController` (namespace `App\Http\Controllers`); carpeta Inertia `Customers/` → `Conversations/`; nav "Clientes" → "Conversaciones" + título de la página. **Colisión resuelta:** ya existía `Admin\ConversationController` (auditoría de chats, modelo `Conversation` real) — para evitar el choque de `use`, el nuevo controller se importa aliased en `routes/web.php` (`ConversationController as CustomerConversationController`). Actualizados: auth redirects (`AuthenticatedSessionController`/`VerifyEmailController`), tests (`CustomerCrudTest`, `AuthenticationTest`, `EmailVerificationTest`), cross-links de Pólizas, blades legacy, `phpstan-baseline.neon` (path del controller) y docs v2 (#02, #05). _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-15** — **CRUD de clientes y pólizas (admin panel) ✅.** Hasta ahora el admin podía cargar documentos a pólizas existentes pero no crear un cliente ni una póliza. Cadena de dominio `Customer → Risk → Poliza` (ambas FK NOT NULL), sin migración (todas las columnas ya existían). **Clientes:** `CustomerController` completa los métodos vacíos (`create/store/edit/update/destroy`) reusando `CustomerRepository` (normaliza email/teléfono, marca `is_anonymous`); alta exige ≥1 identificador (DNI/email/teléfono), `dni`/`email` únicos; baja = soft-delete **bloqueado si el cliente tiene una póliza vigente** (vía `risks.polizas`); `show()` ahora lista las pólizas del cliente. `Customers/{Create,Edit}.vue` + botón "Nuevo cliente" en Index + acciones Editar/Eliminar (modal inline) y sección Pólizas + "Agregar póliza" en Show. **Pólizas:** sección propia (nav "Pólizas") con `PolizaController` (resource sin `show` — el "show" es el doc-manager existente) + atajo "Agregar póliza" desde el cliente (`?customer={id}`). Alta **search-first** del cliente (Inertia GET, mismo UX que Docs Pólizas) → selector de Risk **reusar-existente o crear-nuevo inline** (patente/marca/modelo/versión/año/...) → campos de póliza. Lógica en `PolizaService` (agnóstico de canal, sin DTOs): resuelve el Risk (valida pertenencia al cliente), aplica el constraint de dominio **"una sola póliza `vigente` por Risk"** (en código — `ValidationException` clara, no auto-degrada la vigente) tanto al crear como al editar. `Polizas/Edit.vue` cross-linkea a "Gestionar documentos" (`policy-documents.show`). Componente compartido `Polizas/PolizaFields.vue`. 18 tests nuevos (`PolizaControllerTest` 11 + `CustomerCrudTest` 7). PHPStan **0** · Pint ✅ · build ✅ · suite **382/382**. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-15** — **Gestión manual de documentos de póliza (admin panel) ✅.** Cierra la deuda abierta el mismo día: post-emisión (renovaciones/endosos/correcciones) NO pasa por Visred (no hay `presale_id`), así que esos documentos entran por carga manual del admin. Nueva sección "Docs Pólizas" (grupo Principal del nav): `PolicyDocuments/Index.vue` busca la `Poliza` (por `numero`/patente/cliente, `ilike` + JSON `metadata->patente`) y `Show.vue` es su gestor — subir (PDF o imagen, `max:51200`), togglear `visible_to_client` (un doc `admin_upload` visible aparece de inmediato en `GET /polizas/{id}/documentos` de mango), y eliminar con modal inline (borra el archivo en R2 vía `DeleteOrphanPhoto`). Backend: `PolicyDocumentController` (sin DTOs, arrays/Eloquent directo), `PolicyDocumentService::storeAdminUpload` (**siempre `create()`** — los documentos se acumulan al contrato, no se reemplazan; path `policy-documents/{id}/{kind}-{uuid}.{ext}`). **Modelado:** `kind`/`source` pasan a enums tipados (`PolicyDocumentKind`/`PolicyDocumentSource`) — única fuente de verdad cross mobile/admin/Visred; migración agrega `original_filename` + `label` (nullable) para distinguir varios docs del mismo `kind`. Factories nuevos `PolicyDocument`/`Poliza`/`Risk`. La emisión queda intacta (su `updateOrCreate` solo "pisa" en un retry idempotente del mismo snapshot). PHPStan **0** · Pint ✅ · suite **364/364**. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-15** — **Corrección — `presale_id` confinado al adapter + documentos de póliza persistidos.** `presale_id` (dato de Visred, solo vive en el ciclo de emisión) estaba embebido como handle durable en `polizas` (tabla que lee mango-mobile) y paseado por el shape neutro y el dominio. **Sacado:** nueva migración dropea `polizas.presale_id`; `EmissionProvider::emit()` ya no lo expone (SUCCESS se decide por `status`); la inspección post-emisión pasa a ser **interna del adapter** (`uploadInspection` removido del puerto, `presale_id` no sale de `VisredEmissionProvider`). La referencia durable es `policy_number` + `company_id`. **Documentos — el túnel queda caduco:** el adapter captura los oficiales dentro de `emit()` (con el presale vivo, `POST /v1/documents/` síncrono → `result.url` pre-firmada → bytes) y los devuelve como blobs neutros; `PolicyDocumentService` los persiste en R2 (`PolicyDocument`, `source=visred_emission`, `visible_to_client`). Nuevo `GET /polizas/{id}/documentos` (URL R2 firmada/expirable, authz propio/compartido). **Renovación = sin Visred, sin presale** (ocurre en la compañía) → renovaciones/endosos van por **carga manual del admin (deuda)**. PHPStan **0** · tests emisión/referencia/adapter/documentos **34/34**. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-07** — **Visred — V2-3: `VisredQuotationProvider` + gate de quotability ✅.** Diseño **aplanado** (a pedido del usuario: "más llano, sin exceso de ingeniería"). **Gate (resolver real):** puerto agnóstico `Quotability::check(Vehicle): QuotabilityResult` — lo único que ve el canal; `identify_vehicle` NO conoce el catálogo, solo el tri-estado `Quotable`/`NeedsFact`/`NotQuotable`. `VisredQuotabilityResolver` (impl Visred) navega el árbol de 5 GETs (rutas singulares, shapes heterogéneos → `{ref,label}`), desambigua (Tier 1a igualdad exacta = candado del re-cotizar → Tier 1b subconjunto léxico → Tier 2 LLM `DisambiguationAgent`, parsea `version_name`, fakeable), beam por groups; error de Visred → rama honesta `NotQuotable`; refina `Vehicle.version` (dominio). Gate en `WhatsAppAdapter::identifyVehicle` (sirve WhatsApp + web-chat): Quotable→crea Quote + token en `risk_provider_refs`; NeedsFact→pide hecho de dominio; NotQuotable→rama honesta. Token/proveedor **nunca** en `ai_state` ni en mensajes (test lo verifica). Migración nueva `risk_provider_refs` (genérica: `risk_snapshot_id`+`provider`+`external_vehicle_ref`, unique); relación `RiskSnapshot::providerRefs()`; **sin** columna de proveedor en el dominio. **Cotización:** `VisredQuotationProvider implements QuotationProvider` lee el token, `cotizar/`→polling acotado (`Sleep`, tolerante a FAILURE parcial y budget agotado), aplana company→covers, `normalized_grade` solo en el adapter; firma `generateAlternatives(RiskSnapshot): array` **intacta**. **Decisiones:** "resolver real siempre" en producción (bind `Quotability→VisredQuotabilityResolver`, independiente del seam de cotización que sigue en `QuotingEngine` — V2-4); en tests `TestCase` bindea `StubQuotability` por defecto, los tests de quotability usan el resolver real con `Http::fake`+`DisambiguationAgent::fake`. `quotation_result_id` por-alternativa **diferido a V2-5** (lo consume la emisión; hoy cada alt lo lleva en `external_quote_id`, `saveResults` intacto). 14 tests nuevos (6 resolver + 3 gate + 5 cotización). Pint ✅ · PHPStan **0** ✅ · suite **302/302**. Env renombradas `MANGO_QUOTATION_PROVIDER`/`MANGO_EMISSION_PROVIDER` → `QUOTATION_PROVIDER`/`EMISSION_PROVIDER`. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-07** — **Visred — V2-2: `VisredClient` (HTTP + JWT) ✅.** Cliente agnóstico de proveedor que espeja el patrón de `WhatsAppOutboundService`. `app/Services/Visred/VisredClient.php`: `get()`/`post()` autenticados, login (`/v1/accounts/token/`) + refresh (`/refresh/`) con tokens cacheados **server-side** (Cache, no en estado de instancia → sirve transient), **refresh-on-401** (refresca y reintenta una vez; si el refresh falla, re-login con credenciales de servicio), `Authorization: Bearer` + `Accept: application/json`, retry transitorio (red/5xx/429, **no** 4xx), `X-Mock-Scenario` **solo** si `config('visred.sandbox')`. `app/Exceptions/Visred/VisredApiException.php`: normaliza el envelope `{success:false,error:{message,code,field_errors}}` a excepción tipada (`status`/`code`/`field_errors`) — capa anticorrupción, el dominio nunca la ve (ADR-001); falla de red → `503`/`external_service_unavailable`. `config/visred.php` espeja `config/whatsapp.php` (`quotation_provider`/`emission_provider` default `mock`, `base_url`, creds, `timeout`, `poll_budget`/`poll_interval`, `sandbox`); claves `VISRED_*`/`MANGO_*` en `.env.example` (placeholders). 14 tests herméticos `Http::fake` + fixtures `tests/Fixtures/Visred/` (login, refresh, 401→refresh→reintento, re-login, 400/401/403/404/409/503, field_errors, gating `X-Mock-Scenario`, falla de red) + **smoke-test REAL** vía `tinker` contra el sandbox (login + refresh OK, sin imprimir secretos). Pint ✅ · PHPStan **0** ✅ · suite **288/288** (882 assertions). El seam de selección sigue en `QuotingEngine` (mock) — el flip es V2-4. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-07** — **Visred — auth real verificada + docs de plan dedicados.** Smoke-test `POST /v1/accounts/token/` contra el sandbox → `200` con `access`+`refresh` (base URL corregido a `https://sandbox-api.visred.com.ar`; el host `sandbox.visred.com` no resolvía). Credenciales en `.env` (no commiteadas), egress OK. Creados `docs/v2/visred-integration/PLAN.md` (copia canónica del plan en el repo) + `docs/v2/visred-integration/ROADMAP.md` (tracking específico del plan). V2-2 desbloqueada. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-06** — **Visred — Fase 1: puerto `QuotationProvider` extraído** (refactor sin cambio de comportamiento). Nuevo `app/Contracts/QuotationProvider.php` (firma neutra `generateAlternatives(RiskSnapshot): array{task_id,status,raw,parsed_alternatives}`); `QuotingEngine implements QuotationProvider`; `ApiQuoteResolution` pasa a depender del puerto (no del concreto); bind `QuotationProvider → QuotingEngine` (mock, default) en `AppServiceProvider` — el switch por config (`mock|visred`) llega en Fase 4. PHPStan 0 ✅ · Pint ✅ · suite **274/274** (845 assertions) verde. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-06** — **Visred — Fase 0 (modelo de dominio, gate) escrita.** `docs/v2/10-modelo-dominio-cotizacion-emision.md` (+ índice #10 en `docs/README.md`): modelo **agnóstico de proveedor** para cotización + emisión (Visred = un Adapter detrás de los puertos `QuotationProvider`/`EmissionProvider`; admite 2º proveedor sin tocar el dominio). Decisiones de la sesión de plan: **límites de contexto** (la compañía vía Visred es el *system of record* de póliza/endosos/estado/documentos — on-demand, última versión; MANGO es *engagement*), **referencia mínima + on-demand** con cache-aside (~2h) para la cartera (sin proyección autoritativa ni store de endosos), **`Risk` = identidad + estado actual sin versionar** (la historia comercial vive en la cadena inmutable de `risk_snapshots`), y propuesta de **repurposear `polizas`** a tabla de referencia (coordinar con mango-mobile). Credenciales sandbox **disponibles** → E2E real en scope. Implementación (Fases 1+: `VisredClient`, adapters, config seam, emisión) **supeditada a la aprobación de este doc**. **En revisión.** _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-05** — **V2-7 — Envío real de WhatsApp verificado + racional del seam documentado.** Con `WHATSAPP_DISPATCH_DRIVER=cloud` (estaba sin setear → caía al default `log`, no-op) se confirmó el flujo completo end-to-end: app "Estoy bien, pero pasó algo" (Estado 1) → `EmergencyController` → `CloudApiWhatsAppDispatcher` → encola `SendWhatsAppTemplate` → worker → `WhatsAppOutboundService::sendTemplate()` → Meta → webhooks de status `sent` y `delivered`. Confirma template `mango_emergencia_estoy_bien` aprobado en Meta, worker `whatsapp-outbound` corriendo y credenciales válidas. La env var vive en `.env` local (no se commitea): para prod, MANGO debe setearla en el deploy. **Documentado el racional contundente** de por qué el seam `log`/`cloud` aplica solo a los avisos por **template** y NO a las respuestas conversacionales (`sendMessage`): son dos superficies de API distintas con reglas de consentimiento opuestas (ventana de 24h de Meta); el kill-switch va en la superficie que puede alcanzar a un destinatario **no-consentido** (solo los templates pueden) — `docs/v2/06-adapters.md` nueva sección + resumen por canal + estado actualizado en `docs/v1/deuda-whatsapp-dispatch.md`. Un futuro corte global "no hablar con Meta" iría en `WhatsAppOutboundService` (transporte), ortogonal a este seam. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-05** — **Fix — PAS por default no resolvía (mismatch de env key).** La directiva "el PAS de último recurso soy yo (Andrés)" no se cumplía: `config/mango.php` leía `MANGO_DEFAULT_PAS_EMAIL` (inexistente en `.env`) mientras el `.env` define `MANGO_DEV_PAS_EMAIL`. Resultado: `SiniestroController::defaultPas()` devolvía `null` y el aviso de siniestro caía al `422 NO_PAS_ASSIGNED` en vez del Tier 3. Fix de una línea: `default_pas_email` ahora cae a `env('MANGO_DEV_PAS_EMAIL')` cuando no está la clave específica. Verificado end-to-end (resuelve a `andresrom@gmail.com` → User PAS real `Andrés / +5493516280778`). El path defensivo `422` se conserva intacto (se ejercita con `config(['mango.default_pas_email' => null])` en `SiniestroTest`). `SiniestroTest` 6/6 ✅. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-05** — **V2-7 — Dispatch de WhatsApp cableado.** Puerto `WhatsAppDispatcher` (`app/Contracts/`) con seam por config `whatsapp.dispatch_driver`: `CloudApiWhatsAppDispatcher` (encola `SendWhatsAppTemplate` en `whatsapp-outbound`) y `LogWhatsAppDispatcher` (no-op, default local/testing). Nuevo `WhatsAppOutboundService::sendTemplate()` (reusa `post()` con retry + manejo 131048). `config/whatsapp.php` con nombres/idioma de los 3 templates por env (placeholders hasta aprobación Meta). Los 3 TODO reemplazados: siniestro→PAS (nombre+contacto del cliente), emergencia Estado 1 (link Google Maps estático), Estado 2 (`tracking_url` público, **nunca** el `update_secret`). Contratos mobile sin cambios. 7 tests nuevos en `EmergencyDispatchTest` + suite emergencia/siniestro verde (33 ✅). Pint ✅ · PHPStan 0 ✅. **Go-live pendiente de MANGO:** templates aprobados en Meta + `WHATSAPP_DISPATCH_DRIVER=cloud`. _(rama `prepare-proyect-for-API-refactor`)_
- **2026-06-02** — Reorganización de documentación: creado `docs/v2/` canónico (8 docs: schema, endpoints, services, repos, controllers, adapters, consolidado v1, Visred adapter) + índice `docs/README.md`. `docs/v1/` pasa a archivo histórico. Creado este ROADMAP. _(commit `d9fbe8e`)_
- **2026-06-02** — Rama `cirugia-v2` creada desde `main` (será la futura `main`). **Extirpación de `pas_mobile`** (V2-6 parcial): eliminados `MobileAppQuoteResolution`, `QuoteOfferedToPas`, `FallbackToApiListener`, `QuoteWebhookController` + ruta, `MobileSyncLog`, `OpportunityTestDataSeeder`; nueva migración dropea tabla `mobile_sync_logs` y columnas `mobile_*`/`sent_to_mobile_at`/`expected_resolution_at` de `quotes`; limpiados config/services `mobile_app`, settings seeder, `SettingsController`, vistas (badges PAS, card endpoint). `QuoteService` colapsado a estrategia única `api`; adapters llaman `resolveQuote()` sin estrategia. Verificado n8n = stub muerto (puente real = `WhatsAppWebhookController`). Pint ✅ · PHPStan 0 ✅ · suite idéntica a baseline (24 fallos pre-existentes ajenos). _(commit `1d45c7c`; docs reorg en `d9fbe8e`)_
- **2026-06-02** — Limpieza residual: `STYLE_GUIDE.md` y skill `frontend-style-guide` → v2.3, removidos los badges `offered_pas`/`rejected_pas` del snippet `statusClass`.
- **2026-06-02** — Borrado stub `app/Adapters/n8n-whatsapp/` (dead, lanzaba excepción) + todas sus referencias en `CLAUDE.md`, `README.md` (×2, reescritas a Cloud API nativa) y `docs/v2/06-adapters.md` (×2). **V2-6 completa.**
- **2026-06-03** — **Barrido de residuo legacy en jobs/adapters** (auditoría jobs/queues/controllers/services). Borrados 2 jobs muertos del viejo flujo async de proveedores PAS: `RequestQuotesFromProviders` (huérfano, no se despachaba; su rol —resolución async— hoy es sincrónico vía `tryResolveQuoteById → resolveQuote`) y `RecieveQuotesFromProviders` (stub vacío, andamio del receipt `offered_pas`). Reescrito copy stale "Oferta enviada a los productores" → "Cotización procesada; preparando las alternativas…" en ambos adapters (`AgentToolAdapter`, `WhatsAppAdapter`). Corregidos docblocks "resolución Mobile" → "vía API" en los 2 adapters + `AIProviderAdapterInterface`. README: la sección "Cotización Asíncrona / `RequestQuotesFromProviders`" reescrita a `ApiQuoteResolution`/`NotifyClientQuoteReady`. Auditado: **cero acoplamiento import/runtime restante** con `pas_mobile`/`pas-web` (lo que matchea —`Mobile/`, `auth:mobile`, `mobile_account_id`, "PAS"/productor, `external_quote_id`— es mango-mobile vigente + dominio de negocio + ADR-001, no legacy). PHPStan 0 ✅. _(commit `405126b` — barrido junto al feat de tracking)_
- **2026-06-03** — **Triage de suite**: confirmado que los 24 tests rojos son **pre-existentes y ajenos** a la limpieza legacy (verificado vía `git stash` — `IdentifyVehicleTest` da idéntico 9F/7P con y sin los cambios; total se mantiene en 24 = baseline). Causa raíz: tests de integración pegan contra AI/HTTP real sin credenciales en entorno de test. Promovidos a **deuda activa "en arreglo"** (ver tabla) — se atacan en sesión aparte fakeando el borde AI/WhatsApp. _(commit `405126b`)_
- **2026-06-03** — **Retiro del residuo final del flujo PAS opportunities** (cierre de V2-6). Eliminado el job `CheckQuoteAcceptance` (`app/Jobs/`) — watchdog cuyo propósito original era forzar el fallback a API cuando ningún PAS aceptaba la oferta dentro del timeout (`offered_pas` → `rejected_pas`); tras extirpar esos estados quedó manejando solo `pending`, sin razón de ser en el flujo WhatsApp-only. Removido su dispatch en `QuoteService::createPendingQuote` y el setting `pas.opportunity_timeout_minutes` (seeder + `SettingsController` + stat card en `Settings/Index.vue`); nueva migración `drop_pas_opportunity_timeout_setting` borra el row de `system_settings`. La resolución sincrónica (`coveragePreference → tryResolveQuoteById → resolveQuote`) y `ApiQuoteResolution` se conservan intactas. Efecto: una quote que nunca alcanza el paso de cobertura queda en `pending` (no se auto-resuelve) — comportamiento **aceptado y deliberado** en el modelo WhatsApp: si el cliente no elige cobertura no hay nada que cotizar, y la resolución real ocurre vía la estrategia `api`. El ciclo de vida de estados de la quote (incluido el destino de las que quedan en `pending`) se va a **rediseñar junto con el polling de `TaskList` de Visred** (ver V2-3) — ese será el momento de definir si `pending` necesita un estado terminal o expiración. Pint ✅ · PHPStan 0 ✅ · tests Quote verdes. _(commit `405126b`)_
- **2026-06-03** — **Suite a verde: 24 → 0 fallos (264/264, 809 assertions).** Los "24 rojos pre-existentes" no eran un único problema de "AI sin mock" como se creía en el triage del 2026-06-03; al diagnosticarlos uno por uno resultaron **5 causas raíz distintas**, ninguna de lógica de negocio:
  1. **`ProcessConversationInbox` `TypeError` (4 tests).** Los mocks de `InsuranceOrchestrator::handle()` devolvían `['text', 'agent']` sin la clave `execution_log_ids`; el job hace `end($reply['execution_log_ids'])` ([ProcessConversationInbox.php:71](app/Jobs/ProcessConversationInbox.php:71)) → `null` → TypeError. **No se agregó defensa `?? []`**: el orquestador real **siempre** retorna la clave (contrato `array{text, agent, execution_log_ids: int[]}`; ambos `return` la incluyen y el path de error hace `throw`), así que era mock incompleto, no bug de prod. Fix = completar los 4 mocks.
  2. **`SendWhatsAppMessage` Mockery mismatch (3 tests).** Prod pasa `config('ai.default')` (`'deepseek'`) como último arg a `sendMessage`/`sendAudioMessage`; las expectativas `->with(...)` tenían un arg de menos. Fix = agregar `config('ai.default')`.
  3. **`IdentifyVehicle` 500 (varios).** El test mandaba `sessionUuid => 'test-session-uuid-vehicle'`, no es UUID válido y `quotes.session_uuid` es columna `uuid` de Postgres → `SQLSTATE[22P02]` → `server_error`/500. Fix = `Str::uuid()` en el `beforeEach`.
  4. **`ConversationShow` orden no-determinista (1 test).** `created_at` no está en `$fillable` de `Message`, el `create([... 'created_at' => ...])` del test se ignora y ambos mensajes quedan con el mismo timestamp; `orderBy('created_at')` sin desempate devolvía orden arbitrario. Fix de prod = `->orderBy('id')` como desempate determinista en `ConversationController::show`.
  5. **`CheckoutAgentPrompt` — archivo de prod destruido (ver abajo).**
  Cambios de prod: **2** — el desempate `orderBy('id')` y la **recuperación de `CheckoutAgent.md`**. El resto, fixes de test. Pint ✅ · PHPStan 0 ✅. _(commit `405126b`)_
- **2026-06-03** — **🔴 Bug de fondo descubierto: `AgentPromptController` escribe prompts `.md` versionados desde un request HTTP, y eso destruyó `CheckoutAgent.md` en el repo.**
  - **Síntoma.** `tests/Feature/AI/CheckoutAgentPromptTest` fallaba afirmando que el prompt no contenía `check_coverage_rule`, `Fallback contrastado`, etc. El archivo en disco era el string `"promoted draft content sufficiently long to pass validation"` (1 línea), y `git show HEAD:...CheckoutAgent.md` confirmó que **el stub estaba commiteado** (entró en `b2e70e9`). El prompt real (203 líneas) se recuperó de `1504224` y se verificó contra todas las assertions.
  - **Causa raíz.** [`AgentPromptController::promoteDraft()`](app/Http/Controllers/Admin/AgentPromptController.php:153) y `::store()` llaman a `writePromptFile()` ([:324](app/Http/Controllers/Admin/AgentPromptController.php:324)), que hace `file_put_contents(resource_path('prompts/agents/{File}.md'), $content)` — una escritura cruda a un archivo **bajo control de versiones** disparada por una acción HTTP autenticada. `AgentPromptDraftFlowTest` ("promotes a draft to active") ejercita el controller real promoviendo un draft de `checkout_closer` con contenido stub → pisa `CheckoutAgent.md`. Como no había restore, el stub quedó en el working tree y se commiteó. Por eso el test estuvo rojo "desde siempre".
  - **Por qué es un problema más allá del test (4 puntos):**
    1. **Duplicación de fuente de verdad.** Según `CLAUDE.md`, el runtime sirve los prompts desde la tabla `agent_prompts` (`AgentPrompt::activeFor($key)`, cacheado con `rememberForever`); el `.md` es **solo fallback/seed**. Escribir el `.md` en cada promote es un side-effect de "sync al repo" que no es atómico con el write a la DB.
    2. **Mutación de artefactos en runtime.** En producción, una acción de un admin reescribe un archivo del *deploy* — no sobrevive a un redeploy, y genera drift entre entornos y entre disco↔git.
    3. **Sin seam fakeable.** `file_put_contents` a un `resource_path` absoluto no lo intercepta `Storage::fake()`; el único blindaje posible desde el test es backup/restore manual (lo que se aplicó como mitigación interina).
    4. **Sin atomicidad ni lock.** Un write fallido deja el prompt a medio escribir; no hay bloqueo ante promotes concurrentes.
  - **Mitigación aplicada (interina).** `AgentPromptDraftFlowTest` ahora respalda el `.md` real en `beforeEach` y lo restaura en `afterEach`, para que el stub de los tests no vuelva a filtrarse al repo. Cierra la hemorragia pero **no** arregla la causa.
  - **Propuesta de fondo — dos opciones:**
    - **Solución A (mínima, recomendada): eliminar la escritura del archivo.** La DB ya es la fuente de verdad y está cacheada; el `.md` es fallback documentado. `promoteDraft`/`store` no deberían escribir el archivo: se borra `writePromptFile()`. La sincronización repo→`.md` (para devs / versionado) pasa a ser un comando artisan explícito y opt-in (p.ej. `prompts:export`, reutilizando el snippet de sync ya documentado en `CLAUDE.md`), corrido a propósito, no como efecto colateral de un HTTP. *Pros:* elimina el vector de pérdida de datos, no muta archivos trackeados en runtime, sin drift entre entornos, tests sin necesidad de blindaje. *Contras:* el `.md` del repo deja de auto-actualizarse cuando un admin promueve en prod (aceptable: ya estaba derivando y la DB es canónica).
    - **Solución B (si se quiere conservar el espejo en archivo): escribir vía `Storage` a un disco no-versionado y fakeable.** Definir un disco `prompts` (p.ej. `storage/app/prompts`, o R2 como el resto de media) y reemplazar `file_put_contents(resource_path(...))` por `Storage::disk('prompts')->put(...)`; la lectura cae a ese disco y luego al seed del repo. *Pros:* mantiene el "persistir una copia"; `Storage::fake('prompts')` hace los tests herméticos; nunca toca archivos bajo git; vive en una ubicación realmente escribible. *Contras:* más piezas; el seed `.md` y las copias vivas pueden divergir (pero esa divergencia ya es inocua y fuera de git).
  - **Recomendación:** **Solución A** — alinea con el diseño documentado ("DB = fuente de verdad, `.md` = fallback") y remueve el modo de falla de raíz con el menor código. Reservar B solo si producto quiere que el panel admin empuje cambios de prompt de vuelta a un artefacto desplegable. _(descubrimiento; recuperación de `CheckoutAgent.md` en commit `405126b`)_
- **2026-06-03** — **Solución A aplicada: eliminado el clobber de prompts `.md` desde HTTP.** Borrado `AgentPromptController::writePromptFile()` y sus dos llamadas (`promoteDraft` [:153], `store` [:197]). `promoteDraft`/`store` ya **no** escriben ningún archivo: la DB (`agent_prompts`, cacheada con `rememberForever`) es la única fuente de verdad en runtime y el `.md` queda como seed/fallback (lo sigue cargando `createDraft` cuando no hay versión activa, vía `resolvePromptPath`). Removida la mitigación interina del test (`beforeEach` backup + `afterEach` restore del `.md`) — ya no hay escritura a disco que blindar — y agregado un **header de HISTORIA** en `AgentPromptDraftFlowTest` que narra el incidente (clobber → stub commiteado en `b2e70e9` → recuperado de `1504224`) para que nadie reintroduzca escrituras a `resource_path()` desde un controller. La dirección `.md → DB` sigue disponible vía el comando existente `agent:sync-prompts`; **no** se agregó `prompts:export` (DB→`.md`) — opt-in para más adelante si se quiere versionar cambios hechos desde el panel. `AgentPromptDraftFlowTest` (15) + `CheckoutAgentPromptTest` (7) = 22 verdes. _(commit `826b482`)_
- **2026-06-02** — **Extirpación de `pas-web`.** Auditoría exhaustiva (`pas-web`, `pas_web`, `PasWeb`, `PAS_WEB`, `external_quote_id`, `quote_provider_refs`, `poliza_api`): **cero huella en código, config y `.env`** de `workflow-assistant` — ya estaba desacoplado de facto (la estrategia `api` usa `QuotingEngine` mock, no pas-web; `quote_provider_refs`/`poliza_api` son genéricos de proveedor). Todas las menciones vivían en docs: actualizadas las convenciones de canal y narrativas en `docs/README.md` y `docs/v2/01`–`07` a `legacy, extirpado`, para `pas-web` **y** `pas_mobile` (criterio unificado: ambos permanecen documentados, marcados legacy + extirpado). `docs/v1/adr/001-quote-provider-refs.md` se deja **intacta** (archivo histórico; el bug ocurrió con pas-web y el invariante sigue vigente para Visred). _(commit `48e2b90`)_
- **2026-06-05** — **Auditoría de arquitectura de queues.** Mapeado job→conexión→cola→worker de los 12 jobs. Tres hallazgos:
  - **🔴 H1 (resuelto).** El worker de IA corría `queue:listen --queue=whatsapp-ai,whatsapp-outbound,default --timeout=180` **sin argumento de conexión** → usaba la default `database` (retry_after=90), anulando la conexión `database_ai` (retry_after=200) que existe justamente para tolerar LLM lento. Como ambas conexiones comparten la tabla `jobs` y el reclaim usa el `retry_after` de la conexión *que consume*, todo job de `whatsapp-ai` que tardara 90–180s era reclamado mientras corría → **doble llamada al LLM + doble respuesta WhatsApp**. `WithoutOverlapping` no protege (serializa el despacho, no el reclaim del driver). Fix en `run.md`: worker de IA ahora `queue:listen database_ai --queue=whatsapp-ai --timeout=180`, separado del liviano (`whatsapp-outbound,default`) y del de media. Tres procesos, uno por conexión (un worker bindea una sola conexión).
  - **⏸️ H2 (latente, statu quo).** Cola `semantic-analysis` sin consumidor. Es Tier 2 de QA: `AnalyzeConversationSemanticsJob` corre DESPUÉS de responder, escribe flags en `conversations.semantic_analysis` que solo lee el panel admin — **no** afecta el chat en vivo (el orquestador no las lee). Hoy apagado por flag → no acumula. Decisión: dejar apagado durante cirugía v2; si se quiere QA, cablear al worker `database_ai`; si se quiere impacto en vivo, es rediseño (que el orquestador reaccione a `semantic_loop`/`context_loss`).
  - **✅ H3 (resuelto, opción A).** `ExtractCoverageDocumentText` (timeout=300) salió de la cola `default` (retry_after=90). Nueva conexión `database_long` en `config/queue.php` (retry_after=360 > timeout=300, cola `documents`); el job ahora `onConnection('database_long')`; worker dedicado en `run.md` (`queue:listen database_long --tries=2 --timeout=300`), ocioso casi siempre (solo corre al actualizar un manual de producto, admin-triggered). Lo saca del hot-path de WhatsApp. NO corrompía pgvector (chunking borra antes de insertar; [ChunkAndEmbedService.php:47](app/Services/ChunkAndEmbedService.php)).
  - **⬜ H5 (documentado).** Prod: `run.md` es cheatsheet de dev con `queue:listen` lanzado a mano. En prod usar `queue:work` bajo supervisor que reinicie procesos caídos (ver tabla de deuda). El modo de falla "worker muere → cola frena en silencio" es real (pasó con `semantic-analysis`). 4 conexiones a supervisar.

  Acoplamiento de los jobs de análisis (referencia): `AnalyzeConversationHealthJob` (Tier 1, **sin IA** — heurísticas: loops por hash de mensajes, stuck≥5 turns, tool_errors, abandoned 24h, long≥20 msgs) corre en cola `default` y SÍ se consume; escribe `conversations.flags`. `AnalyzeConversationSemanticsJob` (Tier 2, **con IA**) es el huérfano de H2. Ambos se despachan desde `ProcessConversationInbox` tras responder y solo alimentan el panel admin. _(commit `3c20d26`)_
- **2026-06-05** — **Doc de arquitectura de queues** creado: `docs/v2/09-queues-architecture.md` (modelo conexión/cola/worker, invariante `retry_after > timeout`, incidente H1, tabla de conexiones, inventario de 12 jobs, topología de 4 workers, pipeline de inbox de 3 etapas, Tier 1/2 de observabilidad, deuda H2/H5). Agregado al índice canónico en `docs/README.md` (#09). _(commit `3c20d26`)_
